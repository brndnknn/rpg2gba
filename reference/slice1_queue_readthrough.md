# Slice-1 queue read-through (2026-07-02)

**What this is:** the grill-D10 #4 obligation — the transpiler queue for the
pathfinder slice (Maps 49/48/32), read to completion, every entry bucketed:
**native** (fork analog exists — emit it), **idiom** (build agent extends the
library, user reviews the mapping), **hand** (story logic — user or the
hand-invoked LLM tail at the user's discretion), **defer** (out of slice-1
scope). Queue file: `output/uranium-build/transpile_unhandled.jsonl` (fresh
each driver run). Counts below are from the run after the diagonal/priority
movement-token additions: **96 slice entries** (+90 CE entries, all deferred —
the slice maps call zero common events).

Resolved during the read-through itself (already in the transpiler):
- Diagonal walks (RMXP 5–8) → `walk_diag_*` — the census's HARD call predated
  the fork check; the expansion ships them (movement.inc:147–150).
- 39/40 → `set_fixed_priority`/`clear_fixed_priority` (movement.inc:99–100).
- Together these cleared both doormat exit routes (Map049 EV3, Map048 EV2).

## Native — fork analog verified, emit instead of queueing

| Entries | Source | Fork evidence | Action |
|---|---|---|---|
| 3× `Kernel.pbRockSmash` branches (M32 EV14/15/33) | smashable rocks | rock smash ships natively (`EventScript_RockSmash`, ledger) | wire the standard rock-smash object script |
| 3× `pbCaveEntrance` + 3× code 204 (M32 EV23/36/37) | cave mouths | standard warp + cave transition, native | warp-class emit; slice-2 frontier |
| 2× `pbShowMap` (M48 EV7/8) | wall map | region map ships natively (`FieldShowRegionMap`) | `special(FieldShowRegionMap)` |
| 1× `pbTrainerPC` (M48 EV4) | bedroom PC | PC ships natively (`Common_EventScript_PC`) | call the standard PC script |
| 1× `$PokemonGlobal.runningShoes=true` (M49 EV1) | running shoes gift | `FLAG_SYS_B_DASH` | `setflag(FLAG_SYS_B_DASH)` |
| 1× code 202 set-event-location (M49 EV21) | NPC teleport | `setobjectxy` macro | emit `setobjectxy` |

## Idiom — build agent extends, user reviews (D8: ≥2 occurrences)

| Entries | Cluster | Direction |
|---|---|---|
| 26× code 101 dialogue | Essentials text codes `\wt[n]`, `\.`, `\c[n]`, `\v[3]`, `\wtnp[n]`, `\fs`, `\sign` + quoted signs | grow `_translate_text`: `\wt[n]`/`\.` → `{PAUSE n}`, `\c[n]` → `{COLOR}`, `\v[n]` → buffered string; `\wtnp`/`\fs` likely drop. One mapping table, user reviews. Biggest single win (26 slice / 627 corpus). |
| 8× `get_character(0).onEvent?` conditionals + 10× code 208 transparent-flag (M32 EV3/5/6/7/17 + EV23/36/37) | one shared NPC-visibility pattern | investigate once, resolve ~18 entries; likely page-condition/visibility translation or strip |
| 3× `Kernel.pbReceiveItem(...)` conditionals (M32 EV27/EV9, M49 EV18) | give-item-with-fanfare branch | same family as the validated `pbItemBall` classifier; 40× corpus |
| 6× code 207 show-animation | emotes (exclamation bubble etc.) | pokeemerald emotes are movement tokens (`emote_exclamation_mark`/`_question_mark`/`_heart`, movement.inc:93-95) — map emote-shaped animation ids now (supersedes novel_cluster_review.md's "Phase 8 polish" for these); only non-emote 207 animations (flourishes with no native analog) stay queued for Phase-8 |
| 6× code 112 loops (M32 EV74/78/80) | Essentials wait-until idiom (`loop { if …; break; wait }`) | inspect the three events once; likely collapses to `waitmovement`/`delay` or a `goto` pair |

## Hand — story logic (user or LLM tail, user's discretion)

| Entries | What it is |
|---|---|
| M32 **EV9** (~20 entries: routes 41/34/32/12/13, player x/y conds, `$game_variables[1/3]`, starter list, `pbPhoneRegisterNPC`, `$Trainer.pokedex=true`, code 224 flash) | the Pokédex ceremony cutscene — the slice's one genuinely branch-heavy story event |
| M32 EV74/78/80 residue (font-size text, choreography) | the Theo "hey, wait up!" chase cutscene |
| 2× `displayNinjaLetter` (M49 EV21) | custom Uranium letter-display scene |
| 1× `!pbGet(1).is_a?(Numeric)…` (M49 EV20) | defensive Ruby type check — probably strippable, but a human should say so |

## Defer — out of slice-1 scope

- All **90 common-event entries** (picture-based dream visualizer/racing CEs,
  code 231/232/235 clusters): no slice map calls any CE. They gate later
  slices, not this one.

## State

Slice `.pory` regenerated by the driver (real write run): Map049/048/032 +
CommonEvents, all four **poryscript-compile clean**, conversion-time
fork-index gate clean, registry mints saved. `assemble_pathfinder`'s staging
gate no longer trips on `healparty` — the re-transpiled scripts don't contain
it. Next per D10: implement the native+idiom buckets above (with the user
reviewing mappings), hand events at user's discretion, then NPC gfx map →
assemble → boot gate.
