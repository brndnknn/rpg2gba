# MEMORY.md Archive

> Append-only graveyard for retired MEMORY.md content: superseded Last Session
> Summary entries and resolved Open Questions. **Not auto-read** — MEMORY.md and
> CLAUDE.md do not point here for routine work. Consult only when you need the
> provenance of a past decision and git blame is too coarse. Newest entries on
> top within each section.

---

## Retired Session Summaries

## Archived 2026-08-02 — Last Session Summary entry 2026-08-01c

**2026-08-01c (pbAddPokemon gift ceremony + the machine-animation root cause; build agent):** User asked for the mon sprite to pop up when you receive it, generalized. **It was never an invention — we were dropping it.** Essentials `pbAddPokemon` (`scripts_dump/170__PSystem_Utilities.rb:1710`) = obtained-message + `\me[PU-PokemonObtained]` + `pbNicknameAndStore`; we emitted a bare `givemon`, dropping sprite/fanfare/message/prompt at **26 sites across 11 events**. Vanilla's own gift flow (`LittlerootTown_ProfessorBirchsLab/scripts.inc:333-372` + `data/scripts/pc_transfer.inc`) is the identical primitive sequence, so this is a §4.7 restoration: `bufferspeciesname`→`showmonpic`→`givemon`→branch `VAR_RESULT` (MON_GIVEN_TO_PARTY/PC/CANT_GIVE)→`playfanfare MUS_OBTAIN_ITEM`+message/waitmessage/waitfanfare→`gText_NicknameThisPokemon` YES/NO→`GetGiftMonPartySlot`+`NameReceivedPartyMon` | `NameReceivedBoxMon`+`TransferredToPC`, full-box→`NoMoreRoomForPokemon`. **`pbAddPokemonSilent` (6 sites) keeps the bare givemon** — Essentials defines that form as ceremony-free, so the old conversion was right there and wrong for the loud one. **Fork-index gap found doing it: `STR_VAR_1` is assigned ONLY in `engine/charmap.txt`** (no constants header, not event.inc), so the gate rejected `bufferspeciesname STR_VAR_1, ...` — vanilla's own idiom. charmap now scanned (`_extract_charmap_constants`, `_INDEX_FORMAT` 3→4), 2+-char ALL_CAPS only so single-letter charmap entries can't launder a typo. **MACHINE ANIMATION ROOT-CAUSED, NOT FIXED (`SLICE1_TODO.md` #25): `PU-PokeballMachine` is in `LARGE_PROP_SHEETS`, so it gets the 64×64 static treatment — `sAnimTable_Inanimate`, `.inanimate = TRUE`, ONE-frame pic table. The sheet has exactly one frame in the ROM**, so neither the pattern selection nor 08-01b's direction carry can ever show anything. Real fix is converter-level (emit used frames into the pic table + mint one gfx constant per (direction,pattern); sprites.py + sprite_emit.py + npc_gfx_map.json + transpiler resolution) — deliberately not attempted in this pass. **Lesson repeated: check the asset, not just the script — "we emit a swap" and "a frame exists to swap to" are different claims.** 1544 tests; moki GREEN 17/17 first attempt with the nickname prompt live in the path; `lab-doorstep.gba` `d39c55d6` (pristine `26408202`) taildropped, boot-walk pending.


## Archived 2026-08-01 — Last Session Summary entries 2026-08-01b / 2026-08-01 / 2026-07-30b

**2026-08-01b (Map050 EV005 retired onto the transpiler; build agent):** The lab's last hand conversion is gone (`hand_conversions/` = Map032_EV009 + Map049_EV021 only); ledger entry deleted with it — its "irreducible by shape" claim is now disproven. Quiz page 14 queue entries → **0**. Three features, each **censused before building** (the habit that made the scoping obvious): (1) **RMXP Label 118 / Jump-to-Label 119** = basic-block hoist, label region → own `script`, fall-through and jumps both `goto`. **Corpus 51 labels / 138 jumps / 23 names** — real general value. Scoped to regions that provably TERMINATE (page-end, or same-indent code-115): a hoisted block cannot fall back into an enclosing arm. 31/51 qualify; multi-label pages queue. **Hoisted blocks take the trigger's EPILOGUE only** — that's the general form of the hand file's W8 freeze patch. (2) **Array-valued game variables** (`$game_variables[N]=[0,0,0]`, `pbGet(A)[pbGet(B)]+=1`, `index(max)` argmax + `if x==1/x=0` permutation) — **corpus reach 1 site**, so deliberately narrow, with `test_aptitude_tally_is_still_one_of_a_kind` pinning the count. Emitted argmax is byte-identical to the boot-verified hand version, 32767 sign-test and all. Buckets = VAR_TEMP_3/4/5; NB `VAR_TEMP_MOVE_CHOICE` is a *minted* Uranium-range var, NOT VAR_TEMP_2. (3) `pbStarterSelector(pbGet(N))` player form (0-based domain vs Theo's 1-based), sharing one emitter. **CORRECTION to 2026-08-01a: the "machine visibly changes" claim was WRONG.** RMXP code-41 sets sheet AND (direction, pattern) and props encode STATE in those; we emitted only the sheet, so a same-sheet swap was a visible no-op. Direction now emits as facing (exact analog — same row of the converted 4-dir sheet); `pattern` has none and now files a queue entry. EV005's machine swaps are direction-2 twice → still a frame-only drop, now loud. **Lesson: "we emit a swap" ≠ "the player sees a change" — check the params, not the opcode.** 1538 tests; moki GREEN 17/17 first attempt incl. N3 (exercises the new hoist); reveal↔grant verified in sync by probe (VAR_POKEMONTEST 0-based 1 → Raptorch reveal, +1 → 2 → `givemon(SPECIES_RAPTORCH)`). `lab-doorstep.gba` `4eb25ce1` (pristine `9c118fec`) taildropped, boot-walk pending → `SLICE1_TODO.md` #23.

**2026-08-01 (stamped review ROMs were shadowed by save residue; build agent, /debug):** User's lab-doorstep ROM booted near the player's-house exit with **no player sprite and dead input**. **The stamped state was never wrong** — `CB2_StartUraniumSlice` tested `gSaveFileStatus == SAVE_STATUS_OK || UraniumEmbeddedSave_TryLoad()`, *flash first*, so any leftover `.sav` on the device beat the blob and the ROM continued an older build's save (stale map/coords → the player object never spawned). Reproduced headlessly by pairing the stamped ROM with a foreign `.sav`: booted to Map049 (7,7) instead of MokiTown (17,12). Fix = swap the order; `TryLoad()` returns FALSE untouched at `magic == 0` so pristine ROMs still continue flash saves. **A fresh-flash boot passes either way — that's why nothing caught it** — so `playtest.stamp.verify_stamped_rom` now re-boots every stamped ROM **with a deliberately foreign `.sav` paired** and asserts map/pos/unlocked. **Collateral, bigger than the bug:** the fork-index gate had no extras for the `OBJ_EVENT_GFX_URANIUM_*` constants the sprite pass mints into generated, *gitignored* `uranium_*.gen.h` headers (the index only ever sees git-tracked `engine/` source), so 07-31's code-41 live sprite swap gated as an invented constant and `transpile_driver run --maps slice` **aborted on Map050 — the slice was unbuildable, and the ROM shipped 07-31 never contained `d5ae2e3e`'s Map050 conversion** (`scripts/Map050.pory` still 07-30; `scripts.inc` had zero `RPG2GBA_SetObjectEventGfx`). Extras now come from `reference/npc_gfx_map.json` in *both* `transpile_driver` and `assemble_pathfinder` — remember the gate exists twice. **Standing lesson: a green commit message is not evidence; check artifact timestamps/content.** 1526 tests pass, 16 skipped; moki GREEN all 17 beats on the real conversion; `lab-doorstep.gba` `9596c8b7` (pristine `128f2edd`) taildropped and **user boot-walk PASSED same day** (correct spawn; lab scene visibly richer on the real EV019) → `SLICE1_TODO.md` #22, committed `a4a97e2a`.

**2026-07-30b (CHAPTER ATLAS — play-order planning layer for all 199 maps; build agent, /grill-me + /delegate):** Interactive grill settled 9 design questions; record → `reference/findings/grill_chapter_atlas_2026-07-30.md`. Model: **Act = walkthrough section (9 + postgame), Chapter = location unit (59), chapter ≡ slice ≡ §9 gate ≡ ROM-test scenario**; a revisited location is its OWN chapter (`visit>1`) over the same maps, so map ids are deliberately non-unique across chapters. Detail decays with distance (2 full / 17 medium / 40 thin) with a **promote-one-tier-where-a-mechanic-first-appears** rule. **THE WIKI IS READABLE — only one route works:** `WebFetch` 402s on every `pokemon-uranium.fandom.com` URL (incl. `api.php`, `?action=raw`) and bare `curl` gets Cloudflare 403, but **the MediaWiki API with a browser User-Agent returns 200** → `scripts/fetch_uranium_wiki.py` (supersedes ROM_TEST_DEV's "402, use WebSearch snippets"). New: **`reference/chapters.json`** (§4.3 SoT for chapter→map-id; 187 bound + 11 explicitly unbound = 198, asserted by a coverage command), **`src/rpg2gba/chapter_atlas/`** (binding loader with curation validation, census, CLI), **`reference/chapters/00-atlas.md`**, 27 tests, **1507 pass**. **BIGGEST DISCOVERY — every trainer battle in the game is hidden in a conditional:** there is not ONE code-301 in the corpus; all **317** trainer battles are code-111 / condition-type-**12** branches wrapping `pbTrainerBattle(...)`. A census reading 301+355 reports **zero trainers for the whole game** (v1 of the tool did exactly that; caught only because `01-moki.md` independently documented Theo's battle). Also normalise the `Kernel.` prefix — `pbItemBall` and `Kernel.pbItemBall` are the same head. **§4.7 FAILURE FOUND AGAIN, IN THE LEDGER ITSELF:** `CLAUDE.md` §4.7 + `BUILD_PLAN.md` §4 pointed "native-analog ledger" at `essentials_to_emerald_map.md` (actually the registry's flag/var pre-seed table); the real ledger `command_pokeemerald_map.md` carried **`healparty` in two mutually-citing rows**, the second marked "✓ vocabulary" on the strength of the first. Full audit → **2 missing symbols + 6 over-claims** corrected with file:line: `healparty`→`special HealPlayerParty`, `MB_PUSHABLE_BOULDER` (does not exist)→`OBJ_EVENT_GFX_PUSHABLE_BOULDER` + native `TryPushBoulder`, Show Animation→`dofieldeffect`+`FLDEFF_*`, Scroll Map→`SpawnCameraObject`/`RemoveCameraObject`, Screen Shake→`special ShakeCamera`, `pbSlotMachine`→`playslotmachine`, `pbLottery`→native Lottery Corner, `pbPhoneRegister*`→likely native Match Call. **Structural lesson: the asymmetry is the bug** — rows 111-115 got confident ✓VERIFIED tags while 62/75/76 got hedged C/JUDGE tags for equally greppable features. **Net: the genuinely-new-C set is Voltorb Flip (zero engine presence) + the Nuclear type, and the whole minigame cluster is confined to Bealbeach City.** `.claude/skills/pokemon-uranium-wiki/SKILL.md` was wrong on 5 counts (invented Seaspray Town + Ara City, omitted Burole + its Gym, gave Gyms to Rochfale/Silverport which have none) — corrected; real order is Nowtoch/Burole/Bealbeach/Vinoville/Amatree/Venesi/Snowbank/Tsukinami. Data beat the wiki twice more: **map 19 "Passage Cave" warps only to/from Kevlar** (traversal is 35→36↔37→40, so it binds to CH03 not CH05), and **Nuclear Plant Epsilon 122↔8 both ways** despite the walkthrough never entering it. **TWO LIVE GAPS:** (1) conversion-readiness is unmeasured for 195/199 maps — the unhandled queue only covers staged maps, and `transpile_driver run --maps full --dry-run` **aborts** on the fork-index gate at Map008 (Uranium `TRAINER_*` staged for slice 1 only); census reports `unhandled: null`, never a misleading `0`. A gate-tolerant survey mode is the highest-value follow-up. (2) **`--dry-run` is not dry** — `transpile_driver.py:405` writes `transpile_unhandled.jsonl` outside the `if write:` guard (backup taken at `transpile_unhandled.slice1.jsonl.bak`).


### Archived 2026-07-30 — 2026-07-27d pacing knobs (text/fade faster, BGM tempo reverted)

**2026-07-27d (pacing knobs: text/fade faster, BGM tempo halved; build agent):** User boot-walk report — text and fade-to-black transitions read far slower than PC Uranium, and the music sounds "twice as fast". **Two of the three premises held; the music one did not.** Measured headless in mGBA before touching anything: the overworld main loop keeps a true 60 fps (**16 hardware frames per walked tile**), and `gMPlayInfo_BGM` was playing `MUS_LITTLEROOT` at `tempoD 108 / tempoU 0x100` — **exactly** the tempo `sound/songs/midi/mus_littleroot.s` asks for. So there is no engine speed bug; the "fast music" is the **audio substitution** — every slice map still gets `MUS_LITTLEROOT` (`metadata_wiring.DEFAULT_MUSIC`; the `reference/audio_map.json` per-map wiring from `audio_decision_2026-07-14.md` was never built), and Littleroot's theme is far peppier than Uranium's interior themes. **The real fix is a calmer per-map `MUS_*`, not a tempo scale.** A half-tempo ROM (`1d1dde30`) was built and taildropped on request; the user heard it and **reverted the audio, keeping the text/fade speedups** — so `RPG2GBA_BGM_TEMPO_SCALE` is back to `256` (inert: the `#if` compiles the call site away) with the reasoning parked in the header so nobody re-tries it. **Live TODO this leaves: build the per-map `reference/audio_map.json` wiring.** All three knobs live in `engine/include/config/rpg2gba.h`, each revertible alone: `RPG2GBA_FAST_TEXT_DEFAULT TRUE` (new-game default → `OPTIONS_TEXT_SPEED_FAST`, `new_game.c:SetDefaultOptions`), `RPG2GBA_FADE_DELTA_Y 4` (vanilla 2; the three `Begin*PaletteFade` entry points in `palette.c` — normal, time-of-day, fast), `RPG2GBA_BGM_TEMPO_SCALE 256` (would apply via `m4aMPlayTempoControl` in `sound.c:PlayBGM`, the single funnel for map+script BGM; **fanfares/ME/cries/SE run on other players and keep their authored tempo**; `MPlayStart` resets `tempoU` so it must be re-applied per song start, and the sequencer recomputes `tempoI = tempoD*tempoU>>8` so it survives in-track TEMPO events). **A/B verified on one fixed scene** (Map049 → stairs → Map048 autorun msgbox): text print time **137f → 35f** (2.28s → 0.58s), `tempoU` **256 → 128**, `gPaletteFade.deltaY` **2 → 4**. Runtime-probe offsets worth reusing: `MusicPlayerInfo` `tempoD`/`tempoU` at `+0x1c`/`+0x1e`; `gPaletteFade.deltaY` = `u32 @ +0x10 >> 28`, `.active` = bit 7 of `u8 @ +0x0f`.

Its "chapter suite is RED" paragraph is fully superseded by the 2026-07-30 entry in MEMORY.md and `SLICE1_TODO.md` #20: the B4 failure was the harness reading the previous map's grid, the collision grid was fine, and fade constants *did* reach the BFS — through *when* it reads, not what it reads.

**2026-07-14 (SLICE1 #13 Moki interiors — build FINISHED, ROM taildropped, awaiting boot-walk; /delegate, 1 Sonnet Explore scout):** Worktree carried the uncommitted #13 expansion from an undocumented prior session: slice 3→8 maps (`SLICE_MAP_IDS`/`ALLOWED_MAPS` +50 Professor Lab / 64 house-2 / 65 house-1 / 172 Theo 1F / 89 Theo 2F), Map032 street-door dests resolved (EV003→50 @17,11; EV006→64 @43,31; EV007→65 @24,42; EV017→172 @56,42; cave triad EV023/036/037→map 33 stays out-of-slice by design; **EV005 left unwired — still a blocked wall**), `WARP_OVERRIDES` +4 street doors + interior exits, +3 `map_name_overrides` (89-only-via-stairs floor pattern mirrors 48/49), +11 `npc_gfx_map` entries, NEW `large_prop` 64×64 sprite class (sprites.py/sprite_emit.py, RayquazaStill-style static object) for PU-PokeballMachine (96×128 source). This session finished the chain per `slice_expansion_runbook.md`: stage --write (8 maps staged, label existence check clean; Map172 dropped 6 orphan pages EV002/003/004×4 — non-emitted events, eye-check Theo's house) → assemble (fork gate + budget guards clean; mints 196 flags/106 vars/**27 self-switches** (16→27)/8 temp-switches) → `make -C engine -j16 modern` clean (ROM 78.88%) → **taildropped 2026-07-14**. Tests: npc_gfx real-file count re-pinned 18→29; `test_build_slice_maps_smoke` ValueError self-cleared once the sprite pass regenerated `uranium_event_objects.gen.h`; **1064 pass, 0 known fails** (the 2 known MAP_MOKI_TOWN fails stayed gone). SLICE1_TODO #13 rewritten BUILT-needs-boot-walk with walk checklist (doors both ways, interior art/NPC palettes by eye — ≤4 shared banks, overflow = silent garbage; lab machine prop; Theo stairs). ALL UNCOMMITTED (prior session's expansion + this build) — commit after boot walk.

**2026-07-13 (Map viewer hot reload — PROJECT_TODO #12 CLOSED, COMMITTED; /delegate, 2 Sonnet builders on disjoint files):** (a) Tileset source PNGs now in the request-time fingerprint: new eager `sources.tileset_source_paths()` (never raises — nominal-path fallback so an absent file stats `(-1,-1)` and flips when it appears) + `_slice_aware_fingerprint` extended with `(str(path), mtime_ns, size)` PNG entries; derivation (needs JSON parsing) memoized in `_png_paths_cache` keyed by the JSON stat fingerprint → steady-state per-request cost stays stat-only. Only the focal map's OWN tileset is resolved: pool membership requires exact tileset_id match (per the 2026-07-12 parity fix), so a sibling-tileset union would be dead code; sibling JSONs stay fingerprinted as before. (b) `map_viewer_server.py` self-restarts via `os.execv` when any imported repo source changes: watch set re-derived from `sys.modules` each 1s poll (covers sibling scripts + exactly the `rpg2gba.*` modules actually imported; lazy imports join later polls; watch-set GROWTH deliberately ≠ change, else restart loop), 0.3s settle, EADDRINUSE bind-retry ×20, `--no-watch` opt-out. Known limit: interpreter flags (`-u` etc.) are NOT preserved across the exec (argv rebuilt from `sys.argv`); env vars survive — use `PYTHONUNBUFFERED=1` when capturing output. Live-verified end-to-end: touch `palette_page.py` → exec-restart + rebind + still serves; touch ts22 sheet PNG → "on-disk inputs changed since load, reloading" on next `/api/map/32`; unchanged third request = cache hit (0.16s vs 1.6s), no reload. +33 tests (`test_map_viewer_fingerprint.py` 20, `test_map_viewer_watch.py` 13); full suite **1024 passed / 14 skipped / 0 failed**; ruff clean. `PROJECT_TODO.md` #12 → Done (file newly committed — it's the cross-cutting post-slice checklist, created 2026-07-13).

**2026-07-11 (Map viewer REFOCUSED: quant-debug tool → tile-feedback tool — `scripts/map_viewer_{common,server}.py` + `reference/map_viewer.md`; delegate + grill-me):** User pivoted the viewer from graphics/quant debugging to giving feedback on specific tiles + groups. Design settled via grill-me: (a) feedback = per-map punch-list of `{cells:[[x,y],...], note}` flags I read + fix (**bare prose**, no category/severity); (b) selection = **tap-to-toggle accumulating set** (drag still pans, 2-finger zooms — NO drag-paint/marquee) + **Expand similar** (additive, multi-identity — adds every cell sharing the focus cell's `colkey_idx`) + Clear; last-tapped = focus cell (drives inspector); (c) quant controls **collapsed** behind an **Advanced** toolbar toggle (`body.adv` gates the knobbar, Diff/Priority/Merge overlays, Palettes link + `/palettes` page, Worst-merges panel, per-cell Palettes+quadrant-fit) — leaving Advanced clears the debug overlays; kept core-visible = RPGMaker↔Quantized toggle, Collision/Events/Warps, zoom/pan, trimmed inspector; (d) **default view = Quantized(family)** (was RPG Maker); (e) flags persist to **git-tracked `reference/map_feedback/MapNNN.json`** (list schema) NOT gitignored `output/…/map_viewer_issues/` — hand-authored review data must survive `rm -rf output/`. Server: `/api/issue`+`/api/issues/<id>` → `/api/feedback` (POST full list) + `/api/feedback/<id>` (GET); `load_issues/save_issues` → `load_feedback/save_feedback`. **Fixed a latent pre-existing breakage found during verify:** `build_map_data` called `our_blocked()` with the pre-2026-07-09 signature (missing the `terrain` arg) → viewer couldn't render ANY map; one-line fix (the sibling `uranium_blocked` call was already updated, so it was a partial fix left behind). Verified: endpoint round-trip (save→persist→reload→400-on-bad-payload), embedded-JS `node --check`, every `getElementById` id resolves, static build of Map032 renders (3.5MB, feedback panel present, Quantized default, old issue-panel gone), ruff clean. `map_viewer.md` rewritten (Sonnet sub-agent) + its stale slice-pool caveat corrected to per-map. **UNCOMMITTED.** NEXT: user walks the refocused viewer (server over Tailscale, or Taildrop a static `Map032.html`).

**2026-07-11 (test debt: fix the 2 known MAP_MOKI_TOWN failures — SLICE1_TODO #10):** User pointed at `SLICE1_TODO.md` #10 for the two failing tests. Two bugs, first masking the second. **(1) False vanilla-collision (the tracked one):** `test_build_slice_constants`/`test_build_slice_maps_smoke` passed `fork_path=None` to `map_constants.build_map_constants` intending an isolated fresh mint, but `None` falls back to `$RPG2GBA_POKEEMERALD`, and `engine/` is a *built* fork whose generated `include/constants/map_groups.h` already `#define`s this slice's own `MAP_MOKI_TOWN` etc. from a prior real assemble — the fresh mint saw its own output as a false "vanilla" collision. First pass fixed it at the test level (`fork_path=<nonexistent dir>`) — green, but weaker (no longer exercised real collision detection at all). **User asked for the real fix:** rewrote `load_vanilla_map_consts` to prefer git-HEAD content over the working tree (`_load_vanilla_map_ids_pristine`, new), mirroring `conversion_agent.fork_index`'s pristine-read pattern for the identical root cause (working-tree contamination). Discovered en route: `include/constants/map_groups.h` is itself upstream-gitignored/build-generated even in pristine pokeemerald-expansion — no pristine version of it exists to read. Reconstructed the vanilla `MAP_*` set instead from what IS committed — every `data/maps/<Dir>/map.json`'s `"id"` field, pulled via one `git archive HEAD -- data/maps` (~900 dirs, one subprocess) — and confirmed it correctly excludes Uranium map dirs (never committed; `data/maps/*/` is gitignored repo-root-side). Both tests reverted to `fork_path=None` (now safe); +3 unit tests for the new reader + its git-unavailable fallback. This is a production fix, not test-only — `assemble_pathfinder`/`phase5`/`stage_slice_scripts` all call the same `build_map_constants`/`load_vanilla_map_consts` and get it for free. **(2) NPC gfx (the bug bug-1 was hiding):** `test_build_slice_maps_smoke` never passed `npc_gfx` to `mw.build_slice_maps`, required since the 2026-07-06 NPC-gfx-map landing for any visible NPC (e.g. Map032 EV008 "PU-Chyinmunk") — fixed by loading the real `reference/npc_gfx_map.json` against the built fork's `event_objects.h` + `uranium_event_objects.gen.h`, `skipif` extended to skip cleanly when either is absent. Suite: **964 passed, 14 skipped, 0 failed** (was 959 passed + 2 known failures). Updated `SLICE1_TODO.md` (#10 → Done) and the matching 2026-06-19 Decision + Open Question entries. Ruff clean. Committed as `d2af838c`.

**2026-06-23 (map-viewer browsable UI — Phase A nav backbone — branch `deterministic-expansion-strategy`; delegate + caveman mode):** Side-task off the §9 graphics gate: make the map viewer navigable. Scoped with user → **2 phases**; **delivery = live server only**, **overworld `connections.dat` skipped** (both user picks). **Phase A built + verified this session.** New `scripts/map_graph.py` (pure metadata, no rendering; delegated to Sonnet): `build_index()` = flat id+name list + RMXP `parent_id` forest; `map_relationships(id)` = name / parent / children / warp-targets. Names resolve `reference/map_name_overrides.json` (override wins) → `map_infos.json` `name` → `Map{id:03d}`; warps = distinct **code-201 literal** dests (`parameters[0]==0`, self excluded). 13 tests. Wired into `map_viewer_server.py` myself (the cross-file stitch): rewrote `_serve_index` → searchable name+tree **landing page** (`_INDEX_HTML`, client-side filter + collapsible tree render); `_serve_map_page`/`_serve_palette_page` configs now carry a `"graph"` key. Added a **server-only cross-map nav strip** to BOTH templates (`MAP_VIEWER_HTML` in `map_viewer_common.py`, `PALETTE_VIEWER_HTML` in `palette_page.py`) — `#mapnav` div + `renderMapNav()` reading `V.graph`: chips ⌂Index · current · **up**(parent) · **sub**(children) · **warp→**(dests), each → `/map/<id>`; hidden when `V.graph` absent (static mode untouched). Verified: ruff clean (4 scripts + test; also auto-wrapped a pre-existing I001 in `map_viewer_common` imports), 13 map_graph tests pass, **live HTTP round-trip** (`/`, `/map/32`, `/palettes/32` all 200, graph injected, heavy `build_map_data` path intact). Docs: `reference/map_viewer.md` (new "Landing page & cross-map navigation" section + architecture/route updates). Uncommitted. **Phase B (next, separate session):** "view a whole multi-floor cave/building on one page" composite — needs a floor-group heuristic (`parent_id` chain + same name) + a small override file; the fuzzy/risky bit, deliberately isolated. Each floor is currently its own per-map view (free). **Main pipeline still AWAITING the 2026-06-21 §9 boot gate** (boot `engine/pokeemerald.gba`, walk Map49↔48↔32).

**2026-06-20 (image pipeline step 4 — palette quantization — branch `deterministic-expansion-strategy`):** Picked up the graphics task after a session refresh (delegate mode). Resolved the 3 stopped-on user decisions (all recommended): **add numpy**, **per-tile alpha classify** (sparse-AA→threshold / shadow→stipple), **confirm full-town first**. Built **step 4** = `tileset_converter/graphics/quantize.py` (numpy; `pyproject` `numpy>=1.26`): `to_5bit` BGR555, `classify_tile`/`resolve_alpha` (per-tile binary alpha), `build_quantized_tileset` = **joint per-8×8 Lloyd quantizer** — greedy FFD tried first, reproduced the §9 "53-palette / 6.9-shift" artifact, so replaced. Validated via `scripts/quantize_moki_preview.py` (full Moki Town Map032/ts22, ≤13-palette set). **Hit THREE render bugs this session, ALL found by the USER LOOKING (every one was invisible to the pixel-averaged mean-shift metric — I wrongly called the first "near-identical"):** (a) **paths→green** (area-weighted Lloyd seeding dropped the small-area path tan) → fixed by distinct-colour seeding; (b) **black tree-tops** (canopy edges mis-classified as stipple kept Uranium's ~black transparent-pixel RGB) → fixed by classifying on **solid-body fraction** not partial-alpha fraction; (c) **orange/white tree-shading** (Lloyd's median-cut refit snapped minority dark-green to nearest in an incoherent palette). Bug (c) exposed Lloyd as the WRONG ALGORITHM: a per-tile colour census (`scripts/tile_color_census.py`) showed 99.1% of 8×8 tiles already have ≤15 colours → this is **15-colour BIN-PACKING, not colour clustering**. **Rewrote the quantizer as a two-phase palette packer** (global similarity-merge vocab → agglomerative bitmask union-merge of tile colour-sets; local-reduce only overflowing palettes). AFTER all three: **on-screen shift 0.95/31, matches by eye** (trees solid green, paths tan, flowers pink, no blotches). Diag tools built: `quantize_diag.py`, `tree_alpha_diag.py`, `tile_color_census.py`. 30 quantizer tests + 49 graphics suite pass, ruff clean. **DURABLE LESSONS: validate graphics by EYE not a mean metric; diagnose via worst-offender true→mapped colours; measure the problem's structure before choosing the algorithm.** (Superseded by the 2026-06-21 faithful-column rework + emitter.)

**2026-06-20 (image pipeline steps 1–3 — branch `deterministic-expansion-strategy`):** Started the "switch the pathfinder slice from Emerald placeholder tiles to real Uranium graphics" task. Locked decisions with user: **scope = tilesets only** (player/NPC sprites are a separate step), **autotiles = faithful variant assembly**; plus defaults (new `gTileset_Uranium_*`, dedicated primary+secondary pair, 8×8 dedup, `MB_NORMAL` except warps, degraded-first palettes). Built + validated **image-pipeline steps 1–3** in new `tileset_converter/graphics/` (pure-PIL): `sources` (tileset_id→PNG+autotile paths; needed `deserialize.rb shape_tileset` to emit `autotile_names`, re-dumped `tilesets.json`), `autotile` (faithful flattener, `AUTOTILE_TABLE` verbatim from Uranium's `039_TileDrawingHelper_v17.rb`; variant baked into map data), `raster` (tile_id→16×16 via crop/flatten + validated ÷2). Recon findings: both slice tilesets are clean 2× upscales (exact pairwise uniformity; a box-vs-nearest proxy misleadingly read 62% for ts22 — ignore it); ts19 base-336 = empty-autotile decorative cells (transparent, wire step must fall through). **Validation: `scripts/render_slice_tiles.py` reconstructs Map049/048/032 via 3-layer compositing → both render as correct, recognizable Uranium art** (interior furniture + Moki Town grass/water/path autotile edges all right). 19 new tests pass; suite **435 passed / 11 skipped / 2 failed** (the 2 are the pre-existing built-fork MAP_MOKI_TOWN false-collision, NOT a regression). **STOPPED at step 4 (palette quantization)** per user — pending decisions on palette count/assignment, dithering, and the numpy/quantizer-lib dependency. Uncommitted. Docs: `reference/graphics_conversion_notes.md` §7b/§8.

**2026-06-19 (cutover to vendored `engine/` finalized — branch `deterministic-expansion-strategy`):** Validated the vendoring by building+booting the pathfinder slice on `engine/` (user: "it worked"; explicit in-game warp-fire/walk not separately reported — the S9 warp fix is presumed-but-unconfirmed). Finalized the cutover: `.env-paths` `RPG2GBA_POKEEMERALD` → `/home/b/repos/rpg2gba/engine` (user-edited); repointed the two scripts that hardcoded the old clone path (`harvest_bucket_metatiles.py`, `charmap_lint.py` → `engine/`); ran pytest = **416 passed / 11 skipped / 2 failed**. The 2 failures are a pre-existing slice-minting-vs-built-fork fragility (Decisions 2026-06-19), NOT a cutover regression. Old-clone `rm -rf` was classifier-blocked → user must run it. Feature work (slice art/NPC gfx, transpiler/fork-index) intentionally **paused**. Then (same session) implemented **vendor follow-up (b): include-hooks** so the assembler no longer edits tracked engine files in place — committed sentinel hooks in `event_scripts.s` + `map_data_rules.mk`, all per-slice content in gitignored `*.gen.json`/`uranium_includes.inc` overlays; tracked manifests now stay pristine after assembly (Decisions 2026-06-19). Verified short of a full `make modern`. **Next:** user runs `make -C engine -j16 modern` to re-confirm the build, deletes old clone; then resume BUILD_PLAN — fork capability index, or finish the slice's art/NPC gfx to make slice 1 genuinely playable.

**2026-06-18 (strategy pivot + doc/memory consolidation — branch `deterministic-expansion-strategy`):** Settled the pivot with the user: **retire the LLM as the conversion spine** for a deterministic command→Poryscript **transpiler** (Option B — generalize `deterministic._dialogue_body`); operate in **vertical playable slices** (each complete incl. quantized art, frontier-widening); add a **fork-index verification guard** (forward: every emitted symbol resolves; reverse: no "needs custom C" without a fork search — verified `HealPlayerParty` was a real special the agent had invented as `healparty`; fork exposes 624 specials + 385 event macros). Recorded it: new **`BUILD_PLAN.md`** (active build sequence, absorbs PATHFINDER/ITERATIVE/OQ3), ROADMAP Phase-4 rewrite + Operating-Model/verification-guard sections + Phase-5 quantize-art, CLAUDE.md rework, this MEMORY restructure (Current Phase shrunk), and 19 root docs → 5 + `reference/archive/`. **No converter code changed.** Next: execute `BUILD_PLAN.md` step 1 (fork capability index).

**2026-06-13 (OQ-3 move-route determinism — Step 1 executed, branch `deterministic-expansion-strategy`):** Built `scripts/moveroute_coverage.py` (zero-spend, mirrors `idiom_frequency.py`) to answer OQ-3 from `OQ3_EMPIRICAL_PLAN.md` — is the RMXP move-command → pokeemerald `applymovement` *vocabulary* deterministic? Census reproduces the plan anchors exactly (**3,804 `209` routes / 1,191 events / 531 player-only**). **Answer: conditional.** Raw 1:1-macro determinism = **24%** (295/1191) — refutes the §5.5 "~70%" as the *raw* rate. But the residue is NOT scattered: the 24%→**65%** gap is ~5 recurring SOFT-C side-effect/property commands (through 37/38 = 1,757 ev, opacity 42 = 527, SE 44, graphic 41, on-top 39/40) — exactly the §5.5 drop/hoist/approximate families. Irreducible HARD tail = **411 ev** (relative/random turns, diagonals, toward/away-player, step fwd/back, script). Jump (14) effectively deterministic (174/205 = `(0,0)` jump-in-place). **158 player-only events are fully-det under A/B macros TODAY = immediate Group-3 reclaim.** Results appended to `OQ3_EMPIRICAL_PLAN.md §7`; ruff clean; uncommitted on the branch. **Next (budget-gated, AWAIT USER): Step 2** = narrow ~30–50-event calibration stratified across those 5 SOFT-C commands (validate drop/hoist/approx preserves Opus behavior), then Step 3 partition → OQ-R4 scoping.

**2026-06-05 (sign validation + bulk-run harness):** **(1) `\sign` deferred-bucket VALIDATED** (3 budget-gated Opus spawns; plan §12 updated). Frozen-Opus emits plain `msgbox("…")` + `lock`/`release`/`end` for sign events — it **strips** `\sign[id]` and uses **no `MSGBOX_SIGN`** and **no `faceplayer`**; our pre-validation `MSGBOX_SIGN` guess was wrong. Faithful determinization is therefore a 7th classifier (`classify_sign_dialogue`, no-faceplayer wrapper, skip text with embedded `"`), not a `_TEXT_SUBS` one-liner. Registry unpolluted (signs mint nothing); throwaway artifacts cleaned. **(2) Bulk-run harness built** (no budget spent; 224 pass, ruff clean on touched files). New: `backends/__init__.py` `RateLimitError`(weekly flag)/`BackendTransportError`; `claude_code` detects usage caps in CLI output → raises `RateLimitError` immediately (no retry), retries transient transport failures (exp backoff, `max_attempts=3`) → `BackendTransportError`, and appends per-spawn token/cost to `token_usage.jsonl` (`usage_log_path`); **orchestrator `_convert` now re-raises both instead of queuing** (was: a rate-limit would poison every remaining event as a permanent "backend error") — so a limit aborts cleanly + resumes via checkpoints+memo. `conversion_agent/run_report.py` (collect/format stats). `pipeline._phase4_backend(usage_log_path=…)`. Three scripts: `scripts/prep_bulk_run.py` (destructive pristine reset to baseline — run ONCE before first run), `scripts/run_bulk.py` (no-arg = run-until-limit-then-stop; `--timed` = 5h-window loop that waits out the rolling limit and resumes, weekly limit stops; always resumes next unfinished task; frozen default model `claude-opus-4-8`), `scripts/run_stats.py` (progress+tokens anytime). New `tests/test_bulk_run.py` (9). (Sign classifier `classify_sign_dialogue` was later built; bulk run later kicked off → 7/199.)

**2026-06-04 (§9 #2 gate closed):** Committed backlog: `_reinstantiate` label-prefix fix (2 new tests), pre-freeze prompt pass (`system.md` move-routes rule + Multi-Page Events correction), script-call dictionary expanded to ≈92% coverage. Merged `phase4-conversion-agent-machinery` into main (fast-forward). Added per-event INFO logging + `scripts/calibrate_map002.sh`. Re-ran Map002 calibration with Opus under the restructured prompt: 10/12 converted, 2 decorative skips, 7 queue entries all expected, EV007 `copyvar` correct, cache engaged (~$1.48). **§9 #2 gate approved by user** — frozen config: `claude-opus-4-8` + `system.md` @ `03dda3f`. EV011 `setTempSwitchOn` unconditional-emit accepted as Option A (Phase 5 concern, TEMP flag).

**2026-06-03 (Phase 4 conversion-pipeline dedup A+B+C):** Implemented the whole `PHASE4_DEDUP_PLAN.md` (3 levers, no budget spent — all tests use MockBackend) + committed a backlog of prior uncommitted work. **Commit 1** bundled the prior **temp-switch minting** + Map002 calibration fixes (intermixed in shared files) with **dedup A — convert common events once**: `orchestrator.convert_common_events()` (flat-`list`→page-shape, `common_event_id` tag, one `CommonEvent_<NNN>` block, no self/temp mint), `system.md` Common Events section, `phase4 --run` does CEs before maps + a `convert-common-events` debug cmd. **Commit 2 — dedup B:** split `prompt_builder.build_prompt` into `build_static_context` (→ cacheable backend system prompt, composed in `pipeline._phase4_backend`) + `build_user_prompt`; `claude_code._log_cache_usage`. **Commit 3 — dedup C:** persisted event memo (`_memo_key` = payload − map_id − id; reuse rewrites SS/TS flag names + guard + compile-gate, fail-safe to spawn; `memo_manifest.json` fingerprinted by system prompt; `--clean` wipes it). **130 pass + phase4 marker, ruff clean, dry counts unchanged (8/5/34/199).** Stopped before any budget-gated live run.

**2026-06-03 (fair Sonnet-vs-Opus Map002 comparison — §9 #2 gate, budget spent, user-authorized):** Ran the controlled snapshot/restore Map002 comparison under the **dedup-B-restructured prompt**. Both models ran from the same pristine baseline into isolated `output/uranium-build/compare/{sonnet,opus}/`. **Both compile through real poryscript (rc 0); both queue the same 5 untranslatables.** **Conclusion — Opus is the higher-fidelity choice** and the dedup-B restructure did **not** regress quality: EV007 decisive (Opus `copyvar` of var 102 → `VAR_TEMP_OWTEN_FACT_CHOICE`; Sonnet wrongly `addvar(VAR_RESULT,1)` dropping the real var); Opus uses `elif`, strips plumbing SE, no orphan flag / no `# CHECK` noise. **Both dedup live confirmations** (Sonnet, ~$1.13): CE pass emits compiling `CommonEvents.pory`; 2-map memo run (Map100→115) fired a cross-map memo hit + confirmed the prompt cache engages (`cache_read=13666`). **The memo confirmation caught a real bug** — `_reinstantiate` wasn't rewriting the `Map{NNN}_` label prefix → cross-map reuse emitted the source map's label (assembly-time collision the compile-gate can't catch); fixed + tested (131 pass, ruff clean).

**2026-06-01 (rung-2/3 feasibility spike + self-switch minting):** Proved a real agent-converted Uranium event runs correctly in-game (**rung 3 — the core feasibility risk, now RETIRED**): converted **Map031 ev48** (talk-once tutorial NPC) via Opus, wired into the fork (repurposed `OldaleTown_House1` NPC + new-game spawn repoint + truck-cutscene fix + Birch-intro skip), user **confirmed talk-twice + flag persistence** on the phone (Taildrop). Spike then reverted; fork clean on `master`. Full detail in Current Phase. **Surfaced + fixed the self-switch flag gap (Layer 1):** the agent emits `FLAG_MAP..SS*` but never registers it and the registry couldn't mint it → undefined at assembly for ~860 events. Added `flag_registry.mint_self_switch` (deterministic/idempotent, keyed map/event/letter, persisted, `dump_header` emits `RPG2GBA_SELFSWITCH_BASE`, bases now params) + orchestrator `_event_self_switches` derivation — **frozen prompt unchanged**. **108 pass, ruff clean.** **Layer 2 (flag-budget overflow → fork flag-space expansion) documented as a Phase 7 task** (ROADMAP 7.4 + `reference/flag_registry_policy.md`). **Next: §9 #2 calibration gate — Map002 iterate-to-green (budget-gated, confirm with user).**

**2026-05-25 (Phase 4 — conversion-agent MACHINERY, code complete):** Built the whole Phase 4 machinery against `PHASE4_PLAN.md` (scoped machinery-only) in one session. Filled the Phase-1 stubs: `flag_registry.py` (pre-seed/mint/validate/persist/dump_header + `validate` CLI), `orchestrator.py` (per-event loop, injectable `compile_fn`, idempotent checkpoints, unhandled queue + `triage`), `backends/{claude_code,ollama}.py`; added `poryscript.py` (compile-gate) + `prompt_builder.py`. Authored `reference/essentials_to_emerald_map.md`, `reference/poryscript_cheatsheet.md`, 3 `prompts/few_shot/*.md`; reconciled `ConversionResult` to system.md's list schema. Wired `pipeline.py phase4` (dry by default; `--run` gated/spends budget) + `convert-map`. New `tests/test_conversion_agent.py` (22 tests: registry collision/validation/roundtrip, backend parse w/ mocked subprocess, orchestrator retry/queue/idempotence w/ MockBackend+fake compiler, prompt assembly) + `phase4` marker. **Suite 97 pass / 1 skip (compile-gate, poryscript absent), ruff clean.** **Key finding:** the obvious badge names `FLAG_BADGE0N_GET` already exist in the fork — the registry's live fork-collision check caught it; badge→vanilla mapping deferred to §9 (fidelity call). `phase4 --clean` dry path: 8 flags + 5 vars pre-seeded, 34 script-switches blocked, 199 maps pending. **Next:** install poryscript (P2), then the §9 #2 calibration gate (budget-gated — confirm with user).

**2026-05-25 (Phase 3 — Map Deserialization, code complete):** Implemented the whole phase in one session against `PHASE3_PLAN.md`. **§3.0:** built `deserialize.rb`'s `rxdata` branch (folded recon_maps.rb's nested RPG stubs; added `marshal_load_lenient` for the nested-`const_missing` gotcha), explicit container shaping into the Phase 4 raw-command contract. **§3.1–§3.4:** new `src/rpg2gba/map_deserializer/` package (`driver`/`validate`/`command_catalog`), wired `pipeline.py phase3 --clean`. Conservation **exact** (199/5301/8429). Emitted committed `reference/rgss_event_commands.md` (59 codes, 250 script-call signatures) + switch/var sidecars (235/119 named). **Tests:** `tests/test_map_deserializer.py` (9: conservation, schema, no-merge, coverage, 4 golden maps, idempotence) + `phase3` marker; full suite 75 pass, ruff clean. **Key finding:** Uranium adds **no custom command codes** — all custom behaviour is in Script commands (355/655) as `pbXxx` calls; the §3.2 doc inventories these 250 signatures as Phase 4's real translation surface. **Exit gate V1–V3+V6 done;** V4 (user map spot-check) + V5 (review command reference) remain advisory. **Next: Phase 4.**

**2026-05-25 (V5 gate passed + env/toolchain setup; Phase 2 logically complete):** Session via `/phase2-walkthrough`. **Setup:** installed devkitPro/devkitARM 15.2.0 + `libpng-dev`, fork builds clean (`make -j16 modern`); renamed tracked `.env`→`.env-paths` (collided with `Read(**/.env)` deny rule) + pointed `_load_dotenv` at it (commits `6187832`); relocated Uranium tree to sibling `/home/b/repos/uranium-src` + updated `RPG2GBA_URANIUM_SRC` (`31eaf7c`). **Gate:** walked §2.0–§2.10, resolved the 4 P4 fidelity decisions (see Decisions), implemented the 2 needing code (hidden-ability sidecar+needs_engine; species-201 unobtainable sidecar+comment) in `pokemon.py` (+1 gate test, 66 pass, idempotent — commit `6fdff92`); ran the worklist/inventory scan + 4 artifact spot-checks (all clean); added the CONVERTED/STRIP/DEFER status table to `uranium_dat_inventory.md`. **Key finding:** the V6 "isolated compile-check" is architecturally fused with the species-roster swap (constant-name collisions; fork config headers need vanilla constants) → **exit #3/#4 deferred to Phase 7** (see Decisions), no fork changes made. **Pending commit:** `uranium_dat_inventory.md` + `MEMORY.md`. **Next: Phase 3 (Map Deserialization).**

**2026-05-23 (§2.5–§2.10 — Phase 2 converters DONE):** Finished the last six sections. **Step 0 prep** (committed `d8eb688`): added `WordArray._load` to `deserialize.rb` + the shared `_marshal.py` (`dump_dat`/`load_json`); verified all 6 Marshal dats dump clean. **§2.5 tm_hm** + **§2.6 trainers** done on the main session (committed `17b92fc`); **§2.7 encounters, §2.8 metadata, §2.9 tmpbs, §2.10 types** done sequentially on main (the earlier parallel worktree fan-out was killed by a session limit and produced nothing durable except the §2.9 agent's `tmpbs.py`, which leaked into main with a `load_fork_constants("MOVE_")` bug — rewrote it clean). Format corrections vs the plan: tm.dat is **Marshal+WordArray** (not indexed), tutor.dat **empty**; trainers/teachables/encounters are fork **build-generated**, so §2.5/§2.6 emit constants-keyed **intermediate JSON** (V6 generates the generator inputs / `.party`), per the user's "stick to PHASE2_PLAN.md + MEMORY supplements" steer. See per-section Key File Notes. Full `phase2 --clean` ×2 `diff -r` clean (idempotent); **65 phase2 tests pass** (+20 new), ruff clean. §2.5/§2.6 committed; §2.7–§2.10 + doc updates **pending commit**. **Now at the V5 / §9 #1 manual review gate** — STOP, awaiting user review before the fork drop-in (V6, still devkitARM-blocked).

**2026-05-22 (§2.4 abilities):** Completed §2.4 abilities via the `/pbs-convert` skill. `abilities.py` emits the **Uranium-original ability constants** — `PBAbilities` sidecar entries (210) whose `to_constant` is absent from the fork enum, a clean contiguous block **ids 192–210 (19 originals)**. Key call: the plan assumed a dexdata in-use scan, but that misses **form-only abilities** (CHERNOBYL=209 URAYNE form 2, NIGHT_TERROR=210); switched to the sidecar-vs-fork definition, kept `collect_ability_ids` as a fail-loud cross-check. Emits `include/constants/abilities.h` (originals only), a placeholder TU `src/data/abilities/uranium_abilities.c` (no literal stubs — fork effects are inline in battle scripts), and `intermediate/ability_codes.json`. §2.1 marked 17 needs_engine; §2.4 → 19 (idempotent union). Idempotent; **45 tests pass** (4 new §2.4), ruff clean.

**2026-05-21 (§2.3 items):** Completed §2.3 items. `items.dat` turned out to be Essentials `writeSerialRecords` TLV format (not the simple fput indexed binary the plan assumed) — corrected the schema in PHASE2_PLAN.md. `items.py` parses 607 items and emits `gItemsInfo[]` (`src/data/items.h`), `ITEM_*` defines (`include/constants/items.h`), and the `intermediate/item_field_codes.json` Phase 6 worklist. Mapped only deterministic fields (name/price/desc/pocket/importance); **deferred all item behavior to Phase 6** (mirrors §2.2/D3). 111 needs_engine. Hit two shared-code fixes: (1) `dump_constants.rb` all-caps regex dropped `POKeBALL=211` and ~70 mixed-case trainer classes — loosened regex, regenerated sidecars (trainer_class sidecar 60→130 entries, **matters for §2.6**); (2) added diacritic folding to `_naming.to_constant` so "Poké Ball"→ITEM_POKE_BALL. Verified idempotent (`diff -r` clean across two runs). **41 tests pass** (5 new §2.3), ruff clean. Pickup: §2.4 abilities.

**2026-05-20 (§2.2 moves):** Fixed the `messages.dat` mojibake in `dump_messages.rb` (UTF-8 bytes were being mis-transcoded as windows-1252) and regenerated all 22 sidecars; re-pinned the §2.1 golden fixture to the corrected `Pok\xE9mon`. **Completed §2.2 moves:** `moves.py` parses flat 14-byte `moves.dat` (637 nonzero of 639) and emits `gMovesInfo[]`, `moves.h` (`MOVE_*` defines), and the `move_function_codes.json` Phase 6 worklist. Mapped target (PBTargets→TARGET_*), category, type, and positive flags faithfully; **deferred all move effects to Phase 6** via `EFFECT_PLACEHOLDER` + worklist (see Decisions) — 324 function codes have no clean map. 9 Nuclear moves flagged needs_engine (32 moves total). Output idempotent (`diff -r` clean across two full `phase2 --clean` runs); id_map now 201 species / 637 moves / 19 types. **9 phase2 tests pass** (4 new §2.2: roundtrip, golden Tackle+Atomic Punch, effect-placeholder/worklist, Nuclear needs_engine), ruff clean. New working-pref memory: prefer temp script files over inline multi-line shell. Pickup: §2.3 items. **Phase 2 manual review gate (§9 #1) still NOT reached — it's at the END of Phase 2.** Not yet committed this session.

**2026-05-20 (§2.1 species):** Cloned the pokeemerald-expansion fork
(`/home/b/repos/pokeemerald-expansion`, shallow, HEAD `21c24202`; `make modern`
not yet run — needs devkitARM). Added `.env` (gitignored) +
`pipeline.py::_load_dotenv()` so `RPG2GBA_URANIUM_SRC`
(`/home/b/Pokemon_Uranium_132/_unpacked`) and `RPG2GBA_POKEEMERALD` resolve
without shell exports; `.env.example` committed. Did P4 (struct verification →
`reference/pokeemerald_struct_shapes.md`). **Completed §2.1 species C-emit:** new
`_naming.py` (shared name→constant rule + fork-enum loader), rewrote evolution
parser (0x3F mask + 0xC0 forward-only filter), and `pokemon.py::run()` emits
`species.h`, `species_info.h` (designated initializers, inline evolutions),
`level_up_learnsets.h`, `egg_moves.h`, `intermediate/tandor_dex.json`. Output
validated (Orchynx Grass/Steel→Metalynx@28, Urayne Nuclear/genderless),
idempotent (`diff -r` clean), needs_engine = 27 moves/17 abilities/7 items/166
species. **37 tests pass** (5 new §2.1). Found the `messages.dat` sidecar mojibake
bug (fixed in §2.2). Pickup: §2.2 moves (`moves.py`). Added working-pref memory:
don't `cd` to the current dir in Bash calls. Allowlist expanded in
`.claude/settings.local.json`.

**2026-05-18 (afternoon):** Wrote PHASE2_PLAN.md (also copied to
`/home/b/.claude/plans/`). Implemented Phase 2 §2.0 scaffolding: `_binary.py`
(DatReader + parse_indexed + Essentials varint string decoder), `_id_map.py`
(single-source-of-truth for SPECIES_*/MOVE_*/etc., fail-loud on conflict),
`_c_emit.py` (escape/banner/header-guard helpers), extended `deserialize.rb` with
`dat <in> <out>` mode (Marshal-format `.dat` → JSON), wired `pipeline.py phase2
--clean` with lazy converter discovery, and `scripts/dump_messages.rb` to extract
the `messages.dat` strings to 22 sidecars under `reference/`. **27/27 unit tests
passing.** Found species 201 = "Gengar" via the names dump. Pickup point:
implement §2.1 (`pbs_converter/pokemon.py`) — parse `dexdata.dat` (76-byte flat
records) + aux files (`attacksRS.dat`, `evolutions.dat`, `eggEmerald.dat`,
`tutor.dat`, `regionals.dat`, `metrics.dat`) and emit C. The fork is **not** set
up yet — V6 (fork drop-in build) deferred until it is.

**2026-05-18 (morning):** Closed Phase 0 and ran the `.dat` deserialization
spike. Confirmed: species data in `dexdata.dat` (76 bytes/species, 201 species);
level-up learnsets in `attacksRS.dat`; `tmpbs.dat` = Uranium-custom extra move
list; Shadow Pokémon STRIP confirmed (0 TPSHADOW hits, `shadowmoves.dat` empty);
Tandor dex = 200 entries; two distinct binary formats (custom Essentials binary
vs Ruby Marshal). Updated `uranium_dat_inventory.md` throughout. Spike script at
`scripts/spike_dat_inventory.rb`. Phase 2 is now unblocked.

**2026-05-12:** Phase 0 verification pass. Walked the user through the seven
Phase-0 deliverables. Then attempted the four quick-win + two medium verification
items from Open Questions via three parallel Opus sub-agents. First-round agents
reported several files as "0 bytes / empty stubs" — investigation revealed a
**dumper encoding bug** in `recon_scripts.rb`: 85 of 260 sections had failed to
write because `File.write(out, source, encoding: 'utf-8')` raised on Windows-1252
bytes. Fixed by transcoding `Windows-1252 → UTF-8` and using `File.binwrite`;
re-ran the dumper (all 260 sections now valid; 40 genuinely empty). Re-spawned the
three Opus agents against the corrected dump. Applied verified updates to
`uranium_custom_features.md` (expanded Nuclear-move list to 9 codenames, corrected
Multiple Fogs #232→#223, rewrote Tandor Championship entry + downgraded
CONVERT→ADAPT, added Custom Mode and Gym 8 puzzle sections, updated Summary
Decision Matrix), `phase0_summary.md`, `uranium_dat_inventory.md`. Populated Flag
Registry Notes with 4 pre-seed candidates from gym-8 and championship scripts.

**2026-05-09:** Did Phase 0 reconnaissance. Wrote `scripts/extract_rgssad.py`,
extracted `Uranium.rgssad` to `~/Pokemon_Uranium_132/_unpacked/`, fixed two
pre-existing bugs in `recon_maps.rb` (RPG class nesting, `_load` vs
`marshal_load`) and one in `recon_scripts.rb` (allowlist normalization). Ran 4/5
recon scripts (skipped `recon_pbs.py` — no PBS source). Spawned a Haiku agent to
produce `reference/uranium_essentials_version.md`, `uranium_dat_inventory.md`,
`uranium_custom_features.md`, `phase0_summary.md`; rewrote the dat inventory after
spotting hallucinations (claimed `pokemon.dat`/`abilities.dat` that don't exist).
Updated ROADMAP.md (removed wrong "team open-sourced it" claim, restructured Phase
2 around `.dat` inputs instead of PBS text).

---

## Resolved Open Questions (breadcrumbs)

Conclusions that still matter live in MEMORY.md's Decisions Made or
Uranium-Specific Discoveries; these lines are kept only for provenance.

- **messages.dat sidecars double-encoded (mojibake).** Resolved 2026-05-20. Root
  cause: `messages.dat` strings are raw **UTF-8 bytes tagged ASCII-8BIT**
  (`Pok\xC3\xA9mon`, `SWIMMER\xE2\x99\x80`=♀); the old `dump_messages.rb` ran
  `force_encoding('windows-1252').encode('utf-8')`, reinterpreting those UTF-8
  bytes as Latin-1 → `Ã©`. Fix: `force_encoding('UTF-8')`, fall back to
  windows-1252 only if `!valid_encoding?`. Regenerated all 22 sidecars; §2.1
  golden fixture re-pinned to corrected `Pok\xE9mon`. (Encoding fact promoted to
  Discoveries; the still-live GBA-charmap downstream concern remains an Open
  Question.)
- **`pbCallBub` emote fidelity (Phase 4 follow-up).** Resolved 2026-07-05: not an
  emote at all — it repositions the dialogue window (floating FRLG-skinned textbox
  near the talking event); the real "!"/"?" bubbles are code-207 Show Animation,
  now mapped to native emote movement tokens. STRIP final; no fork analog exists.
  Corrected rationale in `reference/uranium_script_calls.md`.
- **Identity of species ID 201** — had no Tandor dex number in `regionals.dat`.
  Resolved 2026-05-18: "Gengar" — placeholder/Easter-egg slot. (See Discoveries.)
- **Which `.dat` holds the species table?** Resolved 2026-05-18: `dexdata.dat`.
- **Exact Tandor dex size.** Resolved 2026-05-18: 200 entries (regionals.dat),
  201 internal species IDs.
- **Confirm no TPSHADOW=true rows.** Resolved 2026-05-18: 0 hits in 331 trainers.
  STRIP confirmed.
- **tmpbs.dat unknown.** Resolved 2026-05-18: Uranium-custom TMPBS extra move list
  per species.
- **Re-examine previously-undumped sections for description accuracy.** Resolved
  2026-05-15 (followed from the recon_scripts.rb Windows-1252 dumper-bug fix).
- **Classifier near-misses (parked then resolved 2026-06-11).** The 133
  trivial-but-unclaimed events after the pre-filter fragment into 35 families
  (`scripts/near_miss_families.py`); decided Tier 1 only (~40 dialogue+Wait/SE
  events, rider on the STRIP-skip implementation), Tiers 2–3 declined. Conclusion in
  Decisions Made + `FABLES_DECISIONS.md`.

---

## Phase 4 Build Narrative (archived 2026-06-11 from MEMORY.md Current Phase)

Verbatim paragraphs retired when Current Phase was compressed to live state
(critique suggestion #7, `FABLES_DECISIONS.md`). Chronology: machinery → dedup
plan → rung-2/3 spikes → self-switch gate blocker → Phase 3 detail →
pre-filter build → pre-bulk-run task note → Phase 5 scaffold.

**Active phase:** Phase 4 — Event → Poryscript (conversion agent) **MACHINERY COMPLETE** (§4.1–§4.7; full suite **98 pass / 0 skip**, ruff clean). Plan: `PHASE4_PLAN.md` (scoped machinery-only). Built: `flag_registry.py` (pre-seed + mint + validate + persist + dump_header; CLI `validate` green), `poryscript.py` (compile-gate wrapper), `backends/claude_code.py` (PRIMARY, headless `claude -p` per event) + `backends/ollama.py` (optional fallback, lazy `requests`), `prompt_builder.py`, `orchestrator.py` (per-event loop: checkpoint→prompt→backend→commit→compile→retry-once→queue, idempotent), pipeline `phase4`/`convert-map` wired. Authored `reference/essentials_to_emerald_map.md` (pre-seed), `reference/poryscript_cheatsheet.md`, 3 `prompts/few_shot/*.md`. `phase4 --clean` (dry) pre-seeds **8 flags + 5 vars, 34 script-switches blocked**, 199 maps pending. **Poryscript 3.6.0 installed** at `/home/b/tools/poryscript/poryscript-linux/poryscript` (`RPG2GBA_PORYSCRIPT` in `.env-paths`; configs auto-passed via `-cc`/`-fc`); compile-gate test green. `claude` CLI present + flags verified (`-p`/`--output-format json`/`--model`/`--system-prompt` all exist). **`ClaudeCodeBackend` hardened:** denies all tools by default (`--disallowed-tools`); `--bare` + `--max-budget-usd` are opt-in knobs (**`--bare` forces ANTHROPIC_API_KEY auth — bypasses Pro OAuth**, so off by default). **Remaining for full Phase 4 = the §9 #2 gate:** calibration on 5 maps via headless CC (`convert-map`, spawns `claude` per event, **spends budget**) → iterate `system.md`+few-shots → freeze → user approval before any bulk `phase4 --run`. Plan = `PHASE4_CALIBRATION_PLAN.md`. **Part A DONE 2026-05-26** (prompt/reference content, no budget spent): authored `reference/uranium_script_calls.md` (~40-row MAP/STRIP/UNHANDLED disposition table for the top script-call signatures; `pbCallBub`=STRIP), wired it into `prompt_builder.build_prompt` (new `script_call_ref` chunk + `load_script_call_reference`) + `orchestrator` cache; fixed `system.md` stale refs (`src/rpg2gba/...`, CLAUDE.md) + added a Script-Calls section (STRIP≠queue rule); added 2 few-shots (`pokemon_center_heal.md` EV001 yes/no-heal pattern, `pokemon_center_pc.md` single-UNHANDLED stub). **Divergence flagged for gate:** `Kernel.pbSetPokemonCenter` tagged **STRIP** (not the plan's `→setrespawn`) — respawn is wired deterministically from §2.8 metadata HealingSpot, and `setrespawn` needs a map-specific `HEAL_LOCATION_*` the agent can't derive. Verified free: `ruff check` clean (3 touched files have a *pre-existing* blank-line-after-docstring `ruff format` deviation, left as-is), **pytest 100 pass**, `pytest -m phase4` green (real poryscript), `phase4 --clean` dry counts unchanged (8/5/34/199). **Part B DONE 2026-05-26** (free): added `--model` option to both `convert-map` and `phase4` (default `_DEFAULT_MODEL="claude-sonnet-4-6"`), threaded via `_phase4_backend(name, model)` → `ClaudeCodeBackend(model=...)` (ignored by ollama). 2 new tests (`test_claude_model_in_cmd`, `test_phase4_backend_threads_model`); **suite 102 pass**, ruff clean. The knob is the Sonnet-vs-Opus mechanism for Part D. **Opus model ID corrected 2026-05-29:** plan/ROADMAP/pipeline help/tests updated `claude-opus-4-7` → **`claude-opus-4-8`** (current strongest; the comparison runs against 4.8). **Part C DONE 2026-05-29** (free, no budget): ruff clean, **pytest 102 pass**, `pytest -m phase4` green (real poryscript), `phase4 --clean` dry counts unchanged (8/5/34/199). Smoke-test map picked via `scripts/pick_smoke_map.py` (ranks maps by event-page command count): **Map200** — 1 event / 6 commands, a clean auto-transfer doormat (SE 250 → tone-fade 223 → wait 106 → transfer-to-map-181 201 → restore → end), **zero `pbXxx` script calls** so it validates the pipe end-to-end at minimal spend. **Part D smoke test DONE 2026-05-29** (first live conversions — spent budget, user-authorized): Map200 converted end-to-end on **both** models — `claude` spawns, response parses, script **compiles through poryscript**, `.pory` + `unhandled.jsonl` flush. **Model divergence observed (early signal for §9 gate):** Sonnet emitted `warp(MAP_MAP181,…)` with a `# CHECK` flag (invents a constant but flags it) + queued the SE; **Opus refused to invent the `MAP_*` constant** — queued the transfer (201) citing fail-loud (§4.5/§6), stripped SE+wait as plumbing, added `lockall`/`releaseall` + richer queue descriptions. Opus is more principle-aligned but leaves the warp (the event's whole point) queued; Sonnet's flagged guess stays runnable. NOT a fair comparison (shared registry/appended unhandled.jsonl) — controlled snapshot/restore comparison is reserved for Map002. **Part D step 2 IN PROGRESS (Map002 iterate-to-green):** First **Sonnet** pass done 2026-06-01 (user-authorized budget) — **11/12 events**, quality good (`pbCallBub` stripped everywhere ✓; nurse-heal→`msgbox`/`MSGBOX_YESNO`/`healparty`, random-dialogue, conditional branches all correct; whole `.pory` compiles through poryscript). 5 issues → **5 calibration fixes applied (code only — re-run NOT yet done):** (1) **EV011 dropped** — branched on script-switch 22, agent proposed a flag → registry rejected → whole event lost; fix = `_registry_state`/`_render_registry` now pass blocked script-switch IDs to the agent + `system.md` rule "never propose FLAG_ for a script-switch; queue the branch"; (2) inconsistent warp placeholders (`MAP_MAP060` vs `MAP_ID_060`) → `system.md` rule `warp(MAP_URANIUM_<N>,x,y)` + queue (real constant = Phase 5); (3) inconsistent Call-Common-Event (117) → rule `call CommonEvent_<NNN>` (3-digit), don't queue; (4) `# CHECK` noise on standard macros → `system.md` affirms `random`/`addvar`/`copyvar`/`setvar`/etc.; (5) **empty decorative events** spawned the agent + emitted junk `script{end}` → orchestrator `_event_has_commands` skip (saves budget across 199 maps). **111 tests pass, ruff clean (src+tests).** Pristine baseline snapshotted at `output/uranium-build/flag_state.baseline.json`. **RE-RUN PAUSED — user low on Pro usage, avoiding a mid-run cutoff.** Resume (on budget): restore baseline → `rm unhandled.jsonl` → `convert-map --map-id 2 --model claude-sonnet-4-6` to confirm green → fair Sonnet-vs-Opus (snapshot/restore) → freeze prompt+model → §9 #2 user approval. **RE-RUN DONE 2026-06-01 (Sonnet, budget): all 5 fixes landed + verified** — EV011 NO LONGER dropped (script-switch 22 page-gate handled, conditional branch queued, rest translated ✓); EV005/EV006 ("left/right pokéballs", 7-page graphic-swap decoratives) skipped cleanly (no spawn, no junk ✓); warps consistent `MAP_URANIUM_60` (EV002+EV008 ✓); `CommonEvent_004/005/006` not queued ✓; no `# CHECK`/macro noise ✓; `pbCallBub` stripped ✓; whole `.pory` compiles (poryscript rc 0 ✓); registry cleanly proposed `FLAG_POKERUS_DETECTED`(sw3)+`FLAG_NURSE_AUTO_HEAL_PENDING`(sw5). 10/12 = 10 converted + 2 decorative-skipped (full accounting). **NOT yet rung-2-green — ONE new systemic gap surfaced (the fix to EV011 exposed it):** EV011 sets a self-switch via the **`setTempSwitchOn("A")` script call (code 355)**, which the agent mapped to `setflag(FLAG_MAP002_EVENT011_SSA)` — but the orchestrator's `_event_self_switches` only detects code 123 + page `self_switch_valid`, NOT the script-call form → flag NOT minted → **undefined symbol at assembly**. **`setTempSwitchOn` = 345× across 77 maps** (systemic, not long-tail). Map002 needs ONE more confirming Sonnet run after the fix, then the comparison. **DISPOSITION RESOLVED = TEMP-flag** (per-map-visit semantics); **temp-switch minting IMPLEMENTED + committed 2026-06-03:** `flag_registry.mint_temp_switch` (deterministic/idempotent, keyed map/event/key, persisted under `temp_switches`, `dump_header` emits `RPG2GBA_TEMPSWITCH_BASE`) + orchestrator `_event_temp_switches` (code 355/655 `setTempSwitchOn`/`tsOn?` family) mint on script-accept; `system.md` Temp-Switches section maps them to `FLAG_MAP..TS{L}` (TS≠SS) from the auto-reset TEMP range. Plus the EV011 fix (`_render_registry` now lists blocked script-switch IDs so the agent queues rather than proposes). Prompt extended (gate not yet frozen).

**Phase 4 dedup plan IN PROGRESS** (`PHASE4_DEDUP_PLAN.md` — A→B→C, all pre-freeze, budget-free to build; decisions locked with user). Closes 3 levers before the §9 #2 freeze: (A) **common-event call targets were never produced** — maps emit `call CommonEvent_<NNN>` (code 117) but nothing converted `common_events.json` → dangling symbols at assembly (same class as the self-switch gap); (B) static prompt context re-billed on every cold spawn; (C) identical events re-converted from scratch. **Phase A DONE 2026-06-03:** `orchestrator.convert_common_events()` adapts each flat-`list` common event → one-page shape, tags `common_event_id`, reuses the shared convert core (`_convert`) with NO self/temp mint (CEs use only global switches/vars — verified zero code-123/355/117 across the 100 CEs), emits one `CommonEvent_<NNN>` block → `scripts/CommonEvents.pory` (per-event flush + `CommonEvents.done` checkpoint); `system.md` Common Events section; `pipeline phase4 --run` converts CEs **before** maps + new `convert-common-events` debug cmd (repeatable `--ce-id` filter = partial run, skips checkpoint). 22/100 CEs have commands (incl. 3 big online-feature scripts GTS/VT/LOBBY, 185–219 cmds — likely mostly-queue, fidelity call deferred to the live run). Dry counts unchanged (8/5/34/199). **Phase B DONE 2026-06-03** (commit 2): `prompt_builder.build_prompt` split into `build_static_context` (cheatsheet+script-call-ref+few-shots — event-invariant) and `build_user_prompt` (filtered command-ref + registry + event JSON — per-event); `pipeline._phase4_backend` now composes the backend system prompt = `load_system_prompt()` + static context (stable → `claude -p` server-side prompt cache, 5-min TTL), for both claude_code and ollama; orchestrator builds only the user prompt (dropped its cheatsheet/script-call/few-shot loads). `claude_code._log_cache_usage` logs `cache_read/creation_input_tokens` from the CLI envelope to confirm caching engages (empirical; harmless if it doesn't). Same text, different channel — quality should be equivalent but **must re-run Map002 calibration after freeze** (gate note). **126 pass, ruff clean. Phase C DONE 2026-06-03** (commit 3): per-event memo in `orchestrator._convert_event` — `_memo_key` = sha256 of the payload minus `map_id`+`id` (keeps name/x/y/all commands, so only truly-identical events collapse → conservative hit rate by design); **miss** = spawn + store `MemoEntry`, **hit** = `_reinstantiate` (rewrite the deterministic `FLAG_MAP..SS*/..TS*` names to the current map/event for each letter/key the event uses) + **stale-token guard** (`FLAG_MAP{src}_EVENT{src}_` must not survive) + re-run compile-gate; any failure → fall through to a real spawn (fail-safe). On a hit, the entry's proposals + this event's mints are replayed so the registry stays correct. Persisted to `memo_manifest.json` fingerprinted by sha256 of the backend system prompt (system.md+static); fingerprint mismatch on load discards it (no cross-prompt reuse). `--clean` now also wipes the manifest. Common events are NOT memoized (~22, fixed). **130 pass + phase4 marker, ruff clean, dry counts unchanged.** **DEDUP PLAN A+B+C COMPLETE — stopped before any budget-gated live run.** Next: §9 #2 calibration gate (budget-gated — restore baseline, re-run Map002 to confirm quality under the restructured prompt, then fair Sonnet-vs-Opus → freeze → user approval). Live confirmations the plan flags: `convert-common-events` on a couple CEs + a 2-map memo-hit run (observe `cache_read_input_tokens`). **Map002 fair comparison DONE 2026-06-03** (budget spent; outputs in `output/uranium-build/compare/{sonnet,opus}/`): both compile (rc 0), both queue the same 5 untranslatables, restructured prompt did NOT regress quality. **Opus = higher-fidelity pick** (decisive: EV007 var-102 — Opus registers `VAR_TEMP_OWTEN_FACT_CHOICE` + `copyvar`, Sonnet wrongly `addvar(VAR_RESULT,1)` dropping the real variable; plus Opus uses `elif`, strips plumbing SE, no orphan flag / no `# CHECK` noise). Trade-off = Opus costs more/event over 5301 events — **open user decision.** **Both dedup live confirmations DONE 2026-06-03** (budget ~$1.13, Sonnet, user-authorized): (1) **dedup A** — `convert-common-events --ce-id 78 --ce-id 11` emitted `scripts/CommonEvents.pory` with correctly-labeled `CommonEvent_011`/`CommonEvent_078` blocks, compiles rc 0 (CE11 racing minigame queues picture-move/non-standard-ControlVar as expected); (2) **dedup B+C** — `convert-map 100` then `115` (Map100/115 share 1 identical event, EV003 pbPokeCenterPC stub): cross-map **memo hit fired** (`reused 100 ev3 for map 115 ev3`, no 2nd spawn), and **every spawn logged `cache_read=13666`** → the dedup-B server-side prompt cache engages. **BUG CAUGHT BY THE MEMO CONFIRMATION (now FIXED + tested):** `_reinstantiate` rewrote self/temp-switch FLAG names but **not** the `Map{NNN}_` script-block label prefix → the reused EV003 block in Map115.pory was labeled `Map100_EV003_Page1` (duplicate-symbol/dangling-ref at ASSEMBLY; per-file compile-gate can't see it). Fixed: added the `Map{src:03d}_`→`Map{cur:03d}_` rewrite + stale-token guard (orchestrator.py `_reinstantiate`); the existing cross-map test used an unrealistic `script npc{}` label (no `Map_` prefix) which is why it slipped — made it realistic + added a no-switch label test. **131 pass, ruff clean.** Verified the fix on the REAL stored memo entry too (`scripts/_tmp_reinstantiate_check.py`). **NOT committed** (await user). **MODEL DECIDED 2026-06-03 = `claude-opus-4-8` for ALL events** (see Decisions — 73/27 judgment/trivial split; routing deferred as optional post-freeze polish). STILL PENDING: **freeze prompt+model → §9 #2 user approval** before bulk `phase4 --run` (the big run = ~3,581 Opus spawns, hours, resumable via per-map checkpoints). **Script-call dictionary EXPANDED 2026-06-03** (free pre-freeze task, no budget): measured the 355/655 signature distribution (`scripts/_tmp_scriptcall_dict.py`) — **7,925 invocations / 250 distinct sigs, brutally top-heavy** (top 20=80%, top 60=92%, 77 one-offs). Audited `reference/uranium_script_calls.md` against the ranked list + read `scripts_dump/` for the uncertain ones; added rows to reach **≈92% coverage (top ~60 sigs)**: MAP `$PokemonBag.pbDeleteItem`→`removeitem`; STRIP `pbSEPlay`/`pbPlayCry`/`addUserSprite`; UNHANDLED `pbShowMap`/`pkmn.setAbility`+`setItem`/`nextBattleNuclearHorde`; fragments `pbGet`/`pbGetPokemon` (getters) + `poke.ev`/`calcStats` (mon-mutation → queue). 56 conversion-agent tests still pass. **Gate implications:** this is a prompt-content change → (a) Map002 should be re-confirmed under the expanded prompt before freeze (additive disposition rows only — shouldn't change behavior, but freeze discipline); (b) the memo fingerprint changes → the throwaway `memo_manifest.json` from the confirmation run is discarded on next load (correct, harmless). The determinism ceiling: ~92% of the *script-call* surface is now dictionary-dispositioned, but idiom-collapse/control-flow/naming/inline-Ruby remain irreducibly-LLM (why Phase 4 is an LLM stage). Working dir now holds throwaway confirmation residue (CE/Map100/Map115; Map115.pory's on-disk EV003 label is pre-fix-stale — output/ is gitignored & gets reset before the real run); restore `flag_state.baseline.json` + `rm` scripts/checkpoints/unhandled/memo before the next calibration pass.

**Rung-2 derisking spike DONE 2026-05-31** (parallel to the gate; user wanted a fast end-to-end "does it build" proof before investing in calibration). Framing: "compiles" has 3 rungs — (1) poryscript transpiles `.pory`→`.inc` (already had it), (2) **assembles into the fork ROM build** `make modern` (real constant-resolution proof), (3) walkable+correct in-game (needs Phase 5 map + Phase 7 + emulator). **Rung 2 proven:** compiled `output/.../scripts/Map200.pory` → `.inc`, dropped it in fork as `data/scripts/rpg2gba_spike.inc`, `.include`d it in `data/event_scripts.s` (after LittlerootTown), `make -j16 modern` built clean → 32MB `pokeemerald.gba`, symbol `Map200_EV001_Page1` linked at ROM addr `0x0828912d`. All on throwaway fork branch **`rung2-map200-spike`** (uncommitted; `master` clean). **Caveat:** Map200's only payload (warp→map181) was *queued*, so what assembled refs only stock constants (`fadescreen`/`FADE_TO_BLACK`/`lockall`/...) — proves plumbing, NOT that Uranium-data-referencing scripts resolve (that's the Phase-2 name-collision wall, deferred to Phase 7). **Rung-2 step B DONE 2026-05-31** (free, no budget — plumbing proof, not a conversion): `flag_registry.dump_header` → `output/.../include/rpg2gba_flags.h` (8 flags @ base 0x500 + 5 vars @ 0x40D0, placeholder bases — "Phase 7 assigns real ranges"); copied to fork `include/constants/rpg2gba_flags.h`, `#include`d in `event_scripts.s`, added a `RPG2GBA_FlagProbe` script using `goto_if_set/setflag/clearflag FLAG_RECEIVED_STARTER`; `make modern` clean, symbols `RPG2GBA_FlagProbe`/`_Already` linked at `0x08289134`/`0x08289141`. Proves registry→header→preprocessor→assembler resolves a minted `FLAG_*`. **Runtime caveat for C:** base 0x500 only needed to *assemble* here; for rung 3 it must be a genuinely *free* fork flag slot (else the branch misbehaves at runtime) — pick from the fork's unused-flag range, the live form of the Phase-7 base-assignment TODO. **Rung-3 wiring DONE 2026-06-01** (budget spent, user-authorized; **in-game confirmation still pending on the user's emulator**). Target = **Map031 event 48** ("npc"), a 2-page talk-once tutorial NPC literally explaining "talk to people multiple times → different things" (picked by `scripts/pick_branch_event.py`; charmap-safe ASCII, no warp/species/items, only `pbCallBub` STRIP). Converted via **Opus** through the real pipeline, scoped to the one event by a trimmed temp map (`scripts/convert_one_event.py`) so it didn't convert all 48 of Map031's events. **Output was clean:** `msgbox` + `setflag`/`clearflag(FLAG_MAP031_EVENT048_SSA)` per page, `pbCallBub` stripped (not queued), `123 [A,1]` correctly read as `clearflag`, `\n`/`\l` line breaks handled, compiled through poryscript. Wired into the fork on branch **`rung2-map200-spike`**: hand-wired page dispatcher `Map031_npc_Dispatch::` (`goto_if_set FLAG → Page2 else Page1`) in `data/scripts/rpg2gba_spike.inc` (replaced the rung-2 Map200/FlagProbe content); repointed `OldaleTown_House1`'s Woman object-event `script` → `Map031_npc_Dispatch`; repointed new-game spawn (`new_game.c` `WarpToTruck` else-branch → `MAP_OLDALE_TOWN_HOUSE1` @ 4,7) to skip the truck intro. **Gotcha (caught on first boot):** repointing `WarpToTruck` alone is not enough — `CB2_NewGame` (`overworld.c:1891`) also sets `gFieldCallback = ExecuteTruckSequence` (the truck-arrival cutscene), which fires on the wrong map regardless of the warp dest; had to override it → `FieldCB_WarpExitFadeFromBlack` for a clean fade-in. Also **skipped the Birch/oak_speech intro** for fast iteration (`main_menu.c` `ACTION_NEW_GAME` → `SetMainCallback2(CB2_NewGame)` directly instead of `Task_NewGameBirchSpeech_Init`; default gender MALE, name left empty — both unused by the test NPC; save is already cleared in `intro.c` so no init is missed). Net: title → New Game → fade straight into the house. All three fork tweaks (spawn dest, arrival callback, intro skip) are spike-only and commented with how to revert. **Rung 3 CONFIRMED in-game by the user 2026-06-01** — the NPC showed both converted lines on repeated talks; the core feasibility risk is RETIRED. The fork spike was then **reverted: branch `rung2-map200-spike` deleted, fork back on clean `master` (HEAD `21c24202`), working tree clean.** All rung-2/3 work survives only here in MEMORY — the fork no longer carries any spike files. (ROM delivery: Taildrop `tailscale file cp <file> iphone182:` works, but iOS doesn't auto-save — must pick up in the Tailscale app; `/tmp` copies + a `~/rpg2gba-rung3c.gba` copy were left on b-desktop.) `make modern` clean; symbols linked (Dispatch `0x08289135` / Page1 `0x08289143` / Page2 `0x08289152`); generated `events.inc` references the dispatcher. ROM (`rpg2gba-rung3.gba`) sent to phone via Taildrop. Reused `OldaleTown_House1` (its `MapScripts` = `.byte 0`, no on-entry cutscene) instead of building dummy rooms — simpler than the scoping plan, and avoids Phase-5 tileset work entirely. **(The literal player's house Map049 = `\PN's house` remains a BAD target — 22 events / ~700 cmds / 2 cutscenes / 28 `pbXxx` / 5 warps.)**

**KEY FINDING from rung 3 → §9 #2 GATE BLOCKER: self-switch flags have no definition source.** The agent emits `FLAG_MAP{id}_EVENT{id}_SS{letter}` and sets/clears it directly but does **not** register it (`few_shot/give_item_with_fanfare.md`: "deterministic pattern — no registry proposal needed"), and `flag_registry.py` has **no self-switch minting** (`propose_flag` is keyed by an integer global switch_id). So nothing `#define`s these constants → **undefined symbol at assembly (rung 2) for all ~860 self-switch events.** The spike worked around it with a manual shim (`include/constants/rpg2gba_spike_flags.h`: `#define FLAG_MAP031_EVENT048_SSA FLAG_UNUSED_0x028`, a genuinely-free SAVED flag in the `0x20-0x4F` block). **Real fix:** give the registry deterministic self-switch minting (key on map+event+letter; allocate from a free saved-flag range; `dump_header` emits them). Must resolve before any bulk `phase4 --run`.

**Phase 3 — Map Deserialization CODE COMPLETE** (§3.0–§3.4; 9 phase3 tests). Plan: `PHASE3_PLAN.md`. `phase3 --clean` deserializes 199 maps → `output/uranium-build/maps/MapNNN.json` + `common_events.json`/`system.json`/`map_infos.json`; conservation **exact** (199 maps / 5301 events / 8429 pages); emits committed `reference/rgss_event_commands.md` (59 codes, 250 script-call signatures) + `reference/uranium_switches.json` (235 named) + `reference/uranium_variables.json` (119 named). **Exit gate V1–V3+V6 done; V4 (user map spot-check) + V5 (user reviews command reference) are the remaining user gates — Phase 3 is NOT a §9 hard gate, so these are advisory.** **Next: Phase 4 (Event → Poryscript / conversion agent).**

**Deterministic pre-filter IN PROGRESS** (branch `phase4-deterministic-prefilter`, plan `PHASE4_DETERMINISTIC_PLAN.md` — a **post-freeze cost optimization** that routes fully-mechanical events through lookup instead of Opus; doesn't touch frozen `system.md`/model, output passes the same compile-gate + mint, non-match falls through). 6 classifiers planned (~41% ceiling claimed, but see below — targets are optimistic). Session decision (2026-06-04): build+test all 6, no-budget acceptance only, **stop before the bulk run**; one commit per classifier. **Classifier 1 (Pure Dialogue) DONE + committed** (`e223e9c`): new `deterministic.py` (scaffolding: `format_pory_string`, label helpers, `_dialogue_body` page walker, `Context`/`load_context`, dispatcher `try_deterministic`; `classify_pure_dialogue`) + orchestrator `_try_deterministic` at top of `_convert_event` + `scripts/count_deterministic_actual.py` (corpus counter, loads .env-paths, repo-root anchored, `--compile` gates each claim). **Claims 372 events (10.4%), all compile clean.** `\PN`→`{PLAYER}` handled deterministically (prescribed by system.md); fixed a real `_DIALOGUE_STRIP_RE` bug (trailing `\b` never matched `need_refresh =`). 33 det tests, suite 164 pass, ruff clean. **KEY FINDING: plan per-classifier targets are inflated** — Classifier 1's ~620 is really 458 (code-filter max) → 372 as-built; expect the ~41%/3,581→~2,150 projection to land closer to ~30%. **Deferred-validation bucket (plan §12):** `\sign[..]` sign dialogue (~70 events) looks like `msgbox(…, MSGBOX_SIGN)` but system.md is silent → unprescribed content decision; HOLD all such "compiles-but-unprescribed" candidates, validate together against one observed-Opus sample at the end (pre-bulk-run, since the pre-filter only saves a spawn if it claims before Opus does). **Classifier 2 (Call Common Event) DONE 2026-06-04 (not yet committed):** `classify_call_common_event` in `deterministic.py` — a `saw_call`-guarded twin of `classify_pure_dialogue` that passes `_dialogue_body(allow_call=True)` (the code-117 path, incl. the `ce_id<=0`→None guard, already existed). Registered after Classifier 1 in `_CLASSIFIERS` (counter/dispatcher auto-pick-up by `fn.__name__`). **Claims 50 events (target 52), all compile clean.** Two as-built design calls: (1) `saw_call` required so pure-dialogue events stay with Classifier 1 / keep count attribution honest; (2) call-only pages reuse `_talk_block` → they get `lock`/`faceplayer`/`release` (deviates from plan §5's literal "no lock/release" but matches shipped Classifier 1 — eyeball vs frozen-Opus's Map002 EV009/10/12 in the deferred §10.4 check, no new spawn). **No unprescribed-content bucket** — `call CommonEvent_<NNN>` is frozen in system.md (Map002 fix #3). 10 new tests (`test_cc_*`), suite 174 pass, `-m phase4` 3 pass, ruff clean, dry counts unchanged (8/5/34/199). **Classifier 3 (Self-Switch Dialogue) DONE 2026-06-04 (committed):** `classify_self_switch_dialogue` — `saw_switch`-guarded twin passing `_dialogue_body(allow_self_switch=True)` (parses code-123 `[letter,value]` → `setflag`/`clearflag(self_switch_flag_name(map,event,letter))`, value 0=on). Does NOT allow_call → 123+117 event falls to LLM. **Claims 58 (target 67), all compile clean.** Flag names agree with the orchestrator's `_mint_event_switches` by construction (same `self_switch_flag_name` fn) — the rung-3 gate-blocker (self-switch flags had no def source) is satisfied on this path. 10 `test_ss_*` (incl. flag-name-agreement + compile-gate), suite 184 pass, `-m phase4` 4 pass, ruff clean, dry counts unchanged. Det pre-filter cumulative after C3: 480 events / 13.4% (372+50+58). **Classifier 4 (Simple Warp) DONE 2026-06-04 (committed):** first non-dialogue classifier + first to **queue**. `classify_simple_warp` in `deterministic.py` — single-page event, exactly one code-201, every other code in `{0,5,6,7,106,201,221,222,223,224,249,250}` or a `_DIALOGUE_STRIP_RE` 355/655. **Claims 304 (target 317), all compile.** **§7 spec was wrong on two counts, corrected from the corpus + fork:** (1) 201 params = `[mode, map_id, x, y, dir, fade]` (6 params) → target=`params[1]`, x=`[2]`, y=`[3]`; `mode != 0` (variable-indirection) falls through. (2) Body shape = `lockall`/`warp(MAP_URANIUM_<N>,x,y)`/`waitstate`/`releaseall`/`end`, chosen by reading the **fork's own** scripted warps (`LittlerootTown_BrendansHouse_1F/scripts.inc:58-67`) — **no `fadescreen`**: `warp` owns the fade-out→load→fade-in; an explicit `fadescreen(FADE_TO_BLACK)` before `warp` would DOUBLE the fade and a trailing `fadescreen(FADE_FROM_BLACK)` is dead code (warp tears down the script context). All macros verified real in the fork (`asm/macros/event.inc`, `field_weather.h`) → assemble, not just transpile. **Evidence settling the design = the captured frozen-Opus Map002 output** (`output/uranium-build/compare/opus/`): EV002 + EV008 are **byte-identical warps in source** yet Opus emitted TWO different shapes (EV002 lockall/waitstate, EV008 fadescreen-pair) — Opus is non-self-consistent here, which is the whole argument for determinizing; both were **queued** (system.md line 103 mandates emit placeholder + 1 unhandled entry). **Queue decision (user-approved): Classifier 4 queues** to preserve the pre-filter's "indistinguishable downstream from an LLM conversion" invariant + keep Phase 5 on one discovery channel. `MAP_URANIUM_<N>` = placeholder (N = raw Uranium map id); Phase 5 rewrites → real generated `MAP_*` once the map lands in fork `data/maps/`→`map_groups.h` (we use Uranium's maps ported INTO the engine, NOT vanilla pokeemerald maps). **New abstraction: `DetResult{script, unhandled}`** — classifiers may return bare `str` (C1–3, no queue) or `DetResult` (C4+); `try_deterministic` normalizes str→DetResult. Orchestrator `_try_deterministic` now logs `match.unhandled` + stores it in the memo (cross-map warp re-queues on a memo hit); counter `claim()` reads `.script`. Lead owned the orchestrator/counter integration + a no-budget queue-wiring test (`test_deterministic_warp_queues_unhandled`: MockBackend not called, 1 unhandled entry, code 201). 9 `test_warp_*` + 1 orchestrator test; suite 194 pass, `-m phase4` 5 pass, ruff clean (touched files), dry counts unchanged (8/5/34/199). Det pre-filter cumulative after C4: 784 / 21.9% (372+50+58+304). **Classifier 5 (Item Ball) INVESTIGATED + DROPPED 2026-06-04** (user-approved): the corpus has NO clean ground-item-balls. Of 45 `pbItemBall` events, 38 are embedded in 111-branches/move-routes/variables; the 7 with simple command lists are **story-gated Dream-World Mega-Stone pickups** (`DRAMSAMAITE`/`OBLIVICORNITE` = Mega Stones, `BLUEFLUTE` etc.) whose page-2 (the item-give) only activates on a **global switch/variable** (e.g. sw172 / var29==2) — that's Phase-5 page-dispatch (plan §11), and a per-page deterministic emit would translate them WRONG (item appears unconditionally, breaks the Sleeping-Girl sidequest gating). Wiki-confirmed (Dream World / Sleeping Girl Sidequest). Also `finditem` is unprescribed (system.md only has `pbGiveItem→giveitem`; ref table wrongly says `pbItemBall→giveitem`) and `item_field_codes.json` is keyed by `ITEM_*` (no symbol→ITEM_* direction exists). The 7 → LLM; `Context.items` stays unused. **Classifier 6 (Trainer Battle) DONE 2026-06-04 (committed) — LAST classifier, pre-filter build COMPLETE:** `classify_trainer_battle` — 2-page single route trainers → **single** `trainerbattle_single(TRAINER, "intro", "defeat")` + post-battle `msgbox` + `release`/`end`. **Claims 150 (target ~250 was inflated), all compile.** Key decisions: (1) **single-script not §9's two-block** — pokeemerald's per-trainer flag handles already-beaten (verified against fork `LittlerootTown`/`Route117` scripts), so no self-switch/page-dispatch; the event's code-123 SSA is minted-but-unused (harmless, Phase-7 flag-budget note). (2) **Inline-string text VERIFIED** to compile through real poryscript (auto-generates `Map..._Text_N` labels for `trainerbattle_single` + `msgbox`) — README/GitHub suggested labels-only but the repo cheatsheet was right; lead compiled both forms first. (3) `pbTrainerBattle→trainerbattle` IS prescribed (system.md line 50 + table) — low unprescribed risk. First classifier to use `Context`: `load_context` now loads `trainers.json` → `(trainer_class_const, name, party_id)→TRAINER_*`, keyed via the shared `to_constant("TRAINER_CLASS",sym)` (CRITICAL: mixed-case syms like `SWIMMERmA`→`TRAINER_CLASS_SWIMMERMA` need to_constant, not naive concat); orchestrator + counter already call load_context with the real intermediate dir → no caller change. Parse of the code-111 `[12,"pbTrainerBattle(PBTrainers::CLASS,\"Name\",_I(\"loss\"),canlose,party_id)"]`: class/name/loss via regex, party_id = arg after canlose (default 0). Detection = exactly 2 pages, one 111 (params[0]==12, `pbTrainerBattle(` not Double), code-123 ['A',0], page-1 codes in `{0,5,6,7,101,401,108,408,123,355,655,111,412}` (Agent caught **412** Branch-End was needed), page-2 dialogue+STRIP; STRIP extended with `pbTrainerIntro|pbNoticePlayer|pbTrainerEnd`. Doubles/phone-rematch/story battles → LLM. 11 `test_trainer_*`/`load_context` tests; suite 205 pass, `-m phase4` 6 pass, ruff clean, dry counts unchanged (8/5/34/199). **Det pre-filter FINAL cumulative: 934 events / 26.1%** (372+50+58+304+0+150). **All 6 classifiers built (C5 dropped). Pre-filter build COMPLETE.** **Classifier 7 (Sign Dialogue) DONE 2026-06-05 (not yet committed):** the deferred `\sign[..]` candidate (plan §12) was **already validated** against frozen-Opus (2026-06-04, 3 spawns) — Opus strips the `\sign[id]` prefix, emits a **plain `msgbox`** (NO `MSGBOX_SIGN`) in a **no-faceplayer** `lock`/`release`/`end` block. Implemented `classify_sign_dialogue` in `deterministic.py` as a `_has_sign_prefix`-gated sibling of C1: reuses `_dialogue_body(strip_sign=True)` (new kwarg — `flush()` strips a leading `_SIGN_PREFIX_RE` match then bails on any embedded `"` since Opus's quote-drop rule is unconfirmed) + new no-faceplayer `_sign_block`; registered **2nd** in `_CLASSIFIERS`. No real overlap (C1–C3 reject `\sign` via `_UNSAFE_TEXT_RE`; sign rejects no-sign events). ~70 events (→ ~1,000 / ~28%). 10 new `test_sign_*` (incl. quote-bail, extra-code-bail, no-faceplayer/no-MSGBOX_SIGN asserts, compile-gate); **full suite 215 pass** (was 205), poryscript compile-gate ran (binary present). `PHASE4_DETERMINISTIC_PLAN.md` §0 tracker + §12 disposition updated to BUILT. Remaining (budget-gated, NOT this session): the bulk `phase4 --run`.

**Next concrete task (after the pre-filter branch):** **bulk `phase4 --run`** — converts all 199 maps + 100 common events via `claude-opus-4-8`. ~3,581 Opus spawns (events with commands, minus decorative skips + memo hits + deterministic claims); hours; resumable via per-map checkpoints. Before starting: restore `flag_state.baseline.json`, wipe `unhandled.jsonl` / `memo_manifest.json` / all checkpoints (or just `--clean`). Confirm budget availability with user first.

**Phase 5 SCAFFOLD CREATED 2026-06-06 (parallel track to the budget-gated Phase 4 bulk run — deterministic, zero LLM budget, touches NO frozen Phase 4 artifacts).** Assignment-style scaffold (professor-framed, per user). Created: `PHASE5_PLAN.md` (brief: RMXP↔GBA tile-model explainer, sections, exit criteria, resolved design-Q table) + `src/rpg2gba/tileset_converter/` package (was empty) with stub modules — `tile_map.py` (5.1 substitution-table loader/validator/lookup), `layout.py` (5.2 tile grid→`map.bin`/`border.bin`/`layouts.json`; working `TileGrid` index + `read_blockdata`), `map_constants.py` (MAP_*/LAYOUT_*/MAPSEC_* registry + alias header), `metadata_wiring.py` (5.3 map.json: object/warp events + per-page dispatcher + encounters), `connections.py` (5.4 adjacency), `phase5.py` (orchestrator) + package `README.md`. Plus `reference/tileset_map.json` seed (schema + placeholders) and `tests/test_tileset_converter.py` (4 real pure tests pass, 10 skipped acceptance stubs = checklist, NOT yet red-green). All section fns `raise NotImplementedError`; import-clean, ruff clean. **Output → `output/uranium-build/porymap/`, NEVER the fork (Phase 7 §7.1 drops it in).** Reads Phase 3 `maps/MapNNN.json` (`tiles{xsize,ysize,zsize:3,data}`, flat idx `z*ys*xs + y*xs + x`), `map_infos.json` (names; `parent_id` = editor tree, NOT adjacency), Phase 2 `intermediate/{wild_encounters,map_metadata}.json`, Phase 4 `.pory` labels. Approach A (reuse pokeemerald tilesets, no new art). **`pipeline.py phase5` wiring deliberately deferred to last** (don't touch shared pipeline code mid-Phase-4-run). **Design Qs RESOLVED 2026-06-06 (full table in PHASE5_PLAN.md):** Q1 layer-collapse = HYBRID (topmost-non-empty default + opt-in `"z0,z1,z2"` composite overrides in tileset_map.json, checked first); Q2 map naming = READABLE `MAP_<REGION_NAME>` from map_infos (sanitize+dedup, fail-loud) reached via ALIAS header `#define MAP_URANIUM_<N> MAP_<NAME>` so frozen `.pory` warps resolve unmutated (canonical = readable; `ALIAS_CONST_FMT` vs `MAP_CONST_FMT`); Q3 collision = inherit target-metatile baseline, per-cell override later; Q4 tilesets = ONE universal (primary,secondary) pair for ALL maps first, refine later (caps metatile vocab → Q1 composites mostly unused until real tilesets added); Q5 border = single neutral metatile now, override later. **Not committed.** Next: implement section-by-section (start `tile_map.py` 5.1), writing each test body before un-skipping.


---

## Archived 2026-06-18 — pre-pivot Current Phase (pathfinder S1–S9, bulk run, classifiers)

> Excised from MEMORY.md Current Phase at the 2026-06-18 pivot (LLM-as-spine retired →
> deterministic transpiler). Durable conclusions live in MEMORY Decisions Made 2026-06-18,
> Key File Notes, and `BUILD_PLAN.md`. Verbatim history below.


**Active:** Executing the **iterative roadmap** (`ITERATIVE_ROADMAP.md`) — Track-A deterministic expansion with the **bulk run PAUSED at 7/199** (invariant 3: front-load classifiers before resuming, else memo gives no spawn refund). Roadmap is **shadowed, not yet ratified into ROADMAP.md** (OQ-R1 open). Still overlaps Phase 5 build + the critique queue (`FABLES_DECISIONS.md`).

- **STRATEGY PIVOT (2026-06-15, user): horizontal bulk run PAUSED for a vertical "pathfinder slice."** The bulk run's defect is that it produces `.pory` we can't validate until Phase 7 (weak per-file compile-gate; two systematic compile-clean-but-broken classes already found: wrong item constants + mistagged dispositions). Plan: drive ONE play-order slice — **Map049 (house ground floor, SPAWN @7,7 + street door to town) ↔ Map048 (upstairs) ↔ Map032 Moki Town (72×64, 52 events)** — through Phase 4+5+6 → `make modern` → boot in mGBA, as a pathfinder to surface systematic-error classes at 3 maps not 199. **S1 warp trace DONE 2026-06-15** (`scripts/pathfinder_warp_trace.py` → `PATHFINDER_FINDINGS.md`): corrected an earlier guess — 049 (not 048) has the street door, so 049=1F/spawn, 048=2F; KEEP 5 in-slice warps, NO-EMIT 4 out-of-slice building doors (Map050/064/065/172), WALL 3 west cave-exit tiles (→Map033 `pbCaveEntrance` = slice-#2 cave candidate). Moki Town = closed sandbox, zero dangling MAP_* refs. ALL Phase-4 machinery (classifiers, registry, orchestrator, frozen prompt, memo, checkpoints) is REUSED, not discarded — only sequencing changes + a real build+boot gate is added. The global map-by-map-vs-resume-horizontal decision is DEFERRED until the slice teaches us per-map cost. Long pole = Phase 5.1/5.2 (`tile_map.py` + layout converter), not the events. See Decisions Made 2026-06-15. **Plan docs (2026-06-15):** `PATHFINDER_SLICE_ROADMAP.md` (all 9 steps S1–S9, slice=49/48/32, mini-Phase-7 boot gate) + `PATHFINDER_STEP2_TILE_MAP_PLAN.md` (tileset substitution — autotile normalization collapses 48→1, only Uranium tilesets **19 interior→`gTileset_BrendansMaysHouse`** + **22 town→`gTileset_Petalburg`** = the secondary `LittlerootTown` itself uses, no separate `gTileset_Littleroot` exists; harvest real metatile ids from vanilla `BrendansHouse_1F`/**`LittlerootTown`** map.bin) + `PATHFINDER_STEP3_LAYOUT_PLAN.md` (Q1 collapse algo + block packing + layouts.json upsert). These refine `PHASE5_PLAN.md §5.1/§5.2` for the slice; Q1–Q5 already resolved there. **S2 prereqs P1+P2 DONE 2026-06-15:** P1 `tilesets.json` oracle generated (`deserialize.rb tilesets`, 60 tilesets w/ passages/priorities/terrain_tags); P2 census (`scripts/pathfinder_tile_census.py`) found **433 distinct tiles** (117 ts19 + 316 ts22) — power-law (ts22 id384 grass=2025occ). **DECISION (user): pure passability buckets first, hand-map later** — v1 maps every tile by source `passages`→generic passable/blocked metatile per tileset (~6 hand-picked metatiles total, `buckets` schema added to `tileset_map.json`, `tiles` table empty in v1), collision from passages (combine layers where `priorities==0`); warps need no special tile (S5 warp_event fires on coord). `tile_map.py` lookup = explicit-`tiles`-then-bucket-fallback; fail loud only if no bucket. **Bucket metatiles HARVESTED 2026-06-15** (`scripts/harvest_bucket_metatiles.py` decodes vanilla `map.bin`) + written to `reference/tileset_map.json`: ts19 (interior, **`gTileset_Building`**+`gTileset_BrendansMaysHouse`) passable=513/blocked=622/void=1; ts22 (town, `gTileset_General`+`gTileset_Petalburg`) passable=1/blocked=468/void=468. Harvest caught a plan bug — interior primary is `gTileset_Building` (indoor), NOT `gTileset_General` (outdoor); fixed. **`tile_map.py` IMPLEMENTED + validated 2026-06-15** (load_tile_map+oracle, _validate, lookup explicit→bucket-fallback, tileset_for, void, passage/priority/is_passable; 8 tests, ruff clean, full suite 353 pass). Oracle replicates passage across autotile variants (ids 48–95 identical) → `passages[tile_id]` correct w/o normalization. Walkability validated (`scripts/pathfinder_collision_preview.py`): Map049 interior walkable. **FINDING: warp/door tiles read BLOCKED (drawn into walls) → must be force-walked.** Rule: S3 `convert_layout` takes a walkable-override set (warp coords from S5/S1), forces those cells collision 0; else the door→town warp is unreachable. In step-3/step-5 plans + findings. **S3 `layout.py` DONE 2026-06-15** (`convert_layout`/`collapse_column`/`_cell_blocked`/`Layout.write`/`to_layouts_entry`/`append_layouts`): visual metatile = topmost-non-empty layer via `lookup`; collision = combined top→bottom layer rule mirroring `pathfinder_collision_preview.cell_blocked` (first blocking tile→blocked; priority-0 passable→passable; priority>0 passable treetop keeps scanning to trunk; all-empty→void); `walkable_overrides` (S5-supplied warp coords) force collision 0; row-major `y*width+x` block order; border = 4× void block (Q5). **Composite-stack hook DEFERRED** — the v1 `TileMap` carries no `stacks` map (noted in docstring; slice-#2 work). layouts.json entry schema verified against the fork (`name`=PascalCase+`_Layout`, dir=name w/o suffix, fork-relative `data/layouts/<name>/...` paths, `layout_version:"emerald"`). 12 layout tests (golden round-trip/precedence/index-order/combined-collision/warp-override/fail-loud/idempotence+append-upsert/schema + parametrized slice smoke); full suite **363 pass**, ruff clean. **Slice smoke: all 3 maps (49 30×15 ts19, 48 20×15 ts19, 32 72×64 ts22) convert with ZERO unresolved tiles + 0% void** (RMXP fills the whole grid → dense layouts). Actual staged `.bin`/`layouts.json` NOT written yet — deferred to S4 (needs minted `name`/`layout_const`) + S8 assembly. **S4 `map_constants.py` DONE 2026-06-15** (`MapConstantRegistry`: mint/get/resolve_placeholder/write_alias_header/load/save/write_map_groups + module helpers `sanitize_name`/`_pascal_dir`/`load_display_names`/`load_vanilla_map_consts`/`build_map_constants`). Readable names via `_naming.to_constant` (Q2); floor disambiguation via **2 new `reference/map_name_overrides.json` overrides** (48→"Moki Town Player's House 2F", 49→"…1F"; evidence = S1 street-door topology, NOT RMXP `order`). Collision-checked against the fork's **936 vanilla `MAP_*`** (enum-parsed by `load_fork_constants` — the modern header is an `enum`, not `#define`s); a vanilla/Uranium dup or blank name **FAILS LOUD → add an override** (no silent numeric suffix). Persisted+idempotent (save/load round-trips). Slice mints **MAP_MOKI_TOWN (32) / MAP_MOKI_TOWN_PLAYERS_HOUSE_1F (49=spawn) / …_2F (48)**, dirs MokiTown/MokiTownPlayersHouse{1F,2F}, **COLLISION-FREE** (verified by `scripts/pathfinder_map_constants_preview.py`, which wrote `output/uranium-build/porymap/{map_constants.json,uranium_map_aliases.h,map_groups.json}`); alias header `#define MAP_URANIUM_{32,48,49}` resolves every in-slice warp. **OPEN (S5/S8): MAPSEC minted per-map (`MAPSEC_<stem>`) but NOT yet in any fork header — either add to `region_map_sections.h` or remap indoor maps to the town's vanilla MAPSEC (deferred).** 8 map_constants tests; full suite **371 pass**, ruff clean. **S5 `metadata_wiring.py` DONE 2026-06-15** (`classify_event`/`classify_map_events`, `build_object_events`, `build_page_dispatcher`, `build_warp_events`+`_return_warp_index`, `wire_encounters`, `MapFile.to_json_dict`, `build_slice_maps` driver; verified by `scripts/pathfinder_map_wiring_preview.py` → writes `output/uranium-build/porymap/maps/<dir>/map.json` + `dispatch/Map0NN_dispatch.pory`). **Generic classification reproduces the S1 keep-list, no hardcoding:** code-201 to out-of-slice→SKIP (drops all 8 Moki building/cave/trainer doors); in-slice 201 on **player-touch trigger**→`warp_event` (object dropped, avoids double warp); else→`object_event` (incl. action-triggered Letter, `.pory` keeps its `warp()`). Event-split conserves (032=43obj+1warp+8skip=52, 048=12+1=13, 049=20+2=22). Warp `dest_warp_id`=destination's RETURN warp index (arrival 0–2 tiles off Uranium coord, 049→Moki EXACT). **`.pory` label convention CONFIRMED from corpus** = `Map{NNN}_EV{eid:03d}_Page{n}`; dispatcher `goto <Label>`+`if (flag())`. **v1 simplifications (user chose "build S5 now, defer dispatchers"):** dispatchers emitted ONLY for self-switch/unconditional multi-page (5 slice events: Map048 EV001/005, Map032 EV014/015/033); all GLOBAL-gated multi-page (SW1_125/22, VAR_101 — names not minted until S6) fall back to base page `_Page1` w/ logged TODO. gfx=`OBJ_EVENT_GFX_NINJA_BOY` for all (RMXP gfx map deferred); music=MUS_LITTLEROOT/weather=NONE; autorun/parallel→static objects. **region_map_section reuses vanilla `MAPSEC_LITTLEROOT_TOWN`** for all 3 (minted MAPSEC_MOKI_* aren't in the fork enum) — **RESOLVES the S4 MAPSEC open item** for the slice. `.pory` label existence check deferred to S8. 13 tests; full suite **379 pass**, ruff clean. **`build_slice_maps` returns the per-map warp-source coords = S3 walkable-overrides** (049:{(10,11),(12,3)}, 048:{(3,3)}, 032:{(28,31)}) — feed to `convert_layout` at S8. **NEXT: S6** (run the Phase-4 orchestrator on maps 49/48/32 — needs user OK, spends Pro budget; mints the global flags/vars so deferred dispatchers can be completed + produces the page-body `.pory`). OR S7 (move-routes static, minimal) / S8 (assembly) — but S8 needs S6's `.pory`. **S6 RUNNER READY: `scripts/run_slice.py`** (mirrors run_bulk resilience scoped to [49,48,32]; convert_map's per-event flush of `.pory`+registry+memo + memo-load-on-construct = interrupt-safe, re-run to resume; `--limit N` bounds spend, `--timed` auto-waits the 5h window; own state `slice_run_state.json`; SHARES registry/memo/checkpoints with the bulk run — **do NOT `prep_bulk_run.py`** (resets baseline, wipes done maps+CEs) and **do NOT edit `prompts/system.md`** (memo fingerprinted by it → discard+re-spend). Verified by `--help` only; NOT yet run.
- **S6 RUN — COMPLETE (2026-06-16, 2nd pass).** `run_slice.py` over [49,48,32]: **all 3 maps `.done`, `slice_run_state.json status=complete`** (1st pass died `transport_error` at Map032 EV027; resumed clean). **Map032 EV074 "Trainer(5)" (30 real cmds, 3 pages) hard-FAILED on malformed agent JSON** (`Extra data: line 14 col 1`) → queued, no block. **RESOLVED 2026-06-16 by retry** (`rm checkpoints/Map032.done` + `run_slice.py 32`; 29 done events = free memo hits, only EV074 re-spawned — confirms the .done-skip retry recipe works): EV074 now has Page1/2/3 blocks, its hard error replaced by 4 routine deferrals (3×209 move-route + 1×207 anim). Map032 31/52 events emit a block. **0 hard errors remain in the slice queue.** Reconciliation (map.json wired-scripts vs `.pory` blocks): 22 wired events lack a block, but **21 are legit-empty (0 real cmds — standing NPCs / globally-gated cutscene-sprite actors: Theo, Bambo, ×many Luz, Theo75/Chyinmunk76/Starter77/BamboAfterGym/TheoChamp); only EV074 is the real miss.** **Triage queue (`scripts/triage_slice.py`, read-only filter of shared `unhandled.jsonl` for {32,48,49}): 112 entries** (49:26, 48:5, 32:81), ALL `agent-flagged unhandled` (expected Phase-5/6 deferrals, NOT failures — 0 hard errors after the EV074 retry). **NOTE: S5 preview `map.json` references un-named labels `Map032_EV{e}_Page1`, but the agent emits NAME-qualified labels `Map032_EV008_Chyinmunk_Page1` — this label reconciliation is the known "deferred to S8" item, not a bug.** Codes: 209 move-route ×42 (→§5.5), 355 script-call ×16, 111 script-cond ×15, 201 warp ×12, 207/202 ×2. **NEW SYSTEMATIC FINDING (the slice's payoff) — S8 ASSEMBLY BLOCKER:** Map032 out-of-slice doors EV003/006/007/017/023 — S5 correctly drops them from `map.json` (verified: absent from MokiTown object/warp events), but S6 still emitted their `.pory` blocks with `warp(MAP_URANIUM_{50,64,65,172,33})`, constants NOT in the alias header (only 32/48/49). poryscript compiles orphaned blocks → `event_scripts.s` references undefined map consts → S8 build fails. **FIX IMPLEMENTED 2026-06-16 — `tileset_converter/assembly.py` (orphan-block prune) + `scripts/prune_slice_orphans.py` driver.** `prune_orphan_blocks`/`prune_map_pory` drop every `.pory` block whose event id isn't wired by `map.json` (`live_event_ids` reads object/coord/bg `script`, NOT warps). **Keys on EVENT ID, not full label — robust to the un-named-vs-name-qualified mismatch above.** Two fail-loud guards: no kept block may goto/call a pruned label; no out-of-slice `MAP_URANIUM_*` may survive in kept text (a *wired* out-of-slice warp surfaces, isn't passed over). Verified on real slice: drops **24 orphan blocks** — Map032 {3,5,6,7,17,23,36,37,78} (the completed run exposed EV036/037/078 warping to 33/50, beyond the 5 partial-run doors), Map049 {2,3}, Map048 {2}. **The dropped 49/48/32 warp scripts (EV002/003/005) are the player-touch doors S5 turned into `warp_events` — VERIFIED redundant: warp_events at (10,11)→town,(12,3)→2F,(3,3)→1F,(28,31)→1F fully cover 2F↔1F↔Town nav (match S3 walkable-overrides exactly), so pruning preserves navigation.** Action-triggered warps w/ in-slice targets (Letter EV021) kept. 13 tests (`tests/test_assembly.py`, incl. real-slice smoke), ruff clean, full suite **392 pass**. Driver is dry-run by default; `--write` stages to `output/.../staging/scripts/` (never edits canonical `scripts/*.pory`). The future `assemble_pathfinder.py` calls `prune_map_pory`. **LABEL-RECONCILIATION DONE 2026-06-16 (Option A, user-chosen over B):** `assembly.normalize_labels` strips the agent's event-name token at staging → un-named `Map{m}_EV{e}_Page{n}` = S5's form (deterministic, both sides agree by construction; chosen because the agent's naming is non-deterministic — EV009 "Trainer(6)"→named, EV074 "Trainer(5)"→un-named — so S5 can't reconstruct it). Verified: 33 labels normalized across slice, dispatcher gotos now resolve. **NEW FINDING — the existence check earned its keep:** `find_dangling_references` flagged **22 undefined script refs** — the S5 *preview* `map.json` wires 21 empty Map032 events (+Map049 EV019) to `Map0NN_EV{e}_Page1` labels, but S6 emits NO block for command-less events (standing NPCs / cutscene-sprite actors) → dangling symbols at link. **S5 STUB FIX — DONE 2026-06-16.** `metadata_wiring.build_object_events`: when `pory_labels` is supplied (post-S6, the *canonical/normalized* def set), an event S6 left bodyless (new `_event_has_body` helper checks every page by event id) is wired as a **static object (`script "0x0"` = `NO_SCRIPT`)** instead of a dangling `Map{m}_EV{e}_Page1`; an event with *some* body but a missing resolved page still **fails loud** (genuine page-body gap, §4.5). `assembly.map_json_script_refs` now skips the `0x0`/`0` no-script sentinels (else `0x0` itself read as an undefined ref). `scripts/stage_slice_scripts.py` is now the full S8 driver: it normalizes every slice `.pory`, collects the canonical labels, **regenerates `map.json` via `build_slice_maps(pory_labels=…)`** (supersedes the optimistic preview), then prunes + checks. **Existence check now PASSES** — 22 command-less events become static objects (21 Map032 + Map049 EV019); 2F↔1F↔Town warps fully intact. 3 new tests (2 wiring stub/fail-loud + 1 assembly sentinel); full suite **404 pass**, ruff clean. NEXT in S8: write the staged `.pory` (`--write`) + the `.bin`/`layouts.json` (S3 with the walkable-overrides), then the `assemble_pathfinder.py` fork-copy + `make modern` (S9 boot gate). Decisions D/E still pending (flag blessings + EV009 starter-cutscene fidelity). **DECISIONS PENDING (user):** (D) bless flag mappings the agent refused to guess — `runningShoes=true`→`FLAG_SYS_B_DASH`? (Map049 EV001 Auntie), `$Trainer.pokedex=true`→`FLAG_SYS_POKEDEX_GET`? (Map032 starter cutscene); (E) fidelity of **Map032 EV009 "Professor Bamb'o" starter cutscene** (~17 of Map032's entries: coord-gated sign, catch demo, rival-starter `pbGet(151)`+list-index+string-var `{STR_VAR_1}`, Pokédex grant, `pbPhoneRegisterNPC`, handover) — full replication vs minimal give-starter+Pokédex. Engine/Phase-6 (defer): Rock Smash EV014/015, `pbCaveEntrance` EV023, `pbShowMap`/`pbTrainerPC` (Map048), `displayNinjaLetter` (Map049 EV021).
- **S8 BUILD GATE — PASSES (2026-06-17). `make modern` exits 0 → `pokeemerald.gba` (32MB) builds clean. All build-blocking classes FIXED across 2 rounds; only the manual boot-in-mGBA step (S9) remains. Full suite 415 pass / 11 skip / 2 env-fail (see end).** Systemic root: the per-event compile-gate runs *only* poryscript, which passes charmap-illegal text, invented commands, and unresolved symbols straight through — everything dies later at arm-as/ld. Whack-a-mole `_sanitize_pory` (replace-chain) replaced by a principled, fail-loud normalizer. **FIXED this session:** (1) **charmap normalization** — `assembly.normalize_pory(text, allowed)` + `assembly.load_charmap_chars(charmap.txt)`: per dialogue string, `\"`→typographic `“`/`”` (open/close toggle), `*`→`~`, `[`→`(`, `]`→`)` (`_CHARMAP_SUBS`); **fail-loud** on any other unrepresentable glyph (so a new offender is a deliberate decision, not silent corruption). Corpus-wide scan (`scripts/charmap_lint.py`, reuses `load_charmap_chars`) over all done+slice scripts found only 4 bad chars: `"`×68, `*`×18, `[`/`]`×2. Sub policy USER-CHOSEN (curly `"`, `*`→`~`, `[]`→`()`). (2) **dispatcher `goto`** — `metadata_wiring.build_page_dispatcher` emitted `goto {dest}` (space form) which **poryscript 3.6.0 splits into two invalid asm lines**; fixed to `goto({dest})` (corpus-wide — every dispatched multi-page event). Verified vs real poryscript binary. (3) **`healparty`** (LLM-invented) → `special(HealPlayerParty)` (a real `def_special`); parens required (bare `special HealPlayerParty` also splits). `deterministic.format_pory_string` band-aid REVERTED to escape `\"` — `normalize_pory` is now the single owner of charmap policy (§4.3). Normalizer wired into `assemble_pathfinder.py` (replaces `_sanitize_pory`), covers per-map staged `.pory` + CommonEvents. New tests in `test_assembly.py` (7 normalize_pory) + updated dispatcher goto + reverted format test. **Round 2 — 7 undefined-symbol link errors (symbol resolution, not syntax — same weak-gate root), all FIXED (user: "do all three"):** **(C) missing `<Map>_MapScripts` label** ×3 — every pokeemerald map needs a `mapscripts <Map>_MapScripts {}` symbol; the agent emits none. FIX: `assemble_pathfinder.run_fork_pass` prepends an empty `mapscripts {dir_name}_MapScripts {}` per map (poryscript → `<Map>_MapScripts::\n.byte 0`). **(B) unminted cross-event self-switch flags** ×3 — `FLAG_MAP049_EVENT019_SSA` (set by EV018), `FLAG_MAP032_EVENT075_SSA` (EV074), `FLAG_MAP032_EVENT081_SSA` (EV080): an event sets the *next*, bodyless event's self-switch (RMXP "spawn next NPC"), so it was never minted → name in text, no address. FIX (user-approved "mint every referenced"): `assembly.referenced_switch_keys` scans the final assembled scripts; `assemble_pathfinder._emit_flags_header` mints every referenced self/temp switch **in-memory before `dump_header`** (idempotent; `flag_state.json` NOT rewritten — header is regenerated deterministically each assembly). self-switch count 45→48. **(A) invented `MULTI_DREAM_VISUALIZER`** ×1 — CommonEvent_086 (dream feature, out-of-slice/P6-deferred) emits `multichoice(...,MULTI_DREAM_VISUALIZER,...)`. FIX: `assembly.patch_undefined_multichoice` (+ `load_multi_constants` reads the fork's `script_menu.h` defined set) stubs any `multichoice(MULTI_undefined)` → `setvar(VAR_RESULT, 0)` in CommonEvents (unreachable in slice). 12 new `test_assembly` tests (7 normalize_pory + 2 multichoice + load_multi + referenced_switch_keys). **KNOWN — 2 persistent `test_tileset_converter` failures (`test_build_slice_constants`/`_maps_smoke`: `MAP_MOKI_TOWN collides with vanilla`) are ENVIRONMENTAL, not a regression:** the fork's build-generated `include/constants/map_groups.h` now contains the assembled `MAP_MOKI_TOWN`, so a *fresh* mint (as the tests do) self-collides; real staging loads persisted `map_constants.json` (no re-mint) so the build is unaffected. Follow-up (deferred): give those tests a pristine-vanilla map-const snapshot instead of reading the live fork. **All changes UNCOMMITTED.**
- **S8.4 SPAWN OVERRIDE — APPLIED 2026-06-18.** Build passed but a fresh boot just started vanilla Emerald (truck→Littleroot) because the roadmap's S8 step-4 new-game-spawn redirect was never done. `WarpToTruck` (`src/new_game.c`) still pointed at `MAP_INSIDE_OF_TRUCK`. FIX: replaced its body with `SetWarpDestination(MAP_GROUP/NUM(MAP_MOKI_TOWN_PLAYERS_HOUSE_1F), WARP_ID_NONE, 7, 7)` (= Map049 spawn, `URANIUM_START_MAP=49 @7,7`; roadmap S8.4's "2F" text is stale — S1 corrected 049=1F/spawn), wrapped in `BEGIN/END URANIUM PATHFINDER SLICE` sentinel comments (revertable; modifies fork baseline, but roadmap-specified). `make modern` EXIT=0, ROM rebuilt. Birch intro still runs pre-warp; truck cutscene skipped; player now lands in Map049. **This is a fork-source change (not from assemble_pathfinder) — re-running assemble does NOT touch new_game.c, so a fresh fork clone / git checkout drops it.** S9 manual boot-in-mGBA is the only remaining gate.
- **S9 BOOT DIAGNOSIS — 3 ROOT-CAUSE CLASSES FOUND 2026-06-18 (ROM boots+runs, but unplayable).** User report: nonsensical layout, crowd of identical NPCs, can't leave the spawn room. Diagnosed (all evidence-verified, fixes NOT yet implemented):
  1. **WARPS DON'T FIRE = "can't leave the room" (THE blocker) — FIXED 2026-06-18 (converter level; needs re-assemble+rebuild+boot).** The S2 design note "warps need no special tile (warp_event fires on coord)" is **WRONG about pokeemerald**. `field_control_avatar.c` `TryStartWarpEventScript`/`TryDoorWarp`/`TryArrowWarp` ALL gate on `IsWarpMetatileBehavior(metatileBehavior)==TRUE` (warp-door/ladder/escalator/non-anim-door/arrow/stair behaviors). The v1 bucket layout gave every warp tile the generic *passable floor* metatile (behavior `MB_NORMAL`), so the player walked onto the door/stairs and nothing happened (BFS on the staged `map.bin` confirmed both Map049 warps reachable but inert). **FIX:** added a per-tileset `warps` metatile to `reference/tileset_map.json` + `TileMap.warp()` (`tile_map.py`, loaded/validated/fail-loud) + `convert_layout` now STAMPS that metatile at each warp coord (renamed param `walkable_overrides`→`warp_overrides`; layout.py). Chose **`MB_NON_ANIMATED_DOOR` step-on doors** (collision 0, fire regardless of approach direction, player arrives safely on the walkable tile — sidesteps animated-door arrival/door-exit-anim complexity): **ts19→metatile 529** (gTileset_BrendansMaysHouse), **ts22→metatile 167** (`MB_NON_ANIMATED_DOOR` in the shared gTileset_General primary). Harvest method (cleanest): read a working vanilla map's `warp_events`, decode the metatile vanilla puts under each (`scripts/harvest_bucket_metatiles.py` now reports warp candidates too) — behavior travels with the metatile id within the shared tileset, so copying is guaranteed correct. **Collision is per-cell in map.bin, NOT in the tileset; behaviors ARE in the tileset's metatile_attributes** — so the warp metatile must be one that already has the behavior, but its collision is ours to set (0). Verified: regenerated staged `.bin`, decoded all 4 slice warp tiles → all carry `MB_NON_ANIMATED_DOOR` col0 elev0. Tests updated (`test_tileset_converter.py`: warp stamp + fail-loud + tile_map.warp + validation), full suite 416 pass (only the 2 known env MAP_MOKI_TOWN failures). **Conclusion** Rebuild worked, able to move between maps
  2. **CROWD OF IDENTICAL NPCs.** Two compounding bugs in `metadata_wiring.build_object_events`: (a) it emits EVERY non-warp event as a visible solid object with **no check for an RMXP graphic** — invisible script triggers (furniture/bookshelf action-buttons, autorun cutscene controllers, the Letter, HEAL points) become solid `OBJ_EVENT_GFX_NINJA_BOY`. Census of emitted objects that are actually graphic-less in source: **Map049 16/20, Map048 12/12, Map032 3/43.** (b) Even real NPCs all use the single `DEFAULT_GFX=OBJ_EVENT_GFX_NINJA_BOY` (gfx map deferred). FIX: skip/invisible+non-blocking for graphic-less events (coord/bg event or `OBJ_EVENT_GFX_NONE`, mind RMXP graphic-less = passable), and build the RMXP `character_name`→`OBJ_EVENT_GFX_*` map.
  3. **LAYOUT "MAKES NO SENSE" (by-design v1, documented).** The passability-bucket scheme collapses each tileset to TWO metatiles (one floor, one wall, +void), so the whole house renders as a uniform floor field in uniform walls — no doors/furniture/stairs/structure. Not a bug per the v1 decision, but it IS why it's visually meaningless; the real per-tile substitution (`tiles` table in `tileset_map.json`, empty in v1) is the fix. RECOMMENDED ORDER: warp behaviors (1) → NPC visibility+gfx (2) → real tile substitution (3).
- **INTRO-SKIP (option C) — APPLIED 2026-06-18 (user-requested).** Boot now jumps straight into the slice: copyright screen (kept; it's where save-block pointers + heap init) → new `CB2_StartUraniumSlice` (`new_game.c`) → stamps default identity (name "RED" via `_()`, `playerGender=MALE` — the Birch speech is the only thing that normally sets these) → stock `CB2_NewGame()` (→ NewGameInitData → patched WarpToTruck → Map049) → `gFieldCallback=NULL` to skip `ExecuteTruckSequence` (truck-only: rewrites InsideOfTruck metatiles + locks player → would corrupt/softlock a normal map). Hooked at `intro.c` `case COPYRIGHT_START_INTRO` (after save init, single copyright→intro transition): `SetMainCallback2(CB2_StartUraniumSlice)`, original intro/title block wrapped in `#if 0`. Prototype in `new_game.h`. All 3 edits `URANIUM PATHFINDER SLICE` sentinel-fenced, revert as a unit WITH the WarpToTruck hack. `make modern` EXIT=0. **Consequence: every boot = fresh new game (flags wiped, 0 party) — fine for walkability, NOT saved-progression. Two stacked test-only baseline divergences (WarpToTruck + intro-skip) — must revert/gate before any real build.**
- **Bulk run state:** CommonEvents done + 7/199 maps (87 spawns, $16.93 provider-reported, 241 queued; last round limit_reached). NOTE: done maps are 001–007 by ID, but player spawns at **Map049** — none of the done work is the playable first slice. Resume (if global decision reverts): `scripts/run_bulk.py --timed`; stats: `scripts/run_stats.py`. ~2,500 Opus spawns remain ≈ ~3 months (throughput decision 2026-06-11: ride the Pro plan).
- **Deterministic pre-filter:** **9 classifiers** claim **1294/3,581** events (**36.1%**) spawn-free; output indistinguishable downstream (same compile-gate/mint/queue). (+49 earlier from the FABLES 3.6 Wait/SE rider; **+231 from the 2026-06-14 ground-item classifier — Group 1 Step 1**; **+17 from the mart classifier — Step 2**.)
- **`classify_ground_item` (Classifier 8, 2026-06-14).** The roadmap's "#1 lever / self-switch gate" `{111,123,411,412}` was a **misread** — drilling showed all **230** are the `pbItemBall` ground-item idiom (a script-type `111` testing `Kernel.pbItemBall(::PBItems::SYM)`, set self-switch, empty else, empty gate page; uniform shape `(111,123,0,411,0,412,0)`, qty always 1). Emits the canonical pokeemerald pickup `lock / giveitem(ITEM_X, 1) / setflag(SS<letter>) / release / end` + bare-`end` gate blocks (no `faceplayer`) — byte-for-byte vs frozen-Opus. Needs the **item-symbol→`ITEM_*` map now populated in `deterministic.load_context`** (`_load_item_symbols`: `reference/item_internal_names.json` + `item_names.json` via `to_constant`, same rule as `items._ItemResolver`); unknown symbol or any structural deviation falls through. Validated by `tests/test_deterministic.py::test_ground_item_*` incl. a **differential test driven off `checkpoints/Map*.done`** (invariant 4 — `.partial` maps excluded; auto-covers new maps as the bulk completes). Human lane shrank 296→65 (ground items were a big slice of Track D).
- **`classify_pokemart` (Classifier 9, Step 2, 2026-06-14).** A mart clerk whose page-1 sole content is `pbPokemonMart([...])` → `lock/faceplayer/pokemart(<label>)/release/end` + a `mart <label> { ITEM_*… }` block (items in source order, resolved via `ctx.items`; handles both `::PBItems::X` and bare `PBItems::X`). 18 clean single-page events, all resolve. **Step 2's only solid win** — drilling showed the roadmap's other Step-2 targets don't qualify: **pbPokeCenterPC (17) + pbShowMap (18) are UNHANDLED-form** (frozen-Opus *queues* them — engine-gated, not native macros; thin 2/4-instance oracle → deferred per §8), and **give-item "`item` (38)" is a mirage** (`item=rand(8)` shard-pickup logic, not give-item; `pbReceiveItem` is embedded in dialogue/branches w/ dynamic `pbGet` forms).
- **BUG F1 — FIXED + REPAIRED (G1 passed 2026-06-12).** Fix `dc8e436`+follow-ups; maps 1–7 regenerated at 0 spawns; corpus dup-label scan = 0 (`scripts/dup_label_scan.py`); targeted Map002 re-assembly confirms the dup gone. Two latent bugs fixed during repair: `_reinstantiate` stale-guard bailed on same-(map,event) replay (any self/temp-switch event re-spawned instead of replaying — also silently re-spent spawns on every interrupted-map resume); `run_replay` used `convert_all` (would walk into unconverted maps). **G1 FINDINGS (open, queue/triage fodder — list in `FABLES_IMPLEMENTATION.md`):** CE-084 `\"` escape mangled by poryscript → preproc-fatal `\$` that aborts ALL of event_scripts.s; `healparty`×2 + bare `call CommonEvent_NNN`×3 (Map002; poryscript passes unknown/bare commands through raw = weak compile-gate); constant-naming drift (agent vs Phase 2/registry: ITEM internal-vs-display names, TRAINER scheme mismatch, invented MULTI_*, one un-minted self-switch flag). Smoke full-green blocked on these (non-F1 scope; most look deterministically repairable, needs design OK).
- **Implementation queue (`FABLES_IMPLEMENTATION.md`) — ALL 4 PHASES DONE 2026-06-12.** P1 F1 label-uniquing + smoke (G1 passed); P2 #3 cluster triage; P3 #2 STRIP skip + 3.6 Wait/SE rider (G2 passed `444fbee`, +49 deterministic); P4 Phase-5-track prep (4.1 §5.5/§5.6 docs + stubs, 4.2 `tilesets` deserializer, 4.3 `map_identity_check.py`, 4.4 `map_name_overrides.json` — all `2bd4e6d`/`a125019`). Last lead commits this session: `e394986` `444fbee` `636e598` `2bd4e6d` `a125019`. **Queue clustered** (`run_stats.py`/`--novel`): live **187** (was 214; 27 stripped-CE entries pruned at 3.4). **§10 fidelity calls RESOLVED 2026-06-12** (racing=stub→P8 ADAPT, dream=stub→P6/8 ADAPT, VS-intro=accept loss — see Decisions Made + `reference/novel_cluster_review.md`). **STILL PARKED:** G1 finding dispositions (below — these are build-agent repairs, not user calls; healparty = post-run prompt fix, rest = Phase-5 assembly). **Bulk run NOT resumed — user's call** (`scripts/run_bulk.py --timed`).
- **Phase 5:** scaffold (`b7a7be2`) + FABLES prep done — §5.5 move-routes & §5.6 reachability written; `Tilesets.rxdata`→`tilesets.json` deserializer (`tilesets` mode, 60 tilesets w/ passages oracle); map identity arbitrated (`map_name_overrides.json`, 0 overrides — see Discoveries). **Next (Phase 5 proper, not started):** 5.1 `tile_map.py`, then `map_constants.py` (consults overrides before map_infos), `reachability.py` BFS, `tileset_map.json` ledge rows, GBA header-length check. `strip_list.json` now also carries whole-`maps` strips (Map999).
- **Phase 4 build history** (machinery, calibration A–D, rung 2/3, dedup A/B/C, pre-filter C1–C7): archived 2026-06-11 → `reference/memory-archive.md`.

## Archived 2026-06-18 — Last Session Summaries (pre-pivot, 2026-06-14)

**2026-06-14 (iterative roadmap Group 1 Step 1 — ground-item classifier, branch `deterministic-expansion-strategy`):** User chose to start executing `ITERATIVE_ROADMAP.md` (bulk paused at 7/199, roadmap shadowed not ratified). First committed `9961398` the loose OQ-3 Step-1 work. Then built the roadmap's "#1 lever." **MEASURE → drill exposed a roadmap misread:** the `{111,123,411,412}` 230-signature is NOT a self-switch dialogue gate — all 230 are the `pbItemBall` **ground-item** idiom (no `101` text; the roadmap's own "drill before trusting" caution paying off). Built **`classify_ground_item` (Classifier 8)** + populated `load_context().items` (`_load_item_symbols`, reusing the `items._ItemResolver` naming rule — all 143 symbols resolve). 13 new tests incl. golden, fall-throughs, compile-gate, and a **differential test off `.done` checkpoints** (caught Map008/Map010 being `.partial`, not divergent — fixed the test to key on checkpoints). **Step 1 result: free 1046→1277 (29%→35.7%, +231), Opus 2535→2304, human lane 296→65.** Committed `e14717b`. **Then Step 2 (same session):** drilled the shop/PC/heal + give-item + show-map targets — only **`classify_pokemart` (Classifier 9)** qualified (18 clean events → `pokemart`+`mart` block; +17 free → **1294, 36.1%**). PC/ShowMap deferred (UNHANDLED-form, thin oracle), give-item was a mirage (see the mart Key File Note). **Step 2 also surfaced the Opus-item-constant unreliability finding** (`ITEM_PARALYZE_HEAL` vs real `ITEM_PARLYZ_HEAL` — see Decisions 2026-06-14); differential test now splits structure (vs Opus) from item spellings (vs `item_field_codes.json`). 349 pass + 9 phase4, ruff clean. Pure Track-A bank both steps (no bulk slice). **Next: the high-value Track-A surface is now largely exhausted (remaining top levers are move-route/warp/door = Group 3, Track-B-gated) → recommend pivoting to Track B (Group 2 foundation: `map_constants.py` + local-object-ids + assembly smoke) before more classifiers.** Uncommitted: mart classifier + tests + MEMORY (Step 1 committed at `e14717b`).

**2026-06-14 (command-vocabulary census + agent-coverage diff — branch `deterministic-expansion-strategy`, no spend):** User asked "how many total unique commands must we drop / match-to-pokeemerald / write custom for?" and explicitly distrusted the ref docs → answered by walking the deserialized corpus directly. Built **`scripts/count_unique_commands.py`** (zero-LLM, all 199 maps + 100 CEs, no handled/unhandled filter) → **195 real mappable commands** = 47 structural RMXP codes + 148 named script-call heads; emits `reference/uranium_real_commands.md` (File 1) + `uranium_excluded_noncommands.md` (File 2, strict-filter w/ documented call-syntax/engine-prefix/bareword/Module.method rule; 16 `$Obj.field=` global state-writes split out as mappable-but-unnamed). Then user asked how this compares to the conversion agent's referenced lists → **`reference/agent_coverage_diff.md`**: **codes exact-parity (59=59)**; script calls — agent ENUMERATES a 250-sig `355/655` superset but DISPOSITIONS only ~54 (~83% lines; ~91% occ w/ `system.md` battles + `onEvent?` trigger), rest queue by the explicit "absent⇒UNHANDLED" rule; **gaps:** the agent's inventory is `355/655`-only so **111-condition action-calls** aren't enumerated (31 heads/613 occ — benign, mostly UNHANDLED-tagged siblings), and `uranium_script_calls.md` **counts are stale** (`pbSetSelfSwitch` 336→191, `pbItemBall` 46→243). New Key File Note + Discovery recorded. **No conversion-logic change, no budget.** Uncommitted: the script + 3 reference docs + MEMORY/archive edits.

**2026-06-21 (faithful-column tileset rework + slice build — branch `deterministic-expansion-strategy`):** Resumed an interrupted bug-fix plan (prior session hit the session limit mid-implementation). Found the uncommitted diff = a coherent rework of the S8a tileset emitter to fix the **flat-layout** defect: the committed S8a keyed one metatile per *topmost-visual* tile id, discarding RMXP's z-stack. Reworked to key each metatile by the **full column** (`layout.column_key`, autotile variants distinct), **two-layer** GBA split by RMXP **priority** (`build_slice_tilesets._render_column`), **flip-aware** 8×8 dedup, and a **fail-loud budget guard**; `emit_tileset` now consumes a `MetatileImage` list; `tile_map` keys `tiles` by serialized column-key string (`serialize_column_key`) + `lookup_column`/`has_columns`; `collapse_column` dual-path (column vs legacy bucket). Code + test edits were present and internally consistent (494 pass), but the plan had NOT been run end-to-end. **Ran the real assemble → the budget guard fired:** ts22 (Moki Town) = **1102 GBA tiles > 1024** (column-keying multiplies tiles; flip-dedup alone wasn't enough). **Fix I added: post-quantization merge** (`emit.py` Step 2.5 — tiles that snap to the same palette+pixels share one stored GBA tile; no-loss, bounded by the quantization we already accept) → ts22 **1011 tiles** (ts19 321→306). Re-assembled (all 3 maps **0% void**) → `make -C engine -j16 modern` **builds clean** (32MB `pokeemerald.gba`, ROM 78.75%). Added 2 tests (post-quant-merge, budget-overrun-fails-loud-before-write); **499 pass / 11 skip / 2 fail** (the 2 = known built-fork MAP_MOKI_TOWN collision, NOT a regression), ruff clean. [Superseded 2026-07-01 by the full-corpus walker build.]

## Archived 2026-07-07 — Map Walker Phase A/B build narrative

> Moved out of MEMORY.md's Current Phase during the post-audit MEMORY diet (the
> engine-mechanic gotchas in here are now the canonical `reference/engine_extension_surface.md`;
> this is the walker feature's own build history, kept for provenance). Walker
> Phase A + Phase B are DONE, Checkpoint 1 + Checkpoint 2 both PASSED, and the
> work is committed/merged to main — see MEMORY.md Current Phase for the
> post-Checkpoint-2 state (warp fidelity + terrain behaviors, native-reuse audit,
> engine extension surface spec).

**MAP WALKER (debug ROM mode) — Phase A in progress** (plan: `reference/map_walker_plan.md`; §9 impl sequence). Builds clean on `engine/` w/ `URANIUM_MAP_WALKER TRUE` (`include/config/uranium_walker.h`). **DONE (uncommitted):** boot chain → `UraniumWalker_FieldCB_MapLoad` (new_game.c→overworld.c CB2_NewGame arms walker cb); invisible anchor + event gating (overworld `TrySpawnObjectEvents`/`TryRunOnWarpIntoMapScript`; field_control `TryStartStepBasedScript`); bounds-clamp collision (`GetCollisionAtCoords`); **step 4** cursor sprite in `src/uranium_map_walker.c` (runtime-built 4bpp tiles: transparent/black-ring/white, blink anim, screen-pinned @120,80 *needs by-eye pixel tune*, warp-tile detect→white flash); **§4.10 raw display** (`OW_ENABLE_DNS` gated FALSE under walker flag in config/overworld.h; `WEATHER_NONE` forced). Link gotcha hit+fixed (GBA-ld-discards-file-scope-`.data`-statics, → `engine_extension_surface.md` §4): made sprite sheet/palette structs `const`, zero-init sprite id. **Activation bug hit+fixed (first boot showed normal slice — player visible, NPCs, no cursor):** (a) walker FieldCB was armed in new_game.c AFTER `CB2_NewGame()` returns = no-op (gFieldCallback-consumed-synchronously trap, → §4) → moved the arm INTO overworld.c `CB2_NewGame` under the flag; (b) `sWalkerActive` now flipped via new `UraniumWalker_Begin()` in `CB2_StartUraniumSlice` BEFORE the map load so runtime gates apply during load; (c) NPCs popped in via the camera-scroll spawn path — gated `TrySpawnObjectEvents` itself (event_object_movement.c), not just the InitObjectEventsLocal call site. NOTE for warps: `SpawnObjectEventsOnReturnToField` (InitObjectEventsReturnToField) still ungated — gate when warp-follow lands. **Step 4 boot-CONFIRMED 2026-06-27** (centered ring + warp white-flash render; anim-frame bug fixed first: `ANIMCMD_FRAME` value is a raw TILE offset not a frame index → ring=FRAME(4)/white=FRAME(8)). **Step 5 (warp A/B + back-stack) DONE+built (uncommitted):** A on a warp tile → `SetWarpDestinationToMapWarp`+`DoWarp` (slice warps resolve via standard `WarpEvent`, NO side-table needed); B pops a 32-deep EWRAM back-stack → `SetWarpDestination(...,WARP_ID_NONE,x,y)`+`DoWarp` (land on exact tile left). `DoWarp` arms `FieldCB_DefaultWarpExit` → walker re-arms `gFieldCallback=UraniumWalker_FieldCB_MapLoad` after; `sWarpPending` ignores input during transition; FieldCB guards dup task via `FindTaskIdByFunc`. Field button handlers (interaction/door-warp/start/select/dexnav) gated off in `field_control_avatar.c ProcessPlayerFieldInput` under the walker. **Step 6 SCOPED (user: HUD now, defer rest):** L-toggle debug HUD built (window modeled on debug.c sDebugMenuWindowTemplateMain, bg0/baseBlock=1; line1 `M{group}-{num} ({x},{y})`, line2 `mt {metatileId}`; recreated after warp since windows reset on map load). **R warp-reveal + Start jump-menu** were deferred from Phase A → **implemented 2026-06-29** (see the Phase B status block below). **NEXT: user boot-walks 3 maps (warp A/B + HUD) = Checkpoint 1 (§9 gate).** Steps 5–6 + the new field_control gate are uncommitted. **Family quantization now realized in the walker ROM** (re-assembled + rebuilt 2026-06-27); the obsolete `URANIUM PALETTE-COMPARE` SELECT hook in field_control_avatar.c was **REMOVED** (family chosen → A/B/C moot; the re-assemble clobbered its variant tilesets, breaking the link). `build_palette_compare.py` + the diversity packer remain untracked/unused.

**PHASE B STARTED 2026-06-27 (expand walker to all 199 maps; user: incremental rollout).** **Step 8 DONE (single source of truth, §5.1):** new `src/rpg2gba/tileset_converter/map_set.py` — `SLICE_MAP_IDS=[49,48,32]` defined ONCE here + `discover_all_map_ids`/`parse_map_ids`/`resolve_map_ids` ("slice"|"full"|comma-id-list, fail-loud on missing ids, applies strip_list maps). 9 tests (`tests/test_map_set.py`). Repointed all 5 prior copies (assemble_pathfinder, stage_slice_scripts, pathfinder_map_{constants,wiring}_preview import it; map_viewer_common's was dead since the full-corpus-pool commit → removed). 199 non-contiguous map ids on disk (`output/uranium-build/maps/Map*.json`). **REMAINING Phase B:** (9) `phase5.convert_all` (stub) — must generalize assemble_pathfinder's 3 passes (run_graphics/layout/fork) which read module-global `SLICE_MAP_IDS` + a HAND-BUILT `WARP_OVERRIDES`; the overrides must instead be COMPUTED from each map's code-201 warps (metadata_wiring.build_warp_events already returns src_coords). (§5.4) per-map tileset packing rework — current build_slice_tilesets POOLS by tileset_id (overflows 19/38 tilesets at full corpus); switch grouping unit tileset→map + fail-loud 1024 budget guard. Warp dest coords currently DROPPED (only dest_warp_id pairing survives, metadata_wiring WarpSpec) — fine for walker (standard WarpEvent path); **gUraniumWarpDests side-table (§4.7) deferred** unless pairing lands warps wrong at scale. Then small-batch validate (slice + neighbors, boot) → full 199 → Checkpoint 2.

**PHASE B design locked 2026-06-27 (analysis done; impl next):**
- **Small batch chosen** = `[49,48,32,33,50,64,65,172]` (slice + Moki Town warp-neighbors: 33 ts22 route, 50/64/65/172 ts19 buildings — all warp-reachable from spawn). Census (`column_keys_for_maps`): per-MAP all fit (max Map033=1016<1022); per-TILESET ts22 pooled(32+33)=**1607 OVERFLOWS** → confirms per-map packing required + exercises the budget guard.
- **Per-map packing ENABLER (the key insight):** feed `build_slice_tilesets` **unique synthetic tileset ids per map** (e.g. `1000+map_id`, avoid colliding real ts 0–60) so its existing group-by-`tileset_id` loop yields one physical tileset per map — NO loop rewrite, and default behavior stays per-tileset (existing tests unaffected). Needs: (a) `build_slice_tilesets` param `source_tileset_of(synth)->real` + write `overlay["source_tilesets"][synth]=real`; inject `rasterizer_for`/`priorities_for` that map synth→real (both already injectable params). (b) `tile_map.load_tile_map`/`_load_oracle`: read `source_tilesets`, load passages/priorities for the REAL ts but store under the synth key, so `collapse_column`/`convert_layout` use the synth key uniformly for BOTH overlay lookup AND passages. (c) `convert_layout`: pass the synth key (override map_json tileset_id in a copy, or add a `tileset_key` param). `emit_tileset` already has the fail-loud 1024 budget guard.
- **Walker is WARPS-ONLY (§5.3) → SKIP the whole script pipeline** (`stage_slice_scripts` is slice-hardcoded `ALLOWED_MAPS={49,48,32}` + always regenerates DEFAULT_SLICE — do NOT try to generalize it). The real driver = implement `phase5.convert_all` (currently `NotImplementedError`) as warps-only: (1) `map_constants.build_map_constants(batch)`; (2) per map a minimal porymap map.json with ONLY warp_events (object/coord/bg counts=0, empty scripts.inc) via `metadata_wiring.build_warp_events(map_json,this_uid,set(batch),registry,warp_lists)` — needs the cross-map return-warp pairing over the batch (warp_lists built from each map's code-201 src coords); (3) per-map graphics (synth-id) + `convert_layout` (synth key) + headers/map_groups.gen.json/map.json copy (generalize assemble_pathfinder's run_layout/run_fork from module-global SLICE_MAP_IDS to `parse_map_ids(--maps)`, and derive WARP_OVERRIDES from each porymap map.json's warp_events x,y instead of the hand-built dict). Warp dest coords still via dest_warp_id pairing (no side-table). metadata_wiring building blocks: `WarpSpec`(src_x,src_y,dest_uid,dest_x/y[dropped]), `build_warp_events`, `build_slice_maps` (full porymap export incl events — want a warps-only subset of it). Boot the batch (spawn 49→32→neighbors) before full 199.

**PHASE B STEPS 9–11 DONE + SMALL BATCH ROM BUILT 2026-06-28 (uncommitted; built under /delegate).** `phase5.convert_all` IMPLEMENTED (`tileset_converter/phase5.py`, rewritten from the stub) as the warps-only walker driver — does all 5 steps in the PACKAGE (leaving `assemble_pathfinder.py`'s proven slice path 100% untouched): (1) `build_map_constants(batch, alias_header_path=…)`; (2) new `metadata_wiring.build_warps_only_maps` (object/coord/bg=[], warp_events only, returns src_coords = WARP_OVERRIDES computed from code-201 — no hand-built dict); (3) per-map graphics via `build_slice_tilesets` fed **synthetic tileset ids `_synth_id=1000+map_id`** + `source_tileset_of=lambda s: synth_to_real[s]`; (4) `convert_layout(..., tileset_key=_synth_id(mid))`; (5) `_assemble_fork` = mechanical fork glue (copy map.json + .bin, write **empty `<Dir>_MapScripts::\n.byte 0` scripts.inc** per map, seed `layouts.gen.json`/`map_groups.gen.json` from pristine + append, `write_alias_header`, **empty `uranium_flags.h` stub** since the event_scripts.s hook `#include`s it, `uranium_includes.inc` w/o CommonEvents). **Synthetic-id contract (3 files, backward-compatible — None path byte-identical, all existing tests pass):** `build_slice_tilesets` got `source_tileset_of` param (writes `overlay["source_tilesets"][str(synth)]=real`, resolves synth→real for the default rasterizer/priorities); `tile_map._load_oracle` got a `source_tilesets` arg (reads tilesets.json oracle for the REAL id, stores under synth key); `layout.convert_layout` got `tileset_key` override. CLI: **`python -m rpg2gba.pipeline phase5 --maps slice|full|49,48,32,…`** (resolves via `map_set.resolve_map_ids`). **Two new map_name_overrides** (64→`MAP_MOKI_TOWN_HOUSE_2`, 65→`MAP_MOKI_TOWN_HOUSE_1`): both buildings share the generic editor name "Moki Town" (parent 32) → mint collided fail-loud; disambiguated west-to-east by their Moki Town door X (65 door x=24 < 64 door x=43); 172 "Moki Town (Theo)"→`MAP_MOKI_TOWN_THEO` minted clean. **BUILD RESULT:** `phase5 --maps 49,48,32,33,50,64,65,172` → per-map tilesets all fit (ts**1032** MokiTown 841 mt / **1022** tiles — vs pooled ts22 1607 OVERFLOW, so per-map packing is the thing that makes this buildable; ts1033 Route01 1018mt/813t; buildings ~56mt each), **every layout 0% void** (synth overlay resolves end-to-end), `make -C engine -j modern` **LINKS CLEAN** → `engine/pokeemerald.gba` (ROM 78.97%/32MB, EWRAM 87.1%, IWRAM 87.1%), walker flag `URANIUM_MAP_WALKER TRUE`. 571 tests pass (+ the 2 known MAP_MOKI_TOWN slice-mint failures, unrelated; agents added focused synth-id tests to test_build_slice_tilesets/test_tile_map/test_layout). All 8 maps warp-reachable from spawn (Moki Town doors → 49/48/50/64/65/172/33), so warp-follow (A) reaches them. **NEXT: user boot-walks the 8-map batch in mGBA (Checkpoint 2, §9 gate) — confirm new maps' art + warps; then full 199.**

**R warp-reveal + START jump-menu IMPLEMENTED + BUILT CLEAN 2026-06-30 (uncommitted; via /delegate, 2 sub-agents on disjoint files + lead seam-fix). 8-map batch ROM built (32MB, ROM 78.48%) + taildropped; NOT yet booted (= Checkpoint 2, §9 gate).** Both in `src/uranium_map_walker.c`, sentinel-fenced. **R** = toggle a sprite-based warp-tile overlay: one OAM marker per warp (cap `UWALKER_OVERLAY_MAX=16`), reusing the cursor's `UWALKER_CURSOR_TAG` sheet + anim-1 (white-flash), repositioned every frame `sx=120+(wx-cx)*16, sy=80+(wy-cy)*16` so highlights track scrolling (a fixed window would not); off-screen markers hidden; rebuilt on map load if on. (Verified vs fork: `AnimateSprites` sprite.c:296-299 auto-calls `AnimateSprite` after each callback, so the no-op marker CB still animates.) **START** = modal map-list menu: `LockPlayerFieldControls` on open, D-pad up/down (`JOY_REPEAT`) wraps cursor, A warps to `SetWarpDestination(grp,num,WARP_ID_NONE,0,0)`+`DoWarp` (pushes back-stack; map-load CB unlocks), B/START cancels+unlocks; `Task_UraniumWalker` short-circuits to `UraniumWalker_UpdateMenu` while `sMenuOpen`. Map list is **generated from the BUILD BATCH** (NOT hand-listed — respects §5.1 SoT): `map_constants.write_walker_maps_header` emits `engine/include/uranium_walker_maps.h` (gitignored, on `-I include`; wired in `phase5._assemble_fork` — passes **its `map_ids` batch, not the fixed `SLICE_MAP_IDS`**, so the menu lists every map in THIS ROM; `assemble_pathfinder.run_fork_pass` still passes its 3-map slice) with `URANIUM_WALKER_MAP_COUNT` + `URANIUM_WALKER_MAP_LIST(X)` X-macro; the .c builds `sWalkerMapList[]` from it. Labels are **id-prefixed** (`"49 Moki Town Player's House"`) so duplicate-named maps (e.g. the two house floors) don't collide after truncation; golden test `test_walker_maps_header_golden`. **Two gotchas hit+fixed during the build: (1)** inline strings must use **`COMPOUND_STRING("...")` NOT bare `_("...")`** — `_()` expands to a braced byte list (only valid for `const u8 name[]`), illegal as the `const u8 *name` struct field (`COMPOUND_STRING` = `(const u8[]) _()`, metaprogram.h:34, reachable via global.h). **(2)** menu window line spacing dropped 12→10px so the `count+3`-tile window fits up to ~11 entries (larger sets need a scrolling list — TODO for full 199). **Build note: the generated header is NEW — re-run the assembler (it emits the header) BEFORE `make`, else the `#include` is missing.**

---

**2026-07-01 (Map Walker Phase B FULL CORPUS + Phase C viewer parity — branch `deterministic-expansion-strategy`; delegate mode):** Completed `map_walker_plan.md`. **ROM BUILDS CLEAN: `phase5 --maps full` → 177 Uranium maps, `make -C engine -j16 modern` → ROM 86.59%/32MB (~29MB), EWRAM 87.1%, IWRAM 91.5%.** Uncommitted. **21 maps excluded** (all in `map_set.WALKER_EXCLUDED_MAP_IDS` = `WALKER_OVERFLOW_MAP_IDS` ∪ `WALKER_EMPTY_MAP_IDS`, subtracted only from the "full" profile; explicit id-lists still fail loud at the emit budget guard): 16 over-budget (9 metatile-census {94,101,187,40,84,117,143,71,144} + **7 tile-budget, measured via real render path** {60,122,128,146,151,209,213} — the census only measured metatiles, but emit enforces a SEPARATE 1024 8×8-tile cap) + 5 empty placeholders {14,30,38,55,56}. **Five data-quirk classes the small batches never hit, each fixed generally:** (1) **32 duplicate-name groups / 80 maps** (multi-floor caves, "(Metro)" variants) → `map_constants.mint(auto_disambiguate=True)` suffixes the INTERNAL const/dir with the map id (`MAP_COMET_CAVE_10`, dir `CometCave10`), display_name preserved (HUD/menu read real name); walker-only, strict fail-loud default kept for the real pipeline. (2) **garbage/out-of-atlas tile ids** (e.g. 3408 in a 304-tile Gatehouse) → `build_slice_tilesets` drops out-of-atlas columns; overlay carries `atlas_max` per tileset; `tile_map.column_in_atlas` + `layout.collapse_column` void them so BOTH the tileset pass and the layout pass agree (a mismatch was the "unmapped column" crash). (3) **empty maps** → excluded. (4) **warps on empty/garbage cells** (no door column) → `build_slice_tilesets` emits a transparent `MB_NON_ANIMATED_DOOR` warp-metatile fallback so the warp still fires. **STOCK EMERALD MAP DATA DROPPED (§5.5, the size lever):** `phase5._stub_stock_layout_bins` repoints all 785 stock layouts' blockdata/border to one tiny `data/layouts/UraniumWalkerStub/` dummy in `layouts.gen.json` — ZERO C changes (every `MAP_*`/`LAYOUT_*` constant intact, no link risk; safe because walker suppresses stock events + Uranium maps have `connections:null`). Default-on in phase5; `--keep-stock-maps` opts out. **This alone fit the ROM — the stock tileset-graphics drop (~1.2MB, more invasive) was NOT needed.** **Scrolling START menu:** `uranium_map_walker.c` rewritten from the ~11-entry hand-drawn window to a pokeemerald `ListMenu` (9 visible rows @ tilemapTop=1, `LIST_MULTIPLE_SCROLL_L_R` paging, `sWalkerMapList` id + name); menu list generated from the build batch. **KNOWN ROUGH EDGE:** `sMenuListItems[URANIUM_WALKER_MAP_COUNT]` is a plain `static` → landed in IWRAM (→91.5%); should be `EWRAM_DATA` (EWRAM has ~28KB free) — 1-line + ~2min recompile, offered to user. **Viewer parity (Phase C):** `map_viewer_common._ensure_tileset_analysis` pool changed from slice `{49,48,32}∪opened` to per-map `[map_id]` (matches the ROM's per-map synth-id packing); dead `_get_tileset_index` removed. New CLI: `python -m rpg2gba.pipeline phase5 --maps full|slice|<ids> [--keep-stock-maps]`. Tests added (map_set exclusions, mint auto-disambiguate, graphics garbage-filter, phase5 stub-drop), ruff clean; pre-existing failures unchanged (2 MAP_MOKI_TOWN built-fork collisions + `test_real_slice_map032_prunes_clean` reading stale warps-only `output/`). Build gotcha learned: `phase5 --clean` does NOT clean the fork's Uranium map/layout dirs → a SHRINKING map set orphans stale dirs (mapjson "No rule for .../connections.inc"); a superset ("full" over the 8-map batch) is safe. `MtTamaranch` (id 217) renders 43% void — spot-check. (Checkpoint 2 §9 gate subsequently PASSED 2026-07-02.)

## Archived 2026-07-13 — Last Session Summary (2026-07-12 viewer↔ROM parity)

**2026-07-12 (Viewer↔ROM tileset parity: ROOT-CAUSED + FIXED — full findings/fix/resume doc: `reference/viewer_rom_parity_2026-07-12.md`; /delegate):** Brandon's viewer-vs-mGBA mismatch (survived a from-scratch rebuild) traced by 3 research agents. The quantized view calls the REAL pipeline code (`analyze_tileset_palettes`→family packer→same `to_5bit`), so pixels are byte-identical **given the same pool** — but the 2026-07-11 refocus set `pool_map_ids=[map_id]` (per-map, justified by phase5 WALKER synth-id packing) while the boot-gate ROM (`assemble_pathfinder`→`build_slice_tilesets`, no `source_tileset_of`) groups by REAL `tileset_id`: **ts19 = Map049+Map048 pooled into ONE 13-palette budget; ts22 = Map032 alone (coincidental match)**. Different pool → different `_allocate_by_overflow` split + merge ties → different palettes on 48/49. Secondary (can stale even Map032): server never re-reads disk (`_ensure_loaded` had no mtime check), knobs = silent global state now HIDDEN behind Advanced, `&g=` cache-bust only bumped on knob Apply; mGBA color-correction = display-only, not data. Ruled out: post-quant merge (storage-only), void/door pool omission (emit.py:213-217), 5-bit math. **FIXED (Sonnet builder, landed + smoke-verified):** slice maps pool by real tileset_id via `SLICE_MAP_IDS` grouping (frozenset({48,49}) shares one analysis entry; 32 alone; non-slice self-pool), stat-fingerprint auto-reload (map JSON + tilesets.json + slice siblings; reload clears caches + bumps generation), `?g=` on ALL image URLs, always-visible non-stock-knob badge, `map_viewer.md` pool section rewritten. **NEW oracle `scripts/verify_viewer_rom_match.py`** (2nd builder): Mode A = reconstruct build's exact metatile list → simulate → byte-compare vs DECODED engine artifacts (tiles.png 4-bit indices/JASC .pal/metatiles.bin) with tileset_map.gen.json order cross-check; Mode B = same decode vs the viewer's render path (run B after both land). Decode spec + build-flavor caveat (walker ROM = per-map pool WAS right; slice ROM = per-tileset) in the parity doc. Server.py unchanged. **PARITY PROVEN 2026-07-12: Mode A 0/499,712 px differ (ts19 134mt + ts22 842mt vs decoded engine bytes, index order == tileset_map.gen.json); Mode B 0 px differ across ALL slice columns (ts19 via Map049+Map048 = 129, ts22 = 839; lead extended run_viewer_mode to walk every pooled map, not one representative).** Engine tree = vendored `engine/` per `.env-paths` (old external clone has NO uranium dirs). ts19 has 135 physical mt (all-zero secondary placeholder — emit.py pads empty secondary metatiles.bin; benign, unreferenced). UNCOMMITTED (rides with the 2026-07-11 viewer rework). Remaining: Brandon's eye-check (restart the viewer server ONCE for the new code; rebuilds auto-reload after that; mGBA color-correction = expected small uniform brightness delta, data identical).

## Archived 2026-07-13 — Last Session Summary (2026-07-13 viewer stale flower colors)

**2026-07-13 (Viewer stale-flower-colors ROOT-CAUSED — docs only, no code fix yet; /delegate, 1 Sonnet tracer + 1 Haiku doc sweep):** Brandon's "flowers wrong colors on server launch, Apply & re-render fixes them, split across many groups" report split into three: (1) **grouping = known structural over-split** (SLICE1_TODO #10, pixel-identical autotile shape variants — requant never touches `colkeys_sorted`); (2) **colors = BROWSER-cache staleness, PROJECT_TODO #13 (open, fix guide written):** all `/api/tile|metatile` PNGs are `Cache-Control: immutable, max-age=1y` (map_viewer_server.py:68/:281/:285) and the ONLY cache-buster `?g=` = `_quant_generation`, a module global **reset to 0 every process start** (map_viewer_common.py:311) — incl. the hot-reload `os.execv` self-restarts — so bytes cached under a low `g` in an old process lifetime re-serve forever on relaunch; Apply bumps `g` to an unseen value → real fetch (packer proven deterministic: stock-knob POST → byte-identical PNGs, verified live). Fix = seed `_quant_generation` with `int(time.time())`. Per-group URL independence explains the mixed-vintage "different colors". (3) **bonus real parity gap, PROJECT_TODO #14:** viewer's analysis pool calls `_render_column` with no `n_frames` (map_viewer_common.py:474; pipeline passes `column_n_frames`, build_slice_tilesets.py:373/:396) → `MetatileImage.frames=None` → `extra_tile_colors` always empty → animated tiles' frame-1+ colors never widen viewer palettes; oracle Mode B frame-blind too. NOTE: the tracer sub-agent pkill'd + relaunched Brandon's running viewer server (tmux `map_viewer_server`, :8765, same invocation) — generation is back at 0, stale colors will show again until #13 lands (hard-reload works around).

## Archived 2026-07-13 — Last Session Summary (2026-07-13 ledge mechanics recovered)

**2026-07-13 (Ledge mechanics recovered — analysis + TODO shuffle only, no code; /delegate, 1 Sonnet + 1 Haiku Explore):** Located SLICE1 #4's 3 warning tiles (ts22 840/841/842 = Map032 cells 37–39,53 — the pond-dock front; Brandon ID'd the devs' intent: ledge-as-fence to keep the player off the water). Proved Essentials v17 stores no jump direction — jump = movement direction, gated by the tile's 4-dir passage bits; stock code, no Uranium edits — so `ledge_directions` hand-mapping is obsolete: directions auto-derive from `tilesets.json` passages corpus-wide (rule verified against all 12 ts22 tag-1 tiles; dock tiles = blockers from land, MB_NORMAL fallback already faithful). Existing empty `ledge_directions` override stays as escape hatch (user: don't grow it). SLICE1 #4 → Done; **NEW `SLICE2_TODO.md`** created (user call: nothing jump-testable in slice 1) — #1 = derivation (full rule + inventory + evidence), #2 = inherited frontier pointers (waterfall transparency, secondary-tileset anims, Route 03 seam). Discoveries entry added; stale 07-02 "hand-map by eye" note supersede-annotated. No pipeline code touched; all uncommitted.

## Archived 2026-07-14 — Last Session Summary (2026-07-13 temp-switch dispatch carve-out)

**2026-07-13 (SLICE1 #1 temp-switch dispatch — carve-out landed + premise killed; /delegate, 1 Haiku + 1 Sonnet Explore + 1 Sonnet builder):** Verified `tsOn?/tsOff?` are event-scoped (`022_Game_Event_v17.rb`: `@tempSwitches` per Game_Event, `s:` conditions eval with `self` = the event) → built the sketched dispatcher extension (`metadata_wiring._resolve_switch_gate_term`: `s:tsOn?/tsOff?("X")` label → `flag()`/`!flag()` on `mint_temp_switch(uid,eid,X)`, shared switch1/switch2, other `s:` labels defer, no mint for the `s:` id) + flushed out a real latent bug: `FlagRegistry.load()` never restores labels → `label_for_switch` AND bug-#7's label-mint path in `resolve_switch_flag` were dead at staging; new `FlagRegistry.seed_labels()` (split from `pre_seed`, schema untouched) now called in `stage_slice_scripts` after `load()`. THEN staging re-run showed zero output change — dug in: the "8 deferring Moki NPCs" are warp doors/cave entrances (P1 = code-201 transfer, P2 = ts-gated arrival autorun), never object-emitted, natively subsumed; corpus census = all 324 ts-gated events are this door pattern, 0 NPCs (→ Discoveries). Slice artifacts byte-identical (15 dispatchers, mints still 196/106/16/8) → no rebuild/boot needed. +6 tests (dispatcher ts-gates, label-less-load defers-then-seed_labels-fixes pin, seed_labels unit); 1029 pass. SLICE1 #1 → Done.

## Archived 2026-07-15 — Last Session Summary (2026-07-13 viewer pixel-classes + #5/#12 prep)

**2026-07-13 (SLICE1 #10 viewer "expand similar" over-split FIXED — viewer-only; /delegate, 3 Haiku Explore recon, lead built):** new `map_viewer_common._pixel_classes` groups column keys by frame-aware rendered pixels — class key = (layer_type, behavior, n_frames, frame-0 bottom/top bytes, every extra frame's bytes via `_render_column(n_frames=column_n_frames(ck))`); a column tripping the 64-frame lcm guard stays UNGROUPED (folding it into a static lookalike off frame 0 would be wrong). Computed once in `_ensure_loaded` (quant-independent, cached on `_MapState.pix_class`); payload ships `pix_class` (colkey_idx → smallest pixel-identical colkey idx); `btn-expand` selects by class. Map032: 839 colkeys → 697 classes (113 merged); the flower autotile's 12 shape variants → ONE 31-cell group, 3 z2-sparkle cells correctly out; animated only merges with animated. Build-side metatile collapse deliberately NOT done — 8×8 tiles are already pixel-deduped in `emit.py` (that's the tight budget, ts22 997/1024); metatile-table entries aren't under pressure — rationale recorded in SLICE1_TODO #10. 7 tests (`test_map_viewer_pixel_classes.py`, stub-raster pattern per the animated-overlay test); 1036 pass. Committed `6ee1c952`; **user-verified by eye same day (one click grabs all the flowers).** SAME SESSION: **SLICE1 #5 closed** (user decision: new_game.c harness KEPT indefinitely as the standing HM test rig; ledger disposition + in-code comment updated, `4b8ca7ce`) + **SLICE1 #12 RESEARCHED, ready to build** (3 sub-agents — pipeline audit / fork inventory / corpus census + RGSS read; full findings + build plan in SLICE1_TODO #12. Headline: the frozen town is move_type **3** custom routes (pacers/lookers/props), not type 1 — type 1 already emits WANDER_AROUND; fork range 0 = axis check skipped = unlimited (event_object_movement.c:6598) so (0,0) is FAITHFUL for type 1, no invented radius; type 2 approach-player = ZERO corpus-wide and fork has no native analog — moot; build = route classifier in `movement_type_for` (props→FACE, turn-only→LOOK_AROUND, h/v pacing loops→WALK_LEFT_AND_RIGHT/UP_AND_DOWN + range from simulated route excursion, other translating→static+drop-report, type 2→fail loud) + range fields through `ObjectEvent`/`build_object_events`).

## Archived 2026-07-18 — session summary evicted from MEMORY.md

**2026-07-15 (boot-walk debug round 1: Map032 NPC movement fine-tune — BUILT, ROM `a107c65c` taildropped, retest pending; /debug, lead built inline):** User walked the 2026-07-14 ROM (findings in `BOOT_WALK_CHECKLIST.md` + `reference/map_feedback/Map032.json`); all dispositions logged in SLICE1_TODO #11, fix detail in #12. Three classifier rules in `npc_gfx.py` (all fork-verified §4.7): (1) axis pacers below **move_frequency 6 → WANDER_*** not WALK_* (RMXP idle-gates EVERY route command `(40-2f)(6-f)` frames — explicit code-15 waits were the wrong signal); (2) **4-leg closed loops → MOVEMENT_TYPE_WALK_SEQUENCE_*** via `_spec_for_loop_route` + `_simulate_walk_sequence` — an exact replay of the fork automaton (per-type `_WALK_SEQUENCE_QUIRKS` snap-to-initial-row/col table, OUTSIDE_RANGE-beats-IMPASSABLE leg advance, at-anchor reset; a TEST re-derives the 24-entry table from event_object_movement.c). Anchor must be a loop CORNER → `MovementSpec.anchor_dx/dy` spawn shift (EV008 Chyinmunk ring: spawn (35,44)→(35,43), DOWN_RIGHT_UP_LEFT rx6 ry6); (3) **`MapPassability` gates** in `build_object_events(passability=)` — RMXP checks the CURRENT tile's exit bits before every step (Game_Map#passable? layered scan, top-down, priority-0 decides), pokeemerald only checks the destination → a mover on an all-exits-blocked spawn demotes static (EV012: the user's "itemball" at (31,44) is a passage-15 ball DECORATION TILE the NPC covers on PC — never moves there either), and a walk-sequence path crossing a non-clear cell demotes loud (EV073, fenced on PC too). **`map_set.WALKABLE_OVERRIDES={32:{(38,43)}}`** (user-approved): the tree-crown cell EV008 crosses via RMXP `through` (no GBA analog) — `convert_layout(unblocked_cells=)` forces collision 0/elev 3, art kept (priority-3 crown draws over sprites); player can now step there (1-cell infidelity, accepted). Plumbing: `build_slice_maps(tilesets_path=, walkable_overrides=)` (stage_slice_scripts passes both). Verified end-to-end: staged MokiTown map.json movement all as designed, map.bin (38,43) collision 0 vs trunk (38,44) still blocked; 1089 tests pass (+~15 new incl. quirk-table-vs-C pin); full chain (driver→stage→assemble→make) clean; sha1 d7e4e722→**a107c65c**. **Non-bugs closed:** M6 "granny" = faithful HGSS_008 young-woman sheet (label was our checklist shorthand); M17 EV005 = door-sheet at (28,31), unwired by design; H3 ninja letter = later-story gate; M10 emotes only exist inside ceremony/Theo events. **NEW #14** = lab Pokédex ceremony (wrong trigger + reposition walks into void, user-deferred behind Map32); **H4 post-warp facing folded into #8** (RMXP 201 params carry arrival direction, we drop it). Known gap noted in #12: viewer collision overlay doesn't show stamp-level overrides. Committed 2026-07-17 with the story-chain rounds.

## Archived 2026-07-21 — Starter species conversion W1-W9 + quiz-scoring fix (full narrative)

**2026-07-18 to 2026-07-21 (Starter species conversion + S6 quiz — CLOSED, boot-walk PASSED; full detail in `STARTER_SPECIES_PLAN.md`, which stays as the permanent record — this is a compressed pointer, not a duplicate.)** Planned 2026-07-18 (4-agent research fan-out), replacing the 2026-07-17 Emerald stand-in starters (Treecko/Torchic/Mudkip) with real Orchynx→Metalynx/Raptorch→Archilles/Eletux→Electruxo (2-stage lines, Megas deferred). New `src/rpg2gba/species_converter/` package (staging emitter, battler/icon/cry converters) reusing `pbs_converter.pokemon` parsers; generated overlays + fenced engine hooks (empty-overlay = pristine-equivalent invariant). W1-W6 built 2026-07-19: species data/art/cries staged, fork gate extras (`fork_index.py` species category) + transpiler `pbHasSpecies?` → `checkspecies` support closing SLICE1_TODO #2's blocker, real starter OW sprites (needed zero new work — already wired via `OBJ_EVENT_GFX_URANIUM_*` auto-select). Two build-time bugs found and fixed in the converter (never in generated output, per CLAUDE.md §11): `uranium_pokedex_*` filename/format drift between `stage.py` and the committed hook, and `MOVE_FLAME_IMPACT` (a Uranium signature move id 568 the fork never defined) substituted with `MOVE_FIRE_FANG` (closest power/category/type match, user-approved). A same-day regression (ROM `21952021` still showed Emerald stand-ins) traced to skipping `transpile_driver run`/`stage_slice_scripts.py --write` after a hand-file edit — the pipeline order (`transpile_driver run` → `stage_slice_scripts.py --write` → `assemble_pathfinder.py` → `make modern`) has NO error-catching step if the first two are skipped; re-ran the full chain, ROM `23c53bb3`. W7 boot-walk (`23c53bb3`) found: no sprite/no party after "yes" (by-design two-step grant flow: quiz announces, a separate Poké Ball machine A-press does `givemon` — but two real defects: missing `releaseall` after `lockall` froze lab NPCs, and EV019's `switch` had no default arm, fail-silently falling through into the whole Theo cutscene at VAR_POKEMONTEST=0); and a retake-after-decline soft-lock traced via the headless playtest harness (libmgba-py script-PC watch) to a stale `ON_FRAME` dispatch latch (`VAR_TEMP_C` never re-armed after a later script wrote a guard-input symbol) — fixed generally via `metadata_wiring.insert_onframe_rearms`, a staging-time pass inserting `setvar(VAR_TEMP_C, 0)` after every guard-input write. W8 (`6d6e372a`) fixed all three; harness-verified E2E. W9 (`e20f2158`) found the party was real (count→1) but invisible in the START menu — `givemon`/`ScrCmd_createmon` never sets `FLAG_SYS_POKEMON_GET`, which Emerald's (but not Essentials') START-menu POKÉMON action gates on; fixed with one `setflag` on the grant path (systematic Essentials→Emerald gap, logged as Phase-7 debt to auto-pair with future `givemon` emission). All of W1-W9 committed 2026-07-21 (`2b045012`) after the user confirmed the party-flag fix working. Final boot-walk (`b0b21993`) found the quiz **always** resolved to Eletux regardless of answers: the argmax sign-test literal `32768` (0x8000) fell inside the engine's `SPECIAL_VARS` range, so the `compare` asm macro auto-selected `compare_var_to_var` instead of `compare_var_to_value` — it silently compared `VAR_RESULT` against `VAR_0x8000` (the engine's own switch-statement scratch var, left holding the last question's raw 0-2 answer index by the quiz's per-question `switch()`) instead of the literal, so both tally-override checks almost always failed and `VAR_POKEMONTEST` stayed at its hardcoded default. Fixed by using `32767` instead of `32768` (same subtract-and-test-sign semantics, outside both the temp-var and special-var ranges), commit `b3b1b623`. **User boot-walked and confirmed all answer combinations now resolve correctly** — plan CLOSED, SLICE1_TODO #2 and BOOT_WALK_CHECKLIST S6 both PASSED.

## Archived 2026-07-21 — Last Session Summary (2026-07-18 starter species W1-W5)

**2026-07-18 (STARTER SPECIES W1–W5 BUILT; /delegate, 5 Sonnet agents, lead kept design + gate policy. W6/W7 NOT started — held at the §9/§10 sign-off boundary):** New pkg `src/rpg2gba/species_converter/` converts the 6 starter-line species (Orchynx/Metalynx/Raptorch/Archilles/Eletux/Electruxo, Uranium ids 1–6) to real engine species. **Lead-authored `common.py` pins the shared contract** (species order — load-bearing, fork ids assigned by position; symbol names via `gfx_ident`; asset paths; engine format constants) so the 4 parallel units never imported each other. **W1 `stage.py`** — reuses `pbs_converter.pokemon` PARSERS only (never its emitters); emits 10 generated overlay files + `species_manifest.json` into `output/uranium-build/species/`; fork ids **1573–1578**, SPECIES_EGG→1579; evolutions read from data (28/29/27) not hardcoded; empty-selection ⇒ no-op stubs (pristine-equivalent invariant). **W2 `battlers.py`** — last-filmstrip-frame extraction, joint front+back quantization via reused `quantize.build_quantized_tileset(max_palettes=1)`, shiny-as-palette-slot derivation; all 6 fit ≤15 colors (Raptorch/Archilles exactly 15). **W3 `icons.py`** — 128×64→32×64 restack, reused `sprites._downscale_2x_majority`; palettes 1/1/1/1/3/3. **W4 `cries.py`** — pure-stdlib+numpy, mono/10512/8-bit. **W5** — species category added to `fork_index.registry_extra_symbols(species_manifest_path=)` threaded to all 3 call sites + `_emit_has_species_idiom` in `transpiler.py` (follows `_emit_door_idiom` preamble precedent, NOT a `condition_expr` change). **Tests 1299 pass, 0 fail** (+13 W5, +6 W2, +13 W3, +9 W4, +9 W1). **FOUR PLAN ASSUMPTIONS FALSIFIED BY MEASUREMENT** (`STARTER_SPECIES_PLAN.md` is now partly historical): (1) **`NATIONAL_DEX_COUNT` is NOT self-sizing** — it's an `#elif` chain pinned to `NATIONAL_DEX_PECHARUNT`, so appended dex entries silently under-count; needs a SECOND hook that `#undef`/redefines it (this is the save-corruption landmine, so it matters). (2) **Cries are 8-bit PCM, not PCM16** — measured off the fork's own vanilla cry wavs. (3) **Learnsets header CANNOT be included from inside `gSpeciesInfo[]`** (file-scope decls in an initializer list = invalid C); vanilla includes learnsets from `pokemon.c` BEFORE `species_info.h` — so **10 hooks, not 9**. (4) **Species 002's 77-vs-80 filmstrip mismatch does NOT trip the shiny-geometry check** (0.23% vs 5% threshold) — each art's own last frame is the same resting pose regardless of frame count; the check is only exercised synthetically. Also: icon palette **slot 0 is a fixed shared teal background** in all 6 palettes, not a spare slot (transparent pixels route there directly). **SLICE1_TODO #2 CLOSED FOR REAL** — Map049 re-transpiled live, queue 89→88, all **3** occurrences (not 1 — RAPTORCH/ELETUX/ORCHYNX nested in EV001 page 3, never negated) emit `checkspecies` + `var(VAR_RESULT)==TRUE`. **LIVE OPEN ITEM:** W2's integer-only 2× downscale shrinks near-miss bboxes (Metalynx/Electruxo bbox ~66×64 → ~33×32 padded up) so they render visibly smaller than Orchynx/Raptorch — **awaiting user eye-gate verdict; fix if rejected is a gentler scale-to-fit in the converter, never a hand-edit.** **W6 REMAINING:** apply the 10 committed engine hooks (needs §10 sign-off — `NUM_SPECIES`/`NATIONAL_DEX_COUNT` bump is a real baseline behavior change and **invalidates every pre-existing embedded-save review ROM**), swap `Map050_EV019`/`EV005` stand-ins (Treecko/Torchic/Mudkip → real species), convert the 3 real starter OW sprites + update `npc_gfx_map.json`, stage species artifacts in `assemble_pathfinder.py`, `make modern`. **`.env-paths` confirms `RPG2GBA_POKEEMERALD=engine/` — D4 cutover de-facto done; CLAUDE.md §3 prose still calls it pending (fix at W6).** (W6-W9 completed 2026-07-19/20/21, then CLOSED with the quiz-scoring bug fixed 2026-07-21 — see MEMORY.md "STARTER SPECIES CONVERSION — CLOSED" + `STARTER_SPECIES_PLAN.md`.)

## Archived 2026-07-21 — Last Session Summary (2026-07-16 RMXP blocked-move semantics)

**2026-07-16 (RMXP blocked-move semantics — `RMXP_MOVEMENT_FIX_PLAN.md` IMPLEMENTED end-to-end; /delegate + /caveman, 7 Sonnet agents, lead kept the gate + judgment calls):** All three Moki-Town NPC bugs fixed at the one root cause. **New `tileset_converter/route_sim.py`** — faithful RMXP move-route simulator (`simulate_route(page,x,y,passability) -> RouteSim{stalled,moved,stall_facing,stall_pos,steps}`): turn-then-check, `direction_fix` suppresses the turn, `skippable` decides stall-vs-skip, through 37/38 bypasses collision, cycle detection on `(index,x,y,facing,through)` + fail-loud step budget; unmodelable codes (diagonals/random/toward-player/jump/relative-turns) return no-verdict → keep the route. **Demotion gate** (`metadata_wiring.py` ~831): `exit_blocked(spawn)`-only `elif` → `simulate_route`; demote to `static_face_spec(facing=sim.stall_facing)` + drop route id ONLY when `stalled and not moved`; stalled-after-moving keeps the live route (the fixed interpreter reproduces it). WANDER/WALK `path_cells` branch deliberately untouched (native types, no bytecode). **`static_face_spec(page, reason, facing=None)`** — explicit facing, validated against `_DIRECTION_TO_FACING.values()`; `None` default preserves `_demote`'s case-h path. **Bytecode v1→v2** `[idle, flags, ops…, END]`, flags bit0=skippable, PC starts at 2 (`route_bytecode.py` + C + `custom_route_interpreter.md` landed lockstep; `route_table_emit.py`/`RouteRegistry` needed NO change — flags flows through as an opaque byte). **C fix** (`engine/src/event_object_movement.c`): collision checked BEFORE `URANIUM_ROUTE_SET_PC`; blocked non-skippable turns + `GetWalkInPlaceNormalMovementAction` and does NOT advance the PC → retries the same opcode (mirrors vanilla `MoveNextDirectionInSequence`, where only `COLLISION_OUTSIDE_RANGE` advances an index); new `URANIUM_ROUTE_SKIPPABLE(route)` reads `route[1]&0x01`; INIT/END_LOOP PC 1→2. Walk-in-place on block is a DELIBERATE divergence (real RMXP shows no animation — a blocked char is never `moving?`). **THE BUG THE PLAN GOT WRONG:** plan §1 said test collision via `passability.cell_clear(target)` — that is a one-sided aggregate and is WRONG. Real Essentials `passableEx?` (`021_Game_Character_v17.rb:106-117`) does TWO one-sided checks: `map.passable?(x,y,d)` AND `map.passable?(new_x,new_y,10-d)`. With `cell_clear`, EV012 walked off her sealed spawn tile (neighbours are individually clear) and stalled 5 steps later `moved=True` → gate never fired. Fixed by new **`MapPassability.can_step(x,y,facing)`** (source bit for d + dest bit for reverse, OOB blocked both sides); `cell_clear`/`exit_blocked` kept untouched for their existing callers. **Corpus diff** (`scripts/route_movement_diff.py`, new — snapshot/diff, log-handler captures `demoted` since `ObjectEvent` never stores it): exactly 2 of 64 slice events changed — EV012 `FACE_DOWN→FACE_LEFT` (old gate fired but took facing from `graphic.direction`), EV073 `CUSTOM_ROUTE(route 3)→FACE_LEFT`/route 0; EV008 unchanged (its fix is C-side). **SCOPE REALITY:** the plan's "regenerate the whole corpus" is NOT achievable — movement conversion needs `npc_gfx_map.json`, which covers only ~29 slice sheets; full-corpus runs fail loud on 154/177 maps. The 1065-route stats came from reading map JSON directly (no gfx map needed), which is why planning missed it. Evidence is slice-wide; the 395 `direction_fix` events are unverifiable at corpus scale until the gfx map grows. **Tests 1165 pass, 0 fail** (+`test_route_sim.py` 8, +3 real-Map032 pins, +`static_face_spec`/v2-flags tests). **One test REWRITTEN as genuinely stale** (CLAUDE.md §8): `test_build_object_events_walk_sequence_blocked_path_demotes` was a synthetic EV073 asserting CUSTOM_ROUTE-kept, justified by "the engine interpreter skips blocked steps at runtime" — that skip WAS the bug; now expects FACE_LEFT/route 0, matching its real-data sibling. **ENGINE CUTOVER DONE (user-approved 2026-07-16):** `RPG2GBA_POKEEMERALD` now targets the vendored `engine/` (the old external clone would have built the OLD interpreter — D's C fix lives in `engine/`); env var is NOT exported in the shell and NOT in `.env` — build needs `PATH+=/opt/devkitpro/devkitARM/bin:/home/b/tools/poryscript/poryscript-linux`. `gen.h` verified v2 (flags 0x00 on all 6 routes; route 3 survives EV073's demotion via global dedup — Theo maps reuse it). Committed 2026-07-17. Boot gate (§9) subsequently walked as part of the 2026-07-17 story-chain rounds (see MEMORY.md "Current Phase").

## Archived 2026-07-25 — Last Session Summary (2026-07-21 S6b Theo battle / S7 / S6 quiz-scoring)

**2026-07-21 (S6b Theo rival battle — BUILT, boot-walk PENDING; build agent, single session):** User reported the post-quiz Theo battle skipped entirely + Theo's sprite stranded in the lab. Root cause: the battle isn't EV020 (that's just a "c'mon let's go" NPC); it lives inside **EV019 "Machine"** page 0 as `pbTrainerBattle(RIVAL,"Theo",...,party=pbGet(151)+18,canlose)`, wrapped in a code-111 win/lose branch (66/55 nodes). The hand conversion (`Map050_EV019.pory`) had stubbed the whole conditional as SLICE1_TODO #17 — so no battle, AND Theo's exit choreography (which lives *inside* the battle branch as `applymovement + Change-Opacity 0`) never ran, stranding his sprite (the page-1 blank graphic can't apply mid-visit per the boot-page-static engine limit). Battle is scripted to happen **in the lab** (no warp; "Oh, please, not in the Lab..." is just flavor); the outside event is the separate catch tutorial (EV009). **Fix built:** (1) added `TRAINER_THEO_9/10/11` (Eletux/Orchynx/Raptorch L5 counter-picks) to `engine/include/constants/opponents.h` (855-857, bumped `TRAINERS_COUNT_EMERALD` 855→858) + `engine/src/data/trainers.party` — the converted parties already existed in `intermediate/trainers.json`, just never staged into the slice engine (still stock Emerald trainers); **committed `3893aac7`** because the fork-index gate reads `git show HEAD:engine` (pristine, never working tree — `fork_index.py:305`). (2) Rewrote the EV019 battle section: `switch(var(VAR_POKEMONTEST))` → `trainerbattle_earlyrival(TRAINER_THEO_N, RIVAL_BATTLE_TUTORIAL, DefeatText, VictoryText)` — the vanilla FRLG Oak's-lab starter-rival idiom (losable, auto-heals after via `RIVAL_BATTLE_HEAL_AFTER`, converges either way; both RMXP branches also converge to the prof's Route-01 farewell + `var101+=1`) + Theo walk-off `set_invisible` to fix the stranded sprite. **Gotchas hit:** (a) new engine constants invisible to the gate until committed (HEAD-only by design); (b) `#` comments in `trainers.party` break the build — the file is run through `arm-none-eabi-cpp`, so comments must be C-style `/* */`. Full chain clean (rebuilt fork_index, transpile → stage → assemble → `make modern`), ROM `51be1e63` (ROM 79.28%, EWRAM 86.77%, IWRAM 86.99%), taildropped as `uranium-slice-51be1e63-theo-battle.gba`. **Cosmetic gap (flagged, not a bug):** Theo's battle sprite is a stock Youngster stand-in — no Uranium trainer-pic pipeline in slice 1. `BOOT_WALK_CHECKLIST.md` S6b updated with what to verify (battle fires, win+lose both continue, party heals, Theo gone after). The `.pory` edit is NOT committed (working-tree hand conversion, read directly by transpile_driver); only the engine data is committed. **Committed after user confirmed S6b PASSED: `3893aac7` (engine data) + `ad70308a` (.pory + docs).**

**Then S7 (PokéPod scene, Map172 EV004) — Theo-invisible fix, boot-walk PENDING (same session):** User boot-walked S7: scene fired but Theo was invisible, and flagged this as a *recurring* class ("do we have rules for making NPCs visible?"). Both mechanisms exist and fire — transpiler opacity→`set_visible`/`set_invisible` (`transpiler.py:687-691`) AND the flag-hidden-actor auto-bracket (`hidden_actor_bracket.py`, Theo = `flag FLAG_TEMP_11`, spawned only via `addobject`). **Root cause: the auto-bracket was NOT control-flow-aware** — it inserted a single `addobject` before the *first textual reference* (`:166,175-179`), which for EV004 landed in the `if (FLAG_LOST_FIRST_BATTLE)` (lose) branch. The `else` (win) path — the common one — ran `applymovement(2, set_visible)` on an unspawned actor (silent no-op) → Theo invisible. **Fix:** made `bracket_hidden_actor_scripts` branch-aware via a dominance rule — new `_branch_paths` (per-line enclosing-branch-opener tuple, tracks if/else/elif/switch braces; `} else {` = pop+push so sibling branches diverge) + `_dominates` (an `addobject` covers a ref iff its branch path is a prefix and it textually precedes). Now inserts `addobject` before the first reference on *each* execution path not already dominated (straight-line refs still get exactly one, so all pre-existing straight-line behavior/tests unchanged). `addobject` idempotency makes redundant spawns harmless. Corpus-wide staging pass fix, not a Map172 patch. 2 new tests (sibling-branch + straight-line-tail), all 9 bracket tests pass. Re-staged (both existence + onframe checks clean) → assemble → `make modern` clean, ROM `8fe9d9ff` taildropped as `uranium-slice-8fe9d9ff-theo-visible.gba`. **NOT yet committed** (pending user boot-walk confirmation) — touches `hidden_actor_bracket.py`, `tests/test_hidden_actor_bracket.py`, `BOOT_WALK_CHECKLIST.md`. `removeobject` bracketing is still best-effort (last-ref only; doc already tolerates "actor left spawned, ON_TRANSITION rehides") — not the reported bug, left as-is. **S7 Theo-visible boot-walk PASSED (user-confirmed); committed `2aeaa46a`.**

**Then S6b loss path (same session):** made the Theo battle actually losable like PC Uranium. Two bugs: (1) I'd used `RIVAL_BATTLE_TUTORIAL`, which adds `BATTLE_TYPE_FIRST_BATTLE` (`battle_setup.c:1322`) — the Birch-intro type where the opponent is framed WILD and FLEES at low HP, so you can never lose. Fix: `RIVAL_BATTLE_HEAL_AFTER` alone (losable + auto-heal, no flee) + record outcome via `if (var(VAR_RESULT)==1) setflag(FLAG_LOST_FIRST_BATTLE)` (engine sets VAR_RESULT=TRUE on EARLY_RIVAL defeat; the earlyrival converge had dropped the RMXP switch-35 write). (2) After that, losing FROZE on the victory-text screen — the earlyrival player-loss-and-heal path is **dead code in vanilla** (earlyrival is only ever TUTORIAL-can't-lose or plain-whiteout), and `BattleScript_RivalBattleLost` (`data/battle_scripts_1.s`) printed the win text then `end2` with **no `waitmessage`**, racing the return-to-field transition. Fixed with a fenced `waitmessage B_WAIT_TIME_LONG`. Both committed `9971428e`. **Loss-path boot-walk NOT yet confirmed** (user moved on after the freeze-fix ROM `56bdd086`). **TYPE-TRIANGLE CORRECTION:** the player ALWAYS has the type advantage — Theo's counter-pick is the starter *weak* to yours (Orchynx>Eletux, Raptorch>Orchynx, Eletux>Raptorch). Winning is expected; losing = deliberately throwing it (prof even asks "did you let him win?"). My earlier "Theo gets the type that beats yours" (in code comments + `trainers.party`) is BACKWARDS — fix the `trainers.party` comment when the test rigs are torn down.

**Can-lose battles + pipeline (design-first):** documented the mechanism in `reference/guides/canlose_trainer_battles.md` and the implementation design in `reference/guides/canlose_battle_pipeline_plan.md` (both committed `80450bcb`/`cfc76bc1`). Native analog = `TRAINER_BATTLE_EARLY_RIVAL` + `VAR_RESULT` branch; constraints = heal-or-whiteout only, two text slots, converted trainers not yet staged into the slice build (Theo parties hand-added). **User chose DESIGN-FIRST — pipeline NOT built.** Next task when resumed: implement per the plan (Gap A transpiler: canlose `pbTrainerBattle` code-111 → earlyrival+VAR_RESULT with branch-polarity swap + `pbGet(N)+K` party switch; Gap B staging: auto-emit `trainers.json` → engine via gen header + gate manifest; then retire `hand_conversions/Map050_EV019.pory`). **LIVE TEST RIGS (uncommitted, tear down after loss walk):** `trainers.party` Theo Body Slam (+ backwards comment), `new_game.c` Geodude-disable; KEEP `new_game.c` post-S7 harness (commented, for S8 testing — Raptorch/beat-Theo/VAR_QUEST_LOG=2/running shoes).

**2026-07-21 (S6 aptitude-test always-Eletux bug — ROOT-CAUSED + FIXED, boot-walk PASSED; build agent, single session — committed the outstanding W1-W9 starter species work first, then investigated the new symptom via a background research agent + applied the fix inline):** User confirmed the W9 party-flag fix worked, then reported the S6 quiz always granting Eletux regardless of answers (tested all 3 slots × several combos). Committed the entire uncommitted W1-W9 starter species arc as `2b045012` before debugging (species staging/art/cry converters, gate extras, transpiler `pbHasSpecies?`, ON_FRAME re-arm pass, party-flag fix — all previously verified working, held uncommitted per protocol pending user retest). **Root cause of the quiz bug:** the argmax sign-test literal `32768` (0x8000) in `Map050_EV005.pory`'s TestBody falls inside the engine's `SPECIAL_VARS` range (`include/constants/vars.h`: `0x8000`–`0x8015`); the `compare` asm macro (`asm/macros/event.inc`) auto-selects `compare_var_to_var` vs `compare_var_to_value` based on whether its literal operand lands in that range (or the temp-var range) — so `if (var(VAR_RESULT) < 32768)` silently compiled to "compare against `VAR_0x8000`", not the literal. `VAR_0x8000` is the vanilla engine's OWN switch-statement scratch variable (`switch`'s codegen is `copyvar VAR_0x8000, <var>; compare VAR_0x8000, <case>` per case), and the quiz's per-question `Tally` script uses `switch (var(VAR_TEMP_MOVE_CHOICE))` — leaving `VAR_0x8000` holding the LAST question's raw 0-2 answer index after the 4th question. A tally delta (0-4 range) is essentially never `<` a leftover 0-2 value, so both override checks (`T4>=T5`, `T3>=T4 && T3>=T5`) almost always skipped, leaving `VAR_POKEMONTEST` stuck at its hardcoded default (`2` = Eletux) no matter what the player picked. **Fix:** use `32767` instead of `32768` in the three sign-test comparisons (same subtract-and-test-sign semantics — `A>=B` ⟺ `(A-B) <= 0x7FFF` — but 32767 sits outside both the temp-var and special-var ranges, so the macro correctly emits `compare_var_to_value`). Single-line-pattern fix, no other `32768` sign-test literals exist elsewhere in the hand conversions (grepped to confirm). Full chain re-run clean (transpile → stage → assemble → `make modern`), pytest **1320 passed/15 skipped**, ROM `b0b21993` taildropped, committed `b3b1b623`. **User boot-walked and confirmed all answer combinations now resolve to the correct starter** — this closes `STARTER_SPECIES_PLAN.md` end to end (W1-W9) and `SLICE1_TODO.md` #2; `BOOT_WALK_CHECKLIST.md` S6 → PASSED. Docs updated same session: `BOOT_WALK_CHECKLIST.md` (S6 checked off, Auntie-RAPTORCH gap note removed), `SLICE1_TODO.md` (#2 removed from Open, #17 moved to Done with the full arc), `STARTER_SPECIES_PLAN.md` (W7 gate section closed out).

## Archived 2026-07-30 — Last Session Summary (2026-07-27c lab starter scene positioning)

**2026-07-27c (Theo/player stagger in the lab starter scene — root-caused + fixed in `hand_conversions/Map050_EV019.pory`; build agent, /delegate):** User boot-walk report: after the starter pick, Theo stops one tile short of the Machine and ends up diagonal from the player instead of squared up beside them. **Cause — the scene is pure relative-delta choreography with no absolute repositioning, so its correctness rides entirely on the player's approach tile P0.** RMXP authored it for **P0=(14,7)** (Theo's `through_on` on the `move_up` into (14,7) is the proof — he steps into the tile the player is vacating that same frame). Our layout forces **P0=(14,6)**: the machine-invi through-blockers make (13,5)/(15,5) collision 1 and seal the row-4 pocket, so **(14,6) is the only tile the Machine can ever be interacted from** (decoded from `engine/data/layouts/MokiTownProfessorLab/map.bin`). One tile north → player lands (13,8) not (13,9) while Theo lands (14,9) → exactly the reported diagonal. **Fix:** deltas re-derived from P0=(14,6) — Theo `walk_up`×2 to (14,6) (the real machine-adjacent tile) then `walk_down`×3 to (14,9); player `walk_left`+`walk_down`×3 to (13,9). Theo's final tile is unchanged so `Theo_Exit` still lines up; all walked tiles verified collision 0. `Move5` deleted (two back-to-back 209s on one target with no 210 = RMXP authoring artifact; last-wins there, engine drops the second as in-flight — only ever one step either way). 1466 tests pass. **BUILT + taildropped: ROM `49cc029d`, boot-walk retest PENDING** (still pending as of 2026-07-30; the fix now rides on ROM `ec2fb783`). **Rebuild sequence gotcha (cost a wasted staging pass): editing a file in `hand_conversions/` is NOT picked up by `stage_slice_scripts.py` alone — the override enters at transpile time, so the order is `python -m rpg2gba.conversion_agent.transpile_driver run --maps <ids>` (deterministic, no LLM, reports `hand-overridden: N`) → `scripts/stage_slice_scripts.py --write` → `scripts/assemble_pathfinder.py` → `make -C engine -jN modern`. Do NOT reach for `run_slice.py` for this — that's the retired LLM driver and it spends budget.** Also confirmed this session: `.env-paths` already points `RPG2GBA_POKEEMERALD` at the in-repo `engine/` (the §3 cutover is done), and `new_game.c` is back on the normal Player's-House @7,7 spawn with the S6b lab override commented out.
**Two real gaps surfaced, neither the cause here, both worth queueing:** (1) **RMXP blocked-move stall is unmodeled for *scripted* (code-209) routes.** `route_sim.py` + the custom C route interpreter model it only for page-level ambient `move_type==3` routes; embedded cutscene routes compile to vanilla `applymovement`, whose `walk_*` actions **never check collision** (`InitNpcForMovement`, `engine/src/event_object_movement.c:7287-7305`). Uranium leans on the "over-long march that stops when it hits someone" idiom — e.g. EV005 page 0 marches the player `move_up`×10 — so any such route can land a different tile on GBA than in RMXP. (2) **The RMXP counter bit (0x80) is discarded corpus-wide** — `tile_map.py:50-53` masks passages with `0x0F`, nothing ever emits `MB_COUNTER` — while the engine supports counter-reach natively with zero custom C (`MetatileBehavior_IsCounter` → `field_control_avatar.c:400-409` looks one tile beyond a counter). Tileset 19 alone has 17 counter tiles, placed on Map050 at (8-10,17). **Unclosed:** what mechanism puts Uranium's own player on (14,7) given Bambo vacates (14,6) at EV005 page 0 [120] — not counter (passages[641]=0), not a proxy event (no event at (14,6) but Bambo). Doesn't block the fix, since our geometry forces (14,6) regardless. **User is playing the chapter on PC to settle it — observation checklist + full write-up in `reference/findings/lab_starter_scene_positioning_2026-07-27.md`. Note the consequence recorded there: if the original is staggered too, the fix makes our version look *better* than Uranium, which is a §10 fidelity call rather than a bug fix.**
**Harness answer (designed, not built):** `gObjectEvents` is already in the linker map (`engine/pokeemerald.map:5142`, 16 slots) and the harness has **never read it** — it observes only the player's position, so every NPC in every cutscene is invisible to the suite. Proposed layers → see Open Questions.

## Archived 2026-07-27 — Last Session Summary entries 2026-07-27b / 2026-07-27 / 2026-07-25 / 2026-07-22

**2026-07-27b (contact-sheet frames now land on the dialogue, not the aftermath; N1 dropped; build agent, single session):** User review of the moki sheet: every text-bearing beat photographed the wrong moment (B3 showed Auntie's room *after* the shoes line, B4 an empty route *after* Theo's cameo), and B2/N1 were the same test twice. **Cause:** `runner.py` takes the waypoint after `beat.run(emu)` returns, and every dialogue beat ends by mashing A until `field_locked()` goes false — so the live frame is structurally always the world after the scene. **Fix:** `Emulator.note_text_frame` stores a candidate frame during the mash and `waypoint` prefers it (last one wins → the payoff line, not the greeting); a beat with no dialogue still captures live. `Waypoint.at_text` marks it and the caption gets a `+`, so a **missing** `+` on a beat whose doc row promises a message is now the visible defect.
**Getting the *right* frame took three tries, and the two dead ends are the reusable knowledge:**
1. **`sFieldMessageBoxMode` does NOT mean "a box is on screen".** `Task_DrawFieldMessage` clears it the frame `RunTextPrintersAndIsPrinter0Active()` goes false — i.e. when the last glyph lands, **not** when the box closes; the box stays up afterwards holding finished text while the script waits for the button. So `text_showing()` means "text is still appearing", and capturing on it gives half-typed tiles ("See you later, R").
2. **`TextPrinter.active` is not "still printing".** It stays TRUE for the printer's whole life and clears only at RENDER_FINISH, on the way to `FreeFinishedTextPrinters` — so a one-page message has *no* printer in the list at the exact moment it's fully drawn. Gating on `text_showing && active` dropped every short message (B2's refusal vanished).
**What works: the falling edge of `text_showing`** — the frame the last glyph lands on — guarded by `text_ready()` (walks `sFirstTextPrinter` reading `state != RENDER_STATE_HANDLE_CHAR`; an empty list reads *ready*, per (2)). Edge detection needs per-frame sampling because a short message finishes and is dismissed inside one 12-frame tap, so `run()` gained an `on_frame` hook and `tap` samples every frame. New probes: `symbols.static_ptr_via_accessor` (literal-pool address of a static *pointer*, sibling of `static_addr_via_accessor`); `offsets.py` gained the `TextPrinter` field offsets + `RENDER_STATE_*`/`WINDOW_TEXT_PRINTER` values (header-probed, and cross-checked in a test against the same immediates in `IsTextPrinterActiveOnWindow`'s disassembly).
**Also:** the four dialogue-mashing loops collapsed onto one `Emulator.advance_dialog(key, max_taps, stop)` (moki's `_advance_dialog` deleted; `_advance_to_battle` is now a one-line `stop=` wrapper). **N1 deleted** — it called `_assert_house1f_exit_blocked` with identical arguments to B2 and could not fail unless B2 failed first; the doc row is marked SUPERSEDED and `tests/test_moki_chapter.py::DROPPED_DOC_BEATS` keeps the divergence explicit rather than silently editing the doc parse. Chapter is **17 beats, PASS on ROM `d6a54593`**, 1466 tests pass. Sheet verified by eye: B2 "I'd better say goodbye to Auntie first.", B3 "Good luck, sweetie!", B4 Theo on screen mid-cameo, B9 "See you later, RED." — all complete lines. B5/B6/B10/B11/B14/B15 correctly carry no `+` (no message completes during them; B14's whole assertion is that none fires). All uncommitted.

**2026-07-27 (ROM rebuild + first deep chapter-suite run: 14/18 beats green, two real ROM findings; build agent, single session):** Rebuilt the slice end to end (stage → assemble → `make modern`, ROM `1066eac4`) so the 2026-07-26 gated-door converter fix was actually on hardware, then drove `python -m rpg2gba.playtest run --chapter moki` until it stopped finding harness bugs. **B2 now PASSES** — the gated door refuses correctly, closing `SLICE1_TODO` #18's "ROM re-run pending". Suite state on ROM **`90757612`**: **B1-B13 + N1/N2/N3 pass (14 beats), B14 fails on a real defect, B15 unrun.** Full suite 1439 pass / 16 skipped throughout.
**Two ROM-side findings (not harness):**
1. **`new_game.c` still carried the S6b post-quiz debug latch** (`FlagSet(RPG2GBA_SELFSWITCH_FLAGS_START + 18)` — commented "SSC", actually **SSB** — plus `VAR_POKEMONTEST=2` and a professor reposition). `Map050_EV005_Page1`'s first instruction is `goto_if_set FLAG_MAP050_EVENT005_SSB` → straight to the quiz body, so a "fresh" game skipped the aptitude offer entirely and B6/N3 were untestable. The 2026-07-24 note that the S6b harness was "swapped back" covered only the *spawn* override. **Disabled 2026-07-27** (block commented, dated, reversible), rebuilt → ROM `90757612`.
2. **Per-page RMXP trigger types are ignored — FIXED same session, see below.** Map032 EV009 ("Trainer(6)", the west-exit ceremony host): pages 0-2 are trigger 2 (player touch), **page 3 is trigger 0 (action button)**. The converter emits one `coord_event` per event and dispatches on page *condition* only, so at `VAR_QUEST_LOG >= 4` page 4's `lock`/`msgbox` fires every time the player walks over the tile — in Uranium it needs an A press on an opacity-0 event, i.e. effectively never. Same family as the gated-door fix (which was exactly a per-page trigger distinction) and the "base-page own condition ignored" deferral. Fix = per-trigger dispatch (touch pages → coord_event; action pages → object/bg host, or drop when there's no host).
**FIXED 2026-07-27 → chapter suite `moki` PASSES all 18 beats, first attempt, on ROM `d6a54593`** (green review ROM + 2-page contact sheet in `output/playtest/review/`; `SLICE1_TODO` #19 carries the detail). Dispatch is now channel-aware in `metadata_wiring`: `CHANNEL_TOUCH` (walk-on `coord_event`) serves only triggers 1/2; `CHANNEL_ACTION` (an event's primary talkable host) keeps the long-standing bump→talk approximation for touch pages, which 228 corpus events rely on; `CHANNEL_ACTION_ONLY` serves a **new secondary host** — a walk-on event that also has action pages now gets a `bg_event` sign on its own tile (`Map###_EV###_ActionDispatch`, `_emit_action_host`), keeping those pages A-press reachable as in RMXP *without* letting A fire the walk-on ones. `_goto_or_end` now takes the channel, so the old autorun/parallel special case is just one instance of the general rule. Corpus reach: of 1183 walk-on hosts, **135 events across 50 maps carry 181 action pages** dispatched on the wrong channel; slice 1 gains 4 signs on Map032 + 1 on Map050. 1444 tests pass.
**Contact-sheet blank frames — FIXED 2026-07-27.** 5 of 19 waypoint tiles were solid black (beat boundaries land mid warp-fade / battle transition), which gutted the sheet's whole purpose as the Branch-G by-eye surface. `Emulator.waypoint` now calls `_settle_frame` first: advance in 4-frame steps while `_frame_unreviewable` — one flat colour over ≥98% of pixels (`getcolors(maxcolors=8)`), or mean luma < 24 — bounded at 240f (4s), after which it captures anyway and appends "screen still blank after 240f" to the note so a blank tile is never ambiguous. A **failed** waypoint never settles: that frame is evidence. Re-ran: zero blank tiles, every beat shows real content. The remaining partial-black bands (B2/N1/B3/B11) are the **map border void** around rooms smaller than the screen — normal Emerald behaviour, deliberately still captured, and the reason the rule rejects "nothing to see" rather than "some black". 4 tests in `test_playtest.py` (`_FakeScreenEmu`, no ROM needed); 1448 pass.
**Harness/scenario work this took** (all in `src/rpg2gba/playtest/`): `walk_to` now **plans routes over the engine's own grid** — `_map_grid` (gBackupMapLayout), `_warp_tiles` (gMapHeader.events, never step on an unintended warp), `_plan_route` BFS, `_route_waypoints` corners, with the old greedy stepper demoted to `_walk_greedy` (returns `(arrived, spent, reason)`) and re-planning after a wait when a leg sticks; map-identity is now checked after **every** step, not just sidesteps; new probe offsets in `offsets.py`. `Emulator.write_u16` was **broken and never exercised live** — mgba's `MemoryView.__setitem__` drops `rawWrite16`'s `segment` arg (`memory.py:60`); now uses `raw_write`. That bug had blocked every battle beat. Chapter fixes: the scenario never left the player's house (B4 owns the crossing now); door warps need a **held northward** step (`TryDoorWarp`, `field_control_avatar.c:1098-1112`, reached only from the held-direction branch :227-231) → `_enter_door`; `_cross_threshold` for script-warp tiles (never `walk_to` a transition tile — the map-change guard is what catches accidental warps); `_settle_scenes` (the first field release after a warp is the arrival fade, not the end of an autorun); `_advance_to_battle` (an A-mash otherwise plays the whole battle by accident); `_leave_theo_house` (both warps stack in that doorway column). **Three chapter-doc corrections, verified against Uranium's own rxdata, not the wiki:** B4's trip tile (26,12) is a wall and its (26,11) approach is *sealed off* — the real coord events sit at (26,13)-(26,17) (same relocation for the ceremony column at (16,43)-(16,45), doc said (16,42)); the tutorial battle is in **event 19 "Machine"**, not event 20 "Theo" (event 20's `VAR_QUEST_LOG>=1` page is a bare `end`), and `VAR_QUEST_LOG` 0→1 lands *after* the battle, not with the starter grant; B12's expected `FLAG_MAP172_EVENT004_SSA` belongs to the **loss** page (page 2's `code 123`), never the win path (page 0 writes only `code 122` var 101 → 2). All uncommitted.

**2026-07-25 (ROM-test dev loop: contact sheet + both static gates wired; build agent, single session):** Closed the last three open items between the harness and an unattended dev loop. **(1) Screenshot contact sheet (§2 / build-order item 6)** — new `playtest/contact_sheet.py` (`Waypoint`, `build_contact_sheet`: paginated PNG grid, 3 cols @2x, 12/page, captions from the chapter-doc beat id + description, failing frame captioned red) + `Emulator.waypoint()` writing into `<screenshot_dir>/waypoints/`. The runner captures at boot and every beat boundary automatically (beats may add their own mid-beat), and renders on BOTH outcomes: green → `output/playtest/review/<chapter>-sheet.png` beside the review ROM, red → inside the repro bundle ending on the failing frame. This is the mechanism Branch G's bargain rests on — a green suite cannot see the cosmetic class, so the by-eye pass becomes scanning images, not replaying the chapter. **(2) Text validator is now a live gate** — `TextGate`/`TextGateViolation` called from `assemble_pathfinder.run_fork_pass` next to the fork-index gate, per staged script rather than per corpus (a slice build must not be failed by the 191 maps it doesn't stage; and staged text is post-`normalize_pory`, i.e. the real ROM bytes). Landing it green required fixing both live defects it found: `deterministic.py` now strips the WHOLE DrawTextEx tag family (`_DRAWTEXTEX_TAG_RE`, `<br>`→`\n`) where only `<fs=n>` was handled before; the hand-authored `Map050_EV005.pory` had its 8 `<b>` tags removed by hand (it bypasses the transpiler entirely — that asymmetry is the general lesson: hand conversions get no transpiler fixes for free); `strip_list.json`'s network stub went 232px→179px against the 216px box. Verified end to end: transpile → stage → assemble all clean, and `MokiTownProfessorLab/scripts.inc` now carries "The Pokémon Trainer Aptitude Test" with no tags. **(3) Queue-evidence is now a live gate** — enforced in `hand_overrides.load_hand_overrides`, chosen because writing a file into `hand_conversions/` *is* the act of bucketing an event `hand`; `queue_evidence.LEDGER_PATH` is the single owner of the ledger location. Backfilled the two overrides that had no ledger entry: **Map050/EV005** with real greps (Lilycove Quiz Lady is the only native quiz analog — single-question, single stored `correctAnswer` compared as an easy-chat word, own SaveBlock state, `lilycove_lady.c:473` — while `multichoice`/`givemon`/`compare`, the primitives it actually uses, are all native; so it is irreducible by SHAPE, §4.1 branch-heavy, not by capability), and **Map050/EV019** as `legacy_unaudited` + PENDING RETIREMENT, because the canlose idiom landed in `d3d57af8` and `trainerbattle_earlyrival` exists — writing `hand` evidence for it would have been false. That `legacy_unaudited` warning now prints on every load and is the reminder to retire the file. Tests **1436 pass / 16 skipped** (was 1328). **Live loop state:** B1 passes (the S6b spawn override in `new_game.c` is swapped back in the working tree and the ROM was rebuilt), **B2 fails twice → real, not a flake**: leaving Player's House 1F before talking to Auntie is NOT blocked — the player reaches `MAP_MOKI_TOWN` (75:2). Bundle + contact sheet at `output/playtest/bundles/moki/20260725T183643525888Z/`; the sheet's third frame is solid black, i.e. caught mid warp-fade into town. That is the next /debug target. **Still open for the loop:** BOOT_WALK_CHECKLIST is not yet tagged `[auto]`/`[eye]` (G2), the unattended F1 loop isn't built (its prerequisites now all exist), and P2 (encounter time-of-day) remains blocked on two user decisions.

**2026-07-22 (S6b Theo loss-path FREEZE — ROOT-CAUSED + FIXED, boot-walk PENDING; build agent + 2 Explore sub-agents):** User: losing the Theo battle still freezes on his last line as the screen starts fading to black, despite the 2026-07-21 `waitmessage` fix. **The `waitmessage` was a symptom-patch; real root cause = the still-set `BATTLE_TYPE_FIRST_BATTLE`.** `RIVAL_BATTLE_HEAL_AFTER`==1 (0b01), `RIVAL_BATTLE_TUTORIAL`==3 (0b11); the pristine-upstream mask at `battle_setup.c:1322` (`GetRivalBattleFlags() & RIVAL_BATTLE_TUTORIAL`) is truthy for HEAL_AFTER too — so switching TUTORIAL→HEAL_AFTER (commit `9971428e`) did NOT remove FIRST_BATTLE as its comment claimed. On a loss, `battle_controllers.c:2658` reroutes the trainer win-text (`STRINGID_TRAINER1WINTEXT`) under FIRST_BATTLE into the FRLG Oak voiceover `PrintOakText_HowDisappointing` → `BeginNormalPaletteFade(...RGB_BLACK)` (`battle_controller_oak_old_man.c:539`), which then waits on Oak-controller graphics/state absent in a normal trainer battle → fades to black and hangs (the print controller never signals done, so `waitmessage` can't help). FRLG's Oak's-lab battle works because it uses TUTORIAL (Oak voiceover correct there). **Fix (`battle_setup.c:1322`, fenced URANIUM PATHFINDER SLICE):** require the full flag — `(GetRivalBattleFlags() & RIVAL_BATTLE_TUTORIAL) == RIVAL_BATTLE_TUTORIAL` — so HEAL_AFTER-only is a plain losable+heal trainer battle (no FIRST_BATTLE, no Oak reroute). Zero reachable vanilla impact (grep: earlyrival used only by FRLG with TUTORIAL/0; nobody uses HEAL_AFTER alone). Win path unchanged (gated on `BATTLE_TYPE_TRAINER`; win prints `TRAINER1LOSETEXT`, which the reroute never touched → S6b win stays PASSED). `waitmessage` kept (correct hygiene). Engine-only rebuild, **hash `2010bbd0`→`f5867449`**, 1328 tests pass, taildropped. **User boot-walk confirmed BOTH loss and win paths → committed `e84b8d0a`** (`battle_setup.c` + `SLICE1_TODO.md` #11 + this entry). **Post-confirmation cleanup:** tore down the `trainers.party` Body Slam rig (restored default L5 movesets) and fixed the inverted counter-pick comment (rival gets the starter WEAK to yours — player always has the type advantage); **kept** the `new_game.c` post-quiz harness for S8 testing. S6b (win + loss) → PASSED.

## Archived 2026-08-01 — Last Session Summary (2026-07-30 map-grid race + beat-chosen contact-sheet tiles)

**2026-07-30 (B4 "collision regression" was the harness reading the wrong map's grid; build agent, /debug):** User framing was right — *"it's the tester, not something I'm seeing in the ROM."* **The ROM was never wrong.** Static BFS over the built `MokiTown/map.bin` routes (28,31)→(27,17) in 38 tiles, so the collision-loss hypothesis died immediately. Runtime probe at the exact failing instant (seeded to B4, then replayed B1–B4 for real): `map_location`=(75,2) MokiTown, `player_pos`=(28,31), **but `gBackupMapLayout` was still 45×29 → the house's 30×15 grid**; 30 frames later it read 87×78 and BFS found a 410-tile reachable set. **Root cause: a warp writes `SaveBlock1.location` in `ApplyCurrentWarp` (`overworld.c:620`) long before it rebuilds the grid in the `InitMap` inside `LoadMapFromWarp` (`overworld.c:982`), and `_wait_for_map` polls *location*.** The goal fell outside the stale grid's bounds → BFS `None` → `_route_waypoints`' documented-harmless greedy fallback stepped off (28,31), **which is itself the warp back into Map049**. Latent race tipped by 27d's `RPG2GBA_FADE_DELTA_Y 4`. **Fix is harness-only** (no converter, no engine): `Emulator._grid_dims_for_current_map` / `map_grid_loaded` / `wait_for_map_grid` keyed on `gMapHeader.mapLayout` dims + border margin (`fieldmap.c:171-174`); `walk_to` waits before planning; `_map_grid` refuses a grid disagreeing with the map header; **`_plan_route` now raises on an out-of-grid goal instead of returning `None`** — that shape is a wrong grid, not a blind spot, and degrading it to greedy is what did the damage. Invariant pinned by 5 new tests in `tests/test_playtest.py` (nothing had ever asserted the grid belonged to the current map). **1471 tests pass; moki chapter GREEN, all 17 beats, first attempt.** Full write-up → `SLICE1_TODO.md` #20. **Rebuild done + taildropped: ROM `ec2fb783` (was `1d1dde30`, the rejected half-tempo build — the tree had been stale vs. source since the 27d revert). This build also carries the still-unretested 27c Map050 EV019 lab-choreography fix, so the boot-walk retest for that rides on this ROM.** Reusable technique: seed blobs under `output/playtest/blobs/<chapter>/<beat>.{blob,json}` are ROM-sha256-gated but let a probe jump straight to a failing beat in seconds; `_FakeGridEmu` in the tests shows the unbound-method pattern for exercising `Emulator` internals without mgba.
**Same session, second unit (user-requested): contact-sheet tiles are now beat-selectable, and B6 was rewired — `SLICE1_TODO.md` #21.** `Emulator.mark_frame()` pins the current frame as that beat's tile (sticky against later dialogue, refuses a blank frame unless `force=True`, cleared per beat). B6 had been running **zero** frames — asserting only that the field was still locked — so it proved nothing B5 hadn't and photographed B5's tile again; the Yes/No prompt is only reached inside N3's mash. New observable **`Emulator.yesno_prompt_up()`** scans `gTasks` as `FuncIsActiveTask` does for the handler `ScriptMenu_YesNo` installs (`script_menu.c:584-596`) — the task's lifetime *is* the prompt's, so no frame counting and no text-speed dependence; explicitly **not** `gSpecialVar_Result == 0xFF`, which multichoice also sets and so can't distinguish the prompt from a quiz `dynmultichoice`. B6 now mashes forward, stops on the prompt, asserts it, and pins the frame (tile reads "RED, are you ready to take the Trainer Aptitude Test?" with YES/NO up). **`Task_HandleYesNoInput` is static → absent from the link map; new `symbols.static_fn_via_literal_pool` recovers it from `ScriptMenu_YesNo`'s literal pool, preserving the Thumb bit** (`gTasks[i].func` holds the odd address; comparing the even one silently never matches — same trap as `battle._thumb_fn`). That helper is the general answer for any static task/callback the harness needs to watch. 1480 tests pass; chapter re-run GREEN, all 17 beats; sheets re-taildropped. Still unobservable: which yes/no option is *highlighted* — answers remain button-driven.
