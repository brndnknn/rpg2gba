# rpg2gba Agent Memory

> This file is maintained by the build agent. Protocol + section template: `reference/memory-protocol.md`.
> Update with targeted edits, not full rewrites. Keep entries concise.
> **Eviction discipline:** keep at most the 2 most recent Last Session Summary entries and only *live*
> Open Questions here; move retired summaries + resolved-question breadcrumbs to `reference/memory-archive.md`.

---

## Current Phase

**Active:** Phase-4 rework — **deterministic transpiler + vertical playable slices** (→ `ROADMAP.md` §Phase 4 + `BUILD_PLAN.md`). The LLM is **retired as the conversion spine**, kept only as a hand-invoked tool for the irreducible tail; the frozen prompt is lifted (`prompts/system.md` is now editable).

**Work unit:** finish the **pathfinder slice** — end-to-end and *actually playable*, as slice 1 of the frontier. ROM boots; S9 left two live defects (tasks 3–4).

**SLICE SCOPE = 8 MAPS, not 3** (user-corrected 2026-07-16; SoT is `tileset_converter/map_set.py:30` `SLICE_MAP_IDS = [49, 48, 32, 50, 64, 65, 172, 89]`, interiors added 2026-07-13 per SLICE1_TODO #13). Boot order + identities: **49** Player's House 1F (spawn @7,7) → **48** 2F → **32** Moki Town → interiors **50** Professor's Lab, **64**/**65** the two unnamed houses, **172**/**89** Theo's House 1F/2F. Older notes in this file that say "Map 49 ↔ 48 ↔ 32" are stale — read them as the 8-map slice. Story chain + the three live event bugs → `reference/findings/moki_slice_story_chain_2026-07-16.md`.

**~~NEXT TASK~~ IMPLEMENTED 2026-07-16 → see Last Session Summary; ROM `332f30e8` taildropped, §9 boot-walk retest PENDING. Plan doc `RMXP_MOVEMENT_FIX_PLAN.md` is now historical — note its §1 `cell_clear` collision rule was WRONG (real `passableEx?` is two-sided → `MapPassability.can_step`), and its "whole corpus" scope is blocked by `npc_gfx_map.json` slice-only coverage.** Original statement of the three Moki-Town boot-walk NPC bugs (EV012 faces DOWN should be LEFT; EV073 "Birbie" girl walks should stand still facing hedges; EV008 Chyinmunk patrol reshapes when player-blocked) are **one root cause — we never modeled RMXP blocked-move semantics**. Verified in `reference/scripts_dump/021_Game_Character_v17.rb`: `move_*` **turn BEFORE** the `passable?` check (440-482); `move_type_custom` advances the index only `if skippable or moving? or jumping?` (line 363) so a non-skippable blocked move **stalls forever, facing the blocked direction, without animating**; `turnGeneric` is `unless @direction_fix` (670-679). Fix = new `route_sim.py` (RMXP simulator) + demote-only-if-frozen-at-spawn gate in `metadata_wiring.py` + `static_face_spec(facing=…)` + bytecode **v2** `[idle, flags, ops…, END]` (flags bit0=skippable, PC starts at 2) + C fix at `engine/src/event_object_movement.c:6234-6242` (**collision check BEFORE `URANIUM_ROUTE_SET_PC`**; on block turn + `GetWalkInPlaceNormalMovementAction`, do NOT advance PC). **Corpus numbers that constrain the design: 1065 page-level move_type==3 routes; skippable True=29/False=1036; direction_fix True=395 (37%, must keep `graphic.direction` — a naive "face first route cmd" fix breaks these); most common first opcode is 41 change_graphic (409) so the first move needs real simulation, not `list[0]`.** Hedge collision converts correctly — it's a gate-coverage gap, not a collision bug.

**CORPUS-SCALING AUDIT DONE 2026-07-14 → `reference/findings/corpus_scaling_audit_2026-07-14.md`** (Fable capacity plan `FABLE_PLAN.md` step 3; census scripts scratchpad-only, method in doc). Decisions it forces: (1) **per-tileset union packing breaks at slice 2** — Map032+Map033 (both ts22) union = 1 607 metatiles / 1 611 tiles > both 1024 caps → promote walker-style per-map packing to the slice/assembler path BEFORE Route 01; (2) seam-adjacent maps can't share a tileset (arithmetic), so connections = border-strip import across per-map tilesets; (3) BG palettes NOT a constraint (worst maps Map094/101/122 all 13/16 via real packer); (4) OBJ 4-global-bank NPC palette scheme dies at corpus (270 sheets; 66 maps >4 banks raw) → per-map packed banks + dynamic tags, converter-level; (5) ROM 26.47/32 MB used w/ full vanilla in — full-corpus additions ~8.5–11 MB vs 7.08 MB headroom → Phase-7 vanilla-stripping pass required; (6) animation: 55 columns trip the 64-frame lcm guard (real need 95), ts28 animated block 543>512 needs the secondary-anim variant; (7) 14–16 monster maps (Map094 worst: 1 806 mt) overflow even per-map — pixel-identity collapse (~17% measured) + intra-map split pass, not frontier-urgent. Loose end: `fk087-machop` sheet referenced once, file missing. **Fable-plan steps 4+5 also DONE 2026-07-14:** audio = substitution table (`reference/findings/audio_decision_2026-07-14.md`, committed `9cde2812`); Phase-6 spec `reference/guides/nuclear_type_spec.md` (**ROADMAP §6.4 corrected — no out-of-battle HP drain; real mechanic = 50% disobedience until EXPUNGE-cured, verified in scripts_dump**); Phase-7 debt ledger `reference/guides/phase7_integration_plan.md` (D1 items.h merge-not-replace is CRITICAL; D2 species gfx/cries pipeline gap; D4 engine cutover first; D3 gate extras unblocks SLICE1_TODO #2). **STEP-2 DIRECTION CHOSEN 2026-07-14 (user approved): Emerald-style shared "Uranium-General" primary per biome family (≤512 common tiles, 6 pinned palettes) + per-map secondaries (7 pals) — supersedes plain per-map packing (audit §6.1–6.2 annotated); fixes same-art-different-colors quantization drift + seams + ROM dedup in one architecture. Full rationale/measurements/watch-outs in `FABLE_PLAN.md` step 2. Seam census: all 14 connections.dat seams enumerated w/ tileset pairing (scratchpad connections.json). Validation prototypes KILLED all shared-tileset variants (secondary starvation) and vocab weighting (cross-family snaps, do-not-retry); **FINAL step-2 architecture (user eye-gate PASSED "component pinned for sure") → `reference/guides/connections_and_palette_families.md`: per-map packing + palettes pinned per seam component (8 components, all fit 13 pals) + border-strip import. Slice-2 implementation sketch in the doc (promote per-map packing into S8a FIRST — Moki+R01 bust per-tileset packing).**

**MAP WALKER (debug ROM mode) — Phase A + Phase B DONE, committed + merged to main.** In-repo debug tooling only (cursor-based map inspector over `URANIUM_MAP_WALKER TRUE`): boot-chain takeover, invisible-anchor cursor + blink/warp-flash sprite, bounds-clamp collision, warp A/B + 32-deep back-stack, L-toggle coord/metatile HUD, R warp-tile overlay, START scrollable jump-menu generated from the build batch; Phase B generalized it to per-map synthetic-tileset-id packing (`tileset_converter/map_set.py`, `phase5.convert_all`) so the walker covers all 199 maps (21 excluded for tile/metatile budget or emptiness) with stock-Emerald map data stubbed out to fit the ROM. Full build narrative (steps, dates, gotchas) → `reference/memory-archive.md` § "Archived 2026-07-07 — Map Walker Phase A/B build narrative"; engine mechanics/fences it exercised → `reference/guides/engine_extension_surface.md`.

**CHECKPOINT 2 (§9 walker gate) PASSED 2026-07-02.** User boot-walked the 8-map batch ROM + the map-viewer perf pass — all functional checks green (boot, cursor, warp A/B, HUD, R overlay, START menu, all warp pairs, viewer speedups). **Two fidelity items deferred past the Phase B merge** (converter-level; gate the *playable-slice* bar, not the walker): (1) warp cells lose their real art — `convert_layout` stamps the generic warp metatile over the cell's actual column (doors→grass, stairs→black void); fix = keep the real column, override only behavior+collision. (2) warps land on the return-warp tile, not Uranium's arrival coords — `WarpSpec.dest_x/dest_y` parsed but dropped; fix = emit per-destination "arrival" warp events at the real coords (supersedes the gUraniumWarpDests side-table idea, no custom C). Full write-up + fix directions: **`reference/viewer/walker_checkpoint2_findings.md`**. Phase B work committed + merged to main after this gate. **Grill target-2 session held 2026-07-02** (critique #2 RESOLVED): findings doc now carries the Checkpoint-2 certification statement + not-certified ledger + 3 more deferrals — warp-class refinement (one MB_NON_ANIMATED_DOOR for all warp types; triggering never exercised, walker calls DoWarp directly), **seamless connections unconverted** (recon: `Data/connections.dat` = 14 route↔town seams incl. Moki Town E↔Route 03 on the slice-2 frontier; per-map synth packing → tileset-bleed at seams), stock-layout stubbing formalized walker-build-only. Full-corpus art walk = sampled only, follow-up open.

**WARP FIDELITY + TERRAIN BEHAVIORS DONE + MERGED 2026-07-02** (via /delegate, 3 Sonnet agents). (1) Per-door-column warp metatiles (overlay `warps` new format `{"tiles":{column_key:idx},"fallback":idx}`, legacy shape still loads; `tile_map.WarpInfo`+`warp_for_column`); tile budgets unchanged (door copies dedup). (2) Arrival warps at Uranium `dest_x/dest_y` (`metadata_wiring._resolve_all_warp_events`, shared by walker + slice paths; source warps keep indices, arrivals appended; dedup + OOB→return-warp fallback; arrival coords NEVER in warp-overrides). **User re-walked: confirmed good; house "rug inert / exit below / arrival above" pattern = faithful Uranium data (verified vs Map049 events), NOT a bug.** (3) Terrain tags→MB_* behaviors: `reference/terrain_tag_map.json` (SoT, tags 0–22; 21/22 = junk PBTerrain never defines) + `tileset_converter/terrain_tags.py` (topmost-nonzero-tag rule, autotile-base fallback, fork-header validation, unknown-tag fail-loud); behavior computed per column in build_slice_tilesets (attrs-only — collision/elevation untouched, surf = later unit). **Ledges (tag 1): direction ambiguous in Essentials (player-facing at runtime) → `ledge_directions` hand-override section in the JSON, ships empty; unmapped ledge warns once + MB_NORMAL (wall-like via passages). 9 pending: ts22 tiles 840/841/842/559/567/1175 (Route 01 x6) + Moki Town x3 — hand-map by eye in the viewer before slice 2.** **[SUPERSEDED 2026-07-13: direction IS deterministically recoverable from RMXP passage bits (Essentials never stores it — see Discoveries); hand-mapping obsolete, derivation rule + inventory → `SLICE2_TODO.md` #1. The Moki x3 = pond-dock front, blockers from land, MB_NORMAL fallback already faithful.]** 600 tests pass, same 3 known failures.

**Slice-1 finish line now tracked in `SLICE1_TODO.md` (repo root, committed 2026-07-11)** — the live checklist of everything left for the §9 gate (temp-switch dispatch, queue residue, ledges, harness decision, audio bar, etc.). Update it as items close; don't re-inline its contents here.

**STARTER SPECIES CONVERSION — CLOSED 2026-07-21, boot-walk PASSED (ROM `b0b21993`) → `STARTER_SPECIES_PLAN.md` (full W1-W9 narrative) + `reference/memory-archive.md` (compressed pointer).** Replaced the S6 Emerald stand-ins with real Orchynx/Metalynx/Raptorch/Archilles/Eletux/Electruxo: new `src/rpg2gba/species_converter/` package, generated overlays + fenced engine hooks, real cries/battlers/icons, fork gate extras + transpiler `pbHasSpecies?`→`checkspecies` (closes SLICE1_TODO #2 for real — `checkspecies(SPECIES_RAPTORCH)` compiles clean in Auntie's dialogue), real starter OW sprites. Boot-walk chain found and fixed 4 real bugs along the way (all committed `2b045012`): lab-NPC freeze (missing `releaseall`), party fell into the wrong cutscene (EV019 switch missing a default arm), quiz-retake soft-lock (stale `ON_FRAME` latch — general fix: `metadata_wiring.insert_onframe_rearms`), invisible START-menu party (`givemon` never sets `FLAG_SYS_POKEMON_GET` — general Essentials→Emerald gap, Phase-7 debt to auto-pair). Final bug (commit `b3b1b623`): the quiz always resolved to Eletux because the argmax sign-test literal `32768` collided with the engine's `VAR_0x8000` switch-scratch var (0x8000 = `SPECIAL_VARS_START`, so the `compare` macro auto-selected var-vs-var instead of var-vs-literal) — fixed with `32767` instead. **User-confirmed working: all answer combinations now resolve to the correct starter.** BOOT_WALK_CHECKLIST S6 → PASSED; SLICE1_TODO #2 → Done.

**Next tasks** (full sequence in `BUILD_PLAN.md`; spine decisions 2026-07-02 → `reference/findings/grill_spine_2026-07-02.md`):
0. **DONE 2026-07-02 — oracle archived + committed** (`reference/archive/oracle_pory/`: all **19** `.pory`, not just the D6 set — bulk-run maps 008–014/017 (008/010 `.partial`) + slice 032/048/049 rescued too; provenance verified from run state/checkpoints/token log → archive README. Anomaly on file: Map032/049 rewritten Jun 17 off the pipeline paper trail, content still Opus-signature).
1. **DONE 2026-07-02 — Fork capability index + staging gate** (`conversion_agent/fork_index.py`, 23 tests, `fork-index` branch). Index via `git show HEAD:` (pristine `engine/`, never working tree — D3): 620 specials / 385 macros / 167 movement tokens / ~23.5k constants incl. `gba/defines.h` (TRUE/FALSE) + **event.inc asm assignments** (`MSGBOX_SIGN = 3` style) — both were live extraction gaps found by running the gate on real slice pory. Cache `output/uranium-build/fork_index.json` keyed (engine tree-hash, `_INDEX_FORMAT`) — **bump the format constant on ANY extractor change** (tree hash can't see logic changes; bit me twice this session). `verify_script` = 5 rules (ALL_CAPS constants / call + bare-statement commands / special()/specialvar() args / movement tokens; string+comment-immune); `registry_extra_symbols(flag_state, map_constants)` = the D3 union glue (note: flag_state `script_switches` is a list of s:-ids, no names). **Staging gate (D4) wired into `assemble_pathfinder.run_fork_pass`** — `_gate()` per-file abort BEFORE `_compile_pory` stages; **assembler now refuses current slice pory on real `healparty`×4** (Map049:46/87/338, Map048:128 — Opus-era bug, cleared by slice re-transpile D8; this is the gate working as designed, not a regression). CLI `build|check [--flag-state --map-constants]|resolve|search`; with registries supplied, only true positives remain on the whole slice+CommonEvents. Conversion-time gate lands inside the transpile driver (task #2); reverse gate = `search` cmd + ledger discipline. **Deferred (ask user):** consolidating `load_fork_constants`/`load_multi_constants`/`load_charmap_chars` onto the index — they intentionally read *working-tree* headers (PBS reconcile needs generated headers), 13 call sites.
2. **Transpiler CORE + DRIVER BUILT 2026-07-02** (`transpiler` branch; /delegate — lead wrote `transpiler.py`, Sonnet wrote tests + driver). `conversion_agent/transpiler.py`: `parse_tree` (indent-driven; closers 411/412/402-404/413 sit at the OPENER's indent, children +1; code-0 rows = structure only), `condition_expr` (111 types 0/1/2 deterministic; 12 script + 6 facing queue — corpus census 12×1667, 1×636, 6×339, 0×62, 2×39), `_PageEmitter` + `TranspileContext.queue()` (in-place `# UNHANDLED` marker + QueueEntry, unhandled.jsonl shape). **Corpus gotchas:** code **509 = editor-display duplicate of the 209 route object — strip, no queue** (was 91 phantom entries on Map049 alone); 121/123 value 0=ON→setflag (inverted); 122 ops 0-2→set/add/subvar, var-operand→copyvar, random/etc queue. **209 tier (D7):** bucket-A tokens + speed-aware walks (29→walk/walk_slow/walk_fast/walk_faster prefix), 15→delay_* chain, 42→set_invisible/set_visible (0/255 only), SOFT-C drops {30,37,38,43,44}; repeat/out-of-tier routes queue AND **paired 210s neutralized** (`_last_route_ok`). **355 idioms (D8 slice-proven):** STRIP set + `setTempSwitchOn`→temp-switch setflag (344× corpus) + `pbSetSelfSwitch(id,"L",bool)` (336×). 221/222→fadescreen; 223 tone ±255/0→fadescreen; 314→special(HealPlayerParty); audio→`# audio` comment; YES/NO→yesnobox+VAR_RESULT; loops/labels/multichoice = queue tier v1. `transpile_driver.py` (D5, no checkpoints/memo): classifiers-first idiom collapse → transpile_event fallback; **conversion-time fork gate per map** (extras = live registry to_state() + map_constants; violation raises); CLI `run --maps slice|full|ids [--dry-run]`; writes `scripts/Map*.pory` + `transpile_unhandled.jsonl` fresh, registry saved only on write. 64 tests; 687 pass. **Slice dry-run: 87 events → 98 queued, gate clean; full corpus 5301 events → ~8.7k queued** (top 355×2504, 111-script×1953, 209×1299). **ORACLE HARVEST DONE 2026-07-02 (D6)** — `scripts/oracle_harvest.py` (block-by-block normalized diff, fuzzy label pairing), all clusters dispositioned in **`reference/findings/oracle_harvest_2026-07-02.md`**; oracle RETIRED (archive stays, gates nothing; CommonEvents.pory still oracle-status pending the CE pass). Harvest found+fixed 3 transpiler bugs: **(A1) labels now canonical id-based `Map{m:03d}_EV{e:03d}_Page{n}`** (= metadata_wiring.page_label; name-based collided on Map002's two "Receptionist TRADE" events; event name → `#` comment above block; classifier-layer labels canonicalized by `transpile_driver._canonicalize_labels`); **(A2) triggers 1/2 non-empty → lock/…/release** (no faceplayer); **(A3) 122 random operand → `random(hi-lo+1)`+addvar(lo)+copyvar** (oracle-validated). Opus-error ledger (discarded, ours correct): invented `healparty` (ours = special(HealPlayerParty)), dropped choreography/fades, silently-flattened `pbPokerus?` branch (ours queues loud), inconsistent touch locking. Fidelity Q from doc §E **RESOLVED 2026-07-04 by census (`reference/findings/item_dupe_census_2026-07-04.md`): NO rename dupes** — fork ships pre-Gen-VI aliases (`ITEM_PARLYZ_HEAL = ITEM_PARALYZE_HEAL`), all 496 kept-vanilla items resolve (472 modern + 24 alias), 103 true originals, 8 near-misses all genuinely distinct; real landmine = items.py full-replacement `src/data/items.h` (behavior zeroed for ALL 607 incl. vanilla) + Uranium-ID constants header — Phase-7 V6 integration must reconcile (already flagged items.py:284-286). **CE PASS + SLICE RE-TRANSPILE DONE 2026-07-02.** `transpile_common_event` (flat list, no pages; **`return` not `end`** — called scripts; self-switch/pbSetSelfSwitch/erase queue in CE context) + driver `transpile_common_events` (empty CEs skipped; **strip_list.json CEs 4/5/6 → stub msgbox, fail-loud expect_name guard**). CE harvest vs oracle: converged (ours-return correct, elif-vs-if cosmetic); **oracle FULLY retired**. Movement tier grew during queue read: **RMXP 5–8 → `walk_diag_*` (expansion ships them — census's HARD call predated the fork check §4.7)** + 39/40 → set/clear_fixed_priority; cleared both doormat routes. **Real write run done:** slice Map049/048/032.pory + CommonEvents.pory regenerated (healparty GONE — staging gate now passes them), all 4 poryscript-compile clean, registry mints saved. **Queue read to completion (D10 #4): `reference/findings/slice1_queue_readthrough.md`** — 96 slice entries bucketed: native ×11 (rock smash/region map/PC/FLAG_SYS_B_DASH/setobjectxy/caves — fork evidence in doc), idiom ×~49 (text-code translator = biggest win 26; onEvent?+208 visibility cluster ~18; pbReceiveItem; emotes; wait-loops), hand ×~25 (M32 EV9 Pokédex ceremony + EV74/78/80 Theo chase + displayNinjaLetter), defer ×90 (ALL CE entries — slice calls zero CEs). **IDIOM BUCKET IMPLEMENTED 2026-07-05** (walkthrough review with user → all 5 clusters approved; built via /delegate — 3 research + 2 build agents, lead owned seams). Text codes: `deterministic.translate_text_codes` → `TextTranslation{text,autoclose,sign}` (LEGACY `_translate_text` untouched — classifier outputs stay frozen-validated): `\wt[n]`→`{PAUSE 0xHH}` **first-guess n*3 formula, CALIBRATE BY EYE at boot gate**, `\.`→0x0F, `\|`→0x3C, trailing `\wtnp`→MSGBOX_AUTOCLOSE (non-trailing queues), `\sign` prefix→validated lock/release-no-faceplayer idiom, `\c[n]`/`<fs>` stripped (approved DROP), `\v`/`\r`/unknown→queue. Transpiler (all fail-loud, unknown shapes still queue): door `get_character(0).onEvent?`→getplayerxy+static-coord if (event x/y plumbed into TranspileContext); 208→applymovement set_invisible/set_visible (player transparency); `pbReceiveItem`→giveitem+VAR_RESULT branch (both-arms-empty→bare giveitem, M49 EV18 user-approved; `ctx.items` wired from det_ctx in driver — was the missing seam); 112 align-loops→getplayerxy+while (509 display-dup children filtered from shape match — real corpus loops have 4 children); 207 emote tier anims **17/19→Common_Movement_{Exclamation,Question}Mark** (movement.inc:4,8), target=literal RMXP event id (**TASK-4 CONSTRAINT: object-event local ids must equal RMXP event ids**), anims 104/18/self-target queue. **NEW SoT: `reference/animation_names.json`** (300 anims, dumped Animations.rxdata via Marshal stubs — Phase 3 never dumped it). pbCallBub open Q RESOLVED-FINAL (floating textbox not emote; script_calls.md corrected, archived). Slice queue **186→132** (cleared: idiom ~49 + emotes 4 + text; remaining: CE defer ~90, native 11, hand ~25, residue incl. `\v[3]`+CE35's new `\1` code); all 19 .pory compile clean (real poryscript); 740 tests, 737 pass + 3 known MAP_MOKI_TOWN fails (`test_text_with_control_code_queues` re-pinned to still-queueing `\r`/`\v`). Corpus payoff note: readthrough's "627 corpus" text-code figure not reproducible — fresh census = **938**/6570 dialogue commands. **HAND BUCKET DONE 2026-07-05** (infra committed `af3f3632`: `hand_overrides.py` splice layer + `hand_conversions/` + EV20-guard/pbPhoneRegisterNPC strips — built by the session-limit-cut session; overrides authored after reset): two overrides, `hand_conversions/Map032_EV009.pory` (Pokédex ceremony — sign-fork = getplayerxy + early `release`/`end` inside if (poryscript-compile-verified); y<=43 reposition route; starter readback = `copyvar(VAR_TEMP_POKEMON_CHOICE, VAR_POKEMONTEST)` (**var 151 minted via registry API** with the transpiler's exact label→to_constant path so a future Map050 run converges) + ==3 clamp + per-branch literal "Go! <starter>!"; `$Trainer.pokedex=true` → `setflag(FLAG_SYS_POKEDEX_GET)`+`special(SetUnlockedPokedexFlags)` (Birch/Oak-lab pattern); anim 104 "Rustle"→exclamation emote, anim 18 dropped (freeze pose carries the hit); [12,13] gestures→walk_in_place_right; **change-graphic routes (PU-starters on EV77, PU-POKEBALL on EV76) deferred to task 4** — fork has NO script-callable live gfx swap: `VAR_OBJ_GFX_ID_x` resolves at spawn only (event_object_movement.c:3236); recipe if needed = setvar+removeobject+addobject; `OBJ_EVENT_GFX_SPECIES(x)` ships (OW_POKEMON_OBJECT_EVENTS=TRUE) but Uranium species lack overworldData) + `hand_conversions/Map049_EV021.pory` (displayNinjaLetter → letter body as one scrolling msgbox, bespoke card-UI scene = Phase-8 custom-C candidate; code 202 → `setobjectxy(20,18,9)`). **EV74/78/80 residue: zero queue entries left** — the idiom bucket already cleared the Theo-chase events, no override needed. Slice queue **130→107** (= 17 slice entries — native cluster: rock-smash 3, cave 9, PC 1, pbShowMap 2, EV1 runningShoes+pbHasSpecies 2 — plus 90 CE defers); Map032/Map049 poryscript-compile clean, fork gate clean, `hand_overridden: 2`. **D9 NOTE:** `test_real_slice_map032_prunes_clean` fails on stale porymap map.json (from the last assemble) vs fresh pory — pre-existing; at re-assembly ensure EV9/EV21 are wired as object events so their hand blocks survive `prune_map_pory`, then re-pin the dropped set. **NATIVE BUCKET DONE 2026-07-05** (built via /delegate, one Sonnet builder — lead reviewed diff + output): 5 shapes in `transpiler.py` + 21 tests (`tests/test_transpiler_native.py`), all whole-shape matches, near-misses queue. (1) `Kernel.pbRockSmash` 111-conditional → `goto EventScript_RockSmash` (field_move_scripts.inc:64; never returns → BOTH arms dropped unwalked); (2) cave-entrance triad stripped w/ breadcrumbs — `pbCaveEntrance` (48× corpus, always bare; native warp fade subsumes), code-204 fog **exact tuple** `(1,"004-Shade02",0,60,0,250,0,0)` only, player step-forward route `[12]` (35× corpus, ALL player+1-step; no facing-relative token in fork — `_last_route_ok=False` so paired 210 neutralizes); (3) `pbTrainerPC` → `goto EventScript_PC` (pc.inc:1, the generic PC field_control_avatar.c:496); (4) `pbShowMap` (18× corpus, always bare) → `special(FieldShowRegionMap)` **without** explicit waitstate (`def_special … waitstate=1` auto-injects — explicit one would double up); (5) `$PokemonGlobal.runningShoes=true` → `setflag(FLAG_SYS_B_DASH)` (flags.h:1463; the `=false` corpus variant still queues). **pbHasSpecies?(::PBSpecies::RAPTORCH) (M49 EV1) left queued deliberately:** fork ships `checkspecies` (event.inc:2541) + `DoesPlayerPartyContainSpecies` special, but SPECIES_RAPTORCH isn't in the pristine index → gate would reject; revisit when Uranium species constants join the gate extras (Phase 7). Slice queue **107→91** (= 90 CE defers + that 1). **BOOT-GATE EYE-CHECK:** rock-smash/PC pages emit `lock`/`faceplayer` then `goto` into fork scripts that `lockall` — statically fine, verify in mGBA. **NEXT — task 4 (NPC gfx) BUILD IN PROGRESS, PIVOTED 2026-07-05 (user decision): CONVERT the real Uranium sheets — lookalike substitution demoted to fallback. Resume from `reference/findings/task4_npc_gfx_notes.md` §BUILD STATUS checkpoint** (session hit limit mid-/delegate-build; ALL task-4 build work uncommitted; on resume run `git status`+pytest FIRST — two sub-agents were still landing files when this was written). Pivot facts (detail in notes): Uranium sheets are 2× GBA scale — mons/Theo/rocksmash EXACT 2× NN upscales (pixel-perfect recovery), 11 HGSS townsfolk 2×-grid-aligned w/ ~2.5% touched-up blocks → majority-vote downscale + by-eye; engine additions append-only/codegen-friendly (INCGFX auto-rules, NO Makefile edits, NUM bump compile-checked); palette budget = pack ALL converted sprites onto ≤4 shared 16-color pals (hard 16 OBJ banks shared w/ player+effects; overflow = silent color garbage; slot-reservation code is DEAD in 21c24202). **DONE (lead): 6 sentinel engine hooks + 7 stub .gen.h (gitignored) — `make modern` BUILD CLEAN.** Gotchas: hooks inside src/data/object_events/*.h need SAME-DIR quoted includes (GCC resolves quotes against the includING file's dir); graphics_info_pointers.h needs decls hook ABOVE the array (included at eom.c:481, BEFORE graphics_info.h:487) + entries hook INSIDE the braces; palette entries hook must sit BEFORE the PAL_TAG_NONE terminator. **DONE (agent): `tileset_converter/local_id_remap.py` + 27 tests** (one-pass simultaneous splice — shifting tables safe, apply EXACTLY ONCE at staging; string/comment-masked; unmapped int → warn+leave; per-map table `Map{id:03d}.json` {"rmxp_id": local}). **LANDED COMPLETE (verified 2026-07-06 via /delegate audit — both interrupted sub-agents finished before the cut): `graphics/sprites.py`+preview script AND npc_gfx.py/npc_gfx_map.json (18 entries)/metadata_wiring rework** (boot-page classification, BgEvent/CoordEvent, drop reports, `write_local_id_tables`, `build_slice_maps` npc_gfx/local_id_dir params; DEFAULT_GFX ninja-boy fallback REMOVED). 862 tests pass (sprites 17, npc_gfx 33, remap 27), ruff clean, engine stubs still `make modern` clean; only failures = the 3 known pre-existing (2× MAP_MOKI_TOWN mint, 1× stale prune re-pin). Full pinned specs in notes §BUILD STATUS. **sprite_emit.py + sprite_pass.py + lead integration LANDED** (assemble_pathfinder.py:138-150 runs `sprite_pass.run_sprite_pass` before compile; 18 strips → `engine/graphics/object_events/pics/uranium/`; EV21 hand blocks prune as accepted). **2026-07-06 REBUILD SESSION: slice ROM built CLEAN walker-OFF (`URANIUM_MAP_WALKER FALSE` flipped so NPCs spawn for the gate) + ROM + preview PNG taildropped to user.** Three build breaks hit+fixed en route: **(1) `goto Label` without parens** — poryscript parses it as TWO bare commands (compiles CLEAN, splits across lines in the .inc, asm error); rock-smash/PC natives fixed to `goto(...)` (transpiler.py + test pins). **(2) inline `[token]` movement lists NOT supported by installed poryscript** — passed through verbatim → link error `undefined reference to walk_down`; poryscript compile AND fork gate both miss it, only `make` catches. `_emit_align_loop` + `_emit_player_transparency` now emit named `movement` blocks via `self.movements` (same as 209 path); smoke test updated. **(3) `staging/layouts/layouts.json` ACCUMULATES across builds** — phase5 walker runs left 177 entries; slice assembler upserts ALL into layouts.gen.json → undefined `gTileset_Uranium10xx` synth refs at link. Deleted the staging manifest + 5 stale batch map/layout dirs in engine/data (MokiTownHouse1/2, ProfessorLab, Theo, Route01). **WART: slice + walker builds share mutable staging + engine gen-state — clean both when switching build flavors.** Remap warn+leave fired on applymovement(16/75/2) = no-boot-page actors, by design. 884 pass; 3 fails = 2 known MAP_MOKI_TOWN mints + `test_sprite_emit.py::test_stub_writer_matches_real_stubs_on_disk` (test DESIGN bug: compares stub-writer output vs on-disk engine gen files which now hold real emitted content; passes only on fresh clones — re-pin to fixed expected strings). Old D9 stale-prune failure GONE (prune test passes on the fresh assemble). **BOOT-GATE BUG #1 FOUND+FIXED 2026-07-06 (user: NPC colors/details all wrong in-game, preview PNG fine):** sprite_emit palette off-by-one — `gUraniumObjEventPal_*[16]` emitted only the 15 OPAQUE colors (slot 0 transparent placeholder missing), C zero-fills tail → every pixel one color early, index-15 → black. PNGs index opaque at 1..15 (`_write_sheet_png`), hardware array must match. Fix = prepend 0x0000 in `pal_words` (sprite_emit.py:574) + regression pin `{0x0000, ` in test_graphics_file_shape (NO test had pinned slot 0). Rebuilt + re-taildropped. **BOOT-GATE BUG #2 FOUND+FIXED 2026-07-06 (user: text runs off viewport + overwrites, e.g. grandma M49 EV1):** Uranium dialogue is laid out for RMXP's wide window (single lines ~90 chars, embedded `\n` layout breaks; a 3rd raw `\n` line draws OVER line 2 — `\l` scroll never used in source). Fix = poryscript `format()` everywhere dialogue is emitted: new `deterministic.format_pory_dialogue` (flattens `\n` runs→space, wraps in `format("...")`) used by transpiler TextRun (plain/autoclose/sign), classifier flush(), trainerbattle intro/defeat/post; hand overrides wrapped by one-shot script (EV21 letter keeps its deliberate `\l` chain — format() width-wraps fragments and passes explicit escapes through UNTOUCHED, empirically verified: it does NOT convert embedded `\n`→`\l`, which is why flattening is required). format() verified OK in msgbox 2nd-arg + trainerbattle arg positions; binary at `/home/b/tools/poryscript/poryscript-linux/` w/ font_config.json auto-passed (-fc) by poryscript.py. ~30 test pins updated via Sonnet sub-agent (2 tests renamed — line breaks no longer pass through) + 4 format_pory_dialogue unit tests. 888 pass + 3 known. Rebuilt + re-taildropped. **BOOT-GATE BUG #3 FOUND+FIXED 2026-07-06 (user: rock-smash rocks invisible, only ground shadows — those are baked into the tile art):** `fk107-rocksmash` is a PROP sheet (intact rock in col0, cols 1/3 fully EMPTY — it's the smash-debris anim, not a walk cycle) → `_detect_cycle`'s "col1==col3" was vacuously true on two blank columns → neutral13 → idle frame = BLANK. Fix in `graphics/sprites.py`: a column pair only counts as idle if non-empty (rock now falls to distinct, idle=col0=intact rock ✓) + fail-loud ValueError when even fallback col0 is all-transparent (sprite would be invisible). 2 new tests. 890 pass + 3 known. Rebuilt + re-taildropped. **BOOT-GATE BUG #4 FOUND+FIXED 2026-07-06 (user: reflections on grass/hedges next to Moki pond, only the fisherman actually on water):** RMXP mappers flood-fill a water layer UNDER solid land tiles; the topmost-NONZERO-tag rule fell through the land tile's tag-0 to the water tag beneath → those columns got MB_POND_WATER (16, reflective — pokeemerald reflects any object whose neighbor tile is reflective). Fix: `terrain_tags.effective_tag`/`column_behavior` take optional `is_opaque(tile_id)` — a FULLY-OPAQUE tag-0 tile stops the fall-through (returns MB_NORMAL); transparent overlays (flowers) still fall through. build_slice_tilesets wires the predicate from the rasterizer (alpha==255 all, cached per tile). Census (scratchpad reflect_census.py pattern): Moki Town 839 columns → exactly 5 flip 16→0, all 79 real nonzero behaviors (incl. actual pond) unchanged. 1 new test (opaque stop + transparent/legacy fall-through). 891 pass + 3 known. Rebuilt + re-taildropped. **BOOT-GATE BUG #5 FOUND+FIXED 2026-07-06 (user: rock smash leaves debris):** `rock_smash_break` (Movement_SmashRock) plays `ANIM_REMOVE_OBSTACLE`(=1) then waits `SpriteAnimEnded` — on our `sAnimTable_Standard` index 1 is a LOOPING walk anim → never ends → `waitmovement` stalls → `removeobject` never runs. Fix = **break-prop sprite class**: `sprites.BREAK_PROP_SHEETS={"fk107-rocksmash"}` converts column-0 rows top→bottom as a 4-frame break sequence (matches fork `sAnim_RockBreak` frames 0..3 + END); sprite_emit gives break_props vanilla-rock graphics_info (`sAnimTable_BreakableRock`, inanimate=TRUE, TRACKS_NONE, SHADOW_SIZE_S) + 4-frame strip (`overworld_ascending_frames` is count-agnostic, relative offsets). 4 new tests. **REFLECTION FOLLOW-UP (user: #4 "didn't work"):** probe (scratchpad reflect_probe.py) showed the terrain-leak fix DID land — remaining reflections are REAL pond water caught by the engine's scan box: `ObjectEventGetNearbyReflectionType` scans x±(width/16-1), y+1..y+(height/16) → our 32×32 frames scan 3-wide (vanilla 16-wide NPCs scan straight down only; y+2 depth is vanilla for ALL 32-tall gfx). `info->width/height` also feed `centerToCornerVec` sprite anchoring → can NOT shrink safely. Fix requires a sentinel-fenced engine tweak (narrow the scan) = baseline-behavior change → **USER DECISION PENDING** (asked; options: fence the scan to direct-below column / leave engine-native). **REFLECTION FIX DONE 2026-07-06 (user chose "narrow scan"):** sentinel-fenced `ObjectEventGetNearbyReflectionType` in event_object_movement.c:9925 — now scans ONLY the tile directly below (current+previous coords, y+1), pristine body kept under `#if 0` for revert. ROM-only change (no re-assemble). Applies to all objects incl. player — shore reflections still work, diagonal/through-hedge gone. This is a NEW baseline-behavior engine divergence → add to the tracked-obligation ledger (sentinel-fenced, revertable). Break-prop + reflection both rebuilt + taildropped. 895 pass + 3 known. **2026-07-07 REFLECTION NARROW-SCAN ABANDONED (user decision — supersedes the 07-06 "REFLECTION FIX DONE" entry above):** the 07-06 narrow-scan edit had already been LOST from the tree (file mtime 00:02 07-07 post-dates the 20:54 ROM build — that taildropped ROM had it, source didn't); it was re-applied 2026-07-07, user then ruled **"not actually a fix" — REVERTED to pristine**. `ObjectEventGetNearbyReflectionType` is now engine-native (wide width/height-driven scan; only the palette-hook divergence remains in event_object_movement.c) and we LIVE WITH the over-eager pond-adjacent reflections for now. Do NOT re-apply the narrow scan; if reflections get revisited it needs a different approach. One fewer tracked-obligation divergence. **PLAYER SPRITE = URANIUM HERO DONE 2026-07-07** (user request; /delegate — 3 research agents + 1 Sonnet builder; lead owned engine files): `sprites.convert_player_sheets` (HERO.png walk + HERO-RUN.png run, 256×256/64×64-frame, NOT exact-2x → majority-vote; 18 frames = walk 0-8 + run 9-17 both in `_FRAME_PLAN` order — `sAnimTable_BrendanMayNormal` run anims verified = plan+9 exactly; **JOINT anchor across all 18** so walk↔run doesn't hop) + sprite_emit "player" class (`hero.png` 576×32 strip; **DEDICATED pal bank** `OBJ_EVENT_PAL_TAG_URANIUM_PLAYER`=0x1138, not packed into the 4 NPC banks; union 30 colors→median-cut 15; `gObjectEventGraphicsInfo_UraniumPlayerNormal` w/ PALSLOT_PLAYER + BrendanMay anims) + sprite_pass wiring (always-on, fail-loud). **Engine seam (lead): walking+running are ONE gfx id** (`OBJ_EVENT_GFX_BRENDAN_NORMAL`, 18-frame concat buffer — no separate running id) → sentinel-fenced repoint of that one pointer entry (graphics_info_pointers.h:409) at the Uranium symbol. **Gotcha: `-Woverride-init`+`-Werror` (→ `engine_extension_surface.md` §4)** — fence the tracked line, don't emit an override entry from the gen hook. Decls STUB `#define`s the symbol back to `gObjectEventGraphicsInfo_BrendanNormal` so fresh clones build unstaged. Legacy `sPlayerReflectionPaletteSets` miss on our tag = safe no-op (sentinel loop, verified); generic pond filter tints the player reflection. Field-move/bike/surf/fishing poses still Emerald Brendan (separate gfx entries; only the rock-smash pose is slice-reachable) — swap later if it grates. HEROINE deferred (slice boots MALE hardcoded; HEROINE sheets ARE exact-2x per census). Stub-writer disk-state test FIXED (re-pinned to literal strings, no disk compare). 913 pass + 2 known MAP_MOKI_TOWN fails. ROM rebuilt clean + taildropped w/ hero preview PNG. **TASK-4 TREE + PLAYER SPRITE COMMITTED 2026-07-07 (`f8803c6d`, transpiler branch — 39 files).** **16px-sprite question ANSWERED 2026-07-07 (user asked; census scratchpad content_census.py): NOTHING fits 16×32 except the rock prop (16×16)** — humans 17–23px wide post-downscale (HGSS-proportioned art; HGSS was natively 32×32 on DS), player hero 22×27, mons 22–23 — and width/height can't be under-declared (they anchor the sprite via centerToCornerVec). **Stay 32×32; 16px rejected.** **BOOT-GATE WALK RESULTS (user, 2026-07-07): player sprite GOOD; rock smash blocked by vanilla gating** — `EventScript_RockSmash` `checkfieldmove` needs FLAG_BADGE03_GET + a non-egg party mon knowing MOVE_ROCK_SMASH → fresh boot (empty party) = "rugged rock" message. **FIELD-MOVE FLASH + ROCK-SMASH HARNESS IN PROGRESS 2026-07-07 (/delegate):** naive repoint of [OBJ_EVENT_GFX_BRENDAN_FIELD_MOVE] at UraniumPlayerNormal = **SOFTLOCK** (fldeff_rocksmash Task_DoFieldMove waits `SpriteAnimEnded`; only `ANIMCMD_END` sets it; ALL BrendanMayNormal anims loop via ANIMCMD_JUMP). Fix (Uranium-faithful — RMXP has NO field-move pose): sprite_emit emits `gObjectEventGraphicsInfo_UraniumPlayerFieldMove` = UraniumPlayerNormal clone but `.anims=sUraniumAnimTable_PlayerFieldMove` (gen-file static, `ANIMCMD_FRAME(0,1)`+`ANIMCMD_END` = 1-tick blip then swap-back via GetPlayerAvatarGraphicsIdByCurrentState, fldeff_rocksmash.c:93); pointer fence at graphics_info_pointers.h:415 (LANDED); stub #define → BrendanFieldMove (LANDED — 916 tests pass, 40 in test_sprite_emit). **new_game.c harness LANDED (sentinel-fenced, marked TEST HARNESS/REMOVE):** after `CB2_NewGame()` (party is ZEROED inside it — NewGameInitData) → `FlagSet(FLAG_BADGE03_GET)` + `ScriptGiveMon(SPECIES_GEODUDE,5,ITEM_NONE)` + `ScriptSetMonMoveSlot(0,MOVE_ROCK_SMASH,0)` (move-slot helper syncs PP; SetBoxMonData handles checksum). **TRACKED OBLIGATION: remove the harness when real progression grants badge/party** (Pokédex ceremony gives starter). **[SUPERSEDED 2026-07-13: user decision — harness KEPT indefinitely as the standing HM/field-move test rig for slice expansion (grows per-HM); strip only for a release ROM. See SLICE1_TODO #5 Done + `engine_extension_surface.md` §3.]** Bike/surf/fish player sheets DEFERRED (exist as HERO-BIKE/SURF/FISH etc., 264×288 & 384×384 NONSTANDARD grids — not 4×4-of-64; none slice-reachable). **ROM rebuilt CLEAN + taildropped 2026-07-07 (field-move variant + harness in). ALL 2026-07-07 work UNCOMMITTED past `f8803c6d` — commit after rock-smash retest.** **Rock-smash retest PASSED 2026-07-07 (smash works, no Brendan flash) — but BOOT-GATE BUG #6 FOUND+FIXED same day (user: rock REAPPEARS when player steps on its tile):** porymap `"flag": "0"` on the rock object events = engine null-flag sentinel (`GetFlagPointer(0)`→NULL, FlagSet no-op, FlagGet always FALSE) → `removeobject`'s FlagSet does nothing and `TrySpawnObjectEvents` (every camera move) respawns it. Fix (via /delegate, 2 Sonnet builders on disjoint files, lead owned schema seam + pipeline rerun): **NEW transpiler→wiring channel — per-map trait sidecar** `output/…/scripts/Map{id:03d}.traits.json` (`{"events": {"14": ["smashable_rock"]}}`, always written incl. empty, fresh per run) — transpiler rock-smash native records `ctx.record_trait("smashable_rock")` (traits keyed (map_id,event_id); no-op for CEs), driver `_write_traits_sidecar` alongside each .pory; `metadata_wiring.build_object_events(event_traits=…)` assigns **FLAG_TEMP_11..1F sequential by ascending event id** (vanilla obstacle range — engine `SetHideObstacleFlag`/`TemplateIsObstacleAndWithinView` recognize it; temp flags clear on map re-entry → rock re-forms per visit, matches Essentials `event.erase` transience), fail-loud: >15/map, unknown trait, stale sidecar (trait on non-emitted event, validated vs `local_id_map`); `build_slice_maps` takes nested `{map_id: {event_id: traits}}`; `stage_slice_scripts` REQUIRES the sidecar (missing → FileNotFoundError "re-run transpile driver"). Map032 EV14/15/33 → FLAG_TEMP_11/12/13. 926 pass + 2 known MAP_MOKI_TOWN fails. Full chain re-run (driver→stage→assemble→make) clean; ROM taildropped 2026-07-07. **Respawn retest PASSED 2026-07-07 — rock smash fully working. All 2026-07-07 work COMMITTED (field-move variant + harness + bug #6 trait channel). NEXT: rest of boot-gate findings from user's slice walk.**
3. **Image pipeline** — quantize Uranium's real tilesets/sprites to GBA 4bpp (replaces the v1 passability-bucket scheme; closes the "flat layout" S9 defect). **Steps 1–3 BUILT + validated 2026-06-20** (`tileset_converter/graphics/`: sources→autotile→raster → 16×16 RGBA tiles; both slice maps reconstruct as correct Uranium art). **Step 4 (palette quantization) BUILT + validated 2026-06-20** (`graphics/quantize.py`, numpy): **two-phase palette packer** (global vocab → agglomerative bitmask bin-packing — NOT k-means; tiles are already ≤15 colours) + per-tile **solid-body** alpha classify (threshold/stipple); full Moki Town → on-screen shift **0.95/31, matches by eye** (`scripts/quantize_moki_preview.py`), 30 tests. User picks this session: **numpy**, **per-tile classify**, **full-town-confirm-first**. Three render bugs (paths→green, black tree-tops, orange tree-shading) all found by user LOOKING, all fixed. **Step 5 (GBA emitter) + S8a integration BUILT + committed 2026-06-21** (`e3c4a867` pipeline + `73848c7e` integration): `graphics/emit.py` (8×8-dedup → `tiles.png`/`palettes`/`metatiles.bin`/`attrs` for a dedicated PRIMARY+SECONDARY pair; formats verified vs `engine/include/fieldmap.h`) + `graphics/build_slice_tilesets.py` (S8a pre-pass: per tileset enumerate **topmost-visual** tile_ids + warp-door tiles → emit into `engine/`, write 4 sentinel-hooked `uranium_*.gen.h` fragments + `reference/tileset_map.gen.json` overlay; one metatile/visual-tile, autotile variants kept distinct, RMXP id 0 = void metatile, rep-door gets `MB_NON_ANIMATED_DOOR`). `load_tile_map` prefers the `.gen.json` overlay; `lookup` exact-id-first (faithful autotiles). Wired as assemble_pathfinder **S8a** (before layout). Validated on real art: ts19 123 mt/329 tiles/13 pal (shift 0.93), ts22 340/867/13 (0.98), both fit primary+secondary; Map032 layout → **339 real metatiles** (was 2 buckets). Scope = **tilesets only**; autotiles **faithful**. ****Faithful-column rework + build DONE 2026-06-21 (uncommitted):** the committed S8a emitted ONE metatile per *topmost-visual* tile id (flat — lost the RMXP z-stack). Reworked to key each metatile by the **full column** (`layout.column_key` = non-empty `(z,tile_id)` pairs, autotiles distinct), split by RMXP **priority** into a **two-layer** GBA metatile (priority>0 → top BG over the player; rest → bottom), with **flip-aware 8×8 dedup** + **post-quantization merge** (tiles that snap to the same palette+pixels share one stored tile) + **fail-loud budget guard** (raises before writing if tiles/metatiles exceed 1024). `emit_tileset` now takes a `MetatileImage` list (bottom/top RGBA + layer_type + behavior), not tile_ids+rasterizer. `tile_map` keys `tiles` by serialized column-key string (`serialize_column_key`, shared writer/reader), adds `has_columns`/`lookup_column`; `collapse_column` dual-path (column mode vs legacy bucket). **Re-assembled + built clean:** ts19 129 cols→131 mt/306 tiles, ts22 (Moki Town) 839 cols→841 mt/**1011 tiles** (post-quant merge brought it under 1024 from 1102), all 3 maps **0% void** (every column resolved via overlay); `make -C engine -j16 modern` → 32MB `pokeemerald.gba` (gbagfx `.4bpp` OK, ROM 78.75%). 499 tests pass (+2 new: post-quant-merge, budget-overrun), ruff clean. **NEXT: user boots `engine/pokeemerald.gba` in mGBA (per-slice §9 gate) — walk Map49↔48↔32, confirm real Uranium art renders + warps fire.** Cross-tileset refs are safe (each Uranium tileset gets a dedicated, always-paired primary+secondary). See `reference/guides/graphics_conversion_notes.md` §7b/§8/§10.
4. **NPC gfx map** — RMXP `character_name` → `OBJ_EVENT_GFX_*` + skip/invisible graphic-less events (closes the "NPC crowd" S9 defect). **Warps already FIXED** (converter level; needs re-assemble+rebuild+boot to confirm in-game).
6. **F1+F2 FIX DONE 2026-07-10 (user chose "grow engine ranges"; /delegate — lead owned engine + docs, 1 Haiku sweep + 1 Sonnet builder on Python):** Tier-2 config gate **`RPG2GBA_EXPAND_EVENT_RANGES`** (`engine/include/config/rpg2gba.h`, default TRUE — full spec in `engine_extension_surface.md` §2): flags grow 0x960→0x1160 as three converter regions (temp-switch 0x960 ×0x180 **cleared by `ClearTempFieldEventData` on map transition** = F2 fix, decoupled from FLAG_TEMP_11..1F rocks; global 0xAE0 ×0x180; self-switch 0xC60 ×0x500), vars grow VARS_END 0x40FF→0x41FF (×0x100); paid for by `FREE_MYSTERY_EVENT_BUFFERS TRUE` (fork's own save.h toggle, ramScript 1104B) — SaveBlock1 15332/15872, STATIC_ASSERT enforced. Sizing census (scratchpad selfswitch_census.py): corpus needs 235 global / 119 vars / **1132 self-switches** / **345 temp-switches** (map JSON key is `list`, NOT `commands`) — killed the "remap into vanilla unused" option (only 29 VAR_UNUSED vs 106 already minted). Python: `dump_header` bases now `int | str` (str = engine constant name, emitted symbolically — cpp resolves in event_scripts.s since flags.h/vars.h are included at :36/:73 before the :1744 hook) + `capacities` dict fail-loud guard; `assemble_pathfinder._load_rpg2gba_capacities` parses the four `RPG2GBA_*_COUNT` from the engine header (missing/gate-FALSE → RuntimeError). Probe trick for SaveBlock1 sizing: compile `char probe[sizeof(struct SaveBlock1)]` w/ devkitARM gcc (`/opt/devkitpro/devkitARM/bin/`, NOT on PATH) + `nm -S`. 951 pass (+9 new) + 2 known MAP_MOKI_TOWN fails; slice re-assembled (mints 196/106/16/8 all within capacity), `make modern` clean, ROM taildropped. Docs updated: extension-surface §2 (planned→implemented) + §6 grep now `-e URANIUM -e RPG2GBA`, audit F1/F2 marked fixed, flag_registry_policy "flag budget" RESOLVED, ROADMAP 7.4, conversion_walkthrough sample. Committed (see `git log -S RPG2GBA_EXPAND_EVENT_RANGES`).
7. **BOOT-GATE BUG #7 (repeated NPC dialogue) FIXED 2026-07-11 (/delegate, 1 Explore + 1 Sonnet builder; lead owned rebuild):** Auntie (M49 EV1) re-gave running shoes, granny (M32 EV27) re-gave Rare Candy — `build_page_dispatcher` deferred ANY multi-page event with a global switch/var page gate (stale pre-transpiler limitation) → object wired straight at Page1 forever, though Page1's `setflag(SS*)` fired fine. Fix: dispatcher now emits per-page conjunction guards (`flag(FLAG_X) && var(VAR_Y) >= n && flag(SS)`) high→low, names resolved through the live FlagRegistry via NEW shared module fns `flag_registry.resolve_switch_flag/resolve_variable_var` (TranspileContext delegates to them — one mint path, §4.3); unresolvable (s:-script switch / unnamed / no registry) still defers whole event; condition-only self-switches now minted (closes latent gate hole); `_resolve_script` fail-loud validates every dispatcher goto target ∈ pory_labels. Plumbed `flag_registry=` kwarg through `build_object_events`/`build_slice_maps`; `stage_slice_scripts._regenerate_map_json` loads `flag_state.json` (fork_path from env), passes it, SAVES after (assembler gate reads a static snapshot — unsaved mints would be gate-rejected). RMXP var semantics verified vs Uranium source (022_Game_Event_v17.rb:151-154): page active iff var >= value. Base-page (index 0) own condition ignored (as before — it's always the fallback; sprite is static anyway). Map032 dispatchers 3→15 (EV9 ceremony now VAR_QUEST_LOG-tiered; EV27 = FINAL_EVENT&&SSB→P4, FINAL_EVENT→P3, SSA→P2, P1), Map049 EV1 5-page dispatch emitted; FLAG_FINAL_EVENT/VAR_QUEST_LOG were already minted, 0 new mints (still 196/106/16/8). +8 tests (test_tileset_converter.py) = 959 pass + 2 known MAP_MOKI_TOWN fails; stage existence-check + fork gate + `make modern` clean; ROM taildropped 2026-07-11. Retest PASSED on device 2026-07-11 (both NPCs advance); COMMITTED as `5fc67dbf` (F1+F2 + registry resolve helpers) + `710e258c` (dispatcher fix). Slice census (scratchpad dispatch_census.py): 39 multi-page events → 31 dispatched, 8 defer — ALL on script-switches 22 `s:tsOff?("A")` / 12 `s:tsOn?("A")` (per-event TEMP-SWITCH predicates; mechanically resolvable as `!flag(FLAG_MAP…_TSA)`/`flag(…)` against the event's own already-minted temp-switch flag WITHOUT minting for the s: switch itself — candidate follow-up, ask user; within-visit dialogue toggles, not the repeat-item bug class). **[RESOLVED 2026-07-13: carve-out landed (`_resolve_switch_gate_term` + `FlagRegistry.seed_labels` staging fix) AND premise corrected — the 8 are warp doors, never object-emitted, natively subsumed; slice output unchanged. See Discoveries + SLICE1_TODO Done.]** Known limits: page dispatch selects the SCRIPT only (boot-page gfx/visibility still static); base-page own condition ignored (always fallback).
8. **TILESET ANIMATION BUILT 2026-07-12 (user feedback `reference/map_feedback/Map032.json`: flowers+water static; /delegate — 3 research + 2 builders, lead owned engine hook + integration):** RMXP animated autotiles → GBA tileset anims, deterministic end-to-end: `flatten_autotile(frame=)`, `TileRasterizer.render(tid, frame)` + `frame_count_for_tile`, per-column lcm frame rendering (`column_n_frames`, fail-loud cap 64), `MetatileImage.frames`, frame-aware flip-canonical dedup + **static-demotion** (all frames==frame0 → static; auto-drops water buried under opaque land AND ts19's degenerate all-black 4-frame strip), union-color quantization (`extra_tile_colors` threaded through `build_quantized_tileset_family`; frames re-quantized against the tile's single palette via new `quantize_tile_to_palette`), post-quant merge key = (pal, ALL frames' bytes), animated tiles packed contiguous per effect (effect = distinct frame count) at head of PRIMARY tiles, anim frame PNGs `<primary>/anim/anim{N}/f{NN}.png` + 5th gen fragment `engine/src/data/tilesets/uranium_anims.gen.h` (INCGFX frame tables + Queue/TilesetAnim/InitTilesetAnim fns, vanilla 16-tick cadence, counterMax=16*lcm; primary header `.callback` wired, forward-decl in tilesets gen.h). **Gotcha: fragment must be `.gen.h` NOT `.gen.c` — engine Makefile globs `src/**/*.c` and compiled it standalone (no u16/statics); renamed after first build break.** Sentinel hook = fenced `#include` at bottom of `engine/src/tileset_anims.c` (tracked; emitter writes stub when no effects; gitignore row added). ts22: pond 57 tiles×19f + flowers 4×4f, 997/1024 tiles — census proved frame-aware dedup costs ZERO extra tiles, zero static collisions; real cost ~20KiB ROM. ts19: 0 effects. **Viewer "Animated" overlay also added same session** (`autotile_frame_count`, `_MapState.animated_slots`, `cell["animated"]` key, cyan `ov_animated` checkbox); grouping feedback root-caused NOT-a-bug (flower autotile's shape variants render pixel-identical → column_key over-split; candidate pixel-identity collapse in SLICE1_TODO #10). 996 pass; `make modern` clean; `InitTilesetAnim_Uranium22` in ELF; ROM taildropped 2026-07-12. **User eye-check PASSED (animation works, cadence approved); COMMITTED `34107e86`** (incl. viewer overlay + user's cleared map_feedback file). Follow-up open questions (cadence fidelity, secondary-tileset effects, waterfall transparency) → SLICE1_TODO #10; the viewer pixel-identity collapse landed 2026-07-13 (SLICE1_TODO #10 Done — `_pixel_classes`).
5. **Map032 boot-gate issues ROOT-CAUSED + FIXED + user RE-WALK PASSED, COMMITTED 2026-07-09 (user approved "go, both skips"):** (plan @ `/home/b/.claude/plans/i-ran-the-map-iterative-aurora.md`; user flagged 24 cells / 3 issue types via viewer; 3 research sub-agents + 199-map priority census, then 2 Sonnet builders on disjoint files) (C, walkable-should-block, 8/8 cells) `layout._cell_blocked` misses Uranium's terrain-tag SKIPS in `playerPassable?` (025__Game_Map_v17.rb:245-284 — Shadow=20 always, Bridge=15 when off-bridge) → shadow decal (p0/pass0x00) early-accepts above the real 0x0F blocker; `scripts/compare_collision.py::uranium_blocked` already has the correct algorithm. (A/B, top-layer over player head) `_render_column` sends ANY p>0 to BG1-always-over; RMXP p1 is row-relative (under char at row y+1) — fix = priority TIER: p>=2→NORMAL top (canopy, correct), p==1→COVERED with overlay in top slot (BG2, under sprite; A-cells all blocked so feet-case impossible; tall-grass feet-cover = native MB_TALL_GRASS); census: bug pattern 1258 cells corpus-wide, conflicts ~all tall grass. (BONUS, unflagged) blank-gfx `through=false` events lose blocking — `page["through"]` never read; 6 Map032 cells walkable → collision-stamp override à la warp_overrides. Note: root `Map032_issues.json` = export of `output/uranium-build/map_viewer_issues/Map032.json` (a sub-agent rm'd the root copy mid-build; restored byte-identical from context — live viewer copy was never touched). **BUILT 2026-07-09:** fix1 = `layout._cell_blocked` skips tags 15/20 (seen_tile set BEFORE skip: shadow-only column = passable, not void-blocked) + `TileMap.terrain_tag()` oracle-loaded; fix2 = `_render_column` tier (`top_min`: p>=2→NORMAL, p==1→COVERED-top-slot, ts22 tiles 1011→999); fix3 = `metadata_wiring.collect_through_block_cells` (blank-gfx+through=False, boot page) → `convert_layout(blocked_cells=…)` stamp, **caller subtracts warp_overrides** (in-slice doors match the same predicate! out-of-slice NO-EMIT doors correctly stay blocked; convert_layout fails loud on overlap); `compare_collision.py`+`pathfinder_collision_preview.py` now delegate to `_cell_blocked`. 945 pass + 2 known fails; flip census (scratchpad collision_flip_census.py): Map032 flips = EXACTLY the 8 C cells, 0 remaining divergence vs Uranium in flagged region (residual 30 south-ledge over-blocks = known empty-ledge_directions deferral); emitted map.bin/attrs verified all 33 checks (8 C + 6 evt blocked, 9 A-cells LAYER_COVERED, B walkable, controls). All UNCOMMITTED — commit after user re-walk passes.

**CUSTOM-ROUTE MOVEMENT INTERPRETER BUILT 2026-07-15 (user-approved engine change; /delegate, lead owned contract + integration).** New engine movement type `MOVEMENT_TYPE_URANIUM_CUSTOM_ROUTE` (0x53, `NUM_MOVEMENT_TYPES`→0x54, sentinel-fenced KEEP) plays a per-object route **bytecode** — faithful deterministic pacing/loops/through-toggle that native `MOVEMENT_TYPE_*` can't do (census: 834 custom routes, 62% not natively expressible; WANDER randomizes, WALK_SEQUENCE can't pause). Full SoT design = **`reference/guides/custom_route_interpreter.md`** (opcode table, FSM, data channel = `trainerRange_berryTreeId` route id + `sprite->data[4..7]` scratch, no struct change). Pipeline: `route_bytecode.py` (`encode_route`+`RouteRegistry`) → `npc_gfx` interpreter-first `_spec_for_custom_route` (out-of-scope codes fall through to native classifier UNCHANGED) → `metadata_wiring` `ObjectEvent.route_id` → `route_table_emit.py` gen.h; single slice-wide `RouteRegistry` in `stage_slice_scripts` feeds both map.json ids + the engine table. Moki Town → 9 CUSTOM_ROUTE movers / 6 deduped routes (Chyinmunk through-toggle patrol intact). 1137 tests pass, `make modern` clean. **USER-VERIFIED DONE 2026-07-15** (ROM `5158b084` walked clean). Two engine bugs fixed post-first-taildrop before it worked: missing `singleMovementActive`, then the ROOT cause — FSM stored PC/through in `data[4]/[5]` which the walk action clobbers; moved to packed `data[6]` (+ idle-0 underflow clamp). See SLICE1_TODO #12. v1 scope + retest checklist + the separate **Theo intro-cutscene** work (SLICE1_TODO #15, different subsystem — actor-reveal, NOT this interpreter) → **SLICE1_TODO #12 round 2 + #15**. Corpus rollout beyond the slice = future (interpreter is corpus-ready).

**WARP-ARRIVAL FACING: TWO FIX ATTEMPTS FAILED RETEST, ROLLED BACK 2026-07-16.** Boot-walk H4 (SLICE1_TODO #8): post-warp facing always DOWN — root cause + full narrative in `WARP_FACING_HANDOFF.md`. Option A (native `MB_*_ARROW_WARP` metatile-behavior stamping, commit `b7c5b8d7`) landed 3/4 directions but a 4th hijacked `TryArrowWarp` (stairs re-trigger). Option B (custom `arrivalDirection` field on `WarpEvent`, end-to-end schema change — struct/warp_def/mapjson.cpp) fixed that structurally and was independently code-reviewed byte-for-byte correct (macro/struct parity, DIR_* mapping, C hook timing all verified against vanilla idiom) — but the user's device retest STILL showed "warps completely broken" after a from-scratch rebuild. Found + fixed one real bug en route (harmless this cycle, real for the future): `engine/map_data_rules.mk`'s events.inc/header.inc/connections.inc/layouts/map_event_ids.h rules never listed the `mapjson` tool binary as a prerequisite — an engine-only rebuild after editing `mapjson.cpp`/`map.inc` could silently skip regenerating stale per-map data. Rebuilt anyway with this fix: ROM hash UNCHANGED (`3a972b62…`, identical to the pre-fix build) — expected, since nothing in the generator itself changed this cycle, but it also means this was now the **4th** from-scratch rebuild reproducing byte-identical, previously-verified-correct warp data while the user still saw it broken on-device. **User decision 2026-07-16: stop chasing it in the pipeline, roll back to isolate whether the base is even sound.** Reset `transpiler` branch `--hard` to `171ac278` (last commit before ANY warp-facing work, right after the custom-route interpreter landed) — nothing lost: Option A is preserved on branch `backup/warp-facing-attempt-2026-07-16`, Option B's uncommitted diff (+ the Makefile fix) is in a git stash (message starts "Option B warp-arrival-direction impl"). Full clean rebuild at `171ac278` hashes `5158b084…` — **the exact hash the user already boot-walked clean on 2026-07-15** (custom-route interpreter retest) — taildropped 2026-07-16 as `pokeemerald-5158b084-rollback.gba` for confirmation. **If this ALSO shows broken warps on device, the fault is not in any of this code** (same bytes the user already walked successfully once) — look device/emulator-side (stale import, auto-resumed save state) before touching the pipeline again. If it works, the warp-facing bug is real and scoped to Option A/B's own changes, and either attempt can be un-stashed/re-applied fresh from here with that isolation confirmed.

**WARP-ARRIVAL FACING FIXED — OPTION C, USER RETEST PASSED 2026-07-16 (committed).** User retested `5158b084`: **warps work fine** → base is sound, and "all warps broken" WAS caused by Option B's own changes. Root-cause hypothesis (not re-litigated, just avoided): `struct WarpEvent` (`global.fieldmap.h:165-172`) is `s16 x,y; u8 elevation,warpId,mapNum,mapGroup` = **exactly 8 B, fully packed, zero spare padding**; Option B's +1 `u8` → 9 B → padded to **10** at align-2, and any C-struct/`warp_def`-macro stride disagreement walks the warp table wrong and silently kills every warp — exactly the symptom. **Do not re-attempt any fix that changes `struct WarpEvent`, `warp_def`, or `mapjson.cpp`.** Option A is likewise dead (see above; `MB_*_ARROW_WARP` re-trigger is structural, not case-specific). **OPTION C (built, data-only, ZERO engine change):** native `MAP_SCRIPT_ON_WARP_INTO_MAP_TABLE` + `turnobject` — `include/constants/map_scripts.h:22-24` documents this hook's purpose verbatim as *"update something about the player as they warp in (e.g. their facing dir)"*. Timing verified: `InitObjectEventsLocal` (`overworld.c:2577-2598`) does `GetInitialPlayerAvatarState()` → `InitPlayerAvatar(...)` (applies the DIR_SOUTH default) → `TryRunOnWarpIntoMapScript()`, at map-load state 3 — player object exists, screen still black until `RunFieldCallback`→`FadeInFromBlack` at state 12, so the turn is invisible. Vanilla precedent is verbatim (`EverGrandeCity_PhoebesRoom`, a dozen Frontier lobbies): `map_script_2 VAR_TEMP_1, 0` → `setvar`+`turnobject LOCALID_PLAYER, DIR_*`+`end`. **Key insight:** the table condition is only `VarGet(a)==VarGet(b)` and **nothing exposes the arrival warp id to scripts** (`gLastUsedWarp`/`location.warpId` have no VAR_*/special — grepped) — but we don't need it: each arrival lands on a distinct known `(x,y)` (the `_resolve_all_warp_events` landing trick), so ONE always-true row + `getplayerxy` coordinate dispatch covers every warp. **Gotchas verified, don't re-derive:** poryscript emits `return` not `end` (safe — `ScriptPop` at depth 0 → NULL scriptPtr → `RunScriptCommand` stops, `script.c:111-115`); `Script_HasNoEffect` dry-runs the entry and would SKIP it, but the leading `setvar` trips `SCREFF_V1|SCREFF_SAVE` instantly; `VAR_TEMP_1` is zeroed by `ClearTempFieldEventData` (`overworld.c:888`, long before our hook); commands must be **non-blocking** (`RunScriptImmediately` runs to completion in one call). **Walker builds skip the hook entirely** (`URANIUM_MAP_WALKER` fence, `overworld.c:2591-2596`) — expected, not a bug. Impl: `metadata_wiring` `WarpSpec.dest_dir` (lifted from Option A) + `_ARRIVAL_DIR_CONST` {2:S,4:W,6:E,8:N} + `ArrivalFacing`/`compute_arrival_facings` (fail-loud on unknown dir + on same-coord/different-dir, which coord dispatch can't tell apart) + `render_arrival_facing_script`, emitted through the **existing dispatcher-`.pory` channel** — `assemble_pathfinder.py:312-326` appends it *before* the empty-`mapscripts` injection, so a real block suppresses the stub with **no assembler change**. **dir=0 (retain) emits nothing** (user decision 2026-07-16): 449/1085 corpus, 7/23 slice, all slice cases door-exits where the native DIR_SOUTH is already right; true retain would need C (nothing exposes pre-warp facing). Census: slice(8 maps) 23 transfers = dir 0×7/4×7/6×3/8×6; corpus 1085 = 0×449/2×289/4×70/6×65/8×212, mode-1 (var target) only 9. Verified in the built asm: 1F (11,3)→DIR_WEST + (10,9)→DIR_NORTH, 2F (4,3)→DIR_EAST, Theo 172/089 mirror it, MokiTown (all dir=0) keeps its empty stub; all 8 `*_OnWarpFacing` symbols linked in `pokeemerald.map`. 1147 pass (+10), ruff clean, `make modern` clean. **ROM `e035dac1` taildropped as `pokeemerald-e035dac1-warpfacing.gba` 2026-07-16; user walked it — all facings correct, COMMITTED.** Boot-walk H4 CLOSED. Note the wider lesson from the A→B→C arc: both failed attempts changed *engine/schema* surface to carry converter data, and the one that worked carried it in **generated script data** instead — prefer the data-only seam when the fork already ships a hook (§4.7), and check `map_scripts.h`'s own doc comments before assuming a hook doesn't exist.

**MOKI STORY-CHAIN BUGS A/B/B'/C/D ALL FIXED 2026-07-17 (/delegate: 3 research + 3 builders, lead owned integration; ROM `59a13ba1`; retested via rounds 2–3, S1–S5 pass as of `c9128e58`; committed 2026-07-17).** Findings doc `reference/findings/moki_slice_story_chain_2026-07-16.md` fully implemented: **(A)** coord events now gated on reserved `VAR_TEMP_F` (`_COORD_GATE_VAR`); `fork_index.check_reserved_var_writes` (RESERVED_WRITE_VARS = VAR_TEMP_F + VAR_TEMP_C) enforced at the transpile_driver gate ONLY (staging gate legitimately sees the wiring's own `setvar(VAR_TEMP_C,1)`). **(B/B')** unified per-map `mapscripts` emitter `render_map_scripts` (ON_TRANSITION + ON_FRAME_TABLE + existing warp-facing, one block, dispatcher-.pory channel): `compute_autorun_entries` reproduces RMXP page-activation (own cond && negated higher-page conds, De Morgan via `_negate_terms`; unresolvable → raise; ungated-higher-page → skip); ON_FRAME = single `VAR_TEMP_C,0 → <Dir>_OnFrame` entry, per-frame goto-dispatch until quiescent then `setvar(VAR_TEMP_C,1)` (mid-visit late activation missed — accepted v1 limit); transpiler wraps trigger-3 bodies `lockall/…/releaseall`; object-side dispatchers route trigger-3/4 pages to `end` (no talk-refire — the wall-walk fix); **autorun scan uses `classify_map_events` objects only** (door WarpSpecs' trigger-3 animation pages must not become ON_FRAME entries). **(C)** hidden cutscene actors: staging scans PRE-prune pory for object-command int targets (`_scan_required_actor_ids`, reuses local_id_remap regex) → `build_object_events(required_actor_ids=…)` emits dropped-but-choreographed events as flag-hidden objects (first graphic page, `_assign_visibility_flags` shared FLAG_TEMP_11..1F pool w/ rocks, ON_TRANSITION recomputes per entry — ClearTempFieldEventData@overworld.c:888 precedes RunOnTransitionMapScript@904); eligibility = no-boot-page OR boot page the loop would DROP (e.g. EV075 invisible Theo runner, opacity-0 non-touch); warp/skip targets skipped (left to strict remap); bg/coord-shape required ids raise. `local_id_remap` unmapped id now RAISES (warnings field kept but always empty). Slice hidden actors: M32 EV002/016/075/076/077/079/081 (locals 20-24+…), M49 EV019/020. **Prune keep-set**: stage unions dispatcher-.pory-referenced event ids into live_ids (ON_FRAME refs blank-graphic-event pages map.json wires nowhere). **(D)** `setvar(VAR_QUEST_LOG, 4)` fixed in hand override; scene got addobject(76/77) + removeobject(76/77/16). New sheets converted for real: `ZP- Professor`, `KELLYN`, `Rivaltheo2` (npc_gfx_map.json entries; sprite_pass fail-loud rejected the lookalike-alias attempt — json keys ARE the sheet-conversion list, gfx constant must equal `gfx_constant_for_sheet(key)`). Test re-pins: `test_real_slice_map032_prunes_clean` dropped-set shrunk (actors now emitted), warp-target-actor test raise→skip. 1208 pass / 0 fail; pre-existing ruff F811 dup `test_normalize_is_idempotent` in test_assembly.py left alone. Retest checklist: beat 4 Theo trip-tile @(26,12); beat 5 lab intro fires ON ENTRY from the door (no wall-walk); aptitude test on talk after intro; beat 10 PokéPod scene in Theo's house (var101→2); beat 11 ceremony choreographs the RIGHT NPCs + quest log →4 (no refire); rocks still respawn; Auntie/granny dispatch unchanged; warp facings unchanged.

**BOOT-WALK ROUND 2 FIXES 2026-07-17 (ROM `762e98aa`; retested in round 3 `c9128e58`: S2–S5 pass; committed 2026-07-17).** User's walk of `59a13ba1` found: lab intro loops forever (walks through wall, dialog restarts) + Theo trip tile never fires. Root causes + fixes (/delegate, 2 diagnostics + 3 builders): **(1) Lab loop** — `_emit_choice` only handled literal ["YES","NO"] text; the ["Yes!","Wait a minute..."] 2-arm choice (code 102) went to the unhandled queue as a comment, DROPPING both arms — which held the RMXP self-switch C/D deactivation writes — so the ON_FRAME guard `!SSC && !SSD` never flipped and the dispatcher re-fired Page1 every frame. Fix: `transpiler.py` `_emit_choice` now handles ANY exactly-2-arm choice via yesnobox (arm0=yes, arm1/cancel=no; custom labels degrade to YES/NO, cosmetic). Unlocked 7 yesnoboxes across the slice; remaining queue 102s are two 3-arm Fennel common events (non-slice). **(2) New invariant** — `stage_slice_scripts.check_onframe_deactivation`: every ON_FRAME-dispatched page body must write ≥1 FLAG_*/VAR_* appearing in the map's OnFrame guards (minus VAR_TEMP_C) or staging raises. Flags the exact shipped bug class (dropped deactivation → ROM loop) at build time. **(3) Trip tile** — NOT the VAR_TEMP_F gate (verified correct): tile (26,12) has RMXP passage 0x0F (layer-2 tile 580) — blocked in Uranium too, faithfully collision=1 on GBA. RMXP trigger-1/2 touch fires on BUMP into a blocked tile; GBA coord events fire only standing ON the tile → step-on trigger on an unstandable tile can never fire. Fix: `metadata_wiring` coord emission relocates a blocked-tile touch trigger to each standable orthogonal neighbor (new `MapPassability.standable`, mirrors `layout._cell_blocked`; zero standable neighbors → raise; passability=None → legacy). Map032: EV074 → (26,11)+(26,13); **EV009 ceremony trigger was ALSO on a blocked tile** (tile 420 passage 15 @16,42) → now (17,42)+(16,43) — beat 11 was silently unreachable before this. Findings doc §3.1's "EV074 converts correctly — right position" was wrong about reachability. 1225 pass / 0 fail; stage prints "onframe check" pass line. Retest checklist: Theo trip fires walking beside the fence at column 26 (row 11 or 13); lab intro fires once, yes/no prompt works, NO refire either answer; "Wait a minute" then re-talk re-offers test; rest of prior checklist (PokéPod var101→2, ceremony @ west exit now fires from (17,42)/(16,43), quest log→4).

**BOOT-WALK ROUND 3 FIX 2026-07-17 (ROM `c9128e58` — user WALKED it same day: Champion misfire GONE, story chain S1–S5 PASS; open frontier = S6+, "issues start answering YES to the aptitude test" (details not yet gathered). Committed with rounds 1–2.)** User's walk of `762e98aa`: postgame Theo "Champion" dialog fired at boot right outside the hero's house (Theo invisible; intro EV009 unaffected). Root cause: `build_page_dispatcher` (`metadata_wiring.py`) never checked RMXP page index 0's OWN condition — index 0 was always the unconditional fallback. Map032 **EV080** (Trainer(4) tripwire @31,31, page0 gated switch 125 FLAG_FINAL_EVENT, page1 self-switch A) reached the ROM via round-2's Trainer(N) no-boot-page tripwire carve-out, which trusts the dispatcher to gate at runtime — dispatcher fell through to Page1 = Champion scene. Theo invisible because the sprite is separate EV081 (correctly hidden). Fix: dispatcher now scans high→low INCLUDING index 0; all-pages-gated-none-match → bare `end` (RMXP no-active-page = inert); single gated page now also emits a guarded dispatcher (was unguarded Page1). Regen verified: EV078 gated FLAG_DEFEATED_GYM1_LEADER, EV080/EV081 gated FLAG_FINAL_EVENT, all inert-fallback; EV074 intro keeps legit unconditional fallback. 1242 pass / 0 fail (+4 dispatcher tests). Retest: walk tiles (31,32)-(31,35) by the house door → nothing fires; rest of round-2 checklist unchanged.

**S6 APTITUDE-TEST BUILD 2026-07-17 (ROM `6e85edb3` taildropped as `uranium-slice-6e85edb3-quiz.gba`, §9 retest PENDING, committed 2026-07-18; /delegate: 2 research + 2 builders, lead owned hand files + integration).** SLICE1_TODO #16+#17 both implemented. **(#17 pipeline)** transpiler `_emit_inline_choice`: Essentials `\ch[var,ifCancel,opts…]` inline-choice text escape (NOT code 102 — semantics verified in `scripts_dump/059_Messages.rb:1218`: 0-based index into game var; ifCancel 0=B-inert/>0=cancel-picks-N-1/<0=queue) → `msgbox` + fork-native `dynmultichoice` (event.inc:1932, `DYN_MULTICHOICE_CB_NONE`, result in VAR_RESULT) + `copyvar` to the registry-resolved var; color tags stripped from labels; poryscript `text` blocks per option; 12 sites/5 maps corpus-wide. **(#16)** `tileset_converter/hidden_actor_bracket.py` — staging pass auto-inserts `addobject`/`removeobject` around refs to flag-hidden actors (removeobject only when last route ends `set_invisible`); reproduced Map032_EV074's hand deltas from raw output → **hand file DELETED** (hand count back to: EV009, EV021 + new Map050 pair); latent fixes free: M32 EV078/EV080 + M49 EV018 + M172 EV004 actors now spawn. **(#17 tail)** hand files `Map050_EV005.pory`/`Map050_EV019.pory` (base = transpiler output verbatim, deltas commented): tally array→`VAR_TEMP_3/4/5` + `call(Map050_EV005_Tally)`; SSB retake jump→`goto(Map050_EV005_TestBody)` (yes-arm carved into own script; RMXP label 118/119 pair); argmax+0↔1 swap→**u16 subtract-and-test-sign via `subvar` (VarGet derefs 2nd operand) — poryscript CANNOT compare two vars directly** (`var(A)>=var(B)` parse error); winner 0-based in `VAR_POKEMONTEST`, +1 on accept (RMXP-faithful); grant in EV019 = `givemon` **Emerald stand-ins (user decision): Orchynx→TREECKO, Raptorch→TORCHIC, Eletux→MUDKIP** + `FLAG_HAS_ORCHYNX/RAPTORCH/ELETUX` + received-msgbox; Theo counter-pick (x=var151-2 wrap) announced via msgbox (`pbStarterSelector` is presentation-only — 230_StarterSelect.rb, real grant was pbAddPokemon). **NOT converted (commented in place): randomizer-mode branches, rival battle 111-conditional (needs trainer conversion; scene continues to meet-up dialog either way), machine ball-gfx swap routes (gap #6). NO Pokédex grant exists in EV005/EV019 — S6's "Pokédex granted" expectation was wrong; dex comes later (ceremony).** 1255 tests pass. Retest: quiz asks 4 real 3-option questions; answers steer result; stand-in starter granted + named; Theo's counter announced; retake path (NO→re-talk→YES) skips intro.

**SLICE1_TODO #8 (warp-class refinement) CLOSED 2026-07-16 — no build needed; its premise was two-thirds wrong.** Stairs: `MB_NON_ANIMATED_DOOR` is *vanilla's own* interior-stairs behavior (decoded BrendansHouse_1F warp (8,2) → metatile 0x211 → attrs byte 0x60), so we already emit the right thing — "stairs behave like doors" was a misreading, not a defect. Map-edge arrow warps: belong to the connections work (#9 / slice 2), not warp classing. Animated doors: REAL gap but **user-accepted skip** — Uranium's 5 Moki Town doors animate as *event sprites* (EV3 `FKdoors1`, EV5/6/7/17 `PU-doorsdew`; charset rows = opening frames cycled by move-route Turn/Wait + SE) and `classify_event` drops the whole object event when a player-touch code-201 becomes a `WarpSpec`. Two rebuild approaches scoped + rejected (faithful-RMXP sprite+coord_event vs native `MB_ANIMATED_DOOR`+`sDoorAnimGraphicsTable` C entry per door, which also needs the sprite-sheet frames to fit a **BG** palette) — **both written up in `SLICE1_TODO.md` Done #8; don't re-derive them.** Also closed the doc's old "step-on triggering never exercised" caveat: the boot walks fire it for real. Findings doc annotated (`walker_checkpoint2_findings.md` §3).

> **Tracked obligation (do not lose):** every fork-baseline sentinel fence (spawn override, intro-skip, truck-cutscene suppression, player-sprite repoints, the removed palette-compare hook, the custom-route movement type) is catalogued with disposition in `reference/guides/engine_extension_surface.md` §3 — that ledger is the source of truth now, not this note. A fresh upstream fork clone drops every fence.

**NATIVE-REUSE AUDIT DONE 2026-07-07 (user request; 3 parallel research agents + lead verification) → `reference/findings/native_reuse_audit_2026-07-07.md`.** Reinvention worry mostly unfounded. Found one **CRITICAL live bug (F1, lead-verified): the flag/var registry bases in `assemble_pathfinder.py` are unsafe** — OOB writes into `vars[]` plus a live-vanilla-var collision — full detail + F2–F4 + fix order in the doc. **F1+F2 RESOLVED 2026-07-10** (task 6 below — user chose "grow engine ranges"). **Still open from the audit: F3** (yesnobox two-step → native `msgbox(text, MSGBOX_YESNO)`, small transpiler fidelity win); F4 = no action.

**ENGINE EXTENSION SURFACE SPEC WRITTEN 2026-07-07 (user-approved follow-up; Sonnet-drafted, lead-reviewed) → `reference/guides/engine_extension_surface.md`** — formal spec of the 3-tier promotion ladder (gen hooks > config gates > sentinel fences) covering every engine divergence, the build-gotcha list, and the add-new-divergence checklist. **Rule going forward: engine-surface knowledge (hook locations, build gotchas, fence rationale) lives THERE, not here — cite the doc, don't re-inline.** Sub-agent briefs touching `engine/` should say "read `reference/guides/engine_extension_surface.md` first". Line numbers pinned as-of 21c24202 — re-verify w/ §7 grep before editing. Spec + audit committed `0d37ff4f`; §6 grep now covers `RPG2GBA` markers too.

Pathfinder S1–S9 + bulk-run/classifier/F1 history → `reference/memory-archive.md`.

**§9 #2 GATE PASSED 2026-06-04.** Frozen config: model = `claude-opus-4-8`, prompt = `src/rpg2gba/conversion_agent/prompts/system.md` at commit `03dda3f`. Map002 calibration re-run (Opus, restructured prompt) was clean: 10/12 converted, 2 decorative skips, 7 queue entries all expected, EV007 `copyvar` correct, cache engaged. One noted non-issue: EV011 emits `setflag(FLAG_MAP002_EVENT011_TSA)` unconditionally (the `setTempSwitchOn` was inside a queued `onEvent?` branch in Uranium) — accepted as-is (Option A; TEMP flag auto-resets, event is mostly queued, Phase 5 re-evaluates). **[SUPERSEDED 2026-06-18: the bulk run is retired — the freeze is lifted; `system.md` is now an editable tail-tool prompt. The active gate is the per-slice boot (ROADMAP §9).]**

**Phase 3 — Map Deserialization COMPLETE** (conservation exact: 199 maps / 5,301 events / 8,429 pages). V4 (user map spot-check) + V5 (user reviews command reference) remain open but advisory — not §9 gates. Full paragraph archived.

**Phase 2 — PBS Data Conversion COMPLETE** (§2.0–§2.10, 66 phase2 tests). **V5 §9 #1 manual review gate PASSED 2026-05-25.** **Exit criteria #3/#4 (compiles cleanly in fork / test ROM species in Pokédex) DEFERRED to Phase 7** — see Decisions.

**Completed phases:** Phase 0 (Reconnaissance ✓), Phase 1 (repo scaffold ✓; pokeemerald-expansion fork **cloned 2026-05-20** at `/home/b/repos/pokeemerald-expansion`, shallow `--depth 1`, HEAD `21c24202`). **devkitARM 15.2.0 installed 2026-05-25** (devkitPro via apt-repo script + `libpng-dev`); **fork builds clean** (`make -j16 modern` → 32MB `pokeemerald.gba`). V6 fork-build **no longer blocked**.

**Env config:** **`.env-paths`** at repo root (**tracked**, paths-only, no secrets) holds `RPG2GBA_URANIUM_SRC=/home/b/repos/uranium-src/_unpacked` and `RPG2GBA_POKEEMERALD=/home/b/repos/rpg2gba/engine`. `pipeline.py::_load_dotenv()` loads it (shell env wins). Renamed from `.env` 2026-05-25 (the bare name collided with a tooling `Read(**/.env)` deny rule). Uranium tree relocated `/home/b/Pokemon_Uranium_132` → `/home/b/repos/uranium-src` (sibling layout). **2026-06-18:** the fork vendored in-repo at `engine/` (pinned @ `21c24202`). **2026-06-19 CUTOVER DONE:** `RPG2GBA_POKEEMERALD` → `engine/`; slice re-assembled + booted on `engine/`; the two scripts that hardcoded the old clone (`harvest_bucket_metatiles.py`, `charmap_lint.py`) repointed. Old external clone deletion **pending** (user runs `rm -rf /home/b/repos/pokeemerald-expansion`; classifier blocked the agent; a stale allowlist entry in `.claude/settings.local.json` still names it).

---

## Key File Notes

> **Archived-doc note (2026-06-18):** references below (and in Decisions Made) to `FABLES_*.md`, `PHASE{2,3,4,5}_PLAN.md`, `PATHFINDER_*.md`, `ITERATIVE_ROADMAP.md`, `DETERMINISTIC_EXPANSION_STRATEGY.md`, and `OQ3_EMPIRICAL_PLAN.md` now live under `reference/archive/` (see its README). Current build guidance is `BUILD_PLAN.md`.

- **`engine/` — vendored pokeemerald-expansion (pinned @ upstream `21c24202`, 2026-06-18).** Flat vendor (no history); 240MB pristine source + 3 sentinel-fenced custom files (`src/new_game.c`, `src/intro.c`, `include/new_game.h`). `build/` + assembler-generated output gitignored (root `.gitignore` + the engine's own). Provenance/rules: `engine/RPG2GBA_VENDOR.md`. **`RPG2GBA_POKEEMERALD` now points at `engine/`** (cutover done 2026-06-19); old external clone slated for deletion (user-run). **In-place-edit wrinkle FIXED 2026-06-19 (include-hooks):** the assembler no longer edits any tracked upstream file — `data/event_scripts.s` + `map_data_rules.mk` carry committed, stable sentinel hooks (commit these), and all per-slice content lands in gitignored generated files (`data/maps/{map_groups.gen.json,uranium_includes.inc}`, `data/layouts/layouts.gen.json`, the `uranium_*.h`/`CommonEvents.inc`, per-map dirs). After assembly only those 2 hook files differ from upstream; `map_groups.json`/`layouts.json` stay pristine. NOTE: the build still regenerates gitignored `include/constants/map_groups.h` WITH the Uranium maps (from the overlay) — that's the source of the 2 slice-minting test failures (see Decisions 2026-06-19).
- `scripts/extract_rgssad.py` — pure-stdlib RGSSAD v1 extractor. Run it once per Uranium release to unpack `Uranium.rgssad` → `<dir>/_unpacked/`. Algorithm: XOR stream cipher, initial key 0xDEADCAFE, advance `key = key * 7 + 3`. File data uses the global key as starting seed and advances per 4-byte group within the file; the global key is *not* advanced by file data, only by name and size fields.
- `scripts/recon_maps.rb` — Marshal stubs for RMXP classes. Critical: `RPG::Event::Page` (and its `Condition`/`Graphic`) must be **nested** inside `RPG::Event`, not flat. `Color`/`Tone`/`Table` use the user-defined `_dump`/`_load` *class-method* pattern, not `marshal_dump`/`marshal_load` instance methods. Both gotchas were live bugs we hit on first run.
- `scripts/recon_scripts.rb` — section name normalization handles spaces, `*`, leading `_`, divider strings (`=====`); use `vanilla?(name)` rather than direct allowlist lookup. Inflated source is transcoded `Windows-1252 → UTF-8` (with replacement) and written via `File.binwrite` — the prior `File.write(..., encoding: 'utf-8')` raised on 85 of 260 sections leaving 0-byte files. If a future change reintroduces UTF-8-strict writes, ~85 sections will silently fail to dump.
- `reference/scripts_dump/` — verbatim Uranium Ruby source extracted from `Scripts.rxdata`. **Gitignored.** Do not commit. Treat as read-only research material.
- **Map viewer perf+fidelity (`scripts/map_viewer_{server,common}.py`).** Investigation + backlog in `reference/viewer/map_viewer_perf_and_fidelity.md`. **Done 2026-06-28 (3 quick wins, uncommitted):** startup `tileset_id→map_ids` index (was a ~300-file glob+parse per page); preserve warm `TileRasterizer` across re-quant (generation token + `_refresh_postquant`, was `_map_cache.clear()`); numpy `raster.downscale_2x` (byte-identical) + `rmxp_tile_colors`. **Measured root cause: the ~22s page/re-quant latency is ~all the quantizer** — `build_quantized_tileset`'s O(k²) agglomerative pairwise-`bit_count` scan (186M calls, `quantize.py:242-255`); quick wins don't move it. Deeper fixes (vectorise/heap the packer; round-to-nearest 5-bit; shadow-recovery mode; layer-inversion census; **viewer over-pools by real `tileset_id` vs the ROM's per-map synth-id packing**) all in the doc.
- **P4 — pokeemerald-expansion struct shapes (verified 2026-05-20, HEAD `21c24202`).** Full field maps, struct layouts, file locations, and emit syntax (SpeciesInfo/designated initializers, level-up/egg/evolution move tables, the §2.1 Uranium→expansion field table) are in **`reference/guides/pokeemerald_struct_shapes.md`** — consult it before any C-emit section. The gotchas still *ahead* of us, kept live here:
  - **`.teachableLearnset` is BUILD-GENERATED** by `tools/learnset_helpers/make_teachables.py` from `all_learnables.json` + TM/HM/tutor JSON — do **not** emit `sXTeachableLearnset` arrays. Affects §2.5 (feed TM/tutor compat into that generator, don't hand-write the arrays).
  - **Hidden-ability drift (affects §2.4 abilities, next):** expansion has `NUM_ABILITY_SLOTS=3` = 2 normal + **1** hidden; Uranium ships up to **4** hidden abilities/species. See Open Questions for the best-effort plan.
  - **Type enum** (include/constants/pokemon.h): `TYPE_NONE=0 … TYPE_FAIRY=19, TYPE_STELLAR=20`. **No Nuclear** (that's §2.10/Phase 6). Uranium type indices differ — map via `reference/uranium_data/type_internal_names.json`.
- **`items.py` (§2.3).** `items.dat` is **SerialRecords TLV** (NOT fput): header = `numrec`×(uint32 off, uint32 len), first uint32 = `numrec<<3`; each body is type-tagged fields (`i`/`"`/`0`/`T`/`F`). 9 fields: ITEMID, ITEMNAME, ITEMPOCKET(1..8), ITEMPRICE, ITEMDESC, ITEMUSE, ITEMBATTLEUSE, ITEMTYPE, ITEMMACHINE(move id). 607 items. Embedded strings = raw UTF-8, discarded (display from sidecars). Emits `gItemsInfo[]` (`src/data/items.h`) + `ITEM_*` defines + `intermediate/item_field_codes.json` worklist. **Behavior deferred to Phase 6** (same as moves): only name/price/desc/pocket/importance emitted. Pocket map is lossy (fork has 5 pockets; Medicine/Mail/Battle Items→POCKET_ITEMS; Key Items→`.importance=1`). 111 needs_engine.
- **`_naming.to_constant` now folds diacritics** (NFKD + drop combining marks): "Poké Ball" → ITEM_POKE_BALL. Added in §2.3; only 3 item names were accented, no move/species/ability names, so §2.1/§2.2 constants unaffected.
- **`moves.py` (§2.2).** `moves.dat` is flat 14-byte records (struct `"<HBBBBBBHBHB"`), indexed by move ID 0..max, zero-padded gaps. Emits `gMovesInfo[]` (`src/data/moves_info.h`) + `MOVE_*` defines (`include/constants/moves.h`) + worklist `intermediate/move_function_codes.json`. `struct MoveInfo` (fork `include/move.h`) consumed via designated initializers; `.category = DAMAGE_CATEGORY_*` (pokemon.h), `.target = TARGET_*` (battle.h enum, **no `MOVE_` prefix**), `.type = TYPE_*`, name/desc via `COMPOUND_STRING`. Target map = PBTargets (080_PBTargets.rb) → TARGET_*; flag bits (085__PokeBattle_Move.rb) → bool32 fields, positive flags only. **Effect is NOT mapped** — see Decisions.
- **`abilities.py` (§2.4).** Abilities have no `.dat`; the ID→name map is the `ability_internal_names.json` sidecar (dumped `PBAbilities`, 210 entries). **Uranium-original set = sidecar entries whose `to_constant("ABILITY", display|internal)` is absent from the fork enum** (NOT the dexdata in-use scan — that misses form-only abilities). Yields a clean contiguous block ids **192–210** (19 originals), incl. CHERNOBYL (209, URAYNE form 2) + NIGHT_TERROR (210), which species records never reference. §2.1 only marked 17 (the dexdata-referenced ones); §2.4 adds 209/210 → `needs_engine.abilities` = 19 (idempotent union). Emits `include/constants/abilities.h` (Uranium-originals only — vanilla referenced directly, not redefined), a **documented placeholder TU** `src/data/abilities/uranium_abilities.c` (fork has no ability-handler table — effects are inline in battle scripts — so no literal stubs), and `intermediate/ability_codes.json` worklist. `collect_ability_ids` (reuses `pokemon.parse_dexdata`) kept as fail-loud cross-check. Fails loud if fork unreachable (can't classify originals).
- **`_marshal.py` + `deserialize.rb` `WordArray` stub (§2.5+ prep).** Marshal `.dat` → JSON path (D2): `_marshal.dump_dat(src, out_json)` shells to `deserialize.rb dat` (resolved relative to the package), `load_json` reads back. `deserialize.rb` AutoStub handles arbitrary classes; `WordArray._load` (added) unpacks the packed uint16 payload (`str.unpack('v*')` → `@a`) so tm.dat decodes to int lists, not `@_raw` blobs. All 6 Marshal dats (tm/trainers/trainertypes/encounters/metadata/types) verified to dump clean.
- **`tm_hm.py` (§2.5).** `tm.dat` is **Marshal** (array indexed by move id → `WordArray` of species ids; move→species). Inverted into the fork's **all_learnables.json** form (species internal name → sorted `MOVE_*`) and emitted as `intermediate/uranium_tm_learnables.json` (V6 feeds `make_teachables.py`; no hand-written arrays — fork build-generates teachables). `tutor.dat` is **header-only/empty** (asserted). 196 species, 180 machine moves.
- **`trainers.py` (§2.6).** `trainers.dat`/`trainertypes.dat` (Marshal). Trainer = `[class_id, name, items, party, party_id]`; party mon = 17-slot TP* array (`TPSPECIES=0…TPBALL=16`, see 115_PTrainer_NPCTrainers.rb). Identity = (class,name,party_id); minted `TRAINER_*` keyed by array index (331), `TRAINER_CLASS_*` from internal names (130). Emits **constants-keyed** `intermediate/trainers.json` + `trainer_types.json` (V6 → `data/trainers.party` for trainerproc; fork build-generates trainers.h). **0 TPSHADOW asserted** (fail-loud in `_parse_mon`). ability_flag is a SLOT selector, not an ABILITY id.
- **`encounters.py` (§2.7).** `encounters.dat` (Marshal) = `{map_id: [densities(13), slots]}`; slots indexed by EncounterType (104_PField_Encounters.rb: 0 Land…12 BugContest). Mapped to fork wild_encounters fields (land/water/rock_smash/fishing w/ old/good/super rod groups); Cave/time-of-day/Headbutt/BugContest preserved under per-map `uranium_extra` (fail-loud — conservation-tested, nothing dropped). Emits `intermediate/wild_encounters.json` keyed by **Uranium** map id (Phase 5 remaps). NOTE: nonzero density ≠ slots present (densities are cloned `EnctypeDensities` defaults; real data is in `slots`).
- **`metadata.py` (§2.8).** `metadata.dat` (Marshal) = array indexed by map id; field ids from 107__PField_Map.rb (Global: 1 Home=[map,x,y,dir] = player spawn; per-map: 1 Outdoor, 5 HealingSpot=[map,x,y], 6 Weather=[enum,chance], 7 MapPosition, 13 BattleBack, …). Emits `include/constants/metadata.h` (URANIUM_START_MAP/X/Y/DIR = 49/7/7/2) + `intermediate/map_metadata.json` (188 maps). **No "escape map" in Uranium** — respawn is HealingSpot.
- **`tmpbs.py` (§2.9).** `tmpbs.dat` indexed uint16 (reuses `pokemon.parse_indexed_u16_list`). Emits `src/data/pokemon/uranium_tmpbs.h` (`sUraniumTMPBS_*` arrays, egg-move style). Semantics still TBD (Open Questions). 196 species.
- **`types_dump.py` (§2.10) — standalone, NOT in `module_order`.** `types.dat` (Marshal) = `[pseudotypes, specialtypes, typechart]`; `matrix[attacking][defending] = typechart[attacking*20 + defending]` (0=immune/1=NVE/2=normal/4=SE, verified via Ground→Flying=0/Ground→Fire=4). Emits committed `reference/uranium_data/types_dump.json` {types(20), matrix, nuclear_index=18, …}. Run via `python -m rpg2gba.pbs_converter.types_dump`. No C (Phase 6 / D7).
- **Phase 3 map deserializer (`deserialize.rb` rxdata mode + `src/rpg2gba/map_deserializer/`).** `deserialize.rb rxdata <data_dir> <out_dir>` shapes maps into the Phase 4 contract (PHASE3_PLAN §E1): map→events(list, sorted by id)→pages→`list` of RAW `{code,indent,parameters}` commands (no continuation merging, no idiom interp). Tiles kept flat (`{xsize,ysize,zsize,data}`). Aux: `common_events.json`, `system.json`, `map_infos.json`. **Gotcha:** Marshal does NOT fire `const_missing` for *nested* names (`RPG::System`, `System::Words`) the way it does for top-level — so `marshal_load_lenient` synthesises the missing class and retries (only `undefined class/module` is recovered; real errors abort = fail-loud). Python side: `driver.py` (shells to ruby), `validate.py` (conservation vs `ORACLE`=199/5301/8429 + schema, reused by tests), `command_catalog.py` (`CATALOG` superset of RGSS codes → coverage guard E7; emits the reference doc + switch/var sidecars). Run via `.venv/bin/python -m rpg2gba.pipeline phase3 --clean`.
- **Phase 4 conversion-agent package (`src/rpg2gba/conversion_agent/`).** Machinery for Event→Poryscript. `flag_registry.py` (see Flag Registry Notes). `poryscript.py` — compile-gate wrapper; resolves binary via `RPG2GBA_PORYSCRIPT`/PATH (fail-loud if absent), `compile_script(str)→CompileResult{ok,stdout,stderr}` (does NOT raise on a *compile* error — that's an expected queue/retry outcome). `backends/__init__.py` — `ConversionResult{script, new_flags[], new_vars[], unhandled[]}` (lists, mirrors `prompts/system.md` schema) + `ConversionBackend` ABC `convert_event(event_json, registry_state, prompt)`. `backends/claude_code.py` — PRIMARY: spawns `claude -p --output-format json --system-prompt <frozen>` per event, `_parse_response` unwraps the CLI envelope (`{"result": ...}`) then the agent's object. `backends/ollama.py` — fallback, `requests` imported lazily (not installed in venv; keeps module import clean). `prompt_builder.py` — `build_prompt` (user msg = cheatsheet+command-ref+few-shots+registry+event JSON) + loaders; system.md is the separate frozen system prompt. `orchestrator.py` — per-event loop, `compile_fn` injectable (tests use a fake compiler + MockBackend, never live binaries); idempotent via per-map `.done` checkpoints + per-map `.pory`; `unhandled.jsonl` + `triage()`. The convert core is factored into `_convert(payload, ctx)` (prompt→backend→commit→compile→retry-once); `_convert_event` adds self/temp mint loops, `_convert_common_event` skips them. **Test/run note:** the orchestrator integration tests need NO external binary; only `test_compile_gate` (phase4 marker) needs real poryscript and skips when absent.
- **Common-event pass (dedup A, `orchestrator.convert_common_events`).** `common_events.json` = list of 100 `{id,name,trigger,switch_id,list}` (flat `list`, **no pages**); `_common_event_payload` adapts each to `{common_event_id, name, pages:[{list, condition:{}}]}` so the existing helpers (`_event_has_commands`/`_event_codes`/`_build_prompt`) work and the agent emits one `CommonEvent_<NNN>` block. Output `scripts/CommonEvents.pory`, checkpoint `checkpoints/CommonEvents.done`; `only_ids` = partial/no-checkpoint debug run. 22 CEs have real commands. Must run before fork assembly so each map's `call CommonEvent_<NNN>` resolves.
- **Event memo (dedup C, `orchestrator` `_memo_key`/`MemoEntry`/`_try_reuse_memo`/`_reinstantiate`).** Skips the per-event spawn when a structurally identical event (same content, any map/event id) was already converted. Key = sha256(payload − `map_id` − `id`). Reuse rewrites the deterministic self/temp-switch flag names via `flag_registry.self_switch_flag_name`/`temp_switch_flag_name` **AND the `Map{NNN}_` script-block label prefix** (`Map{src:03d}_`→`Map{cur:03d}_`, case-sensitive so it's disjoint from the upper-case `FLAG_MAP…` rewrite; guarded only when src≠cur); guard + compile-gate gate acceptance, else fall through to a real spawn (fail-safe). **The label rewrite was added 2026-06-03 after the live memo confirmation caught the original `_reinstantiate` leaving the source map's label in the reused script (`Map100_EV003_Page1` inside Map115.pory) — a duplicate-symbol/dangling-ref bug at ASSEMBLY that the per-file compile-gate can't see. The dedup-C plan's premise "labels are name-derived only" was wrong (labels are `Map{mapid}_<name>_Page<n>`).** Persisted to `output/uranium-build/memo_manifest.json`, fingerprinted by the backend system prompt (`getattr(backend, "system_prompt", "")`) — discarded on mismatch. Prompt split (dedup B): `prompt_builder.build_static_context` (→ backend system prompt, composed in `pipeline._phase4_backend`) + `build_user_prompt` (per-event); `claude_code._log_cache_usage` logs cache token counts.
- **Bulk-run harness (added 2026-06-05; uncommitted).** Drives the full `phase4 --run` resiliently. `backends/__init__.py` adds `RateLimitError`(`.weekly`,`.reset_hint`)/`BackendTransportError`; `claude_code._run` classifies usage caps (`_classify_rate_limit`, editable marker tuples) → `RateLimitError` (immediate, no retry), retries transient transport (exp backoff, `max_attempts`) → `BackendTransportError`, and `_record_usage`→`token_usage.jsonl` when `usage_log_path` set. **`orchestrator._convert` re-raises both** (does NOT queue — a limit/transport failure aborts cleanly so checkpoints+memo resume; only true parse/output errors queue). `run_report.py` = `collect_stats`/`format_stats` over checkpoints+token_usage+scripts+unhandled+`run_state.json`. Scripts: `prep_bulk_run.py` (one-time pristine reset to `flag_state.baseline.json`), `run_bulk.py` (no-arg run-until-limit; `--timed` 5h-window wait-and-resume loop, weekly=stop; always resumes; default `claude-opus-4-8`), `run_stats.py`. Tests: `tests/test_bulk_run.py`. **First live run 2026-06-05 hit `rate_limit` ~21 spawns into the common-event pass → "every round failed": `convert_common_events` only wrote `CommonEvents.done` after the WHOLE pass + CEs aren't memoized, so each restart re-spent the same CEs and never reached the maps. FIXED 2026-06-05 (uncommitted):** (1) **`--limit N`/`-n` on `run_bulk.py`** = stop cleanly after N LLM spawns this run, resumable (the user's "run once/five/any number" knob); via `backends.CappingBackend` wrapper + new `BudgetReached` exc (orchestrator re-raises it alongside RateLimit/Transport; deterministic/memo reuse don't count). (2) **per-CE resumability** = `convert_common_events` records each processed CE (script-or-null) in `checkpoints/CommonEvents.blocks.json`, skips done ids + rebuilds `.pory` on restart, so bounded/interrupted rounds accumulate (maps were already fine via per-map checkpoint + memo). 229 pass, ruff clean. **Old on-disk run left NO resumable CE state (pre-fix), so first bounded run re-does some CEs once — recommend `prep_bulk_run.py --yes` first for a clean baseline.** **FALSE-POSITIVE RATE-LIMIT BUG FIXED 2026-06-08 (uncommitted):** the 2026-06-08 run died `status=rate_limit` mid-Map004 (EV003 "Deliveryman") with real usage remaining. Cause: `claude_code._run` scanned `stdout+stderr` of EVERY spawn — incl. successful ones — and `_RATE_LIMIT_MARKERS` had the bare substring `"429"`, so a clean envelope's telemetry (`session_id` UUID / `duration_ms`) or the agent's own conversion text tripped a phantom cap; the orchestrator correctly re-raised (caps aren't queued) → runner stopped. Fix: new `_error_text(proc)` returns None for a success (rc 0 + envelope not `is_error`) so clean output is NEVER scanned; markers are tested only on a genuine error (non-zero exit OR `is_error` envelope, scanning `result`/`subtype`+stderr); replaced bare `"429"`→`"http 429"/"status 429"/"error 429"`. 2 regression tests (429-in-telemetry success doesn't raise; is_error+cap-phrase does). Resume is safe: CommonEvents+Map001–003 checkpointed, Map004 (no `.done`) re-converts cleanly. **235 pass, ruff clean.**
- **`FABLES_DECISIONS.md` (added 2026-06-11).** Decision log for the `FABLES_OBSERVATION.md` critique walkthrough — one section per suggestion: what was decided, the evidence, and implementation details/checklists. Suggestions 1 (throughput, decided) and 2 (STRIP skip, design settled) recorded; 3–8 pending discussion. Check it before implementing any critique item.
- **`reference/guides/conversion_walkthrough.md` (added 2026-06-03).** Hand-authored end-to-end explainer of the Phase 4 event→Poryscript conversion process: pipeline context + two-agent distinction, the per-event orchestrator loop (mermaid flowchart), all 9 steps with real code snippets, a full worked example (Potion-NPC `giveitem` idiom), the two fail-loud queue paths + `triage()`, common-events + `dump_header`, CLI commands, the A/B/C dedup levers, and a source-file map. Audience = humans + build agent; advisory, code is authoritative.
- **`.claude/skills/pbs-convert/SKILL.md` (added 2026-05-22).** Project skill encoding the repeated Phase 2 per-section pipeline (inspect→reconcile→map→emit→worklist→test), keyed to the real helpers (`_binary`/`_naming`/`_id_map`/`_c_emit`) with §2.1–§2.3 as golden references. Advisory only — the *how*, not the *what*; deliberately does NOT track which section is next (user drives that). Includes a sub-agent delegation guide (delegate inspect/fork-lookup/test-scaffolding/validation to Haiku/Sonnet; keep resolver + fidelity decisions on the main Opus session). Triggers when asked to convert a §2.x PBS section. Built from `pbs-convert-concept.md` (High-impact rec, `recommendations/2026-05-21.md`).
- **Hand-conversion pass (`HumanBackend` + `scripts/run_human.py`, 2026-06-12).** Lets the user convert easy events BY HAND (zero Claude usage) so the bulk run doesn't eat their whole Pro window. `backends/human.py::HumanBackend` — interactive `ConversionBackend`: compact mobile/Termius-friendly event render (decoded command list + orchestrator-minted self/temp-switch flag names + the `Map{m}_EV{e}_` label prefix, all handed to the operator), read-until-`EOF`, `opus`=punt (raises `EventDeferred`), `?`=dump full prompt/refs, `quit`/Ctrl-D=stop. Carries the SAME frozen `system_prompt` as the Opus backend → memo fingerprint matches → hand- and Opus-seeded entries share one memo. **Memo-SEEDING pass, NOT map-completing:** `Orchestrator.convert_single(map_id, event)` (new public wrapper over `_convert_event`) runs one event through the full core (deterministic→memo→backend→compile-gate→switch-mint→`_store_memo`) but writes NO `.pory`/checkpoint; later `run_bulk.py` sees each as a free memo hit (+ all structural twins). `run_human.py` builds a difficulty-sorted "easy lane" (`_LANE_CODES` whitelist = dialogue/choice/branch/self-switch/wait/comment/call-CE; **excludes 121/122 global switch/var, warps, move-routes 209/210/509, script-calls 355/655** — held for Opus), skips done-maps + memoized + deterministic-claimable, serves easiest first; `--limit`/`--max-score`/`--map`. **`EventDeferred`** (new exc, `backends/__init__.py`) re-raised by `_convert`, NOT queued (punt ≠ failure). Live corpus: **289-event lane**. Tests: `tests/test_human_backend.py` (10, incl. punt-not-queued + accepted-seeds-memo integration). HumanBackend does NOT collect global flag/var proposals (lane excludes 121/122 — "hold for Opus" decision). **UX (2026-06-12):** render hard-wrapped to **45 cols** (`_WIDTH`, `_wrap` via textwrap, never splits flag-name tokens) for phone/Termius; `?` shows a compact **lane quickref** (`reference/guides/human_quickref.md`, ≤45-col, hand-authored: code→Poryscript table + skeleton + text-code-strip + flag/var/item/yes-no patterns) loaded into `HumanBackend(quickref=…)` by `_phase4_backend`; **`?term` filters it inline** (substring over the body, intro excluded) so a phone isn't flooded — `?` no longer dumps the giant system prompt or the event JSON; run_human prints the quickref path for opening in a 2nd Termius tab; **input loop (fixed 2026-06-12):** visible `> ` prompt (was bare `input()` → looked frozen after a `?` dump); control commands work at ANY point (removed the old "only before first line" gate that made `q`/`opus` become script text mid-typing) — `q`/`:q` quit, `opus`/`:opus` punt, `:undo` drop last line, `:clear` restart, `EOF`/`:done` submit; `_render_command` truncates big nested params (move-route/audio dicts) so off-lane junk can't wall the screen. **`scripts/run_tutor.py`** = vimtutor-style hands-on walkthrough of 3 already-translated Map001-007 events (sign/control-code-strip → call-CE 117 → ground-item `pbItemBall`→giveitem + self-switch + empty gate page); reads `.pory` answers via `_extract_blocks` by label prefix; read-only, no spend. Tests: `tests/test_tutor.py` (4, output/-independent). **Spend partition (`run_bulk --skip-lane`, 2026-06-12):** shared lane predicate moved to `conversion_agent/lane.py` (`in_lane`/`real_commands`/`LANE_CODES`); `Orchestrator(skip_lane=True)` raises `LaneSkipped` in `_convert_event` **after** deterministic+memo (so only genuine-spawn lane events are held — set == run_human's queue), caught in `convert_map` → writes `.partial` (sibling of `.done`) instead of `.done`. **Checkpoint is now ternary:** `.done` short-circuits at top (unchanged); `.partial` maps re-enter (done events = free memo hits) until the lane is filled, then a no-`--skip-lane` `run_bulk` mop-up completes them (`.done`, removes `.partial`). **Workflow: grind `--skip-lane` → do lane by hand (run_human) → finish with plain `run_bulk`** (human pass only seeds memo, never writes `.pory`, so the mop-up assembles hand work). run_human offers events from `.partial` maps (keys on `.done` only). Tests: `tests/test_skip_lane.py` (5). 322 tests pass total.
- **Pathfinder tileset converters (`tileset_converter/{tile_map,layout,map_constants,metadata_wiring}.py`, S2–S5).** Deterministic Phase-5 slice pipeline. `tile_map.py` — `tileset_map.json` lookup (explicit `tiles` → passability `buckets` fallback, fail-loud) + per-tileset `warps` metatile. `layout.py` `convert_layout` — visual = topmost non-empty layer, collision = combined layer rule, `warp_overrides` force collision 0 **and stamp the warp metatile** (the S9 warp fix: pokeemerald gates warps on `IsWarpMetatileBehavior`, so a warp tile needs an `MB_*_DOOR` metatile, not plain floor — chose `MB_NON_ANIMATED_DOOR`: **ts19→529, ts22→167**; collision is per-cell in map.bin and ours to set 0, behavior travels with the metatile id). `map_constants.py` — `MapConstantRegistry` mint/persist, consults `map_name_overrides.json` before `map_infos`. v1 art = passability buckets (floor/wall+void) → to be replaced by the quantize pipeline (`BUILD_PLAN.md §7`).
- **Map Inspector (`scripts/map_viewer_*`, Phase A + B1, 2026-06-22, uncommitted).** Spec: `reference/viewer/map_inspector_spec.md`. Three files: **`map_viewer_common.py`** (shared core — `build_map_data(map_id)` no inline images + per-map int index for each distinct column-key; `render_tile_png`/`render_metatile_png(map_id,idx,layer)` on-demand PNG bytes w/ module-level `_map_cache`/png caches; the single `MAP_VIEWER_HTML` template); **`build_map_viewer.py`** (thin ~120 lines — STATIC mode: inline base64 → self-contained `output/map_viewer/Map{NNN}.html`, Taildrop fallback; CLI `32 49 48`/`--all`); **`map_viewer_server.py`** (stdlib `http.server`, no Flask; SERVER mode, lazy images). **Same UI via a JS seam** `getTileURL(tid)`/`getMetatileURL(idx,layer)` branching on `window.__VIEWER__.mode` (static=data-URI dict, server=`/api/...`). Read-only re converter (never writes output/ except the static HTML; B2 override hooks NOT built). **Features:** A1 layer selector (RMXP composite/L0/L1/L2 + GBA composite/bottom/top) + cell-inspector sidebar; A2 overlays (collision red/green, diff magenta, priority heatmap, events, warps via code-201); client-side issue flag/export-JSON (§10 #4). **B1 responsive rework (user chose "Both" phone+desktop):** lazy `imgCache`, unified Pointer Events (mouse drag/wheel + 1-finger pan + 2-finger pinch, 4px tap threshold), `@media(max-width:720px)` bottom-sheet sidebar, `touch-action:none`, ≥44px targets. **A3 (post-quant ROM-byte decode) deferred.** Reuses (no reimpl): `layout.{TileGrid,column_key}`, `graphics.{raster.TileRasterizer,sources.load_tileset_sources,build_slice_tilesets._render_column→MetatileImage}`, `tile_map.serialize_column_key`, `scripts/compare_collision.{our_blocked,uranium_blocked}`. **Verified:** server endpoints `/map/<id>`,`/api/map/<id>`,`/api/tile/<m>/<tid>.png`,`/api/metatile/<m>/<idx>.png?layer=` all 200 (PNGs real 16×16 RGBA, api/map JSON 1.39MB has meta/cells/events/colkeys_list/metatile_attrs); static Map032 2.19MB; ruff clean, no typing-import violations. **Run server:** `python scripts/map_viewer_server.py --port 8765 --host 0.0.0.0` on the desktop, open over Tailscale from phone. **Untested:** true touch/viewport on a real iPhone (the §9 device check). **NOT built:** B2 (`collision_overrides`/`layer_overrides` converter hooks + POST /api/override + /api/rebuild) — spec §6.
- **Map Inspector — palette + colour-mapping view (`scripts/map_viewer_*`, 2026-06-22, uncommitted).** Built on the above. Adds: per-cell RMXP source palette + GBA sub-palette swatches + orig→final colour-change list; a true **post-quant** canvas layer + raw-vs-post-quant inspector thumbnails; a **Merge** overlay (heat-maps palette-merge loss) + per-8×8-quadrant fit badges + a **Worst palette merges** ranked panel (click-to-jump to the worst tiles). Data is the REAL emit/quantize path (faithful by construction, supersedes A3's deferred ROM-byte decode): `quantize.QuantizeResult.color_map` (distinct orig8→final8 per tile), `emit.analyze_tileset_palettes()→PaletteAnalysis` (per-quadrant `palette_index`/`color_changes` + reassembled quantized bottom/top via the refactored `_canonicalize_and_quantize`; `emit_tileset` output byte-identical), `build_slice_tilesets.column_keys_for_maps`. **Pool is SLICE-scoped** (`map_viewer_common.SLICE_MAP_IDS=[49,48,32]` ∪ the opened map, filtered by tileset) — matches the ROM's per-tileset pool (`assemble_pathfinder` groups maps by tileset) AND fixes the map-32 crash (globbing every map on a shared tileset pulled in a non-slice Route map referencing an out-of-atlas tile). **Finding:** `max_palettes=13` is a HARD engine boundary, not headroom — BG 14/15 = dialogue window pals rewritten on every text box, 13 is excluded from weather/time blending → `graphics_conversion_notes.md §11`; Moki ts22 has 381 distinct colours vs the 195 ceiling, so its merging is a colour-budget constraint, not a packer flaw. Tests: +5 `test_graphics_quantize`, +2 `test_build_slice_tilesets` (41 pass). Built under /delegate (Sonnet rounds, lead synthesis). Plan/resume checklist: `reference/viewer/map_viewer_palette_plan.md`. **User-facing usage guide: `reference/viewer/map_viewer.md`** (2026-06-22) — one-tool/two-views/two-modes explainer: server vs static launch, every layer/overlay control, both views, the slice-scoped pool caveat, and that flagged issues are in-memory only (export before closing). Notes `scripts/tree_debug.py` is a SEPARATE PNG layer-collapse debugger, not part of the viewer.
- **`reference/findings/critique_2026-07-01.md`** — build-agent critique of ROADMAP/BUILD_PLAN/codebase (strengths, weaknesses, 8 ranked grill-me targets). Top findings: ROM at 86.59%/32MB with no scripts/NPCs/audio and 21 maps cut (no size ledger exists); transpiler+fork-index "next tasks" have zero code since 2026-06-18; planning-doc drift (ROADMAP Phase 4 self-contradictory, BUILD_PLAN omits the walker, VENDOR.md omits walker divergence, `map_walker_plan.md` still "Proposed"); local test failures are 3 not 2 (third = `output/` artifact drift, test_assembly.py:367); CI exercises a weaker suite (513 pass/76 skip without env). Advisory — no code changed.
- **Palette-packing investigation + in-ROM A/B/C compare ROM (2026-06-25, debug-only, uncommitted).** Q: Moki ts22 large-tree CROWN tiles lose green gradient (3–4 shades → 1) while body tiles look fine. **Finding — it's the hard 13-palette ceiling, NOT a packer bug.** Scratchpad attribution (3-config A/B/C): **95%** of colour-collapse is Phase-2 over-budget local reduction, only 3% Phase-1 vocab cut, 2% irreducible (single tile >15 colours). But the cause is structural budget starvation: the **green family alone has 238 distinct 5-bit colours > the 195-slot (13×15) budget**, and **118 of 1102 tiles are "dense" (≥12 colours)** — pervasive foliage, not a protectable few (median-cut sacrifices tight green CLUSTERS first; a crown tile has up to 22 distinct greens, hard-capped at 15/tile). **Three repacking strategies all land within ±2%:** production global (`build_quantized_tileset`), **hue-family concentrate** (+1%) and **diversity reallocation** (≈wash/slightly worse) — the latter two are new in `tileset_converter/graphics/experimental_packers.py` (`build_quantized_tileset_family`/`_diversity`, debug-only, **NOT wired into the slice**). Diversity also **overflows the 1024-tile budget** at full strength (1042>1024 — splitting dense tiles reduces post-quant merge) → auto-falls-back thr12/budget6 → thr14/budget5. **Only lever with headroom = reduce PERCEPTUAL source diversity coherently** (a shared green ramp / human-picked palette) — exactly where the user's original "let me pick the colours / which maps to which" idea belongs; pure repacking is a dead end. Green-budget curve: foliage wants ~40 palettes to stop losing detail, ~80 for near-lossless. **Tooling:** `emit.emit_tileset`/`_canonicalize_and_quantize` gained optional `quantizer=` (default unchanged → production byte-identical). `scripts/build_palette_compare.py` emits 3 variant tileset pairs for ts22 (`gTileset_Uranium22_cmp{cur,fam,div}`) from ONE shared metatile list (identical metatile ids → a single layout drives all three; only tiles/palettes differ; 841 metatiles each, cur 1011/fam 1022/div 999 tiles), registering the 6 structs by appending sentinel blocks to the gitignored `uranium_*.gen.h`. **Compare ROM:** sentinel-fenced `URANIUM PALETTE-COMPARE` block in `engine/src/field_control_avatar.c` — on Moki, **SELECT cycles current→family→diversity in place** (RAM `MapLayout` w/ swapped tileset pointers + `CopyMapTilesetsToVram`/`LoadMapTilesetPalettes`/`DrawWholeMapView`, select-blip). **Bug hit+fixed:** the handler must consume the SELECT press and NOT return TRUE from `ProcessPlayerFieldInput` — returning TRUE makes `DoCB1_Overworld` (`overworld.c:1625`) `LockPlayerFieldControls()` and skip `PlayerStep`, freezing the player (the "works once then frozen" symptom). `output/moki_palette_compare.gba` Taildropped to the phone for the by-eye call. **Pending: user's on-hardware verdict** (does family/diversity read better than current, or are they all-of-a-muchness = the metric was right). **Revert obligation** = the field_control_avatar.c block (see Current Phase tracked-obligation). Extends the prior `max_palettes=13`-is-a-hard-boundary finding (`graphics_conversion_notes.md §11`).
- **Map viewer — live family-quant toggle + tunable knobs (2026-06-26, COMMITTED).** The map-grid layer control is now a 2-way **RPG Maker ↔ Quantized (family)** toggle; the viewer's default quantizer is **family** (`quantize.build_quantized_tileset_family`), not production. The old 8-way layer radio is kept but hidden (`#layer-debug`). A server-mode **knob bar** exposes the family packer's now-parametrized `FamilyParams` (`green_cuts` = interior hue° splitting the 70–170° green band, `dark_value`, `neutral_sat`, `palette_floor`, `overflow_weight` colors|coverage) + `max_palettes`, live: **Apply** → `POST /api/quantize` (`map_viewer_common.set_quantize_params` clears the 3 quant caches + bumps a `generation` cache-bust token appended to post-quant image URLs as `&g=`; eager re-quant catches bad knobs → **400 + rollback**, locked via `threading.Lock`) → page reload re-renders map + both palette views. `emit.analyze_tileset_palettes` gained `max_palettes`/`quantizer` passthrough. `experimental_packers` parametrized via a `FamilyParams` dataclass + tested (`tests/test_family_params.py`, 36) — **no-params output byte-identical, so the debug ROM compare tool is unaffected**. Same knob bar on the palette page. Doc: `reference/viewer/map_viewer.md`. Built under /delegate (Sonnet parametrized the packer; lead did viewer/server/palette integration). **Still uncommitted/debug** (NOT in this commit): the in-ROM SELECT compare hook (`field_control_avatar.c`) + `scripts/build_palette_compare.py` (now also emits ts19 house variants over maps 49+48).
- **Family packer is now the PIPELINE standard (2026-06-26, uncommitted).** User approved the family render across maps → made it the production packer. **Moved** `FamilyParams` + `build_quantized_tileset_family` + helpers (`_dominant_family`/`_pack_subset`/`_assemble`/`_allocate_by_overflow`) from `experimental_packers.py` **into `quantize.py`** (production packer module). `experimental_packers.py` now holds only `build_quantized_tileset_diversity` (the genuinely experimental A/B alternate) + re-exports family for back-compat (compare script / viewer / tests still import either location). `build_slice_tilesets` now passes `quantizer=build_quantized_tileset_family` to its `emit_tileset` call, so the ROM tilesets quantize per-hue-family — matching what the map viewer previews (closes the viewer↔ROM fidelity gap). `emit_tileset`'s **default stays** `build_quantized_tileset` (it's a general utility; callers choose), so `test_graphics_emit` is unaffected; only `build_slice_tilesets` opts in. Also fixed a latent F821 (`keys`→`ordered`) in the build_slice dry-run log + 2 pre-existing E501s in `quantize.py`. **555 tests pass** (2 failing = the known `MAP_MOKI_TOWN` slice-mint collision, unrelated). **DONE 2026-06-27: re-ran `assemble_pathfinder` (full) + rebuilt the walker ROM → family is now realized in the engine tileset data** (ts22 1022 GBA tiles = family variant; was 1011 production). User confirmed family looks right in the viewer; this closes the viewer↔ROM gap on hardware.
- **`conversion_agent/deterministic.py` — the pre-filter, and the transpiler's base.** 9 pure-function classifiers (dialogue/sign/call-CE/self-switch/ground-item/mart/warp/trainer); each claims a confident whole-event shape or returns `None`. `_dialogue_body` is a command-by-command page walker that currently *bails* on unknowns. **Pivot role:** `_dialogue_body` generalizes into the deterministic `transpile_page` (`BUILD_PLAN.md §3`); the classifiers become an idiom-collapse layer on top. Pre-filter reached **1294/3581 (36.1%)** byte-identical to frozen-Opus. Item/trainer constants resolve via `load_context` (Phase-2 sidecars) — canonical over Opus (the `ITEM_PARLYZ_HEAL` finding).
- **`tileset_converter/graphics/` — image pipeline steps 1–3 (BUILT 2026-06-20, pure-PIL, no numpy).** Greenfield PNG→GBA art (BUILD_PLAN §7), stops before step 4 (quantization). `sources.py` `load_tileset_sources(tileset_id)`→`TilesetSources` (tileset PNG + 7 autotile-slot paths, `None`=empty; fail-loud on named-but-missing). `autotile.py` `flatten_autotile(template, variant)`→32×32; `AUTOTILE_TABLE` transcribed verbatim from Uranium's `scripts_dump/039_TileDrawingHelper_v17.rb` `bltSmallAutotile` — variant=`tile_id%48` (Uranium **bakes** the resolved variant into map data, so no neighbour recompute), height-32 templates = anim strips (frame-0 32×32), else 4 quadrants via `AUTOTILE_TABLE[v>>3][v&7]` (1-based piece ids, 6-col grid). `raster.py` `TileRasterizer(sources).render(tile_id)`→16×16 RGBA cached: static(≥384)=atlas crop `((id-384)%8,(id-384)//8)`, autotile(48–383)=flatten then ÷2, empty-slot/id0=transparent; `downscale_2x`=top-left of each 2×2 block. **Required a `deserialize.rb` change:** `shape_tileset` now emits `autotile_names` (stub already declared it; dump dropped it) → re-dumped `tilesets.json`. Findings: both slice tilesets are clean 2× (exact pairwise uniformity Indoor(1) 100% / PU-Route01-02-Moki-Kevlar 98.67%-opaque — a box-vs-nearest *proxy* misleadingly read 62% for ts22, don't use it); ts19 slot 6 (base 336) is an empty autotile, Map048's 4 top-layer 370/372/374/376 cells over real floor render transparent (wire step's visual collapse must fall through). Validation: `scripts/render_slice_tiles.py` reconstructs each map via 3-layer alpha compositing → `output/slice_map0{49,48,32}_reconstruct.png` (both correct). Tests: `tests/test_graphics_{sources,autotile,raster}.py` (19).
- **`tileset_converter/graphics/quantize.py` — image pipeline step 4 (palette quantization, BUILT+REWRITTEN 2026-06-20; numpy, new dep).** GBA 4bpp: each 8×8 tile references ONE ≤15-colour sub-palette, ≤N/tileset, 15-bit colour. `to_5bit` (BGR555: `q=c>>3; (q<<3)|(q>>2)`); `classify_tile`/`resolve_alpha` = **per-tile binary alpha keyed on SOLID-BODY fraction** (`SOLID_BODY_FRAC=0.02`: opaque-frac ≥0.02 = real body/tree/fence ⇒ **threshold** @128, drops low-alpha fringe; bodyless wash/shadow ⇒ **drop** = all partial→transparent, ground shows through; no partial ⇒ binary). **Stipple was built then REJECTED by user 2026-06-20** ("handle house shadows like the tree-base") — shadows now DROP (Moki Town's 10 shadow tiles are uniform ~33% alpha over grass → clean ground, no holes); supersedes the AskUserQuestion "stipple" pick. `build_quantized_tileset(tiles, *, max_palettes=13, colors_per_palette=15, global_colors=None(→195), weights, iterations)→QuantizeResult{palettes, tile_palette(-1=empty), quantized, stats}` = **TWO-PHASE PALETTE PACKER** (NOT k-means/Lloyd): **(1)** median-cut distinct colours → shared `global_colors` vocab (similar-merge only, never cross-family), remap tiles; **(2)** each tile's colour-set as a **bitmask over the vocab**, greedily merge smallest-UNION palette pair (`(a|b).bit_count()`) until ≤max_palettes, locally reduce only a palette still >15. **Why bin-packing not clustering:** census `scripts/tile_color_census.py` → 99.1% of 8×8 tiles already ≤15 colours (median 5), so loss is ONLY forced palette-SHARING. **THREE bugs, ALL found by USER LOOKING (metrics hid each):** (a) paths→green = area-weighted seeding dropped small-area path tan; (b) **black tree-tops** = canopy edges mis-classified stipple (kept Uranium's ~black transparent-pixel RGB) → fixed by solid-body classify; (c) **orange/white tree-shading** = Lloyd's median-cut refit snapped minority dark-green to nearest in an incoherent palette → exposed Lloyd as wrong algorithm, replaced by the two-phase packer. AFTER: Moki Town → **on-screen shift 0.95/31, matches by eye** (path tan/water blue/roofs red/trees solid green/flowers pink). Diag tools: `quantize_diag.py`, `tree_alpha_diag.py`, `tile_color_census.py`. 30 tests + 49 graphics suite, ruff clean. **LESSONS: validate graphics by EYE not a pixel-avg metric; diagnose by ranking worst offenders' true→mapped colours; measure problem structure before picking the algorithm.** **NEXT = 4bpp binary emitter** (`gTileset_Uranium_*`; pending sign-off; stipple/shadow path still under-exercised). See `graphics_conversion_notes.md` §10.
- **`tileset_converter/assembly.py` (S8 script staging, 2026-06-16).** Pure/tested helpers to stage slice `.pory` for fork assembly. **PRUNE:** `split_blocks` (lossless, anchors `^script LABEL` at col 0 — slice `.pory` is 100% `script` blocks, nested braces/strings always indented), `block_event_id`, `live_event_ids`, `prune_orphan_blocks`/`prune_map_pory` (drop blocks whose event isn't wired by `map.json`; keys on event id; 2 fail-loud guards — no kept→pruned ref, no out-of-slice `MAP_URANIUM_*` survives). **NORMALIZE (Option A):** `normalize_labels` strips the agent's inconsistent event-name token → canonical un-named `Map{m}_EV{e}_Page{n}` matching S5's `page_label`/dispatcher refs (whole-text sub so def+gotos move together; idempotent; leaves `_Dispatch` + upper-case `FLAG_MAP…` untouched). **CHECKS:** `find_dangling_references` (referenced − defined across the staged set: map scripts+dispatchers+CommonEvents+map.json `script` fields) + `find_duplicate_definitions`. Driver `scripts/stage_slice_scripts.py` (normalize→prune→checks; dry-run default, `--write`→`staging/scripts/`, never edits canonical `scripts/*.pory`). See the S6-RUN slice note for the verified result + the empty-wired-event finding the existence check surfaced.
- **Phase-5-prep artifacts (FABLES 4.x, 2026-06-12).** `deserialize.rb tilesets <data_dir> <out_dir>` → `tilesets.json` (per-tileset `passages`/`priorities`/`terrain_tags` flat arrays; passages = walkability oracle for §5.6). `scripts/map_identity_check.py` (read-only) — signals (sign text / BGM / connections / parent tree / dup groups) vs `map_infos.json`; run with no args. `reference/map_name_overrides.json` — §4.3 source of truth for corrected map identity (consult before `map_infos` at Phase-5 `mint`); currently `overrides: {}` (none needed — see Discoveries), `orphans` (Map999), `phase7_confirm` (6 cave maps). `strip_list.json` gained a `maps` key (whole-map strips, Phase-5-mint-honored; Map999).
- **`scripts/moveroute_coverage.py` (OQ-3 Step 1, 2026-06-13).** Zero-spend move-route census, mirrors `idiom_frequency.py` (corpus walk + `lane.real_commands`). Decodes every `209` Set-Move-Route command (`params[0]`=target -1/0/>0 = player/self/other; `params[1].list`=inner RMXP move-cmds 1–45), applies the editable `CANDIDATE` map (A=fixed `MOVEMENT_ACTION_*` / B=param-deterministic / C=no-analog), and reports the histogram, the bucket-C breakers, the bucket-B param caveats, the target crosstab, and the **conservative vs potential** 3-way determinism partition. Answer lives in `OQ3_EMPIRICAL_PLAN.md §7` (durable): raw macro determinism **24%**, potential **65%** after 5 SOFT-C rules, irreducible HARD tail **35%**. Single edit point for re-bucketing = the `CANDIDATE` dict + `SOFT_C` set. Run: `python3 scripts/moveroute_coverage.py`.
- **`scripts/count_unique_commands.py` + `reference/recon/uranium_real_commands.md` / `uranium_excluded_noncommands.md` / `agent_coverage_diff.md` (2026-06-14).** Zero-LLM census of the FULL command vocabulary across all 199 maps + 100 CEs (no handled/unhandled filtering, no ref-doc input). **195 real mappable commands** = **47 structural RMXP codes** (59 distinct total − 11 scaffolding/continuation {0,401–413,509,655} − the `355` Script umbrella) + **148 named script-call heads** (`355/655` + action-bearing `111` script-conditions). Excludes (File 2): scaffolding codes, Ruby keywords/`<expr>`, 17 local scratch vars, 1 receiver, 7 pure `111`-condition reads, and **16 `$Obj.field=` global state-writes** (these ARE mappable → setflag/setvar/give-*, just not *named* commands — `badges[]=`, `money-=`, `runningShoes=`, `nextBattleNuclearHorde=`…). Classifier rule documented in the script header + File 1 (call-syntax / engine-prefix / bareword-statement / Module.method). `agent_coverage_diff.md` diffs the census vs the conversion agent's frozen reference — see Discoveries.
- **`reference/recon/maps_and_common_events.md` (2026-06-14).** Hand-authored explainer of the map↔common-event relationship (humans + build agent). Covers: CE = a flat global page (no pages/graphic/position/self-switch) vs map events; the two link types (**code 117 explicit call** — 73 sites / 6 CEs, incl. the 11-Pokémon-Center receptionist CE4/5/6 reuse; vs **trigger 1/2 switch loops** — 3 parallel CEs, e.g. racing CE10/11); **caller-context semantics** (a called CE's `this event`/target-0 move route binds to the calling event); and the conversion mapping (`convert_common_events` → one `CommonEvent_<NNN>` block in `CommonEvents.pory`, `117 [N]`→`call CommonEvent_<NNN>`, STRIP emits a stub so calls don't dangle, CE pass before fork assembly). Worked example: Map002 EV009 "Receptionist PVP" `117 [5]` → `call CommonEvent_005`. 22/100 CEs have real commands.
- **Map-viewer browsable UI (`scripts/map_graph.py` + landing page + cross-map nav strip, 2026-06-23).** Phase A done (server-only): searchable name+tree landing + per-map nav chips (up/sub/warp→). Details + Phase B (multi-floor composite, not built) in `reference/viewer/map_viewer.md`.
- **Map viewer = tile-FEEDBACK tool (refocused 2026-07-11, uncommitted) — CURRENT state; supersedes the "Map Inspector/Issues/slice-pool" framing in the notes above.** `scripts/map_viewer_{common,server}.py`; guide `reference/viewer/map_viewer.md`. Purpose: flag specific tiles/tile-groups → per-map punch-list the build agent fixes. **Selection:** tap-to-toggle accumulating set (drag pans, 2-finger zooms; NO drag-paint/marquee) + **Expand similar** (additive, multi-identity, by `colkey_idx`, this-map only) + Clear; last-tapped = focus cell (drives inspector). **Flag** = `{cells:[[x,y]…],note}` bare prose → git-tracked **`reference/map_feedback/MapNNN.json`** (server writes on Save via POST `/api/feedback`; GET `/api/feedback/<id>`; static mode = in-memory + Export JSON). **Quant/debug controls collapsed** behind an **Advanced** toolbar toggle (`body.adv` reveals knobbar, Diff/Priority/Merge overlays, `/palettes` page + link, Worst-merges panel, per-cell Palettes/quadrant-fit; leaving Advanced clears debug overlays). **Default grid view = Quantized(family)** (RPGMaker = reference, `q` hotkey). Core always-visible: view toggle, Collision/Events/Warps overlays, zoom/pan/fit, trimmed inspector. NB: `build_map_data`'s `our_blocked`/`uranium_blocked` calls need the `terrain` arg (compare_collision signature since 2026-07-09). **2026-07-12 parity fix:** quantized view now pools slice maps by REAL tileset_id (49+48 together) matching the boot-gate ROM, auto-reloads on disk changes, badges non-stock knobs; oracle = `scripts/verify_viewer_rom_match.py` — full story `reference/viewer/viewer_rom_parity_2026-07-12.md`.

---

## Decisions Made

- [2026-07-21] **Randomizer game mode = NO-PORT strip (user-approved).** `$PokemonGlobal.randomizer` (Uranium's optional playthrough-wide species/moveset/type/encounter shuffler, also gates `nuzlocke_heal`) is treated as a **statically-false predicate**, not a convertible branch. Fork-verified 2026-07-21 (grep engine/ include+src+config): pokeemerald-expansion has NO randomizer/nuzlocke run-mode and no `GetRandomPokemonSpecies` analog — the `randomiz` hits are all incidental (RNG primitive `random.h`, `AI_FLAG_RANDOMIZE_SWITCHIN`, lottery/contest/TV). Supporting it = a from-scratch subsystem, zero story weight, and the then-arm is unfillable (no random-species primitive). Rule: a code-111 conditioned on the predicate emits the **else (normal)** arm; the randomizer arm is dropped as dead code (negated conditions invert). Recorded as SoT in `reference/strip_list.json` → new `static_predicates` category (§4.3 layer boundary comment updated). **IMPLEMENTATION PENDING:** transpiler does not yet read this — EV019's two randomizer branches are handled in the `Map050_EV019.pory` hand file today; the future static-predicate idiom should consume the strip_list entry, not hardcode the string. Context: surfaced while auditing EV019 (Moki lab starter+rival) after I wrongly called its starter-grant "irreducible Ruby" — it is not; the grant is `givemon(SPECIES_X) if VAR_POKEMONTEST==N` (Birch's-bag), dropped only because it's wrapped in this randomizer branch.
- [2026-07-13] **`reference/` reorganized into subfolders** (user request, scope approved): `uranium_data/` (passive recon JSON dumps), `recon/` (phase-0/1 investigation docs), `guides/` (active how-to/mapping docs), `viewer/` (map viewer/walker docs), `findings/` (dated session findings). SoT JSONs (§4.3) + memory files stay top-level. Code paths updated (pbs_converter → `uranium_data/`; prompt_builder/triage/command_catalog/pipeline → `guides/`/`recon/`); `archive/` + `memory-archive.md` intentionally keep pre-reorg paths. Layout guide: `reference/README.md`.
- [2026-07-02] **Spine grill session — 10 decisions; transpiler work re-anchored (user).** Full record: `reference/findings/grill_spine_2026-07-02.md` (+ slice command census appendix). Walker-first was DRIFT, not a decision. Spine anchored to FINISHING the pathfinder slice (six-point done bar; slice 1 stays 3 maps — starter/Route 1 = slice 2 = first "data changes, not code changes" guardrail test). Capability index reads git-tracked PRISTINE `engine/` content (working tree is contaminated by pipeline-injected symbols — the MAP_MOKI_TOWN class); forward gate enforces at conversion time AND staging, resolving index ∪ flag/map-constant/PBS registries; gate failure = our bug = abort loud, never queued. Transpiler = NEW thin driver beside `deterministic.py`, no checkpoints/memo (BUILD_PLAN §8 "repurpose" line stale — corrected); reuse only queue format + triage; `orchestrator.py`/`backends/` stay the hand-invoked LLM tail tool. Oracle = one-time harvest→retire (normalized-text diff, cluster dispositions; goldens minted from reviewed transpiler output, never Opus text); PREREQ: archive the oracle out of gitignored `output/` first. 209 v1 = deterministic tier (OVERTURNS BUILD_PLAN §5 stub-all; census: 91 routes/80 paired waits in the slice's intro cutscenes); any stub must neutralize its paired 210. Queue residue: build agent first w/ reverse-gate evidence, user gates fidelity calls; LLM tool discretionary. Slice 1 assembles via `assemble_pathfinder` as-is; phase5 unification = separate post-slice work unit. BUILD_PLAN.md carries a dated amendment block.
- [2026-06-19] **Include-hooks — assembler no longer edits tracked engine files in place (vendor follow-up b DONE).** Problem: the assembler edited 3 tracked upstream files in place (`data/event_scripts.s`, `data/layouts/layouts.json`, `data/maps/map_groups.json`) → per-slice churn + accidental-commit risk against the pristine vendor. Investigated the engine build: `event_scripts.s` is the manual `.include` point for every map's `scripts.inc` (AS-time); `mapjson` reads `map_groups.json`/`layouts.json` at FIXED paths with NO merge (per-map `map.json` ARE wildcard-discovered, so only the 2 manifests truly need editing). Fix = two committed, sentinel-fenced **include-hooks** (treated like the 3 C divergences): (1) `event_scripts.s` — `#include`s the generated alias/flags headers (CPP-time, must stay there) + `.include`s the generated `data/maps/uranium_includes.inc` (the volatile per-map scripts.inc list); (2) `map_data_rules.mk` — `URANIUM_MAP_GROUPS`/`URANIUM_LAYOUTS := $(or $(wildcard …gen.json),…json)` so the build reads assembler-written `*.gen.json` overlays when present, else pristine upstream (so a stock build is byte-identical; `$(wildcard)` resolves at parse time, assembler runs before make). Assembler (`assemble_pathfinder.py`) rewritten: writes `map_groups.gen.json`/`layouts.gen.json` (seeded from PRISTINE upstream + slice entries, deterministic) + `uranium_includes.inc`; touches no tracked file. Verified: after assembly `git status` shows only the 2 hook files (manifests pristine); gen files gitignored; `make -n -B` confirms recipes read the `.gen.json`; pytest unchanged (416/2/11). **Pending:** full `make modern` re-confirm (user). Like the spawn/intro divergences, the committed hooks assume the assembler ran before `make modern`. Closes the long-standing in-place wrinkle (`RPG2GBA_VENDOR.md` + `.gitignore` updated).
- [2026-06-19] **Finding: 2 slice-minting tests fail against a built+assembled fork — PRE-EXISTING, not a cutover regression.** `tests/test_tileset_converter.py::test_build_slice_constants` + `::test_build_slice_maps_smoke` mint slice maps (e.g. Moki Town → `MAP_MOKI_TOWN`) and read the LIVE fork's generated `include/constants/map_groups.h` (`map_constants.FORK_MAP_HEADER`) for the vanilla-collision baseline. Once the slice is assembled (assembler edits `data/maps/map_groups.json` in place) **and** built (`make modern` regenerates `map_groups.h` WITH the injected Uranium maps), `MAP_MOKI_TOWN` is in the "vanilla" set → false collision → fail. Confirmed pre-existing: BOTH `engine/` and the old clone's `map_groups.h` contain `MOKI_TOWN` (3 each) — red since the slice was first built (S8/S9), regardless of which fork. **NOT a real collision** (Moki Town isn't vanilla) → do NOT add a `map_name_overrides.json` entry. Proper fix (deferred = paused feature track): make the registry/tests robust to a non-pristine fork — exclude `MAP_URANIUM_*` + already-minted Uranium maps from the collision baseline, or have these two tests pass an explicit pristine `vanilla_consts`. Root enabler = the assembler's in-place edits (vendor follow-up b). Suite at cutover: 416 passed / 11 skipped / 2 failed. **RESOLVED 2026-07-11** (SLICE1_TODO #10), properly this time — root-caused in the registry, not papered over in the tests. First pass made both tests pass `fork_path=<nonexistent dir>` (isolating the mint from any built fork) — that made the suite green but weakened real collision coverage, since it also stopped exercising the vanilla-collision path at all. Correct fix (user-requested follow-up): `map_constants.load_vanilla_map_consts` now prefers `_load_vanilla_map_ids_pristine` — reads the real vanilla `MAP_*` set from **git HEAD**, not the working tree, mirroring `conversion_agent.fork_index`'s pristine-git-read pattern (same root cause class: a build-contaminated working tree). Turns out `include/constants/map_groups.h` (what the old code read) is upstream-gitignored/build-generated even in pristine pokeemerald-expansion — never committed, so there's no pristine version of THAT file. Reconstructs the same info instead from what IS committed: every `data/maps/<Dir>/map.json`'s `"id"` field, via one `git archive HEAD -- data/maps` of the fork's map tree (~900 vanilla dirs, one subprocess, not per-file). Verified: pristine read sees `MAP_LITTLEROOT_TOWN`, correctly does NOT see `MAP_MOKI_TOWN` (Uranium map dirs are gitignored repo-root-side, `data/maps/*/`, so never committed regardless of build state). Both tests reverted to their original `fork_path=None` (real env-var fork) now that it's safe; +3 new unit tests for the pristine reader + its git-unavailable fallback. `test_build_slice_maps_smoke`'s separate, previously-masked bug (never passed `npc_gfx` to `build_slice_maps`, required since the 2026-07-06 NPC-gfx-map landing) fixed by loading the real `reference/npc_gfx_map.json` against the built fork headers, `pytest.skip` if either is absent. 964 pass, 0 known failures, ruff clean.
- [2026-06-18] **Vendored the pokeemerald-expansion fork into `engine/` (user).** Pinned at upstream `21c24202` via flat `git archive` (no upstream history). Custom engine C now lives in version control next to the pipeline; the version is pinned (no silent drift); a fresh rpg2gba clone is reproducible. Chosen over a submodule (ergonomics; custom C would live in a separate repo) and a patch overlay (less ergonomic). Vendored = **pristine upstream source + the 3 hand-written custom files** (`src/new_game.c`, `src/intro.c`, `include/new_game.h`, sentinel-fenced) — **240MB**; pipeline-generated output + `build/` are gitignored, NOT committed (see `engine/RPG2GBA_VENDOR.md`). Legal: decomp source carries no ROM data + no LICENSE (gray area) → personal/non-distributed only. Reverses CLAUDE.md §2/§5 "C lives outside the repo" (both updated). **Follow-ups (NOT done):** (a) cut `RPG2GBA_POKEEMERALD` over to `engine/` — needs a one-time `make modern` (generates the headers `load_fork_constants` reads) + a re-assemble so the custom C compiles; (b) stop the assembler editing `event_scripts.s`/`layouts.json`/`map_groups.json` in place (use include-hooks) so engine source stays clean; (c) general gitignore convention for ALL Uranium-generated engine paths (only slice paths listed now); (d) delete the external clone after cutover.
- [2026-06-18] **STRATEGY PIVOT (user): retire the LLM as the conversion spine.** Event→Poryscript becomes a deterministic command→Poryscript transpiler (Option B — generalize `deterministic._dialogue_body`); the 9 classifiers become an idiom-collapse layer on top; the LLM survives only as a hand-invoked tool for the irreducible tail. Reason: the dangerous error class is *compile-clean-but-wrong* (silent/unbounded — cf. the `ITEM_PARLYZ_HEAL` divergence), the pre-filter already reproduces ~36% byte-identical to Opus, and the pathfinder slice's playability blockers were ALL deterministic-pipeline gaps, not LLM gaps. Supersedes the 2026-06-03/06-11 single-Opus bulk-run strategy. → `BUILD_PLAN.md`, ROADMAP §Phase 4.
- [2026-06-18] **Frozen prompt lifted.** Bulk run retired ⇒ `prompts/system.md` is no longer frozen; the §9 #2 "don't touch system.md mid-run" rule is void. It becomes an editable tail-tool prompt.
- [2026-06-18] **Frozen-Opus output retained ONLY as a differential oracle.** Maps 001–007 + CommonEvents `.pory` validate the transpiler (diff its output; every divergence = transpiler bug or an Opus error we're glad to drop), not as product. The done maps are off the playable path anyway (spawn = Map049).
- [2026-06-18] **Operating model: vertical playable slices.** Convert one play-order section completely — events AND quantized art — to a booting, genuinely-playable result, then widen the frontier slice by slice. Supersedes strict horizontal phase ordering for the conversion campaign (Phases 4/5/6 interleave per slice). Guardrail: converters stay general/idempotent/config-driven — slice 2 = data changes, not code. First slice = pathfinder (49/48/32). Reason: the slice "lied" about being done by deferring art; genuinely-complete slices are the fix for "did work, found it was wrong."
- [2026-06-18] **Fork-index verification guard (both directions).** A deterministic index of the fork's real vocabulary (624 specials/`specials.inc`, 385 macros/`event.inc`, all `include/constants`) gates every conversion: forward = every emitted command/special/constant must resolve or fail loud at convert time (catches invented symbols before `make modern`); reverse = nothing queued "needs custom C" without a fork-search entry proving no native analog. Ledger = `reference/guides/essentials_to_emerald_map.md`. Reason: `HealPlayerParty` was a defined special (`specials.inc:20`) the agent had invented as `healparty`. Supersedes the open "verify UNHANDLED script-call semantics" question. Most "engine feature" tags ship natively (cave/Flash, bridges, PC, region map, relearner, trade, rock smash) — only the Nuclear type is genuinely new C.
- [2026-06-18] **Image fidelity = quantize Uranium's real art** (tilesets/sprites → GBA 4bpp), NOT Hoenn-tile substitution. The reusable capability + the only path to a Uranium-looking ROM; degraded first pass, hero-asset polish in Phase 8. Greenfield PNG→GBA pipeline replaces the v1 passability-bucket scheme. → `BUILD_PLAN.md §7`.
- [2026-06-18] **Fork-baseline divergences MUST be reverted/gated before any real build.** `new_game.c` (WarpToTruck → Map049 spawn override) + `intro.c` (intro-skip), sentinel-fenced + uncommitted, are test-only slice conveniences; a fresh fork clone/checkout drops them. Echoed in Current Phase so it survives.
- [2026-06-15] Decision: **Pathfinder-slice pivot (user).** Pause the horizontal bulk run; validate by build+boot on a vertical play-order slice (Map049→048→Moki Town) before committing the whole corpus to any sequencing. Chosen over (a) full immediate pivot to map-by-map for all 199 (premature — commits build-cycle×199 + tileset×60 work before we know per-map cost) and (b) fix-layers-and-resume-horizontal (keeps the un-validatable-until-Phase-7 defect). The slice IS the first step of any path, so it's done regardless; it de-risks the assembly/build/boot loop (never exercised) and surfaces systematic-error classes early. Constraint surfaced: `reference/recon/uranium_script_calls.md` feeds the FROZEN conversion-agent system prompt (memo-fingerprinted) → must NOT be edited until the bulk run is truly retired/restarted; its mistagged rationales are corrected downstream in triage/Phase-5 planning, not in the prompt. See Current Phase → STRATEGY PIVOT.
- [2026-06-15] Finding: **Cave darkness/Flash and over/under bridges are ENGINE-NATIVE in the fork — NOT Phase-6 custom C** (verified in `/home/b/repos/pokeemerald-expansion`). Cave darkness = the `gMapHeader.cave` field (`include/global.fieldmap.h:235`) + map.json `"requires_flash"` + the already-built Flash field move (`src/fldeff_flash.c`); `overworld.c:1107` gates the radius on `gMapHeader.cave`/`FLAG_SYS_USE_FLASH`. Bridges = `MB_BRIDGE_OVER_*`/`MB_FORTREE_BRIDGE`/`MB_BIKE_BRIDGE_OVER_BARRIER` metatile behaviors. So Uranium's `pbCaveEntrance/Exit` + `pbBridgeOn/Off` event calls map to **Phase-5 map-attribute/metatile settings + STRIP the call**, not engine work. The conversion agent's runtime disposition (queue/UNHANDLED) stays correct (it can't emit a map-header change); only the *triage rationale* ("Phase-6 engine feature") in `uranium_script_calls.md` is wrong-and-deferred. **Generalizes:** most of the UNHANDLED "engine feature" tail (PC `pbPokeCenterPC`, region map `pbShowMap`, move relearner `pbLearnMove`, trade `pbStartTrade`, berry trees, daycare, rock smash) ships natively in the expansion → wiring tasks, not new C. The genuinely-new-C surface collapses to ~just the Nuclear type. Each "engine feature" tag must be grep-verified against the fork (the conservative tags can't be trusted). TODO when building the custom-C-surface ledger.
- [2026-06-14] Decision/Finding: **Frozen-Opus is an UNRELIABLE oracle for Uranium item constants — deterministic classifiers (reading the Phase-2 sidecars) are canonical** (iterative-roadmap invariant-4 resolution). The mart differential test caught Opus emitting `ITEM_PARALYZE_HEAL` (vanilla spelling) where Phase 2 defined `ITEM_PARLYZ_HEAL` (Uranium display name "Parlyz Heal" → `to_constant`; confirmed in `item_field_codes.json`). Since the final fork carries Uranium's item IDs (vanilla constants replaced at the Phase-7 roster swap), Opus's guess would be an **undefined symbol at assembly** — a compile-clean-but-assembly-breaking bug. **Implications:** (1) existing done-map `.pory` carry latent wrong-item-constant refs wherever a Uranium display name diverges from vanilla — the deterministic re-claim fixes them (classifier runs before memo), and the Track-B **assembly smoke must catch any residue**; (2) likely extends to move/species/ability constants — treat Opus constant-name output as suspect, prefer deterministic emit. The differential test now compares **structure** vs Opus (ITEM_* tokens normalized) and **item spellings** vs `item_field_codes.json` (`test_emitted_item_constants_are_real`).
- [2026-06-12] Decision: **`run_bulk --skip-lane` spend partition (user-approved plan `snug-juggling-starfish`).** Goal: stop Opus spending on events the user would hand-convert (the memo already prevents *double* spend, but Opus's map-order sweep could reach a lane event before the human). Solution = partition, not order-coordination: `--skip-lane` makes Opus skip the hand-conversion lane entirely (spends only on hard events), user owns the lane, a final plain `run_bulk` mop-up assembles + completes. Chosen over (a) `--order opus` for run_human (reduces but doesn't eliminate waste — rejected) and (b) order-matching (fragile). Key design calls: skip placed **after deterministic+memo** (free events still convert; skipped set == run_human queue); **`.partial` marker** (vs deriving partial-ness — marker is simpler, matches `.done`); **no new mop-up command** (plain `run_bulk` re-enters `.partial` maps); **map-events only** (CommonEvents untouched). The sensitive part was the binary→ternary checkpoint change (resumability invariant, live run) — done conservatively (top guard still keys on `.done`; `.partial` re-enters cheaply via memo hits). See Key File Notes → Hand-conversion pass.
- [2026-06-12] Decision: **Hand-conversion lane added so the user can offload events from the usage-bound bulk run.** Motivation (user): `run_bulk.py --timed` running continuously burns the whole Pro window → locks them out of Claude for everything else. Solution = `HumanBackend` (interactive, zero-spend) + `run_human.py` difficulty-sorted driver, structured as a **memo-seeding** pass that interleaves freely with Opus rounds (shared memo via identical frozen system prompt; both write the same checkpoints). **Lane scope (user decision): "hold for Opus" — global-switch/var events (code 121/122) are EXCLUDED**, so the human never makes a global flag/var naming call; their lane is pure dialogue + self-switch + flag-gated branches (289 events). Punt (`opus`) is the per-event escape (no hard-stop, per user); it leaves the event un-memoized for the bulk run. Rejected alternative: a "let me name globals too" wider lane (more typing + naming calls the registry must approve). Key realization that made it clean: the human does NOT complete maps — `convert_single` only seeds the memo, so order/coverage don't matter and Opus claims everything left. See Key File Notes → Hand-conversion pass.
- [2026-06-12] Decision: **Three §10 fidelity calls resolved (user).** (1) **Racing minigame (CE10/11)** = STUB now → **Phase 8 ADAPT** (outcome substitute for the bulk run; engine minigame later — Custom Mode precedent). (2) **Dream/vision sequence (CE86/87)** = STUB now → **ADAPT Phase 6/8** (party-stash engine state later). (3) **Custom VS-intro (CE76)** = **accept the loss** — standard pokeemerald `trainerbattle` intro, no custom VS screen (not even a Phase 8 polish item unless revisited). Recorded in `reference/findings/novel_cluster_review.md`. Clears the last user-parked §10 items; bulk run is now unblocked on the fidelity-call front.
- [2026-05-09] Decision: Source artifact is the distributed `Pokemon_Uranium_1.3.2.zip`, unpacked to `~/Pokemon_Uranium_132/_unpacked/` via `scripts/extract_rgssad.py`. Reason: Uranium was *not* open-sourced (the previous ROADMAP claim was wrong); the rgssad archive is the only path. Audio/Graphics/Fonts symlinked into `_unpacked/` so a single `RPG2GBA_URANIUM_SRC` env var works.
- [2026-05-09] Decision: Phase 2 reads compiled `.dat` files via Ruby `Marshal.load`, not text PBS. Reason: distribution ships only `.dat`. Roadmap §Phase 2 updated; new spike task added before any per-record converter starts.
- [2026-05-09] Decision: `reference/scripts_dump/`, `reference/maps_dump/`, `reference/dat_dump/` are gitignored. Reason: verbatim Uranium source per CLAUDE.md "do not commit Uranium source tree"; only derived summary docs are tracked.
- [2026-05-12] Decision: `recon_scripts.rb` writes inflated source as transcoded UTF-8 (Windows-1252 → UTF-8 with replacement) and uses `File.binwrite`. Reason: prior `File.write(..., encoding: 'utf-8')` raised `Encoding::UndefinedConversionError` on 85 of 260 sections, leaving them as 0-byte files on disk — including `175__Compiler.rb`, `124_Pokemon_ShadowPokemon.rb`, `220_Custom_Mode.rb`, `222_Gym_8.rb`, `227_Tandor_Championship.rb`. CLAUDE.md §5 already flagged the Windows-1252 hazard for Python; the Ruby side now applies the same rule.
- [2026-05-15] Decision: Custom Mode (script 220, Nuzlocke/randomizer) is **ADAPT (Phase 8)**, not STRIP. Reason: user feedback — preserving Nuzlocke/randomizer as an opt-in post-MVP feature retains a meaningful Uranium play option; no flag-registry debt since the script uses no hardcoded `$game_switches`/`$game_variables`. Implementation requires building Nuzlocke + randomizer infrastructure in the fork (no native support).
- [2026-05-15] Decision: Tandor Championship (script 227) is **CONVERT**, not ADAPT. Reason: user feedback — preserve the 4-round randomized bracket in full. Poryscript can express the bracket as `random var + lookup table` over `TCTRAINERS`/`TCTRAINERS2`; the waiting-room NPC graphic swap maps to per-event sprite changes. Higher Phase-4 effort than ADAPT but full fidelity.
- [2026-05-20] Decision: §2.2 move **effects are deferred to Phase 6**, not mapped in Phase 2. Reason: 324 distinct Essentials function codes (range 0..361) have no formula to the fork's `enum BattleMoveEffects`; guessing maps near-but-wrong constants (violates D3/§9). Every move emits `.effect = EFFECT_PLACEHOLDER` + an inline `// TODO Phase 6: function code N` comment; the raw code+chance for all 637 moves is preserved losslessly in `intermediate/move_function_codes.json` (the Phase 6 worklist). The 9 Nuclear-type moves (type idx 18) are additionally flagged `needs_engine`. Inverse Essentials flags (b=Protect / e=Mirror-Move / f=King's-Rock affinity) left at struct default (their FALSE default matches the common case; inverting risks errors).
- [2026-05-25] Decision: **Phase 2 exit-criteria #3 (generated C compiles cleanly in fork) and #4 (test ROM species in Pokédex) are DEFERRED to Phase 7.** Reason: the "drop in and compile" check is architecturally inseparable from the species-roster swap. Our generated `SPECIES_*`/`MOVE_*`/`ITEM_*` use Uranium (Tandor) ID space and collide *by name* with the fork's vanilla constants (e.g. `SPECIES_GENGAR` in both, different IDs). To compile our tables they must occupy the fork's include paths, but the fork's own headers (`config/battle.h`, `config/pokemon.h`, `config/species_enabled.h`, `pokemon.h` chain via `global.h`) require the vanilla constants — so our constants and the fork's can't coexist, and resolving the collision *is* the roster replacement (Phase 7's job). No synthetic-harness shortcut taken (brittle, low fidelity). Struct-shape conformance is already covered by `reference/guides/pokeemerald_struct_shapes.md` (verified field-by-field vs fork HEAD `21c24202`) + round-trip tests. Exit #1 (every `.dat` CONVERTED/STRIP/DEFER) now explicitly satisfied via the status table added to `reference/recon/uranium_dat_inventory.md`. No fork changes were made (fork still clean on `master`).
- [2026-06-03] Decision: **Scripted move routes (codes 209/210/509) are DEFERRED TO PHASE 5** — the agent emits a `# UNHANDLED: move route` breadcrumb + one queue entry and **translates the rest of the event**; it does **not** emit `applymovement`. Reason: measured (`scripts/_tmp_moveroute_targets.py`) **1,191 events / 3,804 `209` commands**, targets split player 38% / this-event 31% / other-event 31% with **heavy mixing** (≈1.3 target classes/event). Every target except the player (`-1`→`OBJ_EVENT_ID_PLAYER`) resolves to a pokeemerald object **local id** assigned only during Phase-5 map wiring; ~half the events contain a self/other target, so event-level movement is Phase-5-blocked, and a Phase-4 placeholder would need both a local-id convention AND an unbuilt RMXP→pokeemerald movement-command vocabulary. Move routes are **more** Phase-5-coupled than warps. `system.md` gained a **Move Routes** section + corrected the **Multi-Page Events** claim that "the orchestrator handles wiring the page-switching logic" (it does NOT — Phase 5 wires page dispatch; the orchestrator only mints the page flags). This decision also **retired the proposed move-route few-shot** (the lever was the handling rule, not an example) and the **variables few-shot** was dropped as low-value (Opus already nails the `random`→var→branch / EV007 case the comparison turned on).
- [2026-06-03] Decision: **Bulk Phase 4 conversion model = `claude-opus-4-8` for ALL events** (single model, not tiered/routed). Reason: a difficulty scan (`scripts/_tmp_difficulty_scan.py`) shows **73.3% of 3,581 command-bearing events need real judgment** (branches 1785, Uranium script-calls 1617, vars/self-switch 1242, move-routes 1218); only **26.7% trivial** (mechanical text/warp/call/SE incl. 306 pbCallBub-only). Cheap-draft→Opus-fix rejected as false economy (events are tiny → review≈rewrite; the dangerous errors are compile-clean-but-wrong — EV007 var-102, the memo label bug — so a cheap pass doesn't cut Opus's evaluation burden and you pay twice on the events that matter). Local-LLM first pass rejected (silent-wrong risk across the 73% judgment corpus on a get-it-right-once build). **Difficulty-routing** (trivial 27%→Sonnet via the deterministic classifier, **same frozen prompt**, just the `--model` flag) saves only ~15–20% and is **deferred as optional post-freeze polish** — one model = one quality bar through the §9 gate. The classifier already exists, so routing stays a small reversible add-on, not a fork in the road.
- [2026-05-25] Decision: **Four P4 content-fidelity calls resolved at the V5 gate** (user picked the recommended option on each). (1) **Hidden abilities** — fork's single hidden slot gets Uranium's *first* hidden; the extras (only **8 species**, max **2** total — MAGIKARP, MINYAN, VILUCARD, COCARAN, CARARALM, COCANCER, JACKDEARY, WINOTINGER) are recorded in a sidecar + flagged `needs_engine`; **no** Phase 6 multi-hidden engine work (not worth it for 8 abilities). (2) **natDexNum** — left at default (NONE); Tandor numbers carried in `intermediate/tandor_dex.json` for Phase 5. (3) **eggCycles** — `round(steps/256)` confirmed (verified: 5120→20, 3840→15, 5355→21; clean distribution). (4) **Species 201 "GENGAR"** — emit-and-mark **unobtainable** (real stats, no Tandor entry); rule "real stats but `regional_dex_number==0`" isolates exactly id 201. **Impl status:** (2)+(3) already in code; (1)+(4) need small deltas (sidecar + `needs_engine` flag; unobtainable sidecar + inline comment).
- [2026-06-11] Decision: **Phase 5 reachability check (critique suggestion #8) = build as a Phase 5 acceptance gate; design settled.** Directed BFS (one-way **ledge edges in v1** — they ARE the soft-lock mechanism) over emitted collision: entries (warp landings via map_constants + connections + spawn + heal spots) → exits; fail-optimistic = defect, fail-pessimistic-pass-optimistic = build-agent wiki review (intended gating, spoiler-free), water = HM-gated class. Needs a small `deserialize.rb` extension: **`Tilesets.rxdata` (terrain_tags + passages) is not yet deserialized** — and passages doubles as a source-walkability **oracle** to diff against emitted collision (catches Q3/Q4 errors directly). Puzzle *solvability* is beyond static analysis → flagged maps become the Phase 7 playthrough checklist. Runs after 5.2–5.4. Details: `FABLES_DECISIONS.md`.
- [2026-06-11] Decision: **Hygiene (critique suggestion #7) EXECUTED:** working tree committed in 3 batches (`ceb9320` bulk harness, `b7a7be2` Phase 5 scaffold, + walkthrough docs), measurement scripts promoted (`measure_trivial_tier.py`, `near_miss_families.py` — `_tmp_` convention retired for cited scripts), MEMORY.md Current Phase compressed to live state with the Phase 4 build narrative archived verbatim in `reference/memory-archive.md`.
- [2026-06-11] Decision: **Move-route Phase 5 scope (critique suggestion #6) = add PHASE5_PLAN §5.5 stub carrying the measured census + Q-MR1–5; questions answered at implementation.** Census: 1,191 events with 209 routes, **531 (45%) player-only (no local-id dependency)**; 527 autorun/parallel = fidelity-critical cutscene tier; page-level wander is NOT 209 work (native `movement_type`, 5.3). Vocabulary ~20k cmds → 3 translator classes: direct macro ~70%, hoistable side-effects (SE/graphic/switches), approximate-or-drop (opacity 2,280→binary visible, through 1,757, on-top 171). Open Qs: local-id convention (+~64 object/map ceiling vs map 148's 190+ events), timing 40→60fps, **injection architecture** (idempotent .pory post-pass vs memo-replay regen). Details: `FABLES_DECISIONS.md`.
- [2026-06-11] Decision: **Q2/MapInfos naming contradiction (critique suggestion #5) = overrides file + signals-first identity check, before `map_constants.py` is built.** Q2's readable-names+alias-header mechanism stands; the trust model changes: new `reference/map_name_overrides.json` (id → corrected name + evidence, one source of truth) consulted by `mint` before `map_infos.json`. Free signals script (sign text / BGM / connections / parent tree) flags disagreements; wiki skill arbitrates flagged + ~50 canonical locations (build-agent pass, spoiler-safe). Stakes: `display_name` feeds `show_map_name` = player-visible header. Measured: map_infos has 198/199 entries (one map missing!), 35 dup-name groups (mostly legit MAPSEC grouping → Q2 amendment: separate MAP_* identity from shared MAPSEC_*/display), and BOTH `Comet Cave`×3 and `Passage Cave`×3 exist (floors may be cross-assigned). Details: `FABLES_DECISIONS.md`.
- [2026-06-11] **FINDING F1 (LIVE BUG) + Decision: name-derived script labels collide — fix at FRONT of the implementation queue.** Labels `Map{NNN}_{name}_Page{n}` aren't unique (RMXP names repeat): Map002.pory TODAY has two `Map002_Receptionist_TRADE_Page1` blocks; corpus-wide **103/199 maps, ~768 would-be duplicate labels**; `deterministic._page_label` has the same flaw. Invisible to every existing gate (per-event compile; whole-file poryscript ACCEPTS dup script names — Map002 compiled rc 0 at the §9 review); only `make modern` catches it. Fix: orchestrator label rewrite at accept (qualify with event id, `_reinstantiate` technique), `_page_label` gains event id natively, memo prefix rewrite extended; repair of done maps ≈ 0 spawns (every accepted event is its own memo entry — wipe checkpoints + replay). **Implementation order decided: F1 → #3 triage → #2 strip+Tier-1 rider, #4 smoke alongside F1 to verify.** Details: `FABLES_DECISIONS.md`.
- [2026-06-11] Decision: **Assembly smoke harness (critique suggestion #4) = build it; design settled, NOT yet implemented.** Persistent fork worktree on a throwaway branch; all `.pory`→`.inc` + generated support headers (registry `dump_header`; `MAP_URANIUM_<N>` dummy aliases via the Phase-5 alias mechanism; `TRAINER_*` real ids from `intermediate/trainers.json`; anything else unresolved = fail loud); `make modern`; error clustering; runs between rounds (5h-pause idle), report-only. First-run regression: must flag the Map002 duplicate. Details: `FABLES_DECISIONS.md`.
- [2026-06-11] Decision: **Cluster-aware triage (critique suggestion #3) = build it FIRST (before the #2 strip skip); design settled, NOT yet implemented.** Today's `triage()` is blind — all 241 queue entries share reason "agent-flagged unhandled". Design: cluster by **source-join** (entry's map/CE+event+page+line → actual RMXP command in Phase-3 JSON → deterministic key: code + 355/655 sig head + 111 condition type + 201 mode), then an auto-disposition rule table tied to recorded decisions (move-routes→Phase 5 ~78; warps→Phase 5 ~35; UNHANDLED-table sigs ~25 — parse `uranium_script_calls.md`, single source; strip-listed-CE entries ~27; 111 subdivided conservatively, unrecognized→NOVEL). ~80% auto-tags; NOVEL residue (~30–50) is reviewed by the **build agent** (wiki skill + scripts_dump), NOT the user — **spoiler-free mechanical summaries only** for §10 fidelity calls (user is avoiding Uranium story spoilers; norm in agent auto-memory). Read-only reporting, mid-run-safe. ROADMAP exit criterion rewords to "every cluster has a decision" at implementation. Details: `FABLES_DECISIONS.md`.
- [2026-06-11] Decision: **Orchestrator-side STRIP skip (critique suggestion #2) = build it; design settled, NOT yet implemented.** Per-game `reference/strip_list.json` (whole-artifact strips: CE ids + optional map-event ids; `expect_name` fail-loud assertion) → `convert_common_events` emits a deterministic compile-gated **stub block** per stripped CE (keeps `call CommonEvent_NNN` sites valid; STRIP ≠ queue), receptionist caller events KEPT (`map_events` ships empty for Uranium). Touches nothing frozen (no prompt change, memo fingerprint intact — mid-run-safe). Budget case this run is dead (CEs 4/5/6 already spent; 30/36 callers memo-covered; ~3 spawns left) — value = future runs/games + queue hygiene (27 entries) + fidelity (deliberate "unavailable" stub beats half-broken flow). Full design, measurements, implementation checklist, open sub-decisions (this-run `--regen-ce` remediation, stub voice, post-run `nextBattleBack` STRIP row): **`FABLES_DECISIONS.md`**.
- [2026-06-11] Decision: **Phase 4 bulk-run throughput = accept calendar time on the Pro plan** (critique suggestion #1, option a). Keep `run_bulk.py --timed` running until done (~2,500 spawns at observed ~22/day ≈ ~3 months), overlapped with Phase 5 build work. API/Batch escalation **rejected** (user: no API-key spend; `AnthropicAPIBackend` not built). Sonnet difficulty-routing **rejected on measurement** (`scripts/measure_trivial_tier.py`): the deterministic pre-filter already claims the mechanical tier — only **133 of the 2,584 LLM-bound events (5.1%) are still trivial** by the 06-03 scan's definition, ≈4% calendar savings, not worth a second quality bar. ROADMAP Phase 4 strategy section rewritten to match (staged Ollama/local-first plan retired; historical note kept). Follow-up RESOLVED same day: near-miss family analysis (`scripts/near_miss_families.py`) → 35 families; **Tier 1 only (~40 dialogue+Wait/SE events) gets claimed, as a rider on the STRIP-skip implementation** (validate strip-as-plumbing vs frozen-Opus first, ~2–3 spawns; Map200 precedent supports); Tiers 2–3 (~93 events, 34 families) declined — fails the Classifier-5 bar. Details: `FABLES_DECISIONS.md`.
- [2026-05-15] Decision: Custom Trainers (script 216) effort/phase upgraded from "None/2" to "Medium/4". Reason: original doc described it as "no custom code needed beyond standard data conversion" based on the filename; agent verification against the corrected dump showed it's a Stochastic-authored runtime trainer builder (`createTrainer`, `customTrainerBattle` with `BR_WIN`/`LOSS`/`DRAW` return codes, dynamic level-scaling rematches) — real Phase 4 conversion-agent work.

---

## Uranium-Specific Discoveries

- **Essentials version: v17.** Evidenced by 29 sections suffixed `_v17` in `Scripts.rxdata`. See `reference/recon/uranium_essentials_version.md`.
- **No PBS source files ship.** Only Essentials' compiled `.dat` files are distributed — see `reference/recon/uranium_dat_inventory.md`.
- **No `pokemon.dat`/`abilities.dat` despite Essentials convention.** Species data lives somewhere else in Uranium's Data/ — candidates are `attacksRS.dat`, `tmpbs.dat`, or inside `dexdata.dat`. **Open question — needs spike before Phase 2.**
- **Map/event scale:** 199 maps, 5,301 events, 8,429 event pages. 132/199 maps have at least one page with ≥30 commands.
- **RMXP event trigger codes (got this wrong once):** `0` = Action Button (talk), `1` = Player Touch, `2` = Event Touch, `3` = Autorun, `4` = Parallel. (Map200's step-on doormat warp is trigger 1, confirming.) The talk-once NPC idiom = a 2-page event, base page trigger 0 sets self-switch A (code `123 [A,0]`), a later page gated on `condition.self_switch_valid`+`self_switch_ch`. **860** events set a self-switch via 123; **999** have a self-switch-gated page. Native global `Control Switches` (121, in-list `if`-branch) is rare (539) — the clean single-page global-switch talk-twice NPC essentially doesn't exist in Uranium.
- **Temp-switches are a SECOND, distinct self-switch-like idiom (`setTempSwitchOn`/`tsOn?`, 345× across 77 maps).** Defined in `022_Game_Event_v17.rb` (`@tempSwitches[c]=true`); `@tempSwitches` is init'd `{}` in `Game_Event#initialize` and is **NOT** in `$PokemonGlobal` (unlike `eventvars`, which IS persisted) → temp-switches are **per-map-visit runtime state that resets on map reload** = pokeemerald **TEMP flag** semantics (0x0–0x1F, auto-reset on warp), **not** persistent self-switches. Set/cleared via script call (code 355: `setTempSwitchOn("A")`/`setTempSwitchOff`), checked via `tsOn?/tsOff?` (or page conditions). **Conversion gap:** the agent maps `setTempSwitchOn("A")`→`setflag(FLAG_MAP..SSA)` (treats it as a saved self-switch — both wrong on persistence AND undefined, since orchestrator minting only covers code 123 + `self_switch_valid` pages). Disposition is a §9 #2 gate decision (TEMP-flag map vs saved self-switch vs queue). `pbSetSelfSwitch` (059/221) is the REAL persistent self-switch (→`$game_self_switches`); code 123 is its RMXP-native form (Layer 1 mints those).
- **`MapInfos` editor names — checked at FABLES 4.4 (2026-06-12), found RELIABLE for the flagged set; the V4 "map 7 = Passage Cave" claim is REFUTED.** The Phase-3 V4 spot-check claimed map 7's `MapInfos` name "Comet Cave" was wrong (= wiki Passage Cave) from its `PU-Passage Cave` BGM + a Strength puzzle. **Wrong:** wiki connection topology shows map 7 links Route 04↔Route 05 = wiki **Comet Cave** (label correct); maps 19/36/37 hang off Kevlar Town = **Passage Cave** (Kevlar→Nowtoch, correct). The shared `PU-Passage Cave` BGM is a **reused cave-music asset** across both dungeons, not an identity signal — V4 over-indexed on it. `scripts/map_identity_check.py` over 31 signal-flags + 35 dup-name groups → **0 genuine name errors** (all flags = neighbour-naming signs or third-party battle BGM). `reference/map_name_overrides.json` is the live-but-empty override mechanism (§4.3, consulted by Phase-5 `mint` before `map_infos`); the 6 cave maps sit in its `phase7_confirm` for playthrough belt-and-suspenders. Caveat: only the flagged/dup-group set was wiki-verified; the rest of the 199 is presumed-correct, not audited.
- **Scripts.rxdata has 260 sections;** ~13 are clearly Uranium-original (Nuclear_*, URANIUM_*, Bambo, Tandor, Custom_Trainers, GenderSelect, StarterSelect, etc.). See `reference/recon/scripts_index.md` ⚠ flags. Note: `220_Custom_Mode.rb` and `222_Gym_8.rb` were in the dump but missed in the agent's `uranium_custom_features.md` summary.
- **Audio middleware: FMOD.** Uranium uses FMOD via DLL (`fmodex.dll`), wrapped by scripts 247–249. Strip — GBA cannot use FMOD.
- **EliteBattle and BW UI overrides** are third-party Essentials mods (not Uranium-original) shipped with the game; both strip on conversion since pokeemerald-expansion has its own UI/battle scene.
- **Tandor dex: 200 entries, 201 internal species IDs.** Confirmed from `dexdata.dat` (15276 bytes ÷ 76 = 201) and `regionals.dat` (200 species with Tandor numbers 1–200; 1 species has no Tandor number — identity TBD). The "~200 species, 18 Uranium-original" claim is resolved for count but fakemon identity still unconfirmed.
- **Two `.dat` binary formats.** Custom Essentials binary (`dexdata.dat`, `attacksRS.dat`, `tmpbs.dat`, `evolutions.dat`, `eggEmerald.dat`, `regionals.dat`, `moves.dat`, `items.dat`, `tutor.dat`, `tm.dat`, `metrics.dat`) — parse from `Compiler.rb` write schema, no Ruby needed. Ruby Marshal (`trainers.dat`, `encounters.dat`, `connections.dat`, `metadata.dat`, etc.) — load with stubs. See `uranium_dat_inventory.md`.
- **`dexdata.dat` = main species table.** 76 bytes per species, no header, field offsets in `Compiler.rb` `requiredtypes`/`optionaltypes` dicts. Start `pbs_converter/pokemon.py` here.
- **`attacksRS.dat` = level-up learnsets.** Indexed binary: (offset, byte_length) table at front, then [level uint16, move_id uint16] pairs per species.
- **`tmpbs.dat` = Uranium-custom TMPBS field.** Same indexed format as attacksRS, single move IDs per entry. Extra move compatibility list — semantics TBD.
- **Species ID 201 = "Gengar".** Resolved 2026-05-18 via `messages.dat` dump (`reference/uranium_data/species_names.json[201]`). Explains why `regionals.dat` has no Tandor dex entry for ID 201 — it's a placeholder/Easter-egg slot, not a real Tandor Pokémon. Treat as STRIP (or `needs_engine` if wanted as an unobtainable curiosity) during Phase 2 species emit.
- **`messages.dat` exists and is required.** All display strings (names, descriptions, Pokédex entries, dialogue) live here, NOT in the custom-binary `.dat` files. `scripts/dump_messages.rb` extracts to `reference/*.json` sidecars. The `OrderedHash` class needs a custom `_load`/`_dump` matching Essentials' `[keys, values]` Marshal format (see scripts_dump/044_Intl_Messages.rb:348-399). **Encoding:** strings are raw **UTF-8 bytes tagged ASCII-8BIT** — `dump_messages.rb` must `force_encoding('UTF-8')` and fall back to windows-1252 only if `!valid_encoding?`. (The old windows-1252→UTF-8 transcode produced mojibake like `Ã©`; fixed 2026-05-20. Breadcrumb in `reference/memory-archive.md`.)
- **Shadow Pokémon STRIP confirmed.** 0 TPSHADOW hits in 331 trainers. `shadowmoves.dat` = Array[0] (empty).
- **types.dat = 20-type effectiveness matrix.** Array[400] = 20×20. Nuclear type already encoded (Uranium extends vanilla 18 types → 20 total).
- **`dump_constants.rb` regex was all-caps-only → silently dropped mixed-case constants.** Item 211's PBS constant is the author typo `POKeBALL` (lowercase `e`), and ~70 `PBTrainers` classes are mixed-case (PkMnTRAINER_Male, LEADER_Roxanne, SWIMMERfE, POKeFAN_Female, …). Loosened the regex to `[A-Za-z][A-Za-z0-9_]*` (2026-05-21) and regenerated sidecars. **`trainer_class_internal_names.json` grew from ~60 to 130 entries — §2.6 trainers depends on this.** Species/moves/abilities/types sidecars were already complete (all-caps), so they're unchanged.
- **Item 211 = "Poké Ball"** (internal `POKeBALL`). The canonical Poké Ball lives at Uranium item id 211, not the vanilla id 1. Accent-folded to ITEM_POKE_BALL.
- **Shadow Pokémon code is complete but inert.** `scripts_dump/124_Pokemon_ShadowPokemon.rb` (806 lines) + `145_PScreen_PurifyChamber.rb` implement the full Colosseum/XD mechanic (heart gauge, hyper mode, Relic Stone, Purify Chamber, Shadow Sky), but no script ever sets `$PokemonGlobal.snagMachine = true`, and only `224_Nuclear_Horde_Battles.rb:7-11` aliases `pbIsSnagBall?` (for Nuclear hordes, not shadows). Treat as dead code — STRIP after Phase 2 spike confirms no `TPSHADOW=true` rows in `trainers.txt`.
- **Gym 8 has a custom tile puzzle.** `scripts_dump/222_Gym_8.rb` defines three hardcoded layouts (5×4, 10×9, 16×15) plus a spawn-event runtime patch and a live HUD. Story-critical (cannot beat 8th gym without it). ADAPT — re-express as a Poryscript movement/event puzzle.
- **Tandor Championship is a 4-round randomized bracket**, not a fixed sequence. `scripts_dump/227_Tandor_Championship.rb` picks 2 from `TCTRAINERS` + 2 from `TCTRAINERS2` (former Gym Leaders). Decision changed to ADAPT (was CONVERT) since the map-event NPC graphic mutation doesn't translate directly.
- **Nuclear-type custom moves (9 codenames).** From `scripts_dump/217_Nuclear_Forms_Moves.rb`: GAMMARAY, RADIOACID, NUCLEARWASTE, HALFLIFE, NUCLEARSLASH, NUCLEARWIND, PROTONBEAM, ATOMICPUNCH, METALCRUNCHER. Plus ability CHERNOBYL (URAYNE form 2). The 1/8-HP-per-turn out-of-battle effect is NOT in script 217 — locate before Phase 6.
- **Agent reference coverage vs data-derived census (2026-06-14, `agent_coverage_diff.md`).** **Codes: exact parity** — the agent's `rgss_event_commands.md` table = the census's 59 codes exactly. **Script calls: the agent ENUMERATES a superset but DISPOSITIONS a subset.** Its 250-row `355/655` inventory (in `rgss_event_commands.md`) ⊇ the census's 148 real heads; only **~54** get a MAP/STRIP/UNHANDLED row in `uranium_script_calls.md` (~83% of script-call lines), +`pbTrainerBattle`/`onEvent?` in `system.md`/few-shots → **~91% of occurrences**; the rest queue by the explicit *"absent ⇒ UNHANDLED"* rule (by design). **Two gaps the census surfaced:** (1) the agent's inventory is **`355/655`-only**, so **action-bearing code-111 script-conditions** aren't enumerated — 31 heads / 613 occ (`if pbWildBattle/pbRockSmash/pbPickBerry/pbCoralBreak/pbMoveTutorChoose…`); **benign** (most resolve to UNHANDLED-tagged `355` siblings → queued anyway) but a cluster-triage/classifier pass should treat the 111-condition call as a first-class signature; (2) `uranium_script_calls.md` **counts are STALE** vs the current deserialization (`pbSetSelfSwitch` 336→191; `pbItemBall` 46→243 incl. the 234 `111`-condition ground-items) — regenerate if ever used for prioritization. **Granularity note:** the census records receivers (`pkmn`/`spriteset`/`XInput`/`nextBattleNuclearHorde`) where the agent names methods (`pbLearnMove`/`addUserSprite`/`vibrate`/UNHANDLED); the census's "excluded" buckets hide ~16 ops the agent treats as first-class. Relates to OQ "verify UNHANDLED-tagged script calls' real semantics."
- **Ledge (terrain tag 1) jump direction is NOT stored anywhere — it's the player's movement direction, gated by the tile's 4-dir passage bits (2026-07-13).** Stock v17 chain, no Uranium edits: `move_*` runs ordinary `passable?` first; only a *legal* step onto a tag-1 tile becomes `pbJumpToward(2)` in that same direction (`023__Game_Player_v17.rb`, `101__PField_Field.rb`, `025__Game_Map_v17.rb`). So the sole open entry side = the jump direction, deterministically derivable from `tilesets.json` passages; 0x0F never reaches the jump code = pure wall (Uranium devs use this — ts22 840/841/842 = Moki pond-dock front, 0x0E, open only toward water = fence from land). NPCs can never step on tag-1 tiles (non-player branch of `Game_Map#passable?` bars them outright). Derivation rule + ts22 inventory → `SLICE2_TODO.md` #1.
- **The `s:tsOn?/tsOff?` page-gate idiom is 100% the door/arrival-choreography pattern — zero NPCs (2026-07-13).** Census: 5 switch labels match the predicate pattern (ids 12–15, 22); 324 gated multi-page events corpus-wide, every one a warp door / cave entrance (136 with gfx — all door sheets: PU-doorsdew 74, door2 57, door habidome 4, FKdoors1 1 — plus 188 blank). Shape: P1 (player-touch) = door choreography + code-201 transfer; P2 (ts-gated autorun) = `get_character(0).onEvent?` arrival walk-out + `setTempSwitchOn("A")`. Both halves natively subsumed (warp events + arrival coords + GBA door-warp), and these events are never object-emitted — SLICE1 #1's "8 deferring Moki NPCs" were these doors; dispatch was moot in-game. The dispatcher still resolves the gate now (`metadata_wiring._resolve_switch_gate_term`) as insurance. Predicate verified event-scoped: `tsOn?/tsOff?` read the event's own `@tempSwitches`, `s:` conditions eval with `self` = the Game_Event (`022_Game_Event_v17.rb:70-86,126-134`). original `recon_scripts.rb` left 85 sections as 0-byte files due to Windows-1252 encoding errors. Several Phase 0 "this script is empty" findings from that period were artifacts of the bug, not facts about Uranium. The corrected dump (Windows-1252 transcode + `binwrite`) leaves only 40 genuinely-empty sections.

---

## Flag Registry Notes

**Registry initialized (Phase 4 §4.1).** `flag_registry.py` is the sole minter. Pre-seed = `reference/guides/essentials_to_emerald_map.md` (hand-authored, currently **8 flags + 5 vars**) + the §3.3 sidecars. **Design choice:** the registry does NOT bulk-mint from sidecar labels (15× "Reserved" / dup "BADGE 8" → noise); it uses the sidecars only to (a) detect+block the **34 script-switches** (`s:` names, never minted) and (b) carry each switch's label as agent context. The agent proposes context-derived names for everything else; `propose_flag/var` validates (convention, junk/blocklist, reuse-collision, **fork-constant collision** via `_naming.load_fork_constants` on the live fork). Mutable state = `output/uranium-build/flag_state.json` (gitignored, regenerable — never hand-edit mid-run). **Badges (sw 251–263) deferred to §9:** the obvious names `FLAG_BADGE0N_GET` already exist in the fork (collision check caught it) — whether Uranium's 12 badges reuse vanilla's 8 is a fidelity call, so no badge rows pre-seeded.

**Self-switch minting (added 2026-06-01).** `mint_self_switch(map, event, letter)` registers per-event self-switch flags (`FLAG_MAP{m:03}_EVENT{e:03}_SS{letter}`) deterministically + idempotently; the **orchestrator** derives them from each event (`_event_self_switches`: code 123 sets + self-switch page conditions) and mints on script-accept, so the **frozen prompt is unchanged**. State persists under `self_switches`. See `reference/guides/flag_registry_policy.md` → "Self-switches" + "The flag budget".

**Temp-switch minting (added/committed 2026-06-03).** `mint_temp_switch(map, event, key)` mirrors `mint_self_switch` for Uranium temp-switches (`setTempSwitchOn`/`tsOn?`, script code 355/655) — per-map-visit state → auto-reset flag named `FLAG_MAP{m:03}_EVENT{e:03}_TS{key}` (**TS** marker keeps it distinct from `SS`). Orchestrator `_event_temp_switches` (`_TEMP_SWITCH_RE`) derives the keys; mints on script-accept. Persists under `temp_switches`.

**Address layout = ENGINE-OWNED (F1/F2 fix, 2026-07-10).** `dump_header` bases are now `int | str`: the assembler passes the engine's `RPG2GBA_*_START` constant **names** (emitted symbolically; cpp resolves them in `event_scripts.s`) plus `capacities` parsed from `engine/include/config/rpg2gba.h` by `assemble_pathfinder._load_rpg2gba_capacities` (fail-loud: missing header/gate-FALSE/missing define/mint-count > capacity). Int bases remain ONLY for placeholder intermediate dumps (not fork-safe). Temp-switch mints live in the dedicated engine temp region cleared on map transition — disjoint from the `FLAG_TEMP_11..1F` rock-obstacle range. Layout + capacities: `reference/guides/engine_extension_surface.md` §2.

**Phase 4 seed inputs (from Phase 3 §3.3):** `reference/uranium_switches.json` (235 named switches, by index) + `reference/uranium_variables.json` (119 named variables). These are the named entries from `System.rxdata`; index 0 is nil, unnamed entries dropped. **Caveat:** some switch "names" are Essentials *script-switches* (e.g. `s:pbIsWeekday(-1,2,4,6)`) — evaluated at runtime, not stored state; the flag registry must special-case (not mint a `FLAG_*` for these).

### Pre-seed candidates (CORRECTED 2026-05-25 against the §3.3 sidecars — authoritative)

**The Phase 3 `reference/uranium_variables.json` / `uranium_switches.json` names supersede the Phase 0 hand-guesses.** Two Phase 0 guesses were wrong: `var[1]` is **"Temp Pokemon Choice"** (NOT the gym-8 white-tile counter), and the white-tile counter is actually **`var[87]` "White Tiles"**. Verified indices below use the real System.rxdata names; mint `VAR_*` from these at the Phase 4 registry build.

| Uranium index | Real name (System.rxdata) | Source script | Purpose |
|---|---|---|---|
| `var[87]` | `White Tiles` | `222_Gym_8.rb` | Live white-tile counter for 8th-gym puzzle HUD |
| `var[121]` | `Gym 8 Progress` | `222_Gym_8.rb` | Gym-8 quest-state sentinel |
| `var[23]` | `CHAMPIONSHIP LIST` | `227_Tandor_Championship.rb` | 4-trainer bracket array (random 2+2) |
| `var[24]` | `CHAMPIONSHIP PROGRESS` | `227_Tandor_Championship.rb` | Current round 1–4 |

The sidecars carry **dozens** more gym/championship/puzzle flags (e.g. `switch[121]` "Gym 8 end", `switch[55]` "Gym 1 defeated", per-gym puzzle switches/vars) — the registry build should mint from the sidecars wholesale, not from this short hand-list.

Bambo reward thresholds (count-of-owned, not switches/vars) from `213_Bambo_Reward.rb`: 10, 20, 30, 50, 75, 100, 125, 150, 175, **194** (SHINYCHARM, final real tier).

---

## Open Questions

- **Automated headless playtest harness — feasibility study DONE 2026-07-17, awaiting user go/no-go** → `reference/findings/mgba_automation_feasibility_2026-07-17.md`. Verdict: feasible via `libmgba-py` (headless Python bindings; PokéBot-Gen3 precedent), NOT the installed `mgba-qt 0.10.2` (no `--script` flag, no headless). Assertions read game memory at addresses parsed from the build's own `engine/pokeemerald.map` (addresses differ per build — never hardcode). Design: poll-state primitives (walk_to/interact/advance_dialog), not frame-counted input tapes; end artifact = scripted in-game save (`.sav`) + savestate. Target scenario: boot → Auntie → `FLAG_SYS_B_DASH`. **ALL 4 SPIKES PASSED 2026-07-17** (scratchpad-only, nothing productionized — spike results §appended to the findings doc): headless `libmgba-py` (prebuilt lunar zip, abi3, links system `libmgba.so.0.10`, no compile; `cffi` added to pyproject) ran the FULL scenario in seconds — boot → greedy walk_to Auntie → 202 A-taps → `FLAG_SYS_B_DASH` true → scripted START-menu save → flag verified inside the `.sav` flash sectors → savestate round-trip in a fresh core. Key techniques (details in doc): struct offsets via probe-compile against engine headers w/ `-mabi=apcs-gnu` + `nm -S` (ELF has no usable DWARF); script-idle = `sLockFieldControls` recovered from `ArePlayerFieldControlsLocked`'s literal pool via objdump (statics not in linker map); `core.autoload_save()` REQUIRED before reset or in-game saves write nowhere. **BUILT + SHIPPED same day (user-directed):** harness productionized as `src/rpg2gba/playtest/` (emulator/symbols/offsets/scenarios/stamp; bindings via `scripts/fetch_libmgba.py`; unit tests always-on, e2e opt-in `RPG2GBA_PLAYTEST=1`, ~11 s green; suite 1228 pass) + **embedded-save review ROMs**: engine blob `gUraniumEmbeddedSave` + `UraniumEmbeddedSave_TryLoad` (`engine/src/uranium_embedded_save.c`, **`volatile` is load-bearing — LTO folded the const-zero magic and GC'd the blob without it**) + `CB2_StartUraniumSlice` boot branch (flash-save continue > stamped blob > new game; walker + regression paths unchanged). One-file review artifact: `python -m rpg2gba.playtest.stamp --scenario moki-running-shoes …` → `review_moki_shoes.gba` (md5 `d06c772b`) taildropped to iphone182, boots standing below Auntie w/ shoes; **user CONFIRMED working on phone 2026-07-17; committed**. Details → findings doc §"Built".

- **Cutover to `engine/` — DONE 2026-06-19** (pointer set, slice re-assembled + booted on `engine/`, the 2 hardcoded-old-clone scripts repointed, pytest run). **Remaining:** (a) delete the old external clone — `rm -rf /home/b/repos/pokeemerald-expansion` (user-run; agent's attempt was classifier-blocked); (b) **DONE 2026-06-19** — assembler no longer edits engine source in place; uses committed include-hooks (`event_scripts.s` + `map_data_rules.mk`) + gitignored `*.gen.json`/`uranium_includes.inc` overlays (Decisions 2026-06-19); (c) repoint/clean the stale `.claude/settings.local.json` allowlist entry naming the old clone; (d) **DONE 2026-07-11** — the 2 slice-minting tests that failed against a built fork are fixed (Decisions 2026-06-19 entry updated with resolution).
- **Locate the Nuclear 1/8-HP-per-turn out-of-battle effect.** Not in `217_Nuclear_Forms_Moves.rb`; likely in a field/step hook or `PokeBattle_Pokemon` patch. Resolve before Phase 6.
- **GBA charmap handling for accented chars.** Sidecars are now correct UTF-8 (`Pok\xE9mon`), but the GBA charmap is custom — accented chars (é, ♀, ♂) need charmap translation downstream. Phase 5/6 integration concern, not a Phase 2 emit bug. **Update 2026-06-18:** `tileset_converter.assembly.normalize_pory` is now the single owner of charmap legality at staging (normalizes or fails loud); the quantize image pipeline owns glyph rendering. (The upstream `messages.dat` mojibake was resolved 2026-05-20; see Discoveries + `reference/memory-archive.md`.)
- **Exact TMPBS field purpose** — `tmpbs.dat` is confirmed a Uranium-custom extra move list per species (identity resolved 2026-05-18); the exact *semantics* (move-reminder vs broad compatibility list) are still open. Determine before deciding how to represent in Phase 2 JSON output.
- **Verify pre-seeded `VAR_GYM8_*` / `VAR_TANDOR_CHAMPIONSHIP_*` names** at Phase 4 flag-registry build (proposed in Flag Registry Notes; subject to renaming based on registry policy).
- **Verify the UNHANDLED-tagged script calls' real semantics.** **[SUPERSEDED 2026-06-18 — the fork-index reverse-gate (Decisions Made) is now the systematic answer; resolve case-by-case via the guard + native-analog ledger, not ad-hoc.]** **Top ~60 sigs now dispositioned** (2026-06-03, verified against `scripts_dump/`, ≈92% of invocations — see `reference/recon/uranium_script_calls.md`); the UNHANDLED ones (`pbPokeCenterPC`, `pkmn.pbLearnMove`, `pbBerryPlant`, `Kernel.pbRockSmashRandomEncounter`, `pbCave*`, `pbBridgeOn`, `pbSetEventTime`, `Kernel.pbNoticePlayer`, `pbShowMap`, `pkmn.setAbility/setItem`, Nuclear-horde, …) are queued **by decision** (each needs an unbuilt engine feature or map-context the agent can't derive). Remaining open = the rare tail (~190 sigs, 77 one-offs) — decide MAP vs needs-engine only if a queued instance turns out to matter.

*Resolved questions (species 201 = Gengar, species `.dat` = dexdata.dat, Tandor dex 200/201, no TPSHADOW rows, undumped-section accuracy, classifier near-misses → Tier-1-only) are archived in `reference/memory-archive.md`; their conclusions live in Decisions Made / Uranium-Specific Discoveries.*

---

## Last Session Summary

**2026-07-22 (S6b Theo loss-path FREEZE — ROOT-CAUSED + FIXED, boot-walk PENDING; build agent + 2 Explore sub-agents):** User: losing the Theo battle still freezes on his last line as the screen starts fading to black, despite the 2026-07-21 `waitmessage` fix. **The `waitmessage` was a symptom-patch; real root cause = the still-set `BATTLE_TYPE_FIRST_BATTLE`.** `RIVAL_BATTLE_HEAL_AFTER`==1 (0b01), `RIVAL_BATTLE_TUTORIAL`==3 (0b11); the pristine-upstream mask at `battle_setup.c:1322` (`GetRivalBattleFlags() & RIVAL_BATTLE_TUTORIAL`) is truthy for HEAL_AFTER too — so switching TUTORIAL→HEAL_AFTER (commit `9971428e`) did NOT remove FIRST_BATTLE as its comment claimed. On a loss, `battle_controllers.c:2658` reroutes the trainer win-text (`STRINGID_TRAINER1WINTEXT`) under FIRST_BATTLE into the FRLG Oak voiceover `PrintOakText_HowDisappointing` → `BeginNormalPaletteFade(...RGB_BLACK)` (`battle_controller_oak_old_man.c:539`), which then waits on Oak-controller graphics/state absent in a normal trainer battle → fades to black and hangs (the print controller never signals done, so `waitmessage` can't help). FRLG's Oak's-lab battle works because it uses TUTORIAL (Oak voiceover correct there). **Fix (`battle_setup.c:1322`, fenced URANIUM PATHFINDER SLICE):** require the full flag — `(GetRivalBattleFlags() & RIVAL_BATTLE_TUTORIAL) == RIVAL_BATTLE_TUTORIAL` — so HEAL_AFTER-only is a plain losable+heal trainer battle (no FIRST_BATTLE, no Oak reroute). Zero reachable vanilla impact (grep: earlyrival used only by FRLG with TUTORIAL/0; nobody uses HEAL_AFTER alone). Win path unchanged (gated on `BATTLE_TYPE_TRAINER`; win prints `TRAINER1LOSETEXT`, which the reroute never touched → S6b win stays PASSED). `waitmessage` kept (correct hygiene). Engine-only rebuild, **hash `2010bbd0`→`f5867449`**, 1328 tests pass, taildropped. **NOT committed** (pending user loss-walk confirmation): `engine/src/battle_setup.c` + `SLICE1_TODO.md` #11 + this entry. Test rigs still live per below (Body Slam, Geodude-disable, post-quiz harness).

**2026-07-21 (S6b Theo rival battle — BUILT, boot-walk PENDING; build agent, single session):** User reported the post-quiz Theo battle skipped entirely + Theo's sprite stranded in the lab. Root cause: the battle isn't EV020 (that's just a "c'mon let's go" NPC); it lives inside **EV019 "Machine"** page 0 as `pbTrainerBattle(RIVAL,"Theo",...,party=pbGet(151)+18,canlose)`, wrapped in a code-111 win/lose branch (66/55 nodes). The hand conversion (`Map050_EV019.pory`) had stubbed the whole conditional as SLICE1_TODO #17 — so no battle, AND Theo's exit choreography (which lives *inside* the battle branch as `applymovement + Change-Opacity 0`) never ran, stranding his sprite (the page-1 blank graphic can't apply mid-visit per the boot-page-static engine limit). Battle is scripted to happen **in the lab** (no warp; "Oh, please, not in the Lab..." is just flavor); the outside event is the separate catch tutorial (EV009). **Fix built:** (1) added `TRAINER_THEO_9/10/11` (Eletux/Orchynx/Raptorch L5 counter-picks) to `engine/include/constants/opponents.h` (855-857, bumped `TRAINERS_COUNT_EMERALD` 855→858) + `engine/src/data/trainers.party` — the converted parties already existed in `intermediate/trainers.json`, just never staged into the slice engine (still stock Emerald trainers); **committed `3893aac7`** because the fork-index gate reads `git show HEAD:engine` (pristine, never working tree — `fork_index.py:305`). (2) Rewrote the EV019 battle section: `switch(var(VAR_POKEMONTEST))` → `trainerbattle_earlyrival(TRAINER_THEO_N, RIVAL_BATTLE_TUTORIAL, DefeatText, VictoryText)` — the vanilla FRLG Oak's-lab starter-rival idiom (losable, auto-heals after via `RIVAL_BATTLE_HEAL_AFTER`, converges either way; both RMXP branches also converge to the prof's Route-01 farewell + `var101+=1`) + Theo walk-off `set_invisible` to fix the stranded sprite. **Gotchas hit:** (a) new engine constants invisible to the gate until committed (HEAD-only by design); (b) `#` comments in `trainers.party` break the build — the file is run through `arm-none-eabi-cpp`, so comments must be C-style `/* */`. Full chain clean (rebuilt fork_index, transpile → stage → assemble → `make modern`), ROM `51be1e63` (ROM 79.28%, EWRAM 86.77%, IWRAM 86.99%), taildropped as `uranium-slice-51be1e63-theo-battle.gba`. **Cosmetic gap (flagged, not a bug):** Theo's battle sprite is a stock Youngster stand-in — no Uranium trainer-pic pipeline in slice 1. `BOOT_WALK_CHECKLIST.md` S6b updated with what to verify (battle fires, win+lose both continue, party heals, Theo gone after). The `.pory` edit is NOT committed (working-tree hand conversion, read directly by transpile_driver); only the engine data is committed. **Committed after user confirmed S6b PASSED: `3893aac7` (engine data) + `ad70308a` (.pory + docs).**

**Then S7 (PokéPod scene, Map172 EV004) — Theo-invisible fix, boot-walk PENDING (same session):** User boot-walked S7: scene fired but Theo was invisible, and flagged this as a *recurring* class ("do we have rules for making NPCs visible?"). Both mechanisms exist and fire — transpiler opacity→`set_visible`/`set_invisible` (`transpiler.py:687-691`) AND the flag-hidden-actor auto-bracket (`hidden_actor_bracket.py`, Theo = `flag FLAG_TEMP_11`, spawned only via `addobject`). **Root cause: the auto-bracket was NOT control-flow-aware** — it inserted a single `addobject` before the *first textual reference* (`:166,175-179`), which for EV004 landed in the `if (FLAG_LOST_FIRST_BATTLE)` (lose) branch. The `else` (win) path — the common one — ran `applymovement(2, set_visible)` on an unspawned actor (silent no-op) → Theo invisible. **Fix:** made `bracket_hidden_actor_scripts` branch-aware via a dominance rule — new `_branch_paths` (per-line enclosing-branch-opener tuple, tracks if/else/elif/switch braces; `} else {` = pop+push so sibling branches diverge) + `_dominates` (an `addobject` covers a ref iff its branch path is a prefix and it textually precedes). Now inserts `addobject` before the first reference on *each* execution path not already dominated (straight-line refs still get exactly one, so all pre-existing straight-line behavior/tests unchanged). `addobject` idempotency makes redundant spawns harmless. Corpus-wide staging pass fix, not a Map172 patch. 2 new tests (sibling-branch + straight-line-tail), all 9 bracket tests pass. Re-staged (both existence + onframe checks clean) → assemble → `make modern` clean, ROM `8fe9d9ff` taildropped as `uranium-slice-8fe9d9ff-theo-visible.gba`. **NOT yet committed** (pending user boot-walk confirmation) — touches `hidden_actor_bracket.py`, `tests/test_hidden_actor_bracket.py`, `BOOT_WALK_CHECKLIST.md`. `removeobject` bracketing is still best-effort (last-ref only; doc already tolerates "actor left spawned, ON_TRANSITION rehides") — not the reported bug, left as-is. **S7 Theo-visible boot-walk PASSED (user-confirmed); committed `2aeaa46a`.**

**Then S6b loss path (same session):** made the Theo battle actually losable like PC Uranium. Two bugs: (1) I'd used `RIVAL_BATTLE_TUTORIAL`, which adds `BATTLE_TYPE_FIRST_BATTLE` (`battle_setup.c:1322`) — the Birch-intro type where the opponent is framed WILD and FLEES at low HP, so you can never lose. Fix: `RIVAL_BATTLE_HEAL_AFTER` alone (losable + auto-heal, no flee) + record outcome via `if (var(VAR_RESULT)==1) setflag(FLAG_LOST_FIRST_BATTLE)` (engine sets VAR_RESULT=TRUE on EARLY_RIVAL defeat; the earlyrival converge had dropped the RMXP switch-35 write). (2) After that, losing FROZE on the victory-text screen — the earlyrival player-loss-and-heal path is **dead code in vanilla** (earlyrival is only ever TUTORIAL-can't-lose or plain-whiteout), and `BattleScript_RivalBattleLost` (`data/battle_scripts_1.s`) printed the win text then `end2` with **no `waitmessage`**, racing the return-to-field transition. Fixed with a fenced `waitmessage B_WAIT_TIME_LONG`. Both committed `9971428e`. **Loss-path boot-walk NOT yet confirmed** (user moved on after the freeze-fix ROM `56bdd086`). **TYPE-TRIANGLE CORRECTION:** the player ALWAYS has the type advantage — Theo's counter-pick is the starter *weak* to yours (Orchynx>Eletux, Raptorch>Orchynx, Eletux>Raptorch). Winning is expected; losing = deliberately throwing it (prof even asks "did you let him win?"). My earlier "Theo gets the type that beats yours" (in code comments + `trainers.party`) is BACKWARDS — fix the `trainers.party` comment when the test rigs are torn down.

**Can-lose battles + pipeline (design-first):** documented the mechanism in `reference/guides/canlose_trainer_battles.md` and the implementation design in `reference/guides/canlose_battle_pipeline_plan.md` (both committed `80450bcb`/`cfc76bc1`). Native analog = `TRAINER_BATTLE_EARLY_RIVAL` + `VAR_RESULT` branch; constraints = heal-or-whiteout only, two text slots, converted trainers not yet staged into the slice build (Theo parties hand-added). **User chose DESIGN-FIRST — pipeline NOT built.** Next task when resumed: implement per the plan (Gap A transpiler: canlose `pbTrainerBattle` code-111 → earlyrival+VAR_RESULT with branch-polarity swap + `pbGet(N)+K` party switch; Gap B staging: auto-emit `trainers.json` → engine via gen header + gate manifest; then retire `hand_conversions/Map050_EV019.pory`). **LIVE TEST RIGS (uncommitted, tear down after loss walk):** `trainers.party` Theo Body Slam (+ backwards comment), `new_game.c` Geodude-disable; KEEP `new_game.c` post-S7 harness (commented, for S8 testing — Raptorch/beat-Theo/VAR_QUEST_LOG=2/running shoes).

**2026-07-21 (S6 aptitude-test always-Eletux bug — ROOT-CAUSED + FIXED, boot-walk PASSED; build agent, single session — committed the outstanding W1-W9 starter species work first, then investigated the new symptom via a background research agent + applied the fix inline):** User confirmed the W9 party-flag fix worked, then reported the S6 quiz always granting Eletux regardless of answers (tested all 3 slots × several combos). Committed the entire uncommitted W1-W9 starter species arc as `2b045012` before debugging (species staging/art/cry converters, gate extras, transpiler `pbHasSpecies?`, ON_FRAME re-arm pass, party-flag fix — all previously verified working, held uncommitted per protocol pending user retest). **Root cause of the quiz bug:** the argmax sign-test literal `32768` (0x8000) in `Map050_EV005.pory`'s TestBody falls inside the engine's `SPECIAL_VARS` range (`include/constants/vars.h`: `0x8000`–`0x8015`); the `compare` asm macro (`asm/macros/event.inc`) auto-selects `compare_var_to_var` vs `compare_var_to_value` based on whether its literal operand lands in that range (or the temp-var range) — so `if (var(VAR_RESULT) < 32768)` silently compiled to "compare against `VAR_0x8000`", not the literal. `VAR_0x8000` is the vanilla engine's OWN switch-statement scratch variable (`switch`'s codegen is `copyvar VAR_0x8000, <var>; compare VAR_0x8000, <case>` per case), and the quiz's per-question `Tally` script uses `switch (var(VAR_TEMP_MOVE_CHOICE))` — leaving `VAR_0x8000` holding the LAST question's raw 0-2 answer index after the 4th question. A tally delta (0-4 range) is essentially never `<` a leftover 0-2 value, so both override checks (`T4>=T5`, `T3>=T4 && T3>=T5`) almost always skipped, leaving `VAR_POKEMONTEST` stuck at its hardcoded default (`2` = Eletux) no matter what the player picked. **Fix:** use `32767` instead of `32768` in the three sign-test comparisons (same subtract-and-test-sign semantics — `A>=B` ⟺ `(A-B) <= 0x7FFF` — but 32767 sits outside both the temp-var and special-var ranges, so the macro correctly emits `compare_var_to_value`). Single-line-pattern fix, no other `32768` sign-test literals exist elsewhere in the hand conversions (grepped to confirm). Full chain re-run clean (transpile → stage → assemble → `make modern`), pytest **1320 passed/15 skipped**, ROM `b0b21993` taildropped, committed `b3b1b623`. **User boot-walked and confirmed all answer combinations now resolve to the correct starter** — this closes `STARTER_SPECIES_PLAN.md` end to end (W1-W9) and `SLICE1_TODO.md` #2; `BOOT_WALK_CHECKLIST.md` S6 → PASSED. Docs updated same session: `BOOT_WALK_CHECKLIST.md` (S6 checked off, Auntie-RAPTORCH gap note removed), `SLICE1_TODO.md` (#2 removed from Open, #17 moved to Done with the full arc), `STARTER_SPECIES_PLAN.md` (W7 gate section closed out).

*Superseded entries (2026-07-18 starter species W1-W5, 2026-07-16 RMXP blocked-move semantics, and older) archived in `reference/memory-archive.md`.*
